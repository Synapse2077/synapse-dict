# en 建库主源的存证锚 —— 2026-09-06 落，由 tests/test_plan_ledger.py 的 P3 读。
#
# 🔴 为什么锚要落成文件而不是写进断言：
#    ① 字面量闸（test_no_literal_counts.py）禁止把大整数写进断言的期望位 ——
#       那条规则的理由是「写死的行数必然过期」。dump 的字节数恰好相反：
#       它**必须永远不变**，变了就是锚被动过。规则对、这里是例外 ⇒ 挪出断言，落成数据。
#    ② de 已有同族做法（tests/dump_drift_baseline.txt 等三份）。
#
# 🔴🔴 `-20250424` 一个字节都不许删：库里那 52.7 万 Wiktionary 层是它灌的，
#      是唯一能说清「那批中文当初依据什么」的凭证（EN_PLAN §1.3 / §1.7）。
#      de 七月那份包被保留策略清掉，直接导致 16,483 个词形的读音说不清来源、也补不回来。
#
# 格式：<dumps/ 下的文件名> <字节数>
kaikki.org-dictionary-English-20250424.jsonl 2882359726
kaikki.org-dictionary-English-20260828.jsonl 3212282689
