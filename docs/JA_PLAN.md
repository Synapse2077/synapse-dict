# ja 作业计划 —— 日语从零到成品

> 2026-09-15 建。本文是日语这一轮的**唯一账本**：判据、已定的决定、欠账、阶段表全在这儿。
> 与另四份文档的分工见 `docs/PLAYBOOK.md` 文件头。
>
> 🔴 **动手前先读本文第三节「不能照搬前六门的」** —— 日语的难不在工程，在判据。
> 前六门的机器大部分能搬，但有 6 处搬过来会**静默出错**：闸全绿而东西是错的。

---

## 一、家底（2026-09-15 全量实测，非抽样）

### 三份源与各自的分工

| 源 | 体量 | 它唯一能给的 | 🔴 它给不了的 |
|---|---|---|---|
| **英文版切片** `kaikki.org-dictionary-Japanese.jsonl` | 315 MB / 199,484 行 | 词条骨架、义项、假名读音、罗马字、IPA | **动词活用**（只给 975/13,921 个动词＝7.0%，中位 2 个形）<br>声调 0 条、录音 6 个词形 |
| **日语版切片** `kaikki.org-jawiktionary-Japanese.jsonl` | 181 MB / 148,279 行 | **动词活用**（19,564 词 / 336,940 行 / 中位 19 个形）<br>和語の漢字表記指针 15,514 条 | **声调整个废掉**：10,215 条里 10,212 条的 `other` 字面是 `"("`，重音位置全丢；存活的 92.6% 带 `Kyoto`+`Osaka` ⇒ 是**京阪式**不是东京式 |
| **中文版整包** `zhwiktionary.jsonl.gz`（`lang_code=='ja'`） | 136,700 条 | **声调唯一可用来源**（东京式 `raw_tags` 占 99.75%，15,012 个词形）<br>**免费中文释义 133,961 条** | 词性：74,215 条 `pos="unknown"` |

### 盘子

```
英文版 199,484 条目
  ├ soft-redirect   44,899   (22.5%)  异表记/同音索引页，一条 gloss 都没有
  ├ romanization    32,062   (16.1%)  罗马字条目
  └ 真词条目       122,523   (61.4%)
英文版 237,328 义项
  ├ 无 gloss         54,330   (22.9%)
  ├ 指针             41,225   (17.4%)
  └ 🔴 真义项       141,773   (59.7%)  覆盖 86,902 个词形 —— 这是要出版、要翻译的分母
```

🔴 **~~中文版白送的 133,961 条与整个翻译任务同量级~~ —— 这句话是错的，2026-09-15 量落点后作废。**
那 133,961 是**源头数**。落点差一个数量级：

```
中文版有可用中文的词形        60,921
  与我们库重叠的             25,440   ← 只有 41%（另 35,481 个词形我们没收，属阶段 3）
1:1 规则实际能落             13,242 条 = 真义项的  9.3%
完美对齐的上界（不可能达到）      40,638 条 = 真义项的 28.7%
```

⚠️ 我整场都在引用「量落点不量源头」，然后自己在这条上栽了
（`[[measure-landing-not-source]]`：新数字先假设我的度量错了）。

### 成本估算（校准过的部分）

例句量用多点 Range 采样测得，**拿德语校准：预测 410,911 / 实际 444,094，差 7.5%**。

| | 例句 | 按 fr/pt 实测单价 |
|---|---|---|
| **ja**（开工前估） | ≈ 26,352 | 例句 ≈6 元 ＋ 义项 ≈15–25 元 ＝ **≈20–35 元** |
| **ja**（跑完折算） | **53,309** | 例句 10.8 ＋ 义项 20.6（英译中）＋ 12.9（日译中）＝ **≈ 44.3 元** |
| 对照 fr（实测） | 740,366 | 例句 180 元 ＋ 义项 40 元 ＝ 241 元 |
| 对照 pt（实测） | 51,315 | 8.7 ＋ 8.9 ＝ 18.2 元 |

⭐ **超支的是例句量不是单价**：估的 26,352 条只量了一版，三版并起来 53,309 条（差 2 倍）。
单价这边反而准 —— 三轮实跑单价 66.2 / 63.5 / 98.7 token/条，全是各自跑 1% 切片量的，
**没有一轮拿上一轮的单价去套**（`[[prove-free-path-before-quoting]]`）。
⚠️ 三笔都是**折算不是实付**，账单未回查；2.43 元/M 这个半价单价是从 pt 的实测反推的。
🔴 义项那一栏分成两笔是因为**它本来就是两道题**：1.5b 英译中、3b 日译中（收词之后新进来的 8.6 万条），
开工前的估算把它们当成一道，所以估的 15–25 元只覆盖了其中一笔。

⚠️ **例句那一栏是校准过的，义项那一栏不是**。本语种版的「义项数」外推极不可靠：
德语版外推出 380 万条义项，而我们最终只出版 260,801 条，差 14 倍。义项费只能当量级看。

---

## 二、已定的判据（每条都回源核过）

| # | 决定 | 支撑它的数 |
|---|---|---|
| 1 | **`entry` 的键就是前六门那套 `(词形, 词性, etym_no, seq)`，读音不进键** | `(词形,词性)` 多条的 5,035 组里，`etym_no` **单独就分开 4,303 组＝85.5%**；而 **3,951 条 entry 一条里就有 ≥2 个读音**（`爺`＝じい/じじ/じじい）⇒ 多值的东西不能当主键分量 |
| 2 | **读音是 `entry` 的一等属性列**，像 de 的 `gender`/`aux` | 非 `character` 的含汉字条目 78,406，读音覆盖 **99.90%** |
| 3 | **读音的权威字段是「head_templates 里第一个纯假名的数字位参数」**，不是 `args["1"]` | `args["1"]` 有 **34,993 条装的是词性名**（`proper` 18,060 / `adverb` 1,789 / `suffix` 625…）；`ja-pos` 的读音在 `args["2"]` |
| 4 | **不存在「15% 读音缺口」**<br>🔴🔴 **2026-09-18 修正：这条判据后半句写滑了，代价是整整一层数据九个阶段没建。** | 缺的 14,161 条**全部是 `pos=character`**（汉字作为「字」，本来就没有单一读音）。按 85% 立项去补，补的是一个不存在的洞。<br>—— 以上前半句成立、**后半句不成立** ——<br>🔴 它从「汉字没有**单一**读音」推到了「**不用补**」，中间少了一步：汉字有的是一组**分类**读音。`青` 的 character 条目在库里 `kana`/`romaji` **两列全空**，而日语版 dump 明明写着 `ショウ(呉音) セイ(漢音) チン・シイ(唐音) あお・あお-い(訓) あをし(古訓)`。<br>⇒ **「源头没写」和「我们没抽」又一次长成了一个样**（`[[dont-say-source-lacks-what-we-skipped]]`）。这次它伪装得格外好：前半句是真的、量过的、还能引用一个具体数字（14,161），于是整条读起来像已经核实过的结论。<br>⭐ **逮到它的不是复查判据，是用户问「库里数据情况怎样」时我去数了一遍表** —— 判据本身写在账上，**闸管不到它**（它没有交付物，正是 `[[record-the-negative-decision]]` 说的那一类）。⇒ 已补阶段 4c 并做成 P6 覆盖率闸，那条闸有分母，掉下来会响。 |
| 5 | **`soft-redirect` 要拆两桶，不能一刀切** | 44,899 条全部有 `redirects`，但 **7,079 条（15.8%）有 ≥2 个目标**：`いぬ → 犬 狗 戌 率寝 寝ぬ 去ぬ` 是**同音索引页**，六个不同的词。把 n≥2 当「异体」写进关系层＝断言 `犬` 和 `去ぬ` 是异体字，**6,681 条规模的错** |
| 6 | **重定向方向不可从字形推断，必须跟 `redirects` 字段** | 纯假名→含汉字 29,390、含汉字→含汉字 9,025、**含汉字→纯假名 5,021**（`暗い`→`くらい`）、纯假名→纯假名 1,134 |
| 7 | **`inflection` 装得下**：总规模约 0.5M 行 | 对照 de 536 万（每词元 59.8 形）、fr 227 万。⚠️ 但**活用怪物是形容词不是动词**：adj 均 53.8 形/80.5% 有活用表，verb 均 6.0 形/仅 7.0% 有表 |

---

## 三、🔴 不能照搬前六门的（照搬会怎么错）

> 这一节是本文最重要的部分。每条都是「闸会绿、东西是错的」那一类。

**① `POS_LABELS` 的 `character: '字母'` —— 已经在表里、已经是错的、闸不会报。**
日语 `character` 是**汉字**（`犬` `馬` `国`，14,970 条），印出来是「字母·犬」。
而 `posLabel()` **没有 lang 参数**（`apps/web/src/App.tsx:647`），改共享表会把 en 的 `h`
（英语字母表第八个字母）一起改错。⇒ **必须先加 per-lang 覆盖层，这是结构改动不是补一行。**
同理 `particle: '小品词'` 对日语的助词也是错的。

**② 「含汉字 ⇒ 是中文」这条判据，仓库里 82 处，在日语上恒真。**
最要命的是翻译质量控制组（`de/pipeline/translate_defs.py:162`、pt 同款）：
```python
if not has_han(z):                              # 日语原文本来就全是汉字 → 恒绿
if len(LATIN.findall(z)) >= 2 and has_han(z):   # 日语残留是假名不是拉丁字母 → 抓不到
```
模型把 `日本語` 原样抄回来，两条都不响。**闸看着在问对错，其实在问一个恒真命题。**
⇒ 判据换成**假名残留检测**（译文里出现平/片假名 ⇒ 没翻完），比拉丁词那条更干净
——中文专名不会写成假名。
✅ 已修一处：`scripts/contract/tag_coverage_all.ts` 的 `alreadyChinese()`（2026-09-15）。

**③ `split_colloc` 在日语上 100% 静默出错。**
`de/pipeline/build_v3_schema.py:328` 靠「第一个汉字之前的最后一个空格」切分，
日语没有词间空格 ⇒ `rfind(" ")` 返回 -1 ⇒ **整条日语原文被当成中文译文**，
源语言那半是 `None`，全库命中且不报错。`pt`/`fr` 同名函数同病。

**④ `pos="character"` 在两份 dump 里是两种东西，不能按 pos 合并。**
- 英文版：漢字單字（`犬` `国`）14,970 条
- 日语版：**和語の漢字表記**（`今日`→きょう，多字词，`tags:["form-of","wago-kanji"]`）15,514 条
- **交集只有 28 个词形。** 任何「按 pos 合并两版」的脚本会把 1.5 万条异表记指针
  当成汉字条目，反之亦然。

**⑤ `inflection` 的入口判据「`forms[]` 里有 tags 的就是变形」会多收 15 万行。**
ja 的 592,334 条 `forms` 里：罗马字/canonical 218,697 + 模板残渣 16,206 要先扔；
剩 357,395 再分成 **形态 202,740 / 罗马字重复 89,513 / 正字法变体 64,900**。
名词那 39,998 行 forms **一条形态都不是**（全是旧字体/历史假名写法）。
照搬会把 20 万的真活用虚报成 35.7 万。

**⑥ PLAYBOOK 第六章「语言版 dump 收割录音」那一步，日语直接跳过。**
`[[cross-edition-harvest]]` 记的是「语言版比英文版多 436 倍」。日语实测：
英文版 6 个词形、日语版 84 个、中文版 144 个，**三版并集 206**。不是 436 倍，是三家都没有。
⇒ 别给它排工，要发音只能走 Commons 直查。

**⑦ 别把英文版当日语动词活用的源。**
它只给 975/13,921 个动词（7.0%）活用表，中位数 2 个形。照「英文版是结构基准」的惯性走，
会得出「日语动词几乎不活用」这个荒谬结论。⇒ `[[multi-edition-methodology]]`：
**英文版是结构基准，不是内容上限。**

---

## 四、待定（列在这儿是为了不假装已经定了）

| # | 待定的事 | 要什么才能定 |
|---|---|---|
| 1 | ~~中文版免费释义的对齐率能提到多少~~ | **✅ 已定（2026-09-15）：不做更好的对齐，照前六门的 1:1 规则。** 按 pt 实测单价 0.000148 元/条折算：<br>　1:1 规则　　免费 13,242 ｜ 要买 128,531 条 ｜ **≈ 19.0 元**<br>　完美对齐　　免费 40,638 ｜ 要买 101,135 条 ｜ ≈ 15.0 元<br>⇒ **做更好的对齐最多省 4.1 元**，而代价是**义项错配**的风险 —— es 那轮自动判重 186 条里 **12.4% 错配**（`peón` 的「棋子」并进了「行人」）。多一条义项是「缺」，义项错配是「错」，**错比缺更伤权威**。为 4 块钱冒这个险不划算。<br>🔴 这是一条**否定结论**，落账是为了下次重跑时分得清「没问过」和「问过了不做」（`[[record-the-negative-decision]]`）|
| 2 | ~~**声调 15.1% 够不够上页面**~~ | **✅ 已结案（2026-09-19）：定为 auxiliary —— 有则显示、不做默认模块、不在文案里宣传覆盖率。**<br>🔴 **换了分母才有决策价值**：全库词元 24.9 万里大量是生僻词，7.4% 那个数既不反映读者体验也不会因回归而动。接上核心词表（Waller N5–N1，CC BY，见 `data/refs/jlpt-waller/README.md`）之后：<br>　核心词 8,293 → **42.4%**　｜　N5 55.1% → N4 52.0% → N3 49.9% → N2 40.2% → **N1 35.6%**<br>　前 1,000 内容词（剔除助词等功能词）**49.9%**<br>⭐ **两把独立的尺子收敛**：自有 `freq_zipf` 前 5,000 给出 **42.6%**，与词表口径差 0.2 个百分点 ⇒ 结论不依赖任何一份词表。<br>⚠️ 排名最前的「缺声调」词是 の・に・て・は・が —— **助词本来就没有独立重音，那不是缺口**；剔除功能词后前 500 是 46.8%。<br>🔴🔴 **结案的前提是先分清「源头只有这么多」还是「我们抽漏了」**（`[[dont-say-source-lacks-what-we-skipped]]`）。逐条回源数过：中文版 dump 真正带声调的 **18,534 个词形**，其中在我们 dict 里的 18,469，**已收 18,460 → 漏 9**。那 9 个（`焙る`/`産む`/`ヰスキー`…）是阶段 5d 补收 soft-redirect 词形时进库的，那时读音层早跑完 —— 又一次「收词之后没重跑」。**已重跑读音层补回，18,460 → 18,469。** ⇒ 42–50% 是**源头天花板**，不是抽漏。<br>🔴 **推翻条件（四条 AND，写成能跑的闸才算数）**：①出现可商用、无 G2P/合成、逐条可回溯的新声调源 ②核心词与 `freq_zipf` 前 5,000 两个口径**均 ≥75%** ③新旧源在 ≥500 条重叠样本上核位置一致率 ≥98% ④有脚本能自动跑出①–③，任一不过就不合并。<br>⚠️ 已知线索但**未核实授权与覆盖**：NHK 発音アクセント新辞典（商业授权）、OJAD、UniDic 的 accent 字段。**不当结论，只当线索。**<br>✅ 账的闸 P6 新增「核心词的声调覆盖率」（实测 42.43%，下限 35.0），分母被稀释会红 |
| 3 | **`counter` 译「量词」还是「助数词」** | 可读性 vs 忠于源头术语，是口味不是事实，要用户拍板 |
| 4 | **`combining_form` 的中文名** | 31 条全是 `っ` `ッ` `ー` `ん`，不是拉丁语义的 combining form。要权威出处，**别拿模型共识当证据**（`[[verify-before-claiming-confirmed]]`） |
| 5 | **`character` 的音读/训读从哪来** | 日语版把音训结构化了（`go-on` 2,261 / `kan-on` 2,137 / `kun` 4,682 / `nanori` 30），但挂在它的 `noun` 上不在 `character` 上，只覆盖约 4.7k 字；英文版有 14,644 字 ⇒ 缺口约 1 万字，可能要第四份源（KANJIDIC 之类）。**未量** |
| 6 | **`etymology_number` 在日语上的语义** | 只验证了它能分开 `猫` 的两条，没回源核过日语维基的「語源1/語源2」是否与欧语同义 |

---

## 五、阶段表

> 编号与 `DE_PLAN`/`PT_PLAN` 对齐（`-2 / -1 / 0 / 1 / 1.5 / 2 …`），跨语种才比得了。

| 阶段 | 做什么 | 状态 | 依赖 | 详情 |
|---|---|---|---|---|
| **-2** | 准备：`ja/paths.py` + `ja/dbtool.py` 写库闸门 | **✅ 已完成（2026-09-15）** | — | `paths.py` 把**三版分工**写进文件头（英文版给骨架和读音、中文版给声调和中文、日语版给活用；录音三版全废）。<br>`dbtool.py` 773 行，`de/dbtool.py` 的拷贝＋改语种特有部分（铁律①不 import 其他语种）。`TRACK` 换成日语一等字段（读音四件套＋声调＋字种等级），列现在一个都不存在 —— **建出来的那一刻闸自动开始守**。<br>🔴 **`has_han` 这个名字故意不存在**：另六门都导出它，而调用方普遍拿它当「是不是中文」用（82 处），那条等价关系在日语上恒真。拆成 `has_han_char`（原语）/ `has_kana`（**单向可靠**：带假名⇒一定不是中文）/ `is_chinese_text(s, src)`（唯一该当判据用的）。纯汉字串字形上分不出中日 ⇒ 拿不到来源时**抛异常不返默认值**（两个方向的默认都是错的）。<br>🔴 **为什么是删名字不是写注释**：从另六门拷来的脚本必然写 `dbtool.has_han(...)`，留着它会静默给出错误答案，删掉它 `AttributeError` 当场炸（`[[lesson-must-become-mechanism]]`）。<br>⭐ **变异验证 4/4**（PLAYBOOK 7.3）：`日本語`/`食べる`/`漢字を書く`/`computer` 四个「模型原样抄回」场景，**前六门的判据只逮到 1 个，ja 的 4 个全逮住**，两条真译文零误伤。<br>⭐ 顺带补了**新语种才撞得上的洞**：`snapshot()` 在库不存在时抛异常（另六门库都已存在，所以从没暴露过）。改成空库返空字典，同时加 `_ALLOW_EMPTY_PREFIX="build"` —— **只有建库 tag 允许从空库开始**，别的 tag 遇到空库说明库被误删或路径写错，静默重建比报错危险得多<br>🔴🔴 **2026-09-15 补建 `ja/tests/` 三件，而它们迟到了九个阶段。**`ja/dbtool.py` 的三道挂钩（`_ledger_gate`／`_regression_check`／`_literal_gate`）写的都是「文件不在就静默 return」—— 拷贝新语种那天它们确实不存在，静默跳过是对的。于是 ja 从阶段 -2 做到阶段 4b，**每一次写库这三道闸都什么都没做**，而日志照样打「■ 不变量核对通过 ✓」。**我盯着那行绿字做完了九个阶段，没注意到「账的闸通过 ✓」那一行从来没出现过** —— 缺一行输出比多一行红字难发现得多。<br>⇒ 补的第一件事就是把这三个文件登记成**阶段 -2 自己的交付物**：阶段 -2 已声明 ✅，少任何一个当场红。**它先逮自己**（`[[lesson-must-become-mechanism]]`）。<br>⭐ `test_plan_ledger.py` **变异验证 11/11**，而**第一轮是 10/11**，挂的那条是真 bug 不是假变异：`_declare("7")` 在全文上 `count=1` 替换，撞上的是**第二节判据表**的 `| 7 | inflection 装得下 |`（它在文件里更早），阶段表根本没被改 ⇒ 变异什么都没做，闸当然不红。正则 first-match 咬人，第四次（`[[regex-alternation-order]]`）。**逮到它的是变异验证不是我读代码。**<br>⭐ **新增 P6 覆盖率闸（ja 独有）**：按**读者口径**问三个数 —— 义项中文覆盖 ≥95%（实测 98.92%）／词元假名读音覆盖 ≥75%（实测 81.5%）／非空白页词形 ≥95%（实测 98.17%）。行数闸问「我写了多少」是自比，后面再插一批空的照样全绿；覆盖率闸的**分母会变**，稀释了就红。M4 那条变异直接重演阶段 3a 的真形状（插 20 万个没读音的新词元），行数闸全绿、P6 红 |
| **-1** | 版本探测与取数 | **✅ 已完成（2026-09-15）** | — | 三份源全量扫完，数见第一节。例句量用多点 Range 采样＋**德语校准**（预测 410,911／实际 444,094，差 7.5%）|
| **0** | 建库：`dict` ＋ v3 十四张表，**一次建成最终结构** | **✅ 已完成（2026-09-15）** | -2,-1 | `dict` **99,632 词形**（词元 86,902／只有指针义项的 12,730），加 14 张 v3 表、20 个索引，库 8.6 MB。<br>🔴 **ja 没有「迁移」这个动作** —— de/pt 的阶段 0 叫「表结构迁移」是历史包袱：它们先建了扁平的 `dict`（带 8 个 v2 遗留列），v2/v3 设计出来后才把遗留列搬进新表，de 那轮还为此做了可逆性回核 —— 后来发现**那个能力几周前就没了**。ja 的 `dict` 一开始就不建那 8 列 ⇒ 不搬、不需要回核、也不会有 de「降列导致回归闸 A2 失效」的连带。这是新语种唯一一处**比前六门占便宜**的地方。<br>**剔除口径**：原始 199,484 行 → `soft-redirect` 44,899 ＋ `romanization` 32,062（占 **39%**）＋ 词形是空白 1 ⇒ 真词条目 122,522。<br>🔴 `soft-redirect` 的词形**本步不收**：现在插进来只有词形、没有指向、没有义项，等于造 4.5 万个「搜得到、点进去空白页」。pt 正是这么栽的（**355,605 个词形＝46.2% 既无义项也无变形链**）⇒ 连同指向一起进阶段 3。<br>🔴 **不建 `COLLATE NOCASE` 索引**：SQLite 的 NOCASE 只折 ASCII，对假名完全无效，而症状不是慢是**查不到词**。前六门都有 `idx_word ON dict(word COLLATE NOCASE)`，日语照抄就是留一个假索引。折叠走 `word_norm`（Python 侧算）。<br>⚠️ `word_norm` 归一后**实测 576 组碰撞、卷进 1,159 个词头**（`あい`/`アイ`、`あ`/`ア`）。比只折假名的口径（541/1,084）大，差额来自 NFKC（还折全角半角、`（´・ω・｀）` 四种写法）——**有意保留**：那几类折叠对检索是对的，而碰撞只影响匹配不影响身份。照 es 的修法：**`word` 原样存，折叠只用于匹配**。<br>⚠️ **源头收了「空格」这个字符**（`pos=punct`，1 条）。词典里一行空白比少收一个空格更伤 ⇒ 跳过并记账；明说是**我们跳过的**，不是源头没有。<br>⭐ 顺带补了**三处只有新语种才撞得上的洞**：`snapshot()`／`_line()`／`backup()` 都假设库已存在（另六门的库都在，所以从没暴露）。第三次撞上时改成一次找全，四处一起修。`backup()` 在空库时返回 `None` 而**不伪造空备份文件** —— 那会让保留策略以为有一个可回滚的里程碑，而它什么都回滚不到 |
| **1** | `entry` 词条层 + `sense_src` 证据层 | **✅ 已完成（2026-09-15）** | 0 | `entry` **122,522**／`sense_src` **149,410**（`sense_id` 全 NULL，等 1.5 裁决）。键＝`(词形,词性,etym_no,seq)`，**src_ref 冲突 0**；三元组冲突 738 组正好由 `seq` 吃掉。八条回核全绿。<br>⭐ **读音覆盖 78,319 / 78,401 ＝ 99.90%**（含汉字、非 character）。来源：`head_templates` 72,635 ／纯假名同一性 25,605 ／`ruby` 兜底 8,494。<br>🔴 **裁决了两位专家的一处分歧**：`sounds[].other` **一条都不用** —— 它与权威读音都在的 43,096 条里只有 **66.3% 一致**，不一致的 91.7% 是长音改写（みょうにち→みょーにち），而 `馬` 的 `other` 是 `[ùmáꜜ]`（**声调标记不是读音**）。甲说能补 151 条，乙说绝不能用，**乙对**。<br>🔴 `kanji_grade` 在 **`senses[].categories`**（jōyō 3,637／kyōiku 1,692／hyōgai 13,023／jinmeiyō 1,159），顶层 `categories` 只有约 300 条。<br>🔴🔴 **罗马字判据收窄了四版才对**，每一版都是「判据比它要描述的东西更宽」：<br>　v1 不设防 ⇒ 1,730 条汉字的罗马字写成 `Fifth grade kyōiku kanji`<br>　v2 见 grade/kanji 就挡 ⇒ 误伤 7 条真罗马字（`教育漢字`、人名 `寛治`→`Kanji`）<br>　v3 全串匹配等级标签 ⇒ 仍误伤 2 条（`常用漢字` 的**正确罗马字就是** `jōyō kanji`）<br>　**v4 再加「有没有读音」** —— 罗马字＝读音的拉丁转写，没读音就不可能有罗马字 ✅<br>⚠️ **我在同一个文件的 docstring 里写了这条警告，然后照样踩了** —— 写了警告没写守卫。<br>⚠️ 而且**闸自己也犯了同一个病**：断言写成 `romaji LIKE '% kanji'`，当场报 6 条红，那 6 条全是真词真罗马字（`教育漢字`/`当用漢字`…）⇒ 断言必须和抽取器用**同一条规则** |
| **1d** | **义项标签层** `sense_tag`（**三版**） | **✅ 已完成（2026-09-18）** | 1,1.5a,**1e** | `sense_tag` **72,139** 条 / 覆盖 **35,181** 条义项（占全部义项 **11.9%**），五桶：topic 52,420 ／ register 11,935 ／ grammar 4,196 ／ usage 3,042 ／ region 546。零模型调用、零成本。<br>⭐ **分两轮做完**：先 en-edition（60,469 条 / 24,611 条义项），阶段 1e 回填了 ja/zh 的 `sense_src.sense_id` 之后**当天补上两版**（+11,670 条 / +10,570 条义项）。对照 fr 415,279 ／ es 103,030 ／ it 36,463 ／ de 23,631 ／ pt 21,164。<br>🔴🔴 **ja 是七门里最后一个建这张表的，而它空了整整一个项目**：表 2026-09-15 就建好了、`dbtool.TRACK_TABLES` 和回归闸 `TABLES` 都登记着它，**却从没有脚本往里写**。⭐ **逮到它的不是闸**（行数闸问「我写了多少」，0 行也是一致的），是用户问「库表数据情况怎样」时我去逐张表数了一遍。<br>🔴 **判据① 指针义项整条跳过**：tag 说的是「它是谁的什么形」不是「这个义项怎么用」，51,250 条带 tag 的义项里 **17,066 条（33%）是指针**，一条规则挡掉，比拉 269 个 tag 的黑名单准，也不会漏。<br>🔴 **判据② 字种/字体族不进这张表（日语特有，另六门撞不上）**：`kanji` 9,370 ＋ `Hyōgai` 4,966 ＋ `Jinmeiyō` 989 ＋ `shinjitai` 793 ＋ `kyūjitai` 621 ≈ **16,760 ＝ tag 实例的 47%**，而它们**已经在库里另有住处**（`entry.kanji_grade` 14,400 条／`sense_relation.kyujitai` 625 条）。收进来就是同一件事存两份；而且它是**字形的属性**，同一个汉字的每条义项都带着它，挂到义项上等于把字的属性复制 N 份。<br>🔴🔴 **判据③ kaikki 会把一个复合标签拆成两个 tag，单独归桶必错**：`Classical`＋`Japanese` ＝ **Classical Japanese（文語）**、`Western`＋`Japan` ＝ **Western Japan（西日本）**。单独一个 `Japanese` 挂上去，日语词典的页面上会印出一个孤零零的「日语」标签。⇒ `Classical` 留（它自己说得清「文語」）、另三个丢。**两例都是回源看样本才发现的**，看拼写永远看不出来（`[[criteria-from-meaning-not-form]]`）。<br>🔴 **判据④ `US`/`UK` 标的是英文译词的地区**：`ファスナー` 的释义是 zipper / (UK) zip fastener 而带着 `US` —— 说的是美式英语管这个叫 zipper。当成日语义项的地区标签，页面上会变成「ファスナー〔美国〕」，那是假的。<br>⭐ **drop-ledger**（`data/work/ja/ingest/tag_dropped.tsv`）：桶外的一个都不静默丢，逐个计数落盘。🔴 **它当场逮到两个真漏，而且都是「只收了一半」**：①`Kanto` —— 我按拼写写了带长音符的 `Kantō`，源头用的是不带的；②`narrowly`／`standard`／`common` —— 而我已经收了它们的反义 `broadly`(523)／`nonstandard`(50)／`uncommon`(254)。**量差三个数量级，靠读自己的表看不见** —— 写表时想的是「有哪些标签」，漏的是「这个标签的对面」。⇒ 归桶表里反义词一律并排写。<br>⚠️ 第一版 ledger 的打印写成 `if n < 20: break`，326 个实例只印了 1 行 —— **截断的 ledger 不是 ledger**，真正的漏全在那一行后面。<br>✅ **ja/zh 两版已接进来（同日）**：走 `intake_edition_words` 的**同一个循环**（先 ja 后 zh、同词只取第一次、zh 版一条 gloss 被 `clean_gloss` 拆成多条各占一个 `#k`），**构造 `src_ref` 而不反解析**，构造 237,278 条、**命中库里 154,777 条 —— 与已认领的证据行一条不差、无重复**。<br>🔴 **`real_senses` 只返回 `(gloss, tags)`，丢掉了 topics**，而 topics 在这两版是大头（ja 15,780 ／ zh 3,351 实例）。不能改它（那是收词器的判据）⇒ 并行算一份 `topics_parallel`，并断言**两者逐项等长**，不等就整条跳过并计数（**实测全量 0 条不等**）。两份过滤条件一旦漂开就是静默错配（topics 挂到别的义项上），这条断言是唯一的守门人。<br>🔴 **`POINTER` 有两份，别合并**：本脚本的含 `no-gloss`，`intake_edition_words` 的不含。ja/zh 走 `real_senses`（无 gloss 的义项由 `split_levels` 返回空 spec 时天然跳过），en 版直接按 tag 判所以要多一个。**形状不同是有理由的。**<br>🔴 **两版带来一个 en 版没有的错法：`French`／`Germany`／`Russia` 标的是「词义所涉的地名」**（`仏語`＝フランス語、`独語`＝ドイツ語、`露語`＝ロシア語、`サハ`＝俄联邦的共和国），**不是「这条义项用在那里」**。写进 region，页面上会变成「仏語〔法国〕」—— 这个词不是法国方言，它就是「法语」这个词本身。⇒ 单列一个 drop 族（62 个实例），**与 `US`/`UK` 那族「标注英文译词的地区」分开记**，两种错不一样。<br>⭐ **新补的四类都回源看过样本**：`Christian` 106→topic（`天`「神がいる場所」／`祝福`／`クリスマス`）、`rhetoric` 10→**topic 不是 register**（`メタファー`「隠喩」／`オノマトペ` 是修辞学**术语**，而共享 REGISTER 里的 `rhetoric:'修辞'` 是当语域用 —— 同码两义，靠 `kind` 分开）、`place` 79→grammar（`境`「茨城县猿岛郡的町」，这条义项**是个地名**，与 `name` 同族）、`initialism` 44→grammar（`AED`／`JD`＝女子大生，与 `abbreviation` 同族）。<br>🔴 **桶外 489→180，剩下的一个都没补** —— 它们我**没回源看过样本**。尤其 `dative`／`ablative`／`ergative` 那批语法术语：在日语词典里很可能是**在解释外语语法**（和 `仏語`＝法语同形），不看样本就归 grammar 是拿拼写猜。**判不准就留在 ledger。**<br>⚠️ **写库时闸逮到一个「第一次恰好对上」的 bug**：`expect` 是**增量**，而我写成了 `len(rows)`（总数）。首轮 en 版跑时表是空的、两者恰好相等所以过了；这次表里已有 60,469 条，闸当场报「+11,670，期望 +72,139」并直接点破「多半是把 expect 写成了总数」。**恰好对上的那一次最危险 —— 它让一个错的写法看起来是对的。**<br>⚠️ 补 `TOPIC_LABELS` 时写重了 `Jainism`／`Sikhism`（表下方早就有，那段用双引号格式所以我没查到）—— **`tsc` 的 TS1117 当场逮到**。两条值恰好一样所以行为没变，但那是一条随时会分叉的隐患。<br>✅ **展示层同日接上**（`[[it-display-layer-stage8]]`：数据进库 ≠ 读者看得见，it 那门 18,818 条及物性标签在库里躺了一个月）：`JaSenseChips` ＋ 四张新映射表（`JA_REGISTER_LABELS`／`JA_REGION_LABELS`／`JA_GRAMMAR_LABELS`／`JA_USAGE_LABELS`，**全部放在 `dict-labels` 包里，`App.tsx` 一条没加**）＋ `.sense-chip.use` 样式。**七门标签映射闸 `gate:tags` 已接入 ja，五桶 100%、topic 448/448**。<br>🔴🔴 **敬语三分必须走覆盖层**：全局表把 `honorific` 译「敬称」、`polite` 译「礼貌」，而日语这三个是**语法范畴**（尊敬語／謙譲語／丁寧語，与 `humble` 构成闭合三分），印成「敬称／礼貌」会把一个语法体系讲成语气强弱，学习者据此是学不对的（`part`「小品词 vs 助词」同款）。<br>🔴 **与词头徽标互补，判据是量出来的不是照 it 抄的**：`suru` 379 条里 **295 条（77.8%）** 词头已印サ変活用类，两处说的是同一件事 ⇒ 去重；而 `transitive`／`intransitive` 703 条**照印** —— `vclass` 说的是「怎么活用」（五段/一段），不说「带不带宾语」。**照抄 it 那门（意语词头直接标 t/i）会吞掉 703 条日语最有用的标签。**<br>⭐ **展示层契约 54 → 60 条，变异 6/6 全逮到**；`usage` 桶是 ja 第一个用的（拟声拟态词 633 条，混进 register 会被「口语」「俚语」淹掉，而那是日语的显著特征）。<br>⚠️ 顺带修了跨语种闸一条**误报**：`tag_coverage_all` 拿「译名与原码不同」当「已覆盖」的判据，而 `BDSM`／`NASA` 的中文名就是英文缩写本身（中文里就这么写，**不译才是对的**）。共享表里这样的有 11 个（demoscene/Unix/IRC/Linux/CAD/DVD/Windows/CSS/Lisp…），en 379 条实例、ja 12 条。⇒ 判据改成「**在映射表里登记过**」—— 登记本身就是一个决定，哪怕决定是保持原样 |
| **1e** | **证据层→出版层的认领**（回填 ja/zh 的 `sense_src.sense_id`） | **✅ 已完成（2026-09-18）** | 1d | 回填 **154,777** 条，`ja-edition` 84,773／84,783 ＝ **100%**、`zh-edition` 70,004／70,029 ＝ **100%**（缺的 35 条是出版层**有意不出版**的，见下）。零模型调用、零成本。<br>🔴 **这两版为什么一直是空的**：不是漏了一步，是阶段 3a 的 `intake_edition_words` **插入时就写死了 `NULL`**。它在**同一个循环**里同时攒 `srcs` 和 `senses`，两边下标天然对齐，所以当时不回填也能把 `sense_gloss` 挂对（`JOIN … ON s.word_id=g.word_id AND s.rank=g.rank`）。⇒ 代价是证据层与出版层之间**没有桥**，任何「从证据行找到它出版成哪条义项」的活（阶段 1d 的义项标签就是）在这两版上都做不了。<br>🔴 **配对判据从建库代码推出来，不是从数据猜的**：`intake_edition_words.py:241-250` 那个循环里 `src_ref` 的 `#k` 与 `sense.rank = k+1` 一一对应 ⇒ 判据是 `(word_id, #k + 1)`。**只取 `#` 后面那一段，不反解析整个 `src_ref`**（`build_entry_layer` 文件头记着：反解析那版在词形带冒号时 1,866 条只对上 1,091 条，**对不上的 775 条不报错、只静默漏修**）。前提量过：词形含 `#` 的 **0 个**，154,812 条各含且仅含一个。<br>⭐ **全量回核比的是内容不是条数**（`[[primary-key-is-not-enough]]`：主键保证认领得上，不保证配对对；计数型闸对错配结构性失明）：拿 `sense_src.text` 与 `sense_gloss.text` **逐条比**。<br>　　150,762 文本完全一致 ｜ 3,985 文本不同 ｜ 30 该义项没有这个语言的 definition ｜ **多配 0 条**<br>🔴 **那 3,985 条一条都不是错配，全是 `sense_gloss` 被改过的痕迹** —— 证据层永不编辑，而出版层修过两轮：繁简转换（记账本 ⑦）3,978 条 ＋ 台湾用词（记账本 ⑩）7 条。**串起这两个转换器再比：3,985 / 3,985 ＝ 100.00% 相等，0 条剩余**（两个转换器都 import 生成侧那一份）。<br>⚠️ **差点在这里栽一跤**：`fix_traditional_to_simplified.convert()` 返回的是 `(新文本, 被保护片段数)` **元组**，我拿整个元组去比字符串，得到「0% 相等」⇒ 差点据此判定"配对全错"、去做一个不存在的修复。**救我的是两个信号打架**：样本肉眼看就是繁简差异，而数字说全错（`[[measure-landing-not-source]]`：新数字先假设我的度量错了）。<br>⭐ **配不上的 35 条全部说得出理由**：30 条 `ONLY_BRACKET` 命中（整条只是【异写列表】，`fix_gloss_residue` 删的）＋ 5 条文本是空串（回归闸 A2 盯的那一类）。**「其他」＝ 0** —— 残差桶必须是空的，它是残差不是度量（`[[residual-bucket-is-not-evidence]]`）。<br>🔴🔴 **写后回核逮到一条躺了整个项目的坏数据**：全库有 **1 条悬空认领**（`kk-ja:和:character:2:0#0`，文本是 wikitext 残渣 `===Etymology 4===`）—— en-edition 阶段 1.5a 认领过，后来那条出版义项被清洗删掉，而引用没跟着清。**此前没有任何一条断言问过「认领指向的义项还在不在」**，所以它一直在。已置 NULL（那才是事实：这条证据没有编入出版层）。<br>🔴 **`sense_src.sense_id` 已列进 `dbtool.TRACK`** —— 此前只有 en 非空、ja/zh 全 NULL，这一列没有守的价值；三版都有意义之后才列。⭐ **列进去的第一件事就是它当场拦住了本脚本**：清悬空让这一列 **−1** 而我没在 `expect` 里声明，写库被拒。**那正是这道闸的用处。**<br>⚠️ 字面量闸也报了一次：我把 `141,773` 写死在断言里 ⇒ 改成「**跑之前量的那个数**」，判据锚在「前后不变」上不锚在具体数字上（写死的期望值会过期，过期之后要么假红、要么被人调大一次就永远绿了）。<br>⭐ **回归闸新增 J1/J2/J3，变异 3/3**：J1 悬空引用（非计数型）／J2 认领到别的词（错配那一面）／J3 ja/zh 的认领整批消失 —— **J3 是必须的**：回填之前那两版全是 NULL，那种状态下 J1/J2 **恒为 0**，两条都绿而桥根本不存在 |
| **1f** | **词源正文层** `etymology`（七门共用一个脚本）| **✅ 已完成（2026-09-20）** | 1 | `etymology` **40,270** 行 / **36,607** 个词形。零模型调用、零成本 —— 源头本来就有，是我们没抽。<br>⭐ **覆盖率 36.74%**（36,607 / 99,632 个有英文版词条的词形）。上限就在这儿：英文版 dump 里只有 **46,196 个条目（23.2%）**带 `etymology_text`，**日语版与中文版各 0 条** ⇒ 涨不上去正常，掉下来才是回归。<br>🔴🔴 **这一层整个缺席了三个月，而阶段表全 ✅、四道闸全绿。** `scripts/ingest_etymology.py` 2026-09-14 就有，ja 09-15 开建却没被加进它的 `LANGS` —— **阶段表对「我没列进阶段表的层」结构性失明**，与 `sense_tag` 空了整整一个项目是同一个形状（行数闸对 0 行是一致的）。逮到它的是收尾时**逐个点名核对六门共有的层**。<br>🔴 **「落点 40,270 < 源头 46,196」这句差点被我写成「剔除口径」而蒙混过去** —— 逐条回源分桶之后（非抽样）才是真的：<br>　源头带正文 46,196 条 → **落库 44,952 条＝97.3%**；落库的条目合并成 **40,270 行**是因为同一 `(词形, 词源号)` 下的**多个词性共用一支词源**（`促` 的 kanji 与 n 两条词条同属词源 0）——**是合并不是丢失**。<br>　没落库的 1,244 条：**1,049 条源头本身是指针条目**（`form_of`/`alt_of`，如 `EU`/`ソ連`）、1 条词形整个不在库、**194 条是真漏**（见欠账 25）。<br>⚠️ 我第一版在本行写的是「差的 5,926 条是 romanization 与 soft-redirect 索引页」——**那是我按阶段 0 的剔除口径推的，一条都没验**，而真实分桶里这两类一条都没出现。`[[measure-landing-not-source]]`：新数字先假设我的度量错了。<br>🔴 **照搬 de 的取法会静默对不上**：ja 走 `has_entry` 那一支，`db_keys()` 取的是 **`entry.src`**（`en-edition`），不是 `src_ref` 的前缀（`kk-ja`）。我一度按 de 改，被脚本自己的前缀核对当场挡下。<br>🔴🔴 **接上展示层之后又栽了一次「两边各自自洽、拼起来对不上」**：服务层的 etymKey 一度只取裸词源号（`1`），而 `etymology.edition` 写的是 `en-edition` ⇒ **40,270 行一条都到不了页面，而且一声不吭** ——入库对齐闸、写后回核、tsc **全绿**。⇒ 回归闸 Z3 与契约闸的判据都改成站在**页面**上问（`[[correct-steps-can-compose-a-hole]]`）|
| **1.5a** | 出版义项层 + **免费**中文/日语释义 | **✅ 已完成（2026-09-15）** | 0,1 | `sense` **141,773**（指针 7,637 条不出版，归阶段 2）／`sense_gloss` **174,375** ＝ en 141,773 ＋ **zh 13,250（9.3%，零模型调用）** ＋ ja 19,352（13.6%）。六条回核全绿。<br>🔴🔴 **相关子查询把写库卡死 7 分钟**：回填 `sense_src.sense_id` 第一版写成 `UPDATE … (SELECT … WHERE l.src_id=sense_src.id)`，临时表上没有 `src_id` 索引 ⇒ 每行全扫 141,773 行 ⇒ 2×10¹⁰ 次操作。杀掉回滚干净（sense 0／认领 0／integrity ok）。改成**先物化成以 `src_id` 为主键的映射表**再回填，**22.8 秒**。⚠️ 这正是 `PITFALLS` F 组的「相关子查询入口」，我整场都在引用它然后自己写了一条。<br>🔴🔴 **`dbtool.has_kana` 有 bug，而且它做过变异验证**：按 U+30A0–30FF 整段收，把 `・`（KATAKANA MIDDLE DOT）当成了假名 ⇒ `（古語・雅語）十一月` 这条纯汉字日语被判成"带假名"。连带影响 `is_chinese_text`：`中文・英文` 会被判成不是中文。**变异用例里没有标点**，所以验了也没验到。已收窄到真假名区间。<br>🔴 **`free_ja` 的判据从形式改成来源**：第一版"带假名才算日语"漏掉 **9,823 条（5.91%）纯汉字的日语定义**（`現代、現在。`／`顔色。表情。`）。日语版为日语词条写的定义**就是日语**，这是来源说的，不用从字形猜 |
| **1.5b** | 义项翻译（付费） | **✅ 已完成（2026-09-15）** | 1.5a | ⭐ **中文覆盖 139,875 / 141,773 ＝ 98.66%**（对照 de 99.83%／fr 99.9%／pt 99.68%）。落库 126,625 条，`src='model:deepseek-v4-flash'`。<br>**实跑**：2,143 批 / **848 万 token** / **零失败** / 约 11 分钟。单价 **66.2 token/条**（pt 是 61，高出的部分是 payload 多带了 `kana` 和 `ja` 两字段）。<br>💰 **折算 ≈ 20.6 元**（848 万 token × 2.43 元/M 半价；单价按 pt 实测反推：366 万 token ＝ 8.9 元半价 ⇒ 2.43 半价 / 4.86 全价）。**账单未回查，这是折算不是实付。**<br>🔴🔴 **2026-09-17 更正：本行原写「跑在非空闲时段…约 41 元，等到 00:30 之后跑约 21 元」，那是按错的窗口算的。**正确口径是**高峰＝周一至周五北京时间 9:00–12:00、14:00–18:00，其余（含周末）全是空闲、半价**；「00:30–08:30」是 DeepSeek 早已下线的旧折扣窗口。本轮实跑 **2026-09-15 周二 21:24–21:35**，**本来就整段落在空闲时段、当时就是半价** ⇒ 不是「本该省一半没省」，是**我按错窗口把账记贵了一倍**。⚠️ 当时我还据此向用户「已当面说明会贵一倍」——**报价报错了，而且是往贵里报**。⇒ 已做成机制而不是第四份笔记：`ja/pipeline/ds_batch.py` 的 `is_peak()`／`announce_window()` 在 `run()` 里**无条件播报**，自检 12/12 覆盖全部八个边界＋两个周末（`[[deepseek-pricing-window]]`）。<br>**质量**（控制组 + 逐条读）：真失败 **1.59%**，其中 E0「模型判定给不出」占 1.47% ⇒ **真正译错 151 条 ＝ 0.12%**。<br>🔴 **切片与全量高度一致**（切片 2.24%／7.25%／90.5% vs 全量 1.85%／7.44%／90.7%）⇒ 1% 切片是有代表性的，那两毛钱花得值。<br>🔴🔴 **六条判据全部收窄过一轮，去掉 338 条假红** —— 逐条读发现和 de 那轮一样「红的多半是判据宽」：<br>　E1 词条讲的**就是假名**时译文带假名是对的（`カ行`→「か行」、盲文条目）153→58<br>　E2 源头的"英文 gloss"里有一批其实是汉字写法（`しゅうじ` 的 gloss 是 `修辞`）269→67<br>　E3 `C++`/`DJ`/`RISC` 原样保留是 SYS 规则 8 要求的 86→33<br>　E4 **功能词的词义就是它的语法功能**（`て`→「接续助词…」）17→5<br>　E5 叹词/助词的问号**是中文的一部分** 19→3<br>　E6 学名原样保留 27→9<br>⚠️ 残余 151 条抽 10 条读，**多数仍是判据宽**（`C++`/`Windows`/`ま行` 都对），真错估计 30–50 条 ≈ 0.03% ⇒ **停在一轮收窄，残差当上界报**，不拿控制组当过滤器（会把正确译文一起丢）。<br>⚠️ W1「译文与词头同形（纯汉字）」7.45% **无法用形式判定**，只报数不当红。<br>✅ 控制组 `ja/pipeline/ctrl.py` 变异验证 11/11；对照组重现旧判据在日语上恒绿 | 要买 **128,531 条 ≈ 19 元**（pt 实测 0.000148 元/条）。<br>✅ 控制组判据已备好：`ja/pipeline/ctrl.py`，**变异验证 9/9**。E1 残留假名／E2 同形回抄／E3 无汉字／E4 元描述／E5 句末标点／E6 残留西文／E7 过长。<br>🔴 对照组重现了**旧判据在日语上是瞎的**：`日本語`／`食べる`／`漢字を書く` 三条原样抄回，前六门的判据**全判合格**，新判据全逮住。<br>⚠️ 开跑前还要：① 先跑 **1% 切片逐条读**（`FR_PLAN` §五.1）；② 排到空闲时段（pt 实测半价）| **1.5a** 免费段：按 1:1 规则收中文版的中文 **13,242 条**（零模型调用），再收日语版原文释义。<br>**1.5b** 付费段：剩 **128,531 条**送翻译，按 pt 实测单价 **≈ 19 元**。<br>🔴 **不做更好的对齐**（省 4.1 元 vs 义项错配风险），依据见 §四.1。<br>⚠️ 开跑前照 `FR_PLAN` §五.1 两条办：① 先跑 1% 切片逐条读；② **输出有几个字段，控制组就要有几组判据**（`[[control-must-cover-every-output-field]]` 那次控制组只量了一个字段，烧掉 418 万 token 作废）。<br>🔴 日语的控制组**不能照抄前六门**：`has_han` 判据在这门语言上恒真，要换成**假名残留检测**，见 §三② |
| **2** | 变形层（活用）| **✅ 已完成（2026-09-15）** | 1 | `inflection` **567,717**／`dict` 99,632 → **551,857**（变形词形 is_lemma=0，照 de/fr/pt 的设计）。零模型调用。<br>⭐ **「点进去空白页」的词形只有 2.26%**（12,445 条）—— 对照 **pt 那轮 46.2%**（355,605 条，阶段顺序错导致）、fr 1.8%。<br>🔴 **入口判据重写过三轮，每轮都是抽样反验逼出来的**：<br>　v1「forms 有 tags 就收」⇒ 113 万行里只有 45.3% 是真形态，会把 20.3 万虚报成 35.7 万<br>　v2 按 tag 分类 ⇒ 抽样逮到 `尾籠` 的「敬体否定」给成 `birō de wa arimasen` —— **86,237 行罗马字没打 `romanization` 标签**，占收进来的 20.3%。改用**内容判据**：日语活用形按定义写成假名或汉字，纯拉丁串不是日语词形<br>　v3 日语版的 tag 词汇更丰富（29 种没映射）⇒ 240,079 行拼不出中文说明<br>🔴 **活用类不是这个「形」的属性**：日语版给每个形都打 `sa-row`（サ行変格）/`godan`/`irregular` ——那是**这个词**的属性，照译会让每行都拖着「サ行変格」。归进 `CLASS_TAGS` 不出字；它该落 `entry` 的一列（像 de 的 `vclass`），**本轮没落，见欠账 4**。<br>✅ `infl_compose` 自检 8/8，**变异验证证明 `ORDER` 真在起作用**（打乱后「使役被动否定过去」变成「使役否定过去被动」）。全量组合逐个跑过，拼不出的补进表而不是丢掉（`-tari`）。<br>⚠️ **`base_id` 悬空 221,135 行 ＝ 38.95%**，指向 **14,658 个原形**，全来自日语版 —— 那是**阶段 3 收词的信号**（日语版有 1.4 万词元我们没收）。<br>🔴🔴 **阶段 3 收完词必须回头重连 `base_id`**（`base` 是原样存的文本，一条 UPDATE 即可，不用重跑整层）。pt 正是栽在这个顺序上。<br>🔴🔴 **2026-09-19 整层重建（见欠账 21）**：英文版的活用行不是平的，是表格，而 wiktextract 在**行列两个方向上都有表头识别失败**。本阶段原来扁平遍历 `forms`，**看不见表**，于是把源头的错原样印上了页面（`食べましょう` 印「敬体命令形」、`食べます` 与 `食べました` 同印「敬体」、`食べない` 根本不在库里）。新增 `pipeline/infl_table.py` 做表结构修复，本阶段改成**幂等的清空重建**。<br>⇒ `inflection` 567,717 → **573,105**（本阶段拥有的行），新增（原形,词形）对 4,425、**消失 0**。<br>🔴 重建前必须对账：`inflection` **不止本阶段一个写入方**（`fixes/recover_pointer_senses.py` 的 375 行也在这儿），`OWNERS` 登记表 + 「对不上账就停机」的闸，否则 `DELETE FROM inflection` 会静默撤销另一次修复。<br>🔴 `src_ref` 补上了「同 (词,词性) 的第几条」—— 原来 5,155 组重复（`[[primary-key-is-not-enough]]`） |
| **3a** | 收词（跨版）| **✅ 已完成（2026-09-15）** | 1,2 | `dict` 551,857 → **679,062**（词元 86,902 → **217,781**，+150%）／`entry` +127,205／`sense` +154,812／`sense_gloss` +154,792。零模型调用。<br>⭐⭐ **`base_id` 悬空 221,135 → 988，重连了 220,147 行**。阶段 2 建变形层时这些原形还不在库里；收词补进来之后一条 UPDATE 连上。🔴 **pt 正是栽在这个顺序上** —— 它变形层建在收词之前，收进来的 30 万变形从此没人连线，355,605 个词形（46.2%）成了空白页。ja 的空白页率 2.26% → **1.83%**（fr 1.8%）。<br>**收的口径**：日语版 66,798 ＋ 中文版 60,407 ＝ 127,205 个新词形；另 3,674 个我们当**变形**收着而别版说它有真义项 ⇒ **升格 `is_lemma` 0→1，不新建行**。<br>🔴 **同词两版都有时取日语版，不做跨版义项配对** —— 那是义项错配那一类灾难，而 1.5a 已量过：更好的对齐最多省 4 元。<br>🔴🔴 **中文版的 gloss 带结构残渣，清洗器写了三版才对**（147,338 条里干净只占 71.84%）：<br>　v1 `startswith(词头)` 就剥 ⇒ `基本システム【きほん system】` 被啃成「きほん system】」<br>　v2 整行恰好是「词头【假名】」才剥 ⇒ `鈹\n（表外漢字）` 洗完剩「鈹」，那是存根不是释义<br>　**v3 再加「有没有别的结构行被删掉」** ✅ —— `英語`→`英語` **留**（同形汉语词），`鈹`→`鈹` **丢**（汉字存根）。两者字面一样，区别在后者还有一行被当结构删了<br>⚠️ **【】不一定是读音**：80 条里它是学科标签或用法注（`【數學】【化學】 公式`、`忽略，放松【多用「お～になる」的形式】`），不能剥。<br>⚠️ **4,576 条（3.1%）一格塞了 ≥2 条编号义项**（`整理【せいり】\n名·他サ\n1. …\n2. …\n3. …`）⇒ **拆成多条义项**，不是拼成一串。<br>⚠️ 顺带洗掉 1.5a 已落库的 32 条残渣（12 条洗好、20 条整条丢弃）。<br>🔴 **代价：中文覆盖 98.66% → 70.77%**（新收的 66,798 个词只有日语原文释义）⇒ 阶段 3b |
| **3b** | 新词义项翻译（日译中，付费）| **✅ 已完成（2026-09-15）** | 3a | 86,701 条（日语原文 84,815 ＋ 英文 1,918）。<br>🔴 **和 1.5b 不是同一道题**：那轮英译中，这轮**日译中**。SYS 另写一份，点名三个同形陷阱（`勉強`＝学习不是勉强／`手紙`＝信不是手纸／`大丈夫`＝没问题）；控制组的 **E1「残留假名」在这一轮是主力判据**（抄回原文最省事也最没用）。<br>🔴 **没拿 1.5b 的单价套**（`[[prove-free-path-before-quoting]]`）：自己跑了 1% 切片（843 条 / 53,552 token）⇒ **63.5 token/条**（英译中 66.2），全量 ≈ 550 万 token。<br>⭐ 切片质量：真失败 **1.54%**（E0 留空 1.42%）／警告 7.59%／干净 **90.9%**；逐条读确认同形陷阱避开了（`受験勉強`→应试学习、`通昔`→通宵）。<br>**实跑**：85,648 条 / **532 万 token** / **零失败**（切片外推 550 万，差 3.2%）。落库 83,488 条，`src='model:deepseek-v4-flash:from-ja'`。<br>💰 **折算 ≈ 12.9 元**（532 万 token × 2.43 元/M 半价）。实跑 **2026-09-15 周二 21:35–22:14**，在空闲时段内 ⇒ 半价。**账单未回查，折算不是实付。**<br>🔴 **2026-09-17 补：本行原来只记了规模、token 和质量，价格一栏是空的** —— 三轮付费跑批里唯一一笔没落价的。**记了 token 不等于记了账**：token 是我自己的日志能打出来的数，钱要再乘一个「当时是不是高峰」，而那一步恰恰是最容易记错的（见 1.5b 那条更正）。<br>⭐ **中文覆盖 70.77% → 98.92%**（293,367 / 296,580）。<br>⭐ **全量质量比英译中那轮还好**：真失败 **1.98%**（E0 留空 1.56%）／警告 5.33%／干净 **92.7%**（1.5b 全量是 90.7%）—— 与 1% 切片高度一致，**切片有代表性**这条在日语上第二次成立。<br>🔴 **顺带逮到 5 条空释义，不是模型的是我的**：阶段 3a 拆编号义项时，`1.` 这种只有编号没内容的行，去掉编号就剩空串。生成侧已修（`intake_edition_words.clean_gloss` 拆完过滤空串），已落库的 5 条连同变空的 5 条义项一起删。<br>⚠️ **判据必须在生成侧和落库侧各查一次**：这 5 条是落库之后逐条读才看见的，生成侧当时没有「拆完可能为空」这个念头（`[[it-regression-gate]]`：写入列与读取路径各查一次）|
| **4** | 读音层（假名 / 罗马字 / 声调 / IPA）| **✅ 已完成（2026-09-15）** | 1 | `pronunciation` **118,796** 行。零模型调用。五条回核全绿。<br>🔴 **专家的两条断言我自己回源核过，都成立**：日语版的声调 **100% 是废的**（10,215 条里 10,212 条 `other` 字面是 `"("`，重音位置全丢）；**中文版是声调唯一可用的源**（27,487 条，100% 带 `roman` 标记，东京式占 99.75%）。<br>⭐ **重音核（`pitch_pos`）自己推的与源头的类型标签一致 27,475 / 27,487 ＝ 99.96%**，不一致的 12 条**只存 `pitch_mark` 不存 `pitch_pos`** —— 推不准就留空，不写一个看起来像数据的猜测。<br>🔴 **不设 `pitch_type` 列**：平板/中高/头高/尾高由「核位置 + 拍数」唯一决定，存它就是存一个会和 `pitch_pos` 打架的冗余列。拍数计算要处理 `ん`（独立一拍）和促音（`tt`/`tch`/`'`）|
| **4b** | **读音回补**（收词之后重跑）| **✅ 已完成（2026-09-15）** | 3a,4 | `entry.kana` 补 **98,224** 行 ⇒ **词元读音覆盖率 39.8% → 81.5%**。零模型调用。<br>🔴🔴 **这一步本来不该存在，是阶段 1 的数过期了没人发现。** 阶段 1 报的读音覆盖率 99.90% 对当时的库成立；阶段 3a 收词之后库里多了 13.4 万个**一个读音都没有**的词元，覆盖率静默掉到 39.8%，而**没有任何一道闸会响** —— 阶段 1 的写库闸门锁的是「当时写进去多少行」，锁不住「后面又插进来多少行没有」。<br>⚠️ **同一个形状第二次**：pt 那轮是「收词之后必须重跑变形层」（本表阶段 2/3a 都在引用它），这轮是「收词之后必须重跑读音层」。我引用了三次那条教训，然后在**另一个层**上原样重犯 ⇒ 教训写成文字只能守住它点名的那一层（`[[lesson-must-become-mechanism]]`）。本步的闸改成按**词元覆盖率**问，不按「本步写了多少行」问 —— 后者是自比，下次收词之后它照样全绿。<br>**三个源**：`identity` 37,396（词头本身是假名）／`ja-edition` 37,381（日语版 `forms[tags=transliteration]` 里**唯一的裸条目**）／`zh-gloss-head` 23,447（中文版 gloss 抬头 `大臣【だいじん】` 的括号内容）。<br>🔴 **优先级按「确定性」排，不按「信息量」排 —— 我第一版排反了。** 原来是 ja-edition > zh-gloss-head > identity（按源头多有料）。实测交集：`しお` 日语版给 `しほ`（**历史假名遣**）、`ブラスバンド` 给 `すいそうがくだん`（**近义词**吹奏楽団的读音）、`ワンゲル` 中文版给 `ワンダーフォーゲル`（**全称**不是读音）。交集小（18 个）但错得整齐 ⇒ 不是噪声，是这两个源**在假名词头上系统性地装别的东西**。而 `identity` 是同义反复、**不可能错**，却被排到最后覆盖掉。<br>⭐ **拿独立锚量过才敢写**（`[[external-anchor-gates]]`）：用英文版 `head_templates` 的读音当锚（与本轮三源完全独立）—— 日语版可比 20,584 命中 **97.24%**，中文版可比 7,438 命中 **98.29%**。不命中的绝大多数是**真实异读**（`今日` きょう／こんにち、`九月` ながつき／くがつ 都对），真错的是源头笔误那一小撮（`位置付ける` いちっける ← づ 写成 っ）。<br>🔴 **有意不做的两件事**：① **多读音词不猜** —— 日语版给 ≥2 个裸 transliteration 的 1,766 个词形（`今日`→こんにち／こんち／こんじつ）整批跳过，`entry.kana` 只有一格，挑一个＝把硬币翻面当数据写进去；② **不补 `romaji`** —— 罗马字的规矩是存源头的不自己算（自己算与源头一致率仅 64.27%），三个源都只给假名，闸直接量「写库前后 `romaji` 非空数有没有变」。<br>⚠️ 那条闸第一版我写成「`kana_src` 不是 identity 的行里 `romaji` 为空」—— **形式代理，而且代理还不成立**：`identity` 阶段 1 就用过（25,605 行），本步写的和阶段 1 写的在列里长得一模一样，按 `kana_src` 根本分不开。<br>🔴 **删一个东西之前先问它里面有没有别的层要的料**：`zh-gloss-head` 这 2.3 万个读音，阶段 3a 当「结构残渣」洗掉了 —— **洗对了**（它确实不是释义），但洗之前没把里面的读音收走。<br>**剩下的 18.5% 缺口是真缺**：`pos=kanji` 12,833（单字本来就没有单一读音）＋ `unknown` 13,321（`あか抜ける` 这类假名汉字混排、三版都没给读音）＋ 425 条**盲文**（⠁⠷⠵，正确地没有假名读音）|
| **4c** | **汉字音訓読み层** | **✅ 已完成（2026-09-18）** | 1,4b | `kanji_reading` **7,272** 行 / **1,519** 个汉字。零模型调用、零下载、零成本。⭐ **常用汉字覆盖 53.5%**（1,115 / 2,085）。<br>🔴🔴 **这一层本该在阶段 4 就建，漏掉它的是 §二 判据 4 的后半句。** 那条写着「不存在 15% 读音缺口，缺的 14,161 条全部是 `pos=character`，汉字作为字本来就没有单一读音」——前半句对，后半句从「没有**单一**读音」滑到了「**不用补**」。汉字有的是一组**分类**读音：`青` ＝ ショウ(呉音)／セイ(漢音)／チン・シイ(唐音)／あお・あお-い(訓)／あをし(古訓)，**日语版 dump 写了，我们没抽**，而 `青` 的 character 条目在库里 `kana`/`romaji` 两列全空。<br>⭐ **它伪装得比一般的漏抽好**：前半句真、量过、还带一个具体数字（14,161），整条读起来像已核实的结论。而判据写在账上**没有交付物，闸结构性地管不到它**（`[[record-the-negative-decision]]`）⇒ 已做成 P6 覆盖率闸，有分母、掉下来会响。<br>🔴 **日语版是唯一来源**：英文版 14,970 个 `character` 条目的 `forms` 里只有 `romanization` 1,732／`kyūjitai` 720／`shinjitai` 633，音训读**一条都没有**，`青` 的 `head_templates` 和 `sounds` 都是 `null` ⇒ 不做跨版合并。<br>🔴 **判据打在 `tags` 上，假名字形只做验证。** 日语惯例是「音読み写片假名、訓読み写平假名」，拿它当判据就是形式代理 —— 交叉验证 7,272 条里不符 **14 条（0.19%）**，逐条读**全部是源头对、惯例错**：`法→フラン`(法郎)／`瓦→グラム`(瓦)／`頁→ページ`(页)／`独→ドイツ`(独逸)／`打→ダース` 是**当て字借词的训读**，用片假名写但功能上是训读。**要是拿字形当判据，这 14 条会全判反**（`[[criteria-from-meaning-not-form]]`）。<br>🔴 **`nanori`(名乗り) 不并进訓読み**：它是人名专用读音，并进去等于断言「なのり 可以当普通训读用」。`joyo` 反过来**不是第七个类别**，是正交标记 ⇒ 单独一列 `is_joyo`。<br>🔴 **送假名的连字符是信息不是噪声**：`い-きる` 标着「`生` 只读 `い`」，剥掉就把「这个**字**读什么」和「这个**词**读什么」混成一件事（1,498 条带连字符）⇒ 原样存 `kana`，另拆 `kana_stem`/`okurigana` 两列。<br>🔴 **主键当场逮到一个 0.01% 的 bug：我文件头写着「`joyo` 是正交标记不参与身份」，然后把它放进了去重键。** 全量只有 **1 组**撞上（`弁/ベン/呉音` —— `弁` 是辨・瓣・辯三字并一的新字体，源头一条标常用一条没标），`UNIQUE(src_ref)` 当场 rollback。⇒ 身份键 `(词,读音,kind,subkind)`、`is_joyo` 取 **OR**（少标是漏、多标才是错）。**0.01% 的冲突率靠抽样看不见**，逮到它的是主键不是我读数据。<br>⭐ **缺口归属逐字验过，零漏抽**：缺的 956 个常用字里 **953 个日语版根本没有条目**（源头没写），`評` 那 2 条源头没给分类标记、属 `entry.kana` 那一层（其中 `ひょう` 已在库），剩 2 个有条目无 `forms` ⇒ 这次没有把「我们没抽」说成「源头没写」。<br>⭐ **变异验证 8/8**，其中 M22 是关键一条：「重跑时顺手把送假名连字符剥了」**条数一条不少**，计数型判据全绿，只有 K3 的可逆性回核（拿库里的 `kana` 重跑生成侧 `split_okuri` 逐条比）逮得到（`[[primary-key-is-not-enough]]`）。<br>🔴 **M27 暴露了闸自己的一个洞**：`DROP TABLE kanji_reading` 之后 K 组查询直接抛异常，**整个 `build()` 崩溃**，而 L1「表被 DROP 掉了」明明已经算出来却打不到屏幕上 —— **崩溃比报红危险**，它看起来像环境问题而不是「有人删了一层数据」。⇒ K 组改成表不在就跳过，L1 负责报（`[[correct-steps-can-compose-a-hole]]`）。<br>✅ **展示层已接（同日）**：`JaKanjiReadings` 组件 ＋ `dict-labels/src/ja.ts` 的三张表（`JA_KANJI_READING_KIND`/`SUBKIND`/`ORDER`，**映射表只放在这个包里**，`App.tsx` 一条没加，`[[dict-labels-package]]`）＋ 6 个 `ja-kr-*` 样式类。<br>🔴 **分区按 `kind` 三分，不把 `subkind` 摊平**：读者第一眼要的是「音读还是训读」，呉音/漢音/唐音是第二眼的事。🔴 **送假名分色**：`あお-い` 里只有 `あお` 是这个字的读音，同色印出来读者分不出断点 —— 连字符不印，用结构和颜色表达。🔴 **朗读读 `kanaStem` 不读整串**（`い-きる` 读 `い`）。<br>⭐ **契约闸 +8 条，变异 7/7 全逮到**，其中两条锚在数据上（「库里每个 kind/subkind 都有非空中文名」），源头将来多一档会自己响；还有一条**反向**断言：普通词不许印字音区（这是字的属性不是词的）。展示层契约 46 → 54 条全绿，布局闸 562 条义项合规，CSS 0 孤儿类名，`青` 查询中位 1.15 ms。<br>🔴🔴 **同日事故：`packages/dict-core/src/japanese.ts` 被 `git checkout` 冲掉了两天的未提交改动。**起因是跑展示层变异验证时用 `git checkout <file>` 撤销变异 —— 而那个文件**本来就是 dirty 的**（9-17/9-18 改过），那条命令把它一起还原到了 9-16 的 HEAD，不可逆（stash／IDE 本地历史／Time Machine 全查过，无副本）。<br>⇒ **按契约闸 54 条断言 ＋ `App.tsx` 的消费形状重建**，全部跑绿。⭐ 最关键的 `accentSkeleton`（声调归位）重写后拿实测复核：多读音声调行**唯一匹配 1,125 / 1,189 ＝ 94.6%**，与原注释记的 1,124 / 1,187 ＝ 94.7% 落在同一个数上 —— **独立重建撞上同一个测量值**，才敢说行为等价，而不是靠"闸绿了"。<br>⚠️ 重建的是行为，**注释里的推导过程不是原文**，已在文件头标明。教训：**撤销自己的改动前先确认那个文件原本干不干净**，dirty 文件只能先 `cp` 备份再动（`[[git-checkout-destroys-dirty-files]]`）|
| **5** | 例句 / 关系 / 频次层 | **✅ 已完成（2026-09-16）** | 1,3a,4 | `example` **53,309**（29,218 条译文＝**免费中文 8,586** ＋ 英文 20,632）／`sense_relation` **357,490**／`freq_zipf` **67,881**。零模型调用。<br>⭐ **例句实际有 6 万条，不是 §一 估的 26,352** —— 那个估算**只量了一版**。三版都有例句，而且**中文版白送 8,288 条中文译文**（英文版给的是英文译文）。<br>⭐ 例句给 `example` 加了两个日语一等字段 `roman`/`ruby`（24,107／24,009 条）——振假名对日语学习者的价值比任何一门拉丁语言都高，不该丢在 dump 里。<br>🔴🔴 **de 的两条筛选判据一条都不能照抄。** de 判据②「词不出现在句中 ⇒ 挂错了词」照搬到日语会丢 **6,539 条（11%）**，而那 6,539 条绝大多数是对的：`痛い`→「痛っ！」是**活用了**、`いろ`→「色をなす」是**假名词头用汉字写**、`国`→「どうして？」是漫画**多行引文**。而且判据里的 `.lower()` 在日语上**是空操作**（只折 ASCII）—— 看着通用，实际只对拉丁语言成立。⇒ 本步**不设这条判据**，证据不足就别装作有证据。<br>🔴 **中文版的 `text` 有时装的是中文译文不是日语例句**（`見せる`→「让我看一下。」）。判据**按结构不按字形**，量过才定：有 translation 的 8,288 条里含简体专用字的只有 **1 条**，无 translation 的 4,073 条里有 **1,716 条（42%）** ⇒ 给了译文的，text 就是日语。<br>⚠️ 「没假名 ⇒ 是中文」这条判据试过，**比它要描述的东西宽得多**：`青空。``核兵器``二時間``数百人` 全是纯汉字的正经日语例句，会误杀 4,691 条。<br>⭐ **简体专用字表从数据推、不手写**：有异体繁体形 且 在**英文版+日语版**词头里出现 <3 次。🔴 参照语料**必须排除中文版收来的 6 万词**（第一版拿整张 `dict` 当参照 ⇒ `显`/`让` 变成「日语里出现过」，判据自己把自己废了）；阈值也不能写「一次都没有」—— `么`(4次)/`门`/`过`/`办` 四个孤例就能把整张字表掏出洞，49 条中文句子照样进了日语列。<br>⭐ **丢之前先问里面有没有别的层要的料**（阶段 4b 的教训）：317 条是「日语例句　　中文译文」挤在一格、kaikki 没拆开，整条丢＝既丢例句又丢一条免费译文 ⇒ 拆开收。<br>🔴 **例句身份与译文是两回事，第一版把它们绑死了**：同词同句多版都有时留信息最多的那一条，**另一版的译文被一起丢掉** ⇒ 免费中文从 8,288 掉到 3,739。`example_gloss` 的主键是 `(example_id, lang)`，它本来就装得下两种语言。⚠️ 改对之后数字没动 —— 因为我把攒译文那两行放在了去重 `continue` **后面**。**位置错和逻辑错，症状一模一样。**<br>🔴🔴 **关系层逼出了阶段 0 欠的一笔账**：`alt_of` 只收到 10,153 条，而源头有 37,820。原因是 **31,202 个 `soft-redirect` 词形根本不在 `dict` 里** —— 阶段 0 写明「连同指向一起进阶段 3」，而阶段 3a 的收词判据是「**有真义项**而我们没有的词形」，soft-redirect 的定义恰恰是**它没有义项**⇒ **判据把一整类词排除在外，阶段 3a 的闸全绿，账上那句话没人兑现**。代价是 `あいする`／`いんりょく`／`あふりか`／`AA`／`美德` 这些**读者最可能敲进搜索框的写法**全都搜不到（99.3% 的目标就在库里）。已补收，见阶段 5d。<br>🔴🔴 **同音索引页的判据要在聚合之后再判一次**：§二.5 的规矩是「指向 ≥2 个目标 ⇒ 索引页不是异表记」，我实现成「**一条 kaikki 词条**的 redirects 有几个目标」—— 那是英文版的序列化方式。**中文版把同一张索引页拆成多条词条、每条一个目标**，于是 `いる → 入る/射る/煎る/要る/鋳る` 五条各自 `len(r)==1` 全部过关，聚合起来正是判据要挡的东西（287 个词形）。同一条规矩、两种序列化，判据只认得其中一种（`[[decision-not-propagated-across-editions]]`）。<br>🔴 **关系的 `word` 字段粘着释义**（`手痛い: severe`，5,074 条），还混着**整条就是英文释义**（`a metaphor for a large group of people`）和**维基模板报错**（`Lua execution error in Module:…`）。判据按含义不按长度（`len>40` 会连 `蟻の穴より堤の崩れ` 这种真谚语一起砍）：**切完粘着的释义之后，整串是空格分开的多个拉丁词 ⇒ 是英文句子不是词条**。⚠️ 顺序不能反，先判英文句子会把切完是谚语的那批误杀；也不能写「没有日文字符就丢」——`GNP`/`AA`/`new` 都是库里真实存在的日语词条。<br>🔴 **顶层关系一律 `sense_id=NULL`，不靠 `_dis1` 往义项上挂**。kaikki 给了 `_dis1`（`'2 96 2'`＝96% 属于第 2 条义项），拿它能多挂 6 万条，但那是**概率不是事实**，而义项错配比缺一条严重得多。义项级关系 97,090 条全部来自源头**自己**挂在义项下的那些。<br>🔴🔴 **频次层：天真地用 wordfreq 会造出一把说谎的尺子。** 日语通路要 MeCab 分词，装好之后它对任何词形都给得出一个数 —— 词形被切成多 token 时返回的是**由部件组合估算**的值，对短语合理，对**词典词条**错，而且方向固定：**偏高**。实测 `分親` **4.90**、`情報管理システム` 4.81、`カバー曲` 4.57，而 `愛する` 才 4.26 ⇒ **生僻复合词被系统性地顶到常用词上面**。而频次的下游用途正是排序和挑核心词 —— **在这个方向上说谎的尺子比没有尺子更坏**。⇒ **只给单 token 的词形落数，其余留 NULL**（覆盖 67,881 ＝ 词元 27.3%）。<br>🔴 **单 token 还不够，token 必须就是这个词**：`は〜` 被切成 `['は']`，一个 token、通过判据、然后领到助词 `は` 的频次 **7.46**，和 `は` 并列全库第一（`お〜`→`お` 同理，共 44 条）。这是 `[[wordfreq-ruler-traps]]` 头号坑的日语版本：不是剥词缀，是**剥符号**。⚠️ 但 `ＡＢ`→`ab`、`ｱｲ`→`アイ` 是 wordfreq **正当的全角/半角归一**，不能一并否掉 ⇒ 两边 NFKC + casefold 之后再比。<br>⚠️ **代价要说清**：56.6% 的词元没有频次，§四.2 那个「声调够不够上页面」的核心词覆盖率**分母得按单 token 那批重新说**，不能假装覆盖全库 |
| **5d** | 补收 `soft-redirect` 词形（阶段 5b 的前置）| **✅ 已完成（2026-09-16）** | 3a | `dict` 679,062 → **710,264**（+31,202）。零模型调用。见阶段 5 那条里「阶段 0 欠的账」。<br>🔴 **P6 覆盖率闸当场逮到它**：补完词、关系还没建的那一刻，「非空白页词形占比」从 98.17% 掉到 **93.9%**，闸红 —— 因为这 3 万个词此刻确实既无义项也无变形也无指向。建完关系回到 **98.25%**。<br>⭐ **判据放宽必须在数据建好之后**：P6 原来的空白页判据是「有义项 或 有变形」，跳转页两样都没有。我把「有 `alt_of` 指向」加进判据 —— 但**是在关系真的落库之后才改的**。反过来先改判据再补数据，就是 `[[proxy-metric-gets-optimized]]`：为了让指标好看而动指标。<br>🔴 **`expect` 的键写错被闸拦下**：`dict` 的行数键是 `__rows__`（打印成「总行」），不是 `#dict`。我写了个谁也不认的键，真正要授权的增量没被授权 ⇒ 闸报「总行数变了 +31,202，期望 +0」。**已回滚重跑，没有含糊过去**。<br>🔴🔴 **「不收」和「收成别的东西」之间我第一版选错了。** §二.5 说同音索引页不许当异表记收，我实现成**整条丢掉** ⇒ 157 个词形变成搜得到、点进去**真空白**，而它们是 `関わる`／`我儘`／`スルメ`／`船唄`／`両想い` 这种正经词（44 个还有真实频次）。逐条读它们的目标才看清结构：多数是**同一个词的两种写法**（`船唄 → ふなうた / 舟歌` ＝假名读音＋汉字异写），少数才是真索引页（`やらい → 夜来 / 矢来 / 遣らい`）—— 而我分不开。<br>⇒ 改成 `kind='see_also'`（12,425 条）：**不断言同一性，只说「这几个词都念这个音」**，那正是源头说它是的东西，也正是读者需要的那一页。§二.5 禁的是「断言 `犬` 和 `去ぬ` 是异体字」，不是「这一页不许存在」——**我把「别做那个断言」读成了「别留那条数据」**。<br>⭐ 逮到它的是阶段 5d 的交付物断言（「补收的词形都拿到了指向」），而**发现那 44 个频次的是写库闸门**（「未声明的列 freq_zipf 变了 -44」）——那 44 个数字本身就是「这些是常用词、别删」的证据。两道闸一前一后各拦一次 |
| **5e** | 例句中文译文（付费）| **✅ 已完成（2026-09-16）** | 5 | **44,723 条（83.9% 的例句没有中文）** —— 这是读者能看到的最大一块空白。<br>⭐ **免费路径已用尽**：中文版白送的 8,586 条阶段 5a 已收，三版再没有别的中文源（`[[prove-free-path-before-quoting]]`）。<br>🔴 **一道题不是两道**：16,100 条有英文译文、28,623 条只有日语原句，但一律**从日语原句翻**，英文只当参考传进去 —— 英文译文是维基编者的二手转述，拿它当源会把误差叠一层。⚠️ SYS 规则 3 专门堵「参考漏进输出」（`[[context-you-give-leaks-into-output]]`）。<br>🔴🔴 **控制组不能复用释义那一套。** `ctrl.check()` 的 E5「带句末标点」对释义成立（释义是短语），对例句译文**正好是反的**（它就是句子）。实测：拿 `check()` 判正确例句，**2/2 条被判成 E5**。⇒ 另写 `ctrl.check_example()`，变异自检 8/8，并在自检里**打印出照搬会误判几条**当对照。<br>⭐ **切片实测单价 97.1 token/条**（释义那两轮是 66.2 / 63.5，例句更长，高 46%）—— **没拿释义单价套**。全量 ≈ 434 万 token ≈ 低谷 10.6 元 / 全价 21 元。<br>⭐ 切片质量：**干净 94.44%**，真失败 5.56% 但逐条读下来多数不是失败 —— E0 留空 13 条是源头那几行根本不是例句（`Regina, æ . reina` 拉丁乱码、`See also: 月` 交叉引用、`狐狼 上扈反` 古汉语注），模型按规则 9 弃权是对的；E2 抄回 9 条里 `上位概念`/`横尾太郎` 都是正确译文。**真错约 0.2%**（和歌那条没翻）。<br>**实跑**：44,723 条 / 2,237 批 / **441 万 token** / **零失败**，单价 **98.7** token/条（切片预估 97.1，差 1.6%）。空闲时段半价 ≈ **10.8 元**。<br>⭐ **例句中文覆盖 16.1% → 98.8%**（52,216 / 52,871 条可见例句）。<br>⭐ 全量质量与切片一致：**干净 94.24%**（切片 94.44%）。逐条读四个失败桶，**E3/E6/E8 基本都是正确译文**：`管理が甘い→管理松懈`、`米国→美国`、`未就学児→学龄前儿童`、数学公式原样保留。判据宽，不是模型错。<br>🔴🔴 **回归闸当场逮到新买的数据把刚修好的缺陷带了回来**：A5 报繁体（抽样 22 条≈全量 330）、A7 报台湾说法 3 条 —— **模型输出的**。重跑 `fix_traditional_to_simplified` 和 `fix_tw_vocab` 清掉。**这正是回归闸存在的理由**：没有它，这批繁体会静静躺在库里，而所有「本步自己的」验收都是绿的。<br>🔴🔴 **E2「抄回」1,224 条里挖出一个数据缺陷（不是翻译缺陷）**：`そろそろ` 的例句正文是「慢慢地走。」—— **日语原句那一列装的是中文**，模型抄回来是因为没东西可翻。<br>　⭐ 这是阶段 5a **明说过判据管不到**的那一类（纯汉字串字形上分不出中日），当时按 §7.5 写了推翻条件「出现能标出日文引用的结构信号，或有人报出实例」——**推翻条件兑现了**，新信号就是「模型原样抄回」。⇒ 它是**被认出来的**，不是又一次偶然撞见（`[[record-the-negative-decision]]`）。<br>　🔴 判据要三条同时成立（中文版 ＋ 源头没给 translation ＋ 模型抄回），单看「抄回」会咬到 `昭和新山`/`上位概念`/`横尾太郎` —— 那些是纯汉字日语，抄回是**正确译文**。再加两条收窄（正文本身是库内词头／逗号分段全是词头），最终 **440 条 `hidden=1`**。⚠️ **hidden 不是 DELETE** —— 判错了要翻得回来；残余误判估计个位数，明说。<br>　⚠️ 这 440 条是**付了钱的**（约 1% 开销）—— 因为发现它们的正是那次付费跑批 |
| **6** | 发音（Commons 真人录音）| **✅ 已完成（2026-09-16）** | 5 | `audio` **1,038 条 / 953 个词形**，`kind='human'`。**零下载，只存 URL。**<br>🔴🔴 **这一步原来标着「⚪ 有意不做」，那个结论是错的 —— 事实对、结论错。**<br>　原话：「三版并集只有 206 个词形有 `mp3_url` …… **别排工**」。我全量重扫过，事实没问题（英文版 4／日语版 84／中文版 144 个词形）。错在**把「dump 里没有」等同于「拿不到」** —— `[[dont-say-source-lacks-what-we-skipped]]` 那条讲的就是这个：「源头没写」和「我们没抽」要在结构上分开。<br>　前六门的录音**全部来自语言版 dump 自带的 `mp3_url`**（de 一百万条就是这么来的），而日语版维基词典**不在词条里嵌音频**。⇒ 对日语，dump 根本不是录音的来源，**Commons 的分类才是**。同样零下载、同样只存 URL，只是入口换一个。<br>⚠️ **这个错误是用户问出来的**，不是闸逮出来的（用户 2026-09-16：「为啥 6 不做？其他语言都有真人发音不是吗」）。一条写进计划表的否定结论，**后面每一轮都会被当前提引用**，而我引用了它三次都没回源核。`[[record-the-negative-decision]]` 要求落账否定结论，但落账的同时得记**它是怎么量出来的**，否则落的是一句不可复核的断言。<br>**实测 Commons 的量**：LinguaLibre 日语 1,043 ／「Japanese pronunciation」顶层 248 ／其余 9 个子分类 + `Ja-` 前缀约 690 ⇒ 去重 1,581 个文件。对照 LinguaLibre 法语 436,115 ／德语 26,112 ／意语 12,637 ／葡语 9,587 —— **日语在维基生态里确实被录得极少，这一点原判断没错。**<br>⭐ **但「少」要按核心词问，不按全库问**：最常用 **500** 个词元里 **16.2%** 有录音，5,000 个里 7.0%。方针④本来就是**三级兜底**，从不要求真人录音全覆盖 ⇒ **953 个常用词上的真人录音严格优于 0 个**，而代价是一次脚本、零元。<br>⭐ **dump 独有 185 条**（占 18%）—— 它直接给「词 ↔ 文件」的对应，比从文件名反推可靠。`[[cross-edition-harvest]]`：各版独有的那部分才是真增量，量小也要收。<br>🔴 **落库前抽 8 条发 HEAD 验活**：存死链比不存更坏 —— 读者点了没反应，而库里那一列看着是「有录音」。<br>🔴 **重试退避我写反了**：第一版 `wait /= 2`（4→2→1 秒，**越等越短**），因为我把 `[[retry-must-converge-or-drop-loud]]` 的「对半切」照搬了过来 —— 而那条讲的是**切分片大小**（让每次重试的活儿更小、保证收敛），不是切等待时间。对**限流**的接口越等越短只会把它惹得更凶。⇒ 改成 2→4→8→16→32 封顶。**同一句话在两种场景里指向相反的动作**：照抄措辞而不是照抄意图，就会反着做。<br>⚠️ 合成音这一档**不做**：it 那轮用户试听判定质量不够、产物已删（`[[it-tts-layer]]`）|
| **7** | 闸与验收 | **✅ 已完成（2026-09-16）** | 5,6 | 两道闸，问的是**没有交集的两个问题**（`[[external-anchor-gates]]`）：<br>**① 回归闸** `ja/tests/test_no_regression.py` —— **31 条断言 / 8 条带基线 / 变异 9/9**，已挂进 `dbtool.session`，每次写库自动跑。<br>**② 外锚闸** `ja/pipeline/verify_vs_dump.py` —— 库 vs 三版 dump **义项级**逐条比对。⭐ **en 版缺 0 条**（建库主源全收）。<br>🔴 **判据一律 import 生成侧那一份**：`romaji_of`/`GRADE_LABEL`、`KIND`/`clean_target`、`simplified_only`、`_same`、`PURE_KANA`、`SECTION`、`real_senses`/`clean_gloss`。阶段 1 的罗马字断言就是因为**自己重写了一条规则**（`romaji LIKE '% kanji'`）报了 6 条假红。<br>🔴🔴 **首轮两条红，两条都是断言的错，一条还是我给它写的理由在说谎：**<br>　**A4** 判据写成「含 `==` 或 `【】` 或汉字等级」⇒ 报 71 条，逐条读**只有 62 条是真的**，另外 385 条是中文版的正当写法 `【decilitre】分升。`（【英文原词】中文释义）。拆成 A4a/A4b/A4c 三条各自收窄，判据 import 修复侧那一份。<br>　**B5** 「假名词头的读音等于它自己」报 1,380 条，其中 **1,312 条是片假名/平假名互写**（`あか`→`アカ`、`タバコ`→`たばこ`）—— 同义反复是「等于它自己 **up to 字体**」不是字面相等。折完字体剩 68 条，是 `ヶ` 读か/が/こ（记号不是表音假名）和长音写法变体（`ぐう`→`グー`），都是真数据。<br>⭐ **真修了 62 条**：`ja/fixes/fix_gloss_residue.py` —— `===Etymology 4======Etymology 5===` 章节标记（2 条）＋ 整条只是 `【現す、顕す】` 异写列表（60 条，不是释义），连同变空的 31 条义项一起删。<br>🔴 顺带修了**生成侧**的同一个 bug：`SECTION = r"==+[^=\n]*==+"` 在 `===A======B===` 上贪婪吃掉共享的 `=`，剥完剩 `B===`。**同一个模式在生成侧和修复侧各有一份** ⇒ 改成修复侧 import 生成侧那一份（`[[etymology-layer-acceptance]]`：同一个假设写在两处、只改一处，三层全绿而端到端才逮到）。<br>🔴 **4 条基线是我估的不是量的**，闸自己报「⬇ 该收紧」我才来改（A1 3,213→3,202／B4 41,996→41,964／C3 12,248→12,206／D2 12→11）。**写估算值当基线＝给自己留静默余量**，下次真涨回来它不会响。<br>🔴🔴 **变异按名字指向断言，改名就会静默拆掉变异**：我把 B5 改名成「…（已折字体）」，变异里还是旧名，M7 永远匹配不到、报「漏了」，我当时以为是变异写错了。⇒ 加了**幽灵变异守卫**：开跑前核一遍 target 在不在断言名里，对不上直接失败。**一条指向不存在断言的变异不是变异。**<br>🔴🔴 **外锚闸逮到的第一件事，是我为它写的解释在说谎。** ja 版缺 36% 超上限，我给的理由是「文本比对的局限，不是漏收」—— 逐条读发现**是真的缺内容**：`男児`／`表現`／`灌漑`／`流入` 这些常用词在库里**一条日语释义都没有**。真原因是：日语版释义要挂到义项上就得知道对应哪条，而实测 23,692 个待补词形里**配对唯一的只有 9 个**，其余都要猜 ⇒ 与 §四.1 同一条决定：**不做跨版义项配对**（es 实测 12.4% 错配，错比缺更伤权威）。已把理由改写成真原因并写上推翻条件。<br>⚠️ 三版上限全部改成**实测值**；原来 zh 版写 70,000 而实际 55,363，**拍一个宽上限等于给自己留静默余量**。<br>⭐ 顺带消了一处重复：`POINTER` 在 `ingest_ja_senses` 和 `intake_edition_words` 里各一份，现在只有一份 |
| **8** | 展示层与性能 | **✅ 已完成（2026-09-16）** | 7 | `packages/dict-core/src/japanese.ts`（读取层）＋ `JapaneseEntryView`（视图）＋ `packages/dict-labels/src/ja.ts`（标签层）＋ 两道契约闸。七门语言全部探活通过。<br>🔴🔴 **展示层的兜底原来会把任何未接线的语种塞进西语视图**（`lang !== 'en' && !== 'it' && …`）。日语不接线也会**看上去能用** —— 词头/义项/例句是共有字段，渲染得出来，而假名、声调、字种等级**静默消失**。`[[it-display-layer-stage8]]`：**兜底越体面缺陷越难发现**。⇒ 改成白名单，未接线的语种**明说没接**。<br>🔴 **`posLabel()` 加了 lang 参数**（欠账 2 已还）。日语一接上全局表就不够：`kanji`/`kana`/`romaji` 六门全无；而 `part` 在全局表是「小品词」（意语的 sì/no），日语的 `particle` 是**助词**（は/が/を/に）—— **同一个码两门语言不是一个东西**，改全局表会把意语一起改坏 ⇒ 必须是按语种的覆盖层。`lang` 省略时行为与从前完全一致，六门调用点没动。<br>🔴🔴 **数据层先逮到一个：同一个概念在库里有两个码。** `prov` 218 / `proverb` 99、`adnom` 96 / `adnominal` 23、`kana` 789 / `syllable` 4、`sym` 100 / `symbol` 37 —— 根因是 `intake_edition_words` 里**另写了一张更短的 POS_MAP**。合并成一份并归一了 **67,020 行**（另有 623 种复合值 `noun/verb`→`n/v` 共 13,981 行）。⚠️ 合并两张表必须**双向查**：局部表有一个正本没有的 `adj_noun`，直接删局部表会把形容动词 1,137 条弄丢成原始码。<br>🔴🔴 **服务层方法名和 API 层对不上，TypeScript 全绿。** 我把方法写成 `stats()`/`lookup()`，而 API 调的是 `getStats()`/`getEntry()` —— `getService()` 返回联合类型、API 层当 any 用，**一路躺到运行时才 500**。`[[correct-steps-can-compose-a-hole]]`：每步都对、跨步的约定没人负责。⇒ 建 `scripts/contract/service_api.ts`，**判据从 API 源码里读**（手抄一份会漂开），七门全绿。<br>🔴 **真渲染出来立刻逮到三个缺陷**（数据层当时全绿）：<br>　① `いぬ` 被印成「**去ぬ 的变形**」—— `baseOf` 写了 `LIMIT 1`，而它同时是 去ぬ/鋳る/射る/寝ぬ/率寝 五个词的活用形。**随便挑一个当原形＝在页面上断言一件假事。**<br>　② `痛い` 的读音裂成三行（`いたい/itai [ita̠i]｜? ♪[ìtáꜜì]｜? [ita̠i]`）——声调来自中文版、挂在 `word_id` 上，IPA 来自英文版、挂在 `entry` 上，**同一个读音被拆在两行**。⚠️ 合并只在**这个词只有一个假名读音**时做；多读音词（`爺`＝じい/じじ/じじい）上无法判断声调属于哪一个，宁可分开印也不猜。<br>　③ 拍数算不出来（`核2/null拍`）⇒ 声调型印不出来。<br>🔴🔴 **展示层契约闸第一版对「空标签」结构性失明。** 它查「有没有印出英文码」，13 个关系 kind 里**只逮到 1 个**（`derived`）：8 个原样返回英文码，而 `related`/`synonym`/`antonym` 三个返回的是**空字符串** —— 页面上是个空的小标签，既不是英文也不是中文，**比印英文码更难发现**。⇒ 判据改成「去库里取全部 kind，逐个问有没有**非空**中文名」，词性码那边同样加一条。判据锚在数据上，源头将来多一个 kind 它会自己响 |
| **9** | 产品化（搜索性能）| **✅ 已完成（2026-09-16）** | 8 | 搜索最慢 **40.9 → 2.2 ms**，词条页 **71 → 0.4 ms**。<br>🔴 **先量再改，三件事按发现顺序排，而预计算是最后一件、也是最不重要的一件：**<br>　**① `ANALYZE` 从来没跑过**（`sqlite_stat1` 不存在）。库里没有统计信息，规划器**从选择性最差那头入手**：中文摘要子查询的计划是 `SEARCH g USING INDEX idx_glosslang (lang=?)` —— `lang='zh'` 有 **29 万行**，每个候选词扫一遍。⇒ 搜索 **40.9 → 7.1 ms**。⚠️ 与 de 阶段 9 记的是**同一个**形状（`[[query-perf-collation-traps]]`）——**那条教训写在账上，而我在 ja 上重新发现了一遍**，因为没有任何东西在建库时问过「跑 ANALYZE 了吗」。现在它是阶段 9 的交付物断言之一。<br>　**② 子查询写法让 SQLite 挑错驱动表**：`JOIN sense_gloss g ON g.sense_id=s.id WHERE … AND g.lang='zh'` ⇒ 改成嵌套之后驱动表变回 `sense`，内层才吃得到主键。<br>　**③ `inflection.base_id` 上没有索引**，而展示层查「这个词的活用形」正是按它查 ⇒ 每开一个词条页**全扫 567,717 行**。⇒ 词条页 **71 → 0.4 ms**。🔴 建库时建了 `idx_infl_base`（按**文本** `base`），那是给「回填 base_id」用的 —— **写入路径的索引不等于读取路径的索引**（`[[it-regression-gate]]`：在写入列与读取路径各查一次）。<br>⭐ **实测确认瓶颈是排序不是过滤**（拿 `あ` 的 11,764 个候选分解）：只过滤+LIMIT30 **1 ms** ／ 加上排序 **9 ms** —— `length(word)` 和 `freq_zipf IS NULL` 都不可索引，**建索引没用**，只能预计算。<br>🔴 **判据按「候选数」定，不按「前缀长度」定。** de/pt 预计算的是短前缀（1–2 字符），照搬到日语是形式代理：日语首字是几千个汉字，**1 字前缀就有 15,102 种**，而绝大多数只有几个候选、本来就 0.1 ms。实测各档：候选 100–300 是 0.11 ms、700–1,500 是 0.36 ms、**1,500–4,000 才 0.87 ms**、4,000–12,000 是 1.70 ms ⇒ 阈值定 1,500，**一共 63 个前缀 / 1,890 行**，表小得可笑而盖住了全部尖峰。<br>⭐ **闸**：`--verify` 做**可逆性回核 100%（非抽样）** —— 每个预计算前缀重跑实时查询、id 序列**逐位**比（63/63 一致）＋ 指纹（`LIVE` 的 ORDER BY 用到哪些列就哈哪些）。回归闸里另加 S1，抽 8 个前缀每次写库都核 —— **预计算表会随收词静默陈旧**，而查询照样返回旧顺序、不报错。<br>⚠️ 缓存未命中 ＝ **正确回退不是错误**；连「表存不存在」都按未命中处理，所以读取层在没跑过本步的库上照样能用<br>🔴🔴 **2026-09-20 补记：本阶段的头号修复（`ANALYZE`）当时是手敲 sqlite3 跑的一次，没做成步骤。** 于是整个仓库里 `ANALYZE` 这个词**只出现在一句注释里，没有任何代码会执行它** —— 加完词源层一查：`sqlite_stat1` 里 dict 写着 710,264（实际 714,173）、`sense_relation` 差 -5,316、**`etymology` 整张表压根没有统计信息**。<br>⚠️ 而本阶段的账查的是「`sqlite_stat1` **存不存在**」—— 对**陈旧**结构性失明。这与「行数闸对 0 行是一致的」是同一族：**判据问了「在不在」，没问「对不对」**。<br>⇒ 已补 `ja/pipeline/run_analyze.py`（走 `dbtool.session`，四道闸全过、数据表一行没动）＋ 回归闸 **X5**「统计信息不许比库陈 0.5% 以上」，判据 **import 生成侧的 `survey()`**，变异＝删掉 `etymology` 的统计行。<br>⭐ 重跑后复量：搜索最慢 **14.7 → 1.9 ms**、词条页最慢 **28.3 → 1.7 ms**。⚠️ **这笔不能全算在 ANALYZE 头上** —— 第二次跑时操作系统页缓存已经热了，两个因素混在一起；能干净断言的是「稳态中位数前后没变（0.4–1.4 ms，本来就正常）」与「统计信息现在是对的」 |

## 六、📋 欠账

| # | 欠什么 | 规模 | 状态 |
|---|---|---|---|
| 26 | ~~**同形异源而读音相同的词，页面上连着两个一模一样的组标题**~~ | 518 词形 / 593 对 | **✅ 已还（2026-09-20）—— 跨语种词源契约闸 G4 逮到的，不是人看出来的。**<br>`3K` 两支词源都念 さんけい、`DID` 都念 ディーアイディー ⇒ 页面上「名词…名词…」贴在一起，**读者看不出为什么分成两块** —— 正是用户 2026-09-12 在 es `pasar` 上那一问的日语版（「如果都是动词为什么不能合在一起」）。<br>🔴🔴 **根子是判据的粒度错了一级**：`showKana`（该不该标读音）是个**词级**判据——「这个词全场有没有 ≥2 种读音」；而「这个标题分不分得开**相邻这两组**」是个**组对级**问题。两支词源读音相同时，词级判据恒为 false，读音标签整个不出现。实测 593 对里 **590 对**是这个机制，另 2 对两组都没读音、1 对一有一无。<br>⭐ **修法是回退不是改判据**：读音救不了场的地方退回印「词源 ①/②」（六门现成的 `etymLabel`，CSS 也是现成的）。**只在救不了的地方印** —— `猫`（ねこ/ねこま）已经说清楚了，再加序号是噪声。<br>🔴 **闸里 G4 的取法同时是补全**：原来截到标题 div 的第一个 `<` 为止，而日语的读音标签是标题**里面**的一个 `<span>` ⇒ 不改取法，`猫` 会报**假红**。六门的标题没有嵌套元素，两种取法同结果 |
| 25 | **ja：源头有词源正文而我们没抽** | **+1,177 行**（干跑实测）| 📋 **ja 自己的一笔账，已量清楚、未动手** —— 用户 2026-09-20 收口「先记着」。库一个字节没动，`db_keys()` 已恢复原状（干跑算出 40,270 ＝ 库里现有，无漂移）。<br>**机制**：`scripts/ingest_etymology.py` 的 `db_keys()` 走 `FROM sense JOIN entry`，**只认「有已出版义项」的词条**。改法是换成 `FROM entry JOIN dict`（严格超集）—— 该函数 docstring 写的「库里每条词源支」本来就该按**词条**数不按**义项**数，**是实现没跟上**。<br>**ja 的规模**：40,270 → **41,447（+1,177）**，其中<br>　· **194 条**＝有 `entry` 没 `sense` 的词条（`闘`/`価`/`促`，**157/194 是汉字条目**）<br>　· 其余约 983 条＝**指针词条**（`EU`/`ソ連`/`OS`），义项**按设计不出版**（阶段 1.5a：指针义项 7,637 条不出版）⇒ 旧桥同样认不到<br>**那 194 条落在什么样的词上**：① 51 条该词有释义，缺的只是另一支的词源（`価`）② 41 条没释义但有字音读/关系/变形（`闘`/`促`）③ 🔴 **102 条整页空白**（`俤`/`糀`/`罅`/`褄`/`釺`）—— **源头这段正文会是它们页面上唯一的内容**，抽进来能让全库空白页 9,763 少 102 个。<br>🔴🔴 **动手前必须先拆回归闸 Z3，且不许放宽它**（它要求每条词源行都能被**义项**侧找到，而新增的行恰恰挂不到义项 ⇒ 直接写会打红）。拆法：<br>　**Z3a** 词源行的键在 `entry` 侧必须找得到（结构完整性，全部行，容忍 0）<br>　**Z3b** **有已出版义项**的词条，其词源行必须能被义项侧找到（＝原 Z3 的页面口径，一字不改，容忍 0）<br>　**Z3c** 挂不到义项的「存档行」有多少（页面不显示，但不许悄悄长）⇒ **记基线**<br>　⇒ 拆完必须跑变异验证三条都真的能红。<br>**还连着一个未决的产品问题**：那 102 个空白页即使抽进来**也不会显示** —— 词源块挂在义项组里，没义项就没组。要显示得先决定「无义项的汉字页把词源印在哪」。**这是两件事，别合成一件做。**<br>⚠️ **这个桥是七门共用的，别门的量不记在这张表里** —— `en`/`de` 不经过它、`es` 结构上免疫、`it`/`fr` 各有小增量、`pt` 会被对齐闸拦下且原因是它独有的。四门的实测数字与 pt 的拦截样本，**记在 `scripts/ingest_etymology.py` 的 `db_keys()` 正上方**（知识住在它描述的那段代码旁边；这张表只管 ja）|
| 12 | ~~**日语页面整个没有 CSS**~~ | 33 个类名 | **✅ 已还（2026-09-17）** —— 用户看页面说「乱糟糟」，字面意义上准确。<br>🔴🔴 `css-audit.ts` 一跑：日语视图用到的类名里 **33 个在 `styles.css` 里一条规则都没有** ⇒ 日语页面渲染的是**裸 HTML**（没有间距、没有层级、没有颜色）。而**数据九个阶段全绿、展示层契约十六条全绿** —— 没有任何一道闸问过「这个页面长什么样」。<br>⇒ PITFALLS I3「自造类名＝页面没样式，而且探针看不见它」**最大的一次实例**（上一次纪录是 de 的 16 个）。同一个坑第三次。<br>**修法两步**：① 有共用角色的一律改用共用名字（`entry-detail`/`entry-word`/`sense-item`/`rel-row`/`rel-item`/`rel-link`/`rel-plain`/`sense-src`/`form-list`）⇒ 33→23；② 剩下 23 个是日语真正独有的角色（假名/罗马字/声调/振假名/指针块/活用表），写一份声明。<br>🔴 顺带把可点词从 `<button className="link-word">` 换成 `<a className="rel-link">` —— `.rel-link` 是给 `<a>` 写的，套在 `<button>` 上会露出浏览器默认按钮外壳（en 栽过一次，5 处）。<br>🔴🔴 **更要紧的是：三张"加语言时要来加一行"的登记表，日语一张都没登记** —— `render-dump.tsx` 的 `View` 分支、`css-audit.ts` 的 `VIEWS`、`contract-check-layout.tsx` 的 `LANGS`。**三道闸全绿**，因为它们查的是"登记了的那几门对不对"，没有一条查"是不是所有门都登记了"。`css-audit` 的孤儿类名甚至**报在了 de 的账上**（分段是"从这个视图到下一个视图"，最后一个视图延伸到文件尾）——**逮到了，但指错了语种**。<br>⇒ 已做成机制：`css-audit.ts` 加自检「源码里有几个 `*EntryView`，`VIEWS` 就得有几行」，变异验证过（摘掉 ja 当场红）|
| 13 | ~~**关系层：类别挂错 ＋ 目标带残渣**~~ | 40,836 行 | **✅ 已还（2026-09-17）**：`ja/fixes/fix_relation_kind_and_targets.py`。<br>🔴🔴 **同一个字段名，两版装的不是同一个东西**：en 版 `proverbs` ＝ 真谚语（384 条，逐条读过），**ja 版 `proverbs` ＝ 熟語**（汉字词条下「含这个字的复合词」那一节，32,932 条）。我照字段名映射 ⇒ en 版对了、ja 版错了，而**两版的行数闸都是绿的**（行数闸问"收了多少"，不问"收的是不是那个东西"）。判据两个方向都量：ja 版目标 ≤4 字 **94.9%**／含词头 96.0%，en 版 ≤4 字仅 **9.9%**。<br>**目标残渣 7,892 条**，四类，判据都打在含义上：振假名 `桜蔭(おういん)会(かい)`→`桜蔭会`（**判据不是"括号里是假名就剥"，是"振假名标的是汉字的读音⇒括号前必须是汉字"** —— 少这一条会咬掉 `（お）楽しみ`/`で（は）ない` 这种可选成分）／英文用法标签／串尾罗马字回显／冒号与命名空间前缀。<br>🔴 **「洗完在不在库里」不能当判据**：只有 47.1% 落库，逐条读发现落不进去的绝大多数是**洗对了但我们没收这个词**（`彼誰時`/`お仕置き場`/`泉下の客となる` 都是正经日语）。在不在库里是"是不是词"的**形式代理**，而我们的词表本来就不完整。<br>🔴🔴 **修一处造出另一处**：清洗完 `死ぬ` 页的近义词 `死(し)ぬ` 剥成了它自己 ⇒ 86 条自指关系。**逮到它的是回归闸 F2，不是我** —— 我当时已经看着「目标残渣清零 ✅」准备收工。<br>🔴 写库闸也响过一次：`__rows__` 我写成了 `-len(dele)`（它是 `dict` 行数，本步不动词形 ⇒ 0），且漏声明被 TRACK 盯着的 `sense_relation.target` ⇒ 当场打红并给出回滚命令。**闸该干的事它干了。**<br>⇒ 新增回归闸 **F5/F6/F7**（残渣清零／ja 版熟語不许回潮／**en 版真谚语不许是 0**，后两条必须成对看），变异 3/3 |
| 24 | ⚪ **`field_src` 这张表在 ja 是空的** | 0 行 | **⚪ 有意为空，2026-09-19 补记理由 —— 此前它空着而账上一个字都没提。**<br>🔴 这正是 `sense_tag` 那个形状（空了整整一个项目，而**行数闸对 0 行是一致的**、判据写在账上更没有交付物可查）。⇒ 空表必须有一句话说清它为什么空，否则下一个人只能重新调查一遍。<br>`field_src` 在 de 记的是「**七月用模型给一等字段补空、没留来源标记**」那 25,996 个值。**ja 从没发生过这件事**：全量查过，写一等字段的只有七处，`backfill_kana`（有 `kana_src`）／`fill_vclass`（源自 dump 标签）／`fill_freq`（wordfreq+MeCab，方法写在文件头）／`intake_core_level`（有 `core_src`）／`intake_edition_words`（`is_lemma` 标志）／两个 fix —— **全部是确定性步骤，零模型填值**。而 `entry` 自带 `src`/`src_ref`，挂在它上面的字段都能逐行追到 dump 条目。<br>🔴 **什么会推翻它**：只要出现「用模型给 `dict`/`entry` 的一等字段补空」这类步骤，这张表就必须同步落行 —— **落的那一步和补值的那一步是同一步，不能事后补**（de 就是事后补的，所以只能写 `unsourced`）|
| 22 | **核心词等级 `dict.core_level` ＋ 读音层重跑** | 7,989 词 / 声调 +9 | **✅ 已做（2026-09-19）—— 但署名义务还挂着，见下。**<br>起因是 §四.2 那个挂了很久的问题「声调够不够上页面」缺一把核心词尺子。<br>🔴🔴 **先查清一件事才敢找表：JLPT 官方自 2010 年改制后就不再公布词汇表**，而且是**有意不公布**（不鼓励照词表背）。⇒ 市面上所有 N5–N1 表都是民间按旧《出題基準》重建并重新切级的，**没有一份是官方的**。这不是「找不找得到」的问题，是「一份**猜的**表够不够格印在词典页上」的问题。<br>⭐ **四条免费路径都核过授权**（`[[prove-free-path-before-quoting]]`）：kaikki 英文版自带 JLPT 类别只有 **14 个词**；日本語教育語彙表 JEV（17,920 词，数据明显更好、还带重音）与 NINJAL BCCWJ 語彙表 **研究教育限定、禁商用禁二次分发 ⇒ 对本产品出局**；KANJIDIC2 的 `<jlpt>` 是**旧四级制**且只标字不标词；**唯一可商用的是 Waller 表**（CC BY，8,293 词，经 stephenmk/yomitan-jlpt-vocab 整理带 JMdict 序号）。<br>🔴 **它自己写着「essentially an educated guess」**，编于 10 年前，整理者还举了反例：Waller 的 N1 词出现在 2021 年 12 月的 N2 考卷上。⇒ **只当内部尺子，不作读者可见的难度标签** —— 印成「JLPT N2」就是断言一件我们保证不了的事，而铁律是**错比缺更伤权威**。用户 2026-09-19 定：**先加列，展示另说**。<br>⇒ `dict.core_level`（1=N5…5=N1，**存序数不存字符串**，序数不会被误读成官方等级）＋ `core_src`。命中 **7,989** 个词形。<br>🔴 **匹配判据改过一轮**：第一版在查库**之前**就把词形定死成汉字写法 ⇒ `迚も`（＝とても）、`亜爾加里`（＝アルカリ）、`若し`（＝もし）这类**异表记/送假名差异**整批落空，未命中 114 条。改成「先汉字、查不到退假名」后降到 **36 条＝0.4%**，且 36 条**逐条看过全是短语/惯用句/连用修饰**（`おかげさまで`／`どうぞよろしく`／`徐々に`／`違いない`），没有一条是漏收的词头。⚠️ **不做「按假名读音兜底」**：只多捞 3 条，却要在多个同音词里随便挑一个 —— 那是 `baseOf LIMIT 1` 的老毛病。<br>⭐ **顺带把读音层改成幂等并重跑**：中文版 dump 有声调的 18,534 个词形里，我们漏了 9 个（`焙る`/`産む`/`ヰスキー`…）—— 它们是阶段 5d 补收 soft-redirect 时进库的，那时读音层早跑完。**18,460 → 18,469。** 这一层原来只 INSERT、`expect` 写的是总数，重跑就翻倍；现在同变形层一样是清空重建 + 写入方对账。<br>🔴 **`expect` 连错三次，三次都是闸拦住的**：①自己写了一遍 `COUNT(ipa)`（只排 NULL），而闸的口径是 `TRIM(COALESCE(col,''))<>''`（**非空且非空串**），声调行的 `ipa` 存空串 ⇒ 两个口径差 18,460。**判据只许有一份 ⇒ 改用 `dbtool.snapshot()` 取基数。** ②`expect` 的键写成 `dict.core_level`，而 TRACK 登记的是裸列名 `core_level`。<br>✅ 闸：回归闸 **X1–X4**（值域／出处／**不许接进展示层**／整批消失），变异 M40–M43；账的闸新增「核心词的声调覆盖率」42.43%（下限 35.0）。<br>🔴🔴 **X3 是这一族里最要紧的**：它**不查数据、查源码** —— 「不上页面」是个决定，决定必须有东西替它站岗。判据是「展示层源码里不许出现这两个列名」，改主意要先改这条断言，而那一步就是在逼自己重读警告。<br>⚠️⚠️ **尚未履行：CC BY 的署名义务。** `dict.core_src` 只是内部溯源，**不构成对终端用户的署名**；产品需要在「关于／第三方数据」处公开署名（来源名、作者、许可名、链接、修改说明）。用户定为「展示另说」⇒ **这条挂着，落地展示之前必须先办**。<br>⭐ 咨询了 v4-pro（用户授权）。它逮到我一个真错：我拿「核心词 65.4% 在我库里汉字/假名是两条独立词条」去推断 JMdict 的常用度会分票，**而 JMdict 一个 entry 里同时装着 `k_ele` 与 `r_ele`、各自带 priority**（回 DTD 核实属实）⇒ 我量的是**我们库的行结构**，推不出它的标注结构。⇒ 采纳它的结论：**不拉 JMdict**，最坏是假名条目少一个「常用」标记，方向是「缺」不是「错」 |
| 21 | ~~**活用表的表结构：行列两个方向的表头识别失败被原样照收**~~ | 108 个核心动词 / 864 个带文語表的词 | **✅ 已还（2026-09-19）—— 用户把 `食べる` 的成品页发外审挑出来的。**<br>外审说「`食べましょう` 不是敬体命令形」。**回源一查不是孤例，是源头的系统性缺陷，而我们扁平遍历看不见它。**<br>🔴🔴 **根子只有一个**：英文版的活用行不是平的，是 wiktextract 解析维基 `ja-conj` 模板**渲染出来的表格**，它在**行、列两个方向上都有表头识别失败**。`build_inflection_layer.scan()` 一行一行看 tag，**看不见表**，于是把源头的错原样印上了页面。六个症状：<br>　① 意志形组继承 `imperative` ⇒ `食べましょう` 印「敬体命令形」<br>　② 使役被动组继承 `conditional` ⇒ `食べさせられます` 印「敬体条件形」<br>　③ 进行体组继承 `desiderative` ⇒ `食べています` 印「愿望敬体」。**`desiderative` 这个 tag 在成品库里真阳性是 0** —— 480 行全是进行体，而真正的愿望形 `食べたい` 带着 ⑥ 的报错标记被整行丢掉了<br>　④ 文語表的 `ja-conj-bungo` 分隔符被整个丢弃 ⇒ 864 个词的文語形混进现代活用里印（70 个词两套并排），且已然形+ば 被标成 `causative`（`食ぶれば` 印「使役」）<br>　⑤ 列方向丢 `past` ⇒ `食べます` 与 `食べました` 的 tag 集**完全相同**，两行同印「敬体」。**108 个带现代活用表的词无一幸免**<br>　⑥ 认不出的格子打 `error-unrecognized-form`，原来整行丢弃（英文版 conjugation 行的 **10.3%**）—— 丢的正好是**普通体那几列**：`食べる` 页上有「食べます」**没有「食べない」**，12 个最常用的形查了 12 个全缺<br>⭐ **外审只逮到 ①②④ 的一半，⑤⑥ 它看不见** —— 它能看的只有页面上**有**的东西。这正是 `[[render-review-with-models]]` 的边界：**挑得出写错的，挑不出没写的。**<br>🔴 **判据全部来自源头自己给的东西，不引入外部词表**：列维度从词形形态读（`ました` 就是敬体过去，这是定义不是形式代理）；三处合并组的分界也从形态读且两侧不相交；已然形+ば 与未然形+ば 用**同一块里的词干**分开（`見れ` vs `見`）。<br>⭐⭐ **每条判据都在「源头标对了的格子」上验过，才用到标错的格子上**：列读取器在 3,748 个没打报错标记的格子里，**唯一的分歧就是 `ました`/`ませんでした` 那 236 个** —— 即本 bug 本身（源头把 `食べます` 和 `食べました` 标成同一个 tag 集，两者不可能都对）。三处分界的连续性 **118/118 零例外**。<br>⚠️ **顺带修的两处，都是同族**：ら抜き言葉（`食べれる` 与 `食べられる` 并列印着，不标出来读者会当成两套都规范）；命令形那一组源头把四件事打成一个 `imperative`，`食べないでください`（礼貌请求）与 `食べるな`（禁止）同印「否定命令形」。<br>🔴 **判据写宽了四次，全是自己逐条核对时看出来的、不是闸逮的**：① `させられ|られ` 漏掉 `書かされます`（五段的口语使役被动不含「られ」）② 进行体分界漏掉缩约形 `合ってない` ③ ら抜き第一版「同原形下有られ对照」把サ変的文語可能形 `熱しえる`（〜し得る）误标 ④ 回归闸 I2 把只在现代表上验过的列读取器用到**全表**，767 条里 645 条是误报（`きたなかった` 撞上后缀 `なかった` 被读成「否定过去」、`明らかだ` 的だ被读成「过去」）—— **判据只在它被验过的域里有效**。<br>🔴🔴 **改成「清空重建」时差点静默撤销另一次修复**：`inflection` 不止本阶段一个写入方，`fixes/recover_pointer_senses.py` 的 375 行也住在这里。第一版的 `DELETE FROM inflection` 会把它们一起冲掉 ⇒ 建了 `OWNERS` 登记表 + **「对不上账就停机」**的闸（判据只认写入方、不认格式版本，否则 src_ref 格式迁移会被误报成外来行）。<br>🔴 **顺手收掉一个原来就有的坑**：`src_ref` 少了「同 (词,词性) 的第几条」，dump 里 5,050 个 (词,词性) 因词源分多条 ⇒ 库里 **5,155 组重复 src_ref**。按它删行会误伤另一条目（`[[primary-key-is-not-enough]]`）。<br>⭐ **两个方向都量过才落库**：新增（原形,词形）对 **4,425**，**消失 0**。第一版的「消失」度量本身是错的（拿被删的子集比新插的子集，报出 124 条假消失），改成比**改动前后的全集**。<br>⇒ `inflection` 567,717 → **573,105**（本阶段拥有的行），`dict` 709,822 → 714,173。零模型调用、零成本。<br>✅ 闸：`infl_table.check()` 判据自检（列读取器 + 分界连续性，读全量 dump）；`infl_compose` 自检 19 条；回归闸新增 **I1–I7**（含两条可逆性回核、两条「只问在不在」），变异 **M32–M38 7/7**；C1 基线收紧 995→863、C3 收紧 9,765→9,763 |
| 20 | ~~**专名义项的中文丢了分类**~~ | 10,496 条 | **✅ 已还（2026-09-18）—— 用户看 `桜` 的页面发现的。**<br>三条义项英文分别是 `a female given name`／`a placename`／`a surname`，**中文全印「樱」** —— 三件不同的事在页面上长得一模一样，读者无从分辨，而源头本来分得清。<br>⭐ **库里本来就有正确写法**（1,413 条：`数（姓氏）`／`月（女性名）`／`女性名`）⇒ 不是没定过格式，是这批翻译没跟上。按已有格式统一，没有发明新写法。<br>🔴 **判据从枚举换成正则，多逮到 1,293 条**：第一版拿 8 个字符串精确匹配（11,262 条），而同族变体一大把 —— `A place name.`（大写+句号）／`歩子: a female given name`（带词形前缀）／`an unknown-gender given name`／`surname, family name`。换正则后覆盖 **12,555 条＝89.9%**（`[[criteria-narrower-than-you-think]]` 第 N 次）。<br>⚠️ **带修饰的 1,408 条有意不碰**：`a surname from Okinawan`／`a male given name of historical usage`／`a transliteration of the English male given name James` —— 套模板会把「冲绳」「历史用法」「英语转写」丢掉，那是**拿一个缺换另一个缺**。🔴 **推翻它需要**：给出能把修饰也译进中文的规则。<br>⭐ **中文侧分三档，只有第二档套模板**：① 已带分类 1,413 条不动 ② 只有译名 **10,487 条** → `樱（女性名）` ③ 中文说的是别的事 **10 条 → 逐条人工定，一条都不套模板**。<br>🔴 **第三档里有一条是"改了才错"**：`下の名前 | given name → 名（人名中去掉姓氏的部分）` —— 这个词的**词义本身**就是「名（相对于姓）」，英文的 `given name` 在那条上是**释义不是分类标记**。套模板会写成「名（人名中去掉姓氏的部分）（名）」，把一条正确的释义改成病句 ⇒ **有意不改**，人工表里记 `None`。<br>🔴 另外 9 条分两种，都是模型翻译的残渣：**译成了词的字面义**（`泉水 | a surname → 庭院中的水池`／`里子 → 寄养的孩子`／`百代 → 世世代代，永久`）和**凭空编了个不相干的名字**（`剣 | a female given name → 麻也香`／`清 → 沙也加`）。后者是**错不是缺**。<br>⚠️ **写后回核第一版三条全红，而三条都是判据写宽了、数据是对的**：①「仍无分类的」期望值算成 1（`下の名前` 的中文含「名」本来就不计入）②「括号没配对」查全库 ⇒ 逮到 `…如左括号「（」或左引号…` 那条**正在讲「左括号」这个符号**的正常释义 ③「只剩括号」查全库以「（」开头的 ⇒ 逮到 709 条 `（非正式）我爱你` 这类既有格式，而我改的那批**一条都不在里面**。⇒ 判据一律收窄成「形如 `X（分类）`」。**同一个病一天犯到第四次。**<br>⭐ 回归闸 **A8** ＋ 变异 M31（把分类后缀整批删回去），判据 `classify` import 生成侧那一份，源头将来多一种模板它会跟着变 |
| 14 | ~~**「猫的三个 a cat」**~~ | 3,266 条 | **✅ 已还（2026-09-17）—— 但先纠正我自己的误判。**<br>🔴🔴 **本条原来写的是「义项跨版重复 19,633 条」，那个诊断是错的。** 回源一查：`猫` 的三块**全部来自 en 版同一份切片**，是三个不同的 `entry`（etym 0/1/2）——<br>　`汉字`（猫这个字）／`名词 ねこ`／`名词 ねこま`。**不是重复，是三个不同的词条。**<br>⭐ 我当时的判据是「同词形下中文释义相同」，它把**三件不同机制的事**混成了一桶。按机制拆开（中英文双同的 6,319 条多余义项）：<br>　**② 不同读音 3,266 条** —— `馬`＝うま/むま/んま/んーま/ば 五读、`国`＝くに/こく。**一条都不能删**，要做的是**在页面上标出读音**。<br>　**① 同一个 entry 内重复 2,819 条** —— 真重复，但根子不是收词，是多层 gloss 被压平（见欠账 18，已修，**2,819 → 1,574**）。<br>　**④ 同读音不同 entry 294 条** —— 多数是**词性不同**（`愛` 的 noun 与 affix），分组本来就分得开。<br>⇒ 本条只修 ②：`JapaneseSense` 加 `kana`，分组标题变成「名词 ねこ」「名词 ねこま」。<br>🔴 **只在真有 ≥2 个不同读音时才标**，单读音词（`痛い`）不标 —— 判据是事实不是"保险起见都标"。契约闸**两个方向各一条**（多读音必须标／单读音不许标），变异 2/2：只查前者的话，一个"全都标上"的实现同样全绿，而那会给每页加一串噪音。<br>⚠️ **教训**：`[[measure-landing-not-source]]` 的反面 —— 我量了「落点长什么样」（中文释义相同）就下了「跨版重复」的结论，**没回源看一眼这三条义项是从哪来的**。一个 `SELECT entry_id, etym_no, kana` 就能推翻它 |
| 18 | ~~**多层 gloss 只收了第 0 层 —— 具体释义整条丢掉**~~ | 1,810 条 / 547 个词形 | **✅ 已还（2026-09-17）**：`ja/pipeline/gloss_levels.py` ＋ `ja/fixes/fix_hierarchical_gloss.py` ＋ `ja/pipeline/translate_hier_gloss.py`。<br>🔴🔴 kaikki 的 `glosses` 是**层级数组**：第 0 层是伞形标题，最后一层才是这条义项真正说的话。三处生成侧写的都是 `glosses[0]` ⇒ **伞形留下、具体释义整条丢掉**。<br>　`ある` 19 条义项里 12 条共用伞形「to exist, to be, to have」⇒ 落库后同一句印 7 遍<br>　`長谷川` 40 条不同河流共用伞形「Nagatani River (…)」⇒ 40 条河全丢，40 行都写「长谷川」<br>　伞形单独看常常没有信息：`in kabuki:`／`short for various terms:`／`After the て-form of a verb:`<br>⭐ **它解释了「同 entry 内重复」那 2,819 条里的 1,247 条（44.2%）** —— 修完实测 **2,819 → 1,574**（同一条查询）。剩下的是源头自己就有两条同文义项（`日向` 的 9 条 `a surname`）和译文压平那一类，不是这个 bug。<br>**落库形状**：`sense_src.text`／`sense_gloss(en,'definition')` ＝ 具体释义；`sense_gloss(*,'umbrella')` ＝ 伞形（**只有多层时才有这一行**，zh 1,739 ／ en 1,810）。<br>💰 **重译实跑**：2,395 条池子（1,810 具体 ＋ 585 伞形）／40 批／**200,366 token**／**零失败**／空闲半价 **≈ 0.49 元**。切片质量 99.17% 干净、**伞形漏进译文 0 条**；全量 96.69% 干净，逐条读四个失败桶 **E1/E6/E4/E2 基本都是正确译文**（词条讲的就是假名、BIG MOTOR 这种专名、学名 `Naja naja` 按规则保留）。<br>🔴 **`src_ref` 不许反解析**：它里面的 `seq` 是运行计数器、词形本身可能带冒号。我先写了一版 `ref.split(':')`，1,866 条只对上 1,091，**对不上的 775 条不报错、只静默漏修** ⇒ 抽出 `build_entry_layer.iter_kk()`，回填与生成走同一个循环。<br>🔴 **两道闸各逮一件**：① 新露出来的具体释义里有一条带 `===Etymology 4===` 章节残渣 —— 回归闸 A4a 报的，重跑 `fix_gloss_residue` 修掉；② 新买的中文又带回繁体 —— A5 报的，重跑繁简与台湾用词两个修复。**第三次是同一个形状：付费跑批的产物一定要过回归闸。**<br>🔴 **字面量闸也响了一次**：上一轮我给 F7 写的 `384 - count` 被它当场打红。它是对的 —— 384 是**这一版 dump 的数**，换一份就过期而且会一直绿。改成**只问在不在，不问几条**。<br>⚠️ **ja 版还欠着**：`intake_edition_words` 的生成侧已经一起修了，但**已落库的 ja 版多层义项没有回填**（同 entry 内 ja 释义相同的多余行 457 条，`たま` ×10）。零成本、纯确定性，下一轮做
| 19 | ~~**罗马字长在词形字面里 ＋ 一个词形只印一个语法标签**~~ | 3,721 词形 / 41,835 组合 | **✅ 已还（2026-09-18）**：`ja/fixes/fix_inflection_form_romaji.py`（数据）＋ `build_inflection_layer.py`（生成侧）＋ 服务层 `baseOf`。<br>用户点开 `食べる` 活用表的「可能敬体」，落到的页面词头印着 **`食べれます [taberemasu]`**。<br>🔴 **方括号那截长在 `dict.word` 里，不是展示层拼的。** 英文版维基词典的活用表把**词形和转写写在同一个单元格**：`{'form': '食べれます [taberemasu]'}`，阶段 2 `f["form"].strip()` 原样收下。后果三样：词头与兜底药丸两处都印着它、TTS 连括号一起念、搜裸词形匹配不上。<br>⚠️ 这**不是「源头没给」也不是「我们没抽」，是抽到了塞错了格子** —— `[[dont-say-source-lacks-what-we-skipped]]` 的第三种形态。日语版活用表不带转写 ⇒ 受影响的 3,721 个词形**全部来自英文版**（en 189,103 行里 3,866 行，ja 378,989 行里 0 行）。<br>⇒ 转写**不丢**，搬进新列 `inflection.romaji`（源头在那个单元格里给的事实）；`dict.word`/`word_norm` 洗成裸词形；洗完撞上已有词形的 152 个，把活用行改挂过去（改名 3,279／合并删除 442／改挂 452 行）。<br>🔴🔴 **顺带逮到更严重的一条：服务层 `baseOf` 写着 `MIN(label_zh)`。** `食べられます` 对 `食べる` 同时是**被动敬体**和**可能敬体**，`MIN` 按字符串序挑一个印出来 —— **在页面上断言「它只是可能敬体」**。全库 465,206 个 (词形, 原形) 组合里 **41,835 个（9.0%）带多个标签**，最多的 5 个。这与那行代码正上方注释里写的「随便挑一个当原形＝断言一件假事」是**同一个毛病低了一层** —— 上一轮把 `bases` 复数化了，没往下看一层（`[[criteria-narrower-than-you-think]]`）。<br>⇒ 版面同时重做：小标题「这是以下词的活用形」（句子，与旁边两个名词指针块语气打架、而且方向印反了 —— 本页**是**活用形）换成 **「原形」**；语法标签做成可重复的小片；读音行缺席时的兜底药丸印**转写**而不是把词头复制一遍。<br>✅ 闸：回归闸 **F8/F9 成对**（F8 词形又长回方括号／F9 转写整列被丢掉 —— 只查 F8 的话「把转写删光」会显示成洗干净了），变异 18/18；展示层契约 **+4 条**，逐条变异验证（糊回方括号／转写置空药丸退回印词头／模拟 `MIN` 只留一个标签／标签黏成一串）4/4 逮到。<br>⚠️ 顺带把 `A1 义项没有中文` 的基线从 3,202 收紧到 3,139 —— **闸报「⬇ 该收紧」报了两轮我才改**（`[[fix-regression-and-gate]]`：不收紧＝这 63 条哪天掉回去它一声不响） |
| 15 | ~~**关系目标里混着中文译名**~~ | 91 行 | **✅ 已还（2026-09-19）**：`ja/fixes/fix_relation_chinese_targets.py` ＋ 生成侧 `is_chinese_target()`。<br>🔴🔴 **三个版本三种机制，一条判据管不了 —— 而我第一版合成了一条，差点删掉真数据。**<br>　**中文版**（81 条）：`derived`/`related` 数组把**日语词与它的中文释义交替排列**，中文那一半**只有 `word` 一个键、没有 `roman`/`ruby`**。判据＝`裸` ∧ `含简体专用字` ⇒ 精确命中 81。⚠️「裸」单用太宽（6,744 条，含 `五月`/`早苗月`）。<br>　**日语版**（19 条）：把日语写法与中文简体写法**成对列出**（`漢語`/`汉语`、`漢奸`/`汉奸`）。<br>　🔴 **英文版那 40 条根本不是中文，是拡張新字体** —— 源头把 `呕唖` 与 `嘔啞` 成对列在 `character` 条目下、tag 写着 `extended shinjitai`。我拿「含简体专用字」一把抓时它们全在里面。**是回源看机制救了这 40 条，不是闸。**唯一已知的假阳性 `侭田`（姓氏）也来自英文版 ⇒ 排除英文版自动躲开。<br>⭐ **用 `hidden=1` 不删**（`sense_relation.hidden` 是既有机制，展示层查 `hidden=0`）——判据虽验过，残留假阳性仍可能，可逆才敢动。<br>⚠️ **残差 14 条已记基线**：带 roman/ruby 躲过生成侧判据的那一档，其中 11 条真中文、**3 条其实是日语**（`唖呕`/`蓬蔂`/`兵馬倥偬`）⇒ 再收窄判据到 0 就是拿「错」换「错」，要清零需要的是逐条裁决不是更狠的判据。<br>✅ 回归闸 **Y2**（带基线 14）＋ 变异 M45
| 16 | ~~**关系目标含空格，一条判据管不了三类**~~ | 2,029 行 | **✅ 已还（2026-09-19）**：`ja/fixes/fix_relation_space_targets.py` ＋ 生成侧 `clean_target` → **`clean_targets`（返回列表）**。<br>实际是**五种**形状不是三种（账上那三类的描述已过期）：并列表 `縞馬, 斑馬` 拆开／罗马字回显 `女郎 (jorō)` 剥括号（欠账 13 清过**串尾**回显，带括号是同一个病的第二种写法）／箭头 `兄様 → 兄さん` 两边都留／`ごみ箱 rubbish bin`、`あたし 30%`（问卷百分比）取前半／`(2ch slang) セクースする`、`Ibaraki) しぐ` 取后半。剩下的活用构成公式（`stem + い`）与表格残渣挂 `hidden=1`。<br>⇒ 改写 834 ＋ 挂起 1,195（其中 563 判为不是词、632 洗完与已有目标重复）＋ 新插 301 片拆分。<br>🔴🔴 **判据在这一轮里写错了四次，四次都是抽样反验或可逆性回核逮的、没有一次是闸**：<br>　① 镜像规则把 `ren'yōkei + ましょう` 的尾巴 `ましょう` 当成词收下来（活用后缀不是相关词）<br>　② 镜像规则从整句英文里抠尾巴（`that consists of suffixing verbs with…` → `遊ばせ`）⇒ 加「前缀必须像标签（`)`/`]` 收尾）或很短」<br>　③ 模板残渣 `ja-cardinals?action=edite…` 的末段有日文无空格，混着拉丁字母被放过 ⇒ `_one()` 加「不许混拉丁字母」<br>　④ 🔴 **并列表把顿号当分隔符 ⇒ 会拆坏 50 多条谚语**（`井の中の蛙、大海を知らず`）。实测已落库的 297 条拆分**全部来自半角逗号、顿号零条** ⇒ 顿号分支在真列表上没用过，却会毁掉谚语。**可逆性回核逮到的。**<br>　⑤ `+` 拦截（挡活用公式）放在「无空格就原样返回」**之前** ⇒ `C++`／`LGBT+`／`+α` 被误杀。我验「含 `+` 的全是公式」时**只在含空格那批上验过**却用到了全部目标 —— `[[criteria-narrower-than-you-think]]` 同一天第三次。<br>🔴 **回归闸 F2 当场逮到 19 条自指**（`MHD 発電` 洗成 `発電`，而它就挂在 `発電` 页上）。生成侧的 `add()` 一直有这条检查，是落库脚本漏了 —— 欠账 13 那轮清洗造出 86 条自指也是 F2 逮的。**同一个形状第二次，两次都靠同一道闸。**⇒ 自指是**删**不是挂起：`hidden` 说的是「真数据但不展示」，而自指行是清洗的副产物；把 F2 放宽去迁就我造出来的行，等于把闸变成背书。<br>✅ 回归闸 **Y1（可逆性回核，0 违例）／Y3**，变异 M44/M46
| 17 | ~~**活用表不按口语/文语分区**~~ | 864 个词 | **✅ 已还（2026-09-19，见欠账 21）。**本条原来写的是「**有意不做，理由是没有证据**」—— 而证据一直在源头手里：kaikki 用 `ja-conj-bungo` 模板**把文語表单独分了块**，我们扁平遍历 `forms` 时**把这个分隔符整个丢了**，于是 864 个词的文語形混进现代活用里印（70 个词两套并排）。<br>🔴 **教训不是「当时判断错了」，是「有意不做」的理由写成了「没有证据」而我没去找**。`[[record-the-negative-decision]]`：否定结论必须写「什么会推翻它」—— 本条当时写的推翻条件是「源头给出分区信息」，**而源头当时就给了**，只是我没去看。⇒ 现在 29,108 行带 `bungo` 标记，页面上印「文語终止形」「文語已然形条件形」。回归闸 I4 + 变异 M35
| 2 | ~~**`posLabel()` 没有 lang 参数**~~ | 结构 | **✅ 已还（阶段 8）**：加了 `lang?` 参数 + `packages/dict-labels/src/ja.ts` 覆盖层。省略 `lang` 时行为不变，六门调用点没动 |
| 4 | ~~**活用类没落库**~~ | 24,204 行 | **✅ 已还（2026-09-16）**：`ja/pipeline/fill_vclass.py` → `entry.vclass`，展示层印在**词头旁边**（`保護` → サ行变格活用）。<br>🔴 **活用类＝标签组合，不是单个标签**：`irregular` 单独没有意义，`irregular+sa-row` 是サ変、`irregular+ka-row` 是カ変，**两者是不同的类**。<br>🔴🔴 **按词形匹配会把类写到该词形的每一条 entry 上**，污染 170 条：`そう`（副词/感叹词/接尾）拿到了动词 `添う` 的 `godan-wa`、`メス` 拿到了 `召す` 的。⇒ 判据改成问**这个词性会不会活用**：动词全收／名词·专名·副词只收 `sa-irregular`（名詞＋する、副詞＋する）／连体词·接尾辞·助词一律不收。⚠️ `adv` 放开是量出来的 —— 挡它会丢 285 条，而**副詞＋する 是能产的**（擬態語＋する）。<br>🔴🔴 **有意不做「按读音传播到汉字写法」。** 日语版把词条挂在假名词头（`あるく`）下，所以 `歩く`／`書く`／`読む` 拿不到活用类。我试过按读音传播并加了「读音唯一对应一个活用类」当保险 —— **那个保险是假的**：`犬[いぬ]` 名词会拿到动词 `去ぬ` 的 `godan-na`、`国[こく]`→`godan-ka`、`数[すう]`→`godan-wa`。唯一性只检查了**日语版动词条目内部**的歧义，看不见「犬 和 去ぬ 是两个完全不同的词」。⇒ 放弃。🔴 **推翻它需要**：日语版给出汉字写法字段，或出现别的能连接两种写法的结构信号 |
| 5 | ~~**755 条颜文字的转义泄漏**~~ | 16 条 | **✅ 已还（2026-09-19）**：`ja/fixes/fix_escape_leak.py`。⚠️ **账上那个 755 到今天只剩 18 条含反引号**（其余是先前几轮清洗顺带带走的）—— **账本里的数字会过期，复量之后才敢动手**。<br>`(^.^)/`tilde``tilde``tilde`` → `(^.^)/~~~`，16 条。🔴 **判据只认成对的 `` `名字` ``，不认裸反引号**：`(´・ω・`)` 与 `(´･ω･`)` 里的反引号**是颜文字字形本身**，「凡反引号就处理」会拿两个真词条换十六个坏词条。<br>⭐ 四个桶（要修 16／字形自带 2／未知转义 0／撞词形 0）**加起来必须等于 18**，并写了断言 —— 第一版漏了「裸反引号」那一桶，总数静默印成 16
| 6 | ~~**阶段 3 之后要重连 `base_id`**~~ | 221,135 行 | **✅ 已还（阶段 3a）**：悬空 221,135 → **988**，重连 220,147 行。空白页率 1.83% |
| 7 | ~~**免费中文释义里 13.5% 是繁体，页面上繁简混排**~~ | 11,244 条 | **✅ 已还（2026-09-16）**：`ja/fixes/fix_traditional_to_simplified.py`，改了 **16,180 条**（释义 11,526 ＋ 例句译文 4,654）。零成本、确定性。<br>🔴🔴 **不能直接 `t2s` 跑一遍 —— 它会改坏引文。** 这些释义会引用日语正字法，而 `opencc` 不认识「这几个字是被引用的日文词形」：`【敢え無くなる】`→`敢え无くなる`、`【見頃】`→`【见顷】`、`「問いて」`→`「问いて」`。`[[source-typo-fix-ours-not-quote]]`：**改我们的出版文本，引文一个字不动。**<br>⭐ **判据量过 177 个 `【】` + 1,257 条含引用结构的释义才定**：①`【…】` 内一律不转（含假名 103／纯汉字 36 —— 其中 9 个会变的**逐条读全是日文词形**：鬱蒼・一所懸命・鳩舎・蝦蛄・絨毯・名詞・明視・毛頭・見頃／拉丁 38，`t2s` 本来不动）；②`「」『』（）` 内**含假名的**不转（29 条）；③其余全转。<br>⚠️ 我原本担心 `【數學】【化學】` 这类**中文学科标签**会被误保护 —— 实测当前数据里一条都没有。**担心要量过才知道是不是真的。**<br>🔴 第一版想把**所有**括号都保护起来，那会漏掉 900 多条该转的（`（職務）`→`（职务）` 正是要的）—— **判据比它要描述的东西宽，代价是「修了等于没修」**。<br>🔴🔴 **`opencc` 的 `t2s` 不是幂等的**：跑一遍之后 30 万条里还剩 **2 条**会变（`芸薹`→`芸苔`、`惯於`→`惯于`，第一遍的产物又命中了第二条规则）。**是闸的「必须为 0」逮到的** —— 写成「基本转完了」就漏过去了。改成跑到不动点（封顶 5 轮、到顶抛异常）。<br>⭐ 回归闸加了**一对**断言，必须一起看：A5「还有繁体吗」（抽样 1/15，0.25 秒；全量 3.7 秒留在修复脚本自己的闸里）＋ **A6「日文词形的原字还在吗」** —— 🔴 **A6 为 0 时 A5 反而是绿的**（全转成简体正是 A5 要的），单看 A5 会把「保护失效」当成「修得很干净」。<br>⚠️ **判据管不到的一类，明说**：不带任何括号、直接内嵌的纯汉字日文词识别不了，会被一起转。量不出来（纯汉字字形上分不出中日），不假装它不存在。🔴 **推翻/收紧它需要**：出现能标出「这段是日文引用」的结构信号，或者有人报出实例 |
| 9 | ~~**指针义项从未到达读者**~~ | 3,891 个词 | **✅ 已还（2026-09-16）**：`ja/fixes/recover_pointer_senses.py`，空白词元 **1,040 → 125**。<br>🔴🔴 **每一步都按自己的规矩做对了，跨步的那件事没人负责。** 阶段 1.5a 有意不出版指针义项（账上写着「归阶段 2」），阶段 2 建 `alt_of` 时认的是 `redirects`/`forms` 两个**结构字段** —— 而英文版把指针写在**义项正文里**（`EU; initialism of 欧州連合`）。5,144 条证据只有 716 个词转成了关系。`[[correct-steps-can-compose-a-hole]]`。<br>⚠️ **是起了服务、真敲进搜索框才看见的** —— 契约闸断言的是词条页，下拉框不在它视野里。<br>🔴 抽取**抓结构不抓词表**：第一版列关系词表只认出 70.1%，漏掉 `Kyūjitai form of`/`past form of` 这些我没想到的说法（**枚举永远列不全**）；改成通用 `<短语> of|for <目标>` ⇒ **99.7%**，89 种短语全部登记、**不留「其他」桶**（`[[residual-bucket-is-not-evidence]]`）。<br>🔴 **四个去向按「这条指针在说什么」分**：异表记→`alt_of`／旧字体→`kyujitai`／缩写→`abbreviation`／**活用形→`inflection` 不是关系层**。把「`見た` 是 `見る` 的过去形」写成语义关系，等于告诉读者那是两个词。中文名复用 `infl_compose.compose()`，顺带给那张表补了 5 个缺的 tag（不另建表）。<br>🔴 **回归闸连着逮到三件**：F1 我加了新 kind `kyujitai` 却没更新闸的值域；F3 新建的 alt_of 让 33 个词形有了 ≥2 个目标、**违反 §二.5** —— 而我第一版只看「本步新建的」，漏了「库里已有的」，**同一条规矩上第二次漏聚合**（阶段 5b 漏的是跨词条，这次漏的是跨步骤）；C1 基线 988→995（7 条新活用的原形不在库里，已写明理由）|
| 10 | ~~**台湾说法**~~ | 32 条 | **✅ 已还（2026-09-16）**：`ja/fixes/fix_tw_vocab.py`。🔴 **不用 `opencc tw2sp`** —— 它词表驱动、比 `t2s` 激进，大概率咬到日语引用，为 32 处引进更宽的判据不划算。<br>🔴 **手工清单的结构性缺陷要写明**：清单漏掉的下次不会有人发现 ⇒ 做成回归闸 A7 锚在表上，守住的只是「已知的不回潮」。<br>🔴 第一版两处错：①「源头已给两种说法」按**分隔符**判（`/`），而 `网际网路，互联网` 用的是逗号 ⇒ 改成按含义逐词问；②`网路` 比 `网际网路` 短、先匹配把长的挡住 —— 正则 first-match 第四次（`[[regex-alternation-order]]`）⇒ 按长度倒序。<br>⚠️ **有意不改**：源头自己给了两种说法的 10 处；`滑鼠蛇`（正经中文蛇名，我第一版的检测清单把它误判成台湾词汇 —— 判据又宽了一次）|
| 8 | ~~**收词之后哪些层要重跑，没有清单**~~ | 结构 | **✅ 已还（2026-09-16）—— 但还的不是一张清单，是一道闸。**<br>🔴 清单我已经写过三次（pt 的变形层、ja 的读音层、ja 的中文覆盖），**三次都是事后补的**，而且每次都写了教训、每次都没拦住下一次 —— 因为教训只在我**主动回忆**时才响，而「我刚发现一批新词可以收」恰恰是我最不会回忆它的时刻（`[[lesson-must-become-mechanism]]`）。<br>⇒ **收词闸**：`dbtool.session()` 里 `__rows__ > 0` 就**必须**传 `invalidates=[...]`，否则当场退出并列出已知与词元数挂钩的层。传 `invalidates=[]` 也行 —— 但那是一次**显式声明**「我想过了，一层都不受影响」，不是默认。**默认值的方向决定了它拦不拦得住人。**<br>⚠️ 名单从 `test_plan_ledger.COVERAGE` 读，**不在 dbtool 里手抄一份**。写完之后在**最后**大声打印要重跑什么（那是人还在看屏幕的时候）<br>🔴🔴 **2026-09-20 合并重复行**：本条原来在表里出现**两次且结论相反** —— 一行 ✅（闸建了）、一行 🔴（清单没建）。两行各对一半，而**账的闸 P1–P7 对「同一编号出现两次」结构性失明**（它查交付物在不在、查 ⚪ 有没有推翻条件，没有一条查账本自己有没有重号）。⇒ 已合并，并补了 P8「欠账编号不许重复」。<br>**现状拆开说**：① **闸已建、这是 ja 交付的那一半** —— `ja/dbtool.py:619`，`__rows__>0` 不声明 `invalidates=[...]` 当场退出；② **清单仍缺**，那一半是跨门的 ⇒ **已搬到 `docs/BACKLOG.md` 的 B3**，本表不再寄存它|
| 11 | 🔴🔴 ~~**写库闸门只守住了 23 列里的 4 列**~~ | 19 列 | **✅ 已还（2026-09-16）**，而这是做欠账 4 时**顺带撞见的、最严重的一件**。<br>`snapshot()` 只在 `dict` 上找列，而 ja 是 v3 原生库 —— **日语全部的一等字段**（`kana`/`romaji`/`ipa`/`pitch_mark`/`pitch_pos`…）住在 `entry`/`pronunciation` 上。TRACK 列了 23 个，**实际守住 4 个**（kanji_grade / pos / level / freq_zipf），另外 19 个静静躺了整个项目、一次都没守过。<br>🔴 **为什么一直没人发现**：`snapshot` 有一条「TRACK 里还不存在的列就跳过」——那是给「先写进清单、再由脚本建出来」留的**合理**口子，但它让**「名字写错了」和「还没建出来」长得一模一样**。这是 `[[criteria-narrower-than-you-think]]` 的极端形态：判据窄到只覆盖一张表，而清单看起来覆盖了十几个字段。<br>⇒ ① TRACK 条目支持 `表.列`，现在守着 **5 张表 21 列**；② 新增 `_track_audit()`：库建完之后还找不到的条目**大声报出来**（已验证会响）。<br>⚠️ **这条要传给别的语种** —— de/pt/fr 的 TRACK 是 v2 布局（列在 `dict` 上）所以没这个病，但**下一门 v3 原生的语言会原样重犯**。已写进 `PLAYBOOK` |

---

# ✅ ja 完结（2026-09-20）

阶段 **-2 → 9 全部完成**（含前六门没有的 1d／1e／1f／4c 四步），**五道闸全绿**。
本节数字是 2026-09-20 收尾时**重新查库量的**，不是过程中的旧账。

---

## 🔴🔴 这个标记 9-20 当天打上、撤回、再打上 —— 中间那次撤回是本轮最贵的一课

**第一次打标记之后，整理文档时发现 `scripts/ingest_etymology.py` 的语种名单里没有 ja。**
一查：**ja 根本没有 `etymology` 表**，而 de 176,857 行／fr 309,869 行／it 101,845 行。
回源量：英文版 dump 里 **46,196 个条目带 `etymology_text`（23.2%，均长 91 字），我们一条都没抽**
（日语版与中文版都是 0 条，那两版确实没有）。⇒ 当天撤回标记，补出阶段 1f。

⚠️ **这正是本节下面写着的那条教训本身** ——「源头给了而我们没抽」，而我在总结它的同一天
又漏了一次，还先盖了章。`词源` 这两个字在本计划表里原来只出现过一次，还是「不同词源分条」的意思：
**这一层从没规划过、没建过、也没记成一个「有意不做」的决定。**

🔴 **「阶段表全 ✅」只说明我列出来的阶段都做完了。** 它对**我没列进阶段表的层**结构性失明 ——
与 `sense_tag` 空了整个项目是同一个形状（行数闸对 0 行是一致的，阶段表对缺席的阶段也是一致的）。

### ⇒ 这次不再写成教训，写成了三道会自己响的闸

教训写成文字的，这一轮已经证明守不住（`[[lesson-must-become-mechanism]]`）——
同一天我一边总结「源头给了而我们没抽」，一边又漏了一层。所以这次的交付物是闸不是段落：

| 闸 | 它问的问题 | 忘了登记会怎样 |
|---|---|---|
| `contract-check-etym.tsx` 的 `registryGate()` | **库里建了 `etymology` 表的语种，在不在这道闸的名单里** | 当场红，且指名道姓说「这门的词源层没有任何闸」|
| `scripts/test_backup_policy_gate.py` 的 `_discover()` | 磁盘上有 `dbtool.py` 的语种，在不在 `LANGS` 里 | 当场红（2026-09-20 加 ja 时正是它发现 ja 从没被检查过）|
| 账的闸 P6「有英文版词条的词里带词源正文的占比」| 这一层还在不在、被收词稀释了没有 | 分母会变，掉破 30% 下限就红 |

⭐ **三道闸的共同点：名单从磁盘/库里推，不从我的记忆里推。**
下一门语言不需要「记得改这三个地方」—— 忘了它们会自己响。
这是本轮与前六轮最实质的区别：前六轮的收尾单写满了「下次要记得」，一条都没守住。

## 成品清单

```
库文件            493,809,664 字节 ＝ 470.9 MiB（词源层落库后重量的；本行原写「487 MB」，单位与口径都对不上）
dict 词形            714,173   词元 248,983 ／ 有频次 67,881（27.3%，只给单 token）
                               核心词等级 7,989（内部尺子，不上页面）
entry 词条           249,727   有假名读音的词元 184,873 = 74.3%
sense 出版义项        296,549   有中文 293,410 = **98.94%**
sense_src 证据行      304,222   已认领到出版义项 296,549 = **97.48%**（三版都有桥）
sense_gloss          542,826     sense_tag 72,139（覆盖 35,181 条义项 = 11.86%）
sense_relation       364,599   可见 363,313（挂起 1,286 ＝ 中文混入 ＋ 不成词）
inflection           573,480   覆盖 458,243 个词形
pronunciation        118,831   覆盖 64,653 个词形；其中**声调 18,469** 个词形
kanji_reading          7,272   1,519 个汉字（常用汉字覆盖 53.5%）—— **日语独有的一层**
etymology             40,270   36,607 个词形（有英文版词条的词里 36.74%）—— 源头天花板 23.2%，日语版/中文版各 0 条
example               53,309   可见 52,871，有中文译文 52,654 = **99.6%**
audio                  1,038   953 个词形，全部 Commons 真人录音，**只落 URL**
search_prefix          1,890   （63 个前缀；日语一字前缀 15,102 种、大多候选很少，见 §四）

空白页（无义项、无变形、无指针）  9,763 = **1.37%**
核心词的声调覆盖                 42.4%（N5 55.1% → N1 35.6%）
```

## 五道闸

```
回归闸  ja/tests/test_no_regression.py    70 条 / 13 组（A–L ＋ I/J/K/S/X/Y ＋ **Z 词源**）· 9 条带基线
        └ X5（2026-09-20 新增）＝**统计信息不许比库陈** —— 判据 import `run_analyze.survey`
                                       · 变异 49/49（M50＝删掉 etymology 的统计行）
账的闸  ja/tests/test_plan_ledger.py      P1–**P9** ＋ **8 项覆盖率下限**（含词源层）· 变异 20/20
        └ P8＝**欠账表不许有重号**（8 号当时是两行且结论相反）
        └ P9＝**别门的账不许寄存在这张表里** ⇒ 顶层账本 `docs/BACKLOG.md`
契约闸  apps/web/src/contract-check-ja.tsx  66 条（真渲染，与 render-dump 同一条路径）
词源闸  apps/web/src/contract-check-etym.tsx  **跨七门** 7 条 · 变异 9/9 · 断言 7/7 有变异守着
判据自检 pipeline/infl_table.py · infl_compose.py   读全量 dump 验列读取器与分组连续性
```

跨语种的还有：布局闸、CSS 孤儿类名闸、七门标签映射闸、服务↔API 契约闸、备份预算闸。
前三道挂进 `dbtool.session`，每次写库自动跑（不变量 → 回归 → 账 → 字面量）。

🔴 **词源闸是这一轮新登记的第七门，而登记这个动作本身现在有闸守着**（`registryGate()`）——
　 它同时逮到了两件事：ja 的词源层缺席、以及**它自己的取样 SQL 会把 en 库撑爆内存**
　（`LIMIT` 写在外层 ⇒ 计划是 `LIST SUBQUERY`，先把全库合格的 `word_id` 整张物化，
　 实测吃到 **13.7 GB**、机器卡死；挪进子查询后计划变 `CO-ROUTINE`，**0.116 秒**）。
　⭐ 教训不是"少写一个 LIMIT"，是**取样语句要按最大的那门库来写**：
　 这道闸的取样量恒定（每门 24×2 个词），而**代价随库的大小走**，在最小的那门上测不出来。

## 成本

**约 44.8 元**：付费翻译 44.3 元（英译中 126,688 条 ＋ 日译中 83,488 条 ＋ 例句 44,068 条），
外加两轮模型咨询约 0.5 元。⭐ 2026-09-18／19 补的四层（音訓読み／义项标签／认领桥／核心词等级）
和所有修复**全部零模型调用** —— 源头本来就有，是我们没抽。

## 这一门语言真正花时间的地方

不是建库，是**「源头给了而我们没抽」**这一类。它和 pt 的「点进去空白页」是同一种病的两种长相：
pt 是阶段顺序错导致没连线，ja 是**判据把一整类数据排除在外而闸全绿**。

本轮逮到四次，每一次的共同点是：**行数闸问「我写了多少」，0 行也是一致的。**

```
sense_tag        建好之后空了整整一个项目      源头有标签，我们没抽        → 72,139 条
kanji_reading    §二判据 4 从「汉字没有单一读音」滑到「不用补」 → 7,272 条
sense_src.sense_id  ja/zh 两版插入时写死 NULL  证据层与出版层之间没有桥    → 154,777 条
普通体活用形      源头打了 `error-` 标记，我们整行丢弃  丢的正好是最常用的几列 → `食べない` 回来了
```

🔴 **四次里有三次不是闸逮的，是用户提问时我去数表、或用户看页面看出来的。**
⇒ 现在四层都有覆盖率闸（分母会变、稀释了就红），而不是只有行数闸。

## 收尾单的交接

| # | 交接项 | 状态 |
|---|---|---|
| 1 | **CC BY 署名义务**（`data/refs/jlpt-waller/`）| 🔴 **挂着**。`dict.core_src` 只是内部溯源，不构成对终端用户的署名；产品需在「关于／第三方数据」处公开署名。用户 2026-09-19 定「展示另说」⇒ **落地展示之前必须先办**。见欠账 22 |
| 2 | 关系目标残留 14 条中文（Y2 基线）| ⚪ 已记基线并写清：11 条真中文、**3 条其实是日语**（`唖呕`/`蓬蔂`/`兵馬倥偬`）⇒ 清零要的是逐条裁决不是更狠的判据 |
| 3 | `dict.level`（JLPT/CEFR）全 NULL | ⚪ **有意的**：能商用的只有一份民间重建的猜测表，印成难度标签撑不住（错比缺更伤权威）|
| 4 | `collocation` / `field_src` 两张空表 | ⚪ 有意为空，理由与推翻条件见欠账 24 与 `[[collocation-layer-is-llm-generated]]` |
| 6 | **词源层：源头有正文而我们没抽（+1,177 行）** | 📋 **已量清楚、未动手，用户 2026-09-20 收口「记进待完成工作项」。** 全部数字、改法、牵动哪几门、以及**必须先拆 Z3 且不许放宽**，见欠账 25。⭐ 其中 **102 条落在整页空白的词上**（源头这段正文会是它们唯一的内容）；⚠️ 但抽进来也不会显示 —— 要显示得先定「无义项的汉字页把词源印在哪」，**那是另一件事** |
| 7 | `data/tts/` 2.3 GB / 196,141 个文件 | ⚪ **已登记进 `data/MANIFEST.md`，没有删除，也不该出现在任何清理清单上。**它是 **196,123 个西语词的合成发音**（自带 `manifest.tsv` 索引）＋ Piper 音色模型 —— 对照 es 库里的真人录音只有 11,203 条，**覆盖的词是它的 17 倍**，正是方针④三级兜底里补大面积空白的那一级。目前成品库还没接线引用它，那是**接线没做**，不是它没用。<br>🔴🔴 **我在这一行上连错两版，方向相同：把用户和我自己做出来的东西写成无主产物。**v1 说「`docs/ES_PLAN.md` 里一个字都没有」—— **那个文件根本不存在**（es 的账本是 `docs/lang/es-README.md`，比 PLAN 格式更早），我 grep 它时 `2>/dev/null` 吞掉报错，**把「我没查到」当成「源头没有」**；v2 改成「删不删由用户决定」—— 事实对了但**框架仍错**，它默认这东西的归宿是删除。⇒ 判据：**登记表只描述「是什么、多大、谁在用」，不替任何人提议删除。** |
| 5 | 跨语种欠账 8「收词之后哪些层要重跑，没有清单」| 🔴 **不是 ja 的账，但 ja 今天又被它咬了一次**（阶段 5d 收词后没重跑读音层 ⇒ 9 个词的声调漏了三个月）。本轮做了**部分机制**：变形层与读音层都改成「幂等清空重建 ＋ 写入方对账」。清单本身仍缺 |

