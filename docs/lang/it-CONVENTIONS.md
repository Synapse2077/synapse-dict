# 意大利语约定宪法 + 记账本

> `FRAMEWORK` 五之二要求每个语种有一份自己的约定记录，**边做边写，不要最后补**。
> es 的教训是这些约定散在对话里，回头没人说得清当初为什么那么定。
>
> 本文件两部分：**① 约定**（定了就别再讨论，改要写明推翻理由和日期）
> **② 记账本**（`IT_PLAN` 规矩一：新发现默认进这里，只有「用户会看到错的内容」才允许打断当前阶段）。

---

## 一、约定

| # | 约定 | 定于 | 理由 / 出处 |
|---|---|---|---|
| A1 | **主源是英文版切片** `kaikki.org-dictionary-Italian.jsonl`，不是意语版整包 | 2026-08-12（沿用七月建库） | 实测意语版 38 MB 只有 40,090 条意语音标，法语版里的意语有 684,198 条。「做意语就下意语版」是错的直觉 |
| A2 | **意语原文释义来自 `itwiktionary`**，与英文版侧义项对齐后并成一条 | 阶段 1.5 定 | 英文版只给英文定义；意语版自己写的释义库里一条都没有 |
| A3 | **释义只保留三语**：中文 + 英文 + 意大利语 | 2026-08-04 用户方针 | 每个维基版只给"自己语言的词"写定义，对外语词只写翻译 |
| A4 | **例句只认 kaikki**，Tatoeba / OPUS 一律排除 | 2026-08-03 用户方针 | 同上 |
| A5 | **音标存裸串**，展示层统一加 `/.../` | 六语种统一 | 改动雷区见 `PITFALLS` |
| A6 | **不用 G2P 重算音标**；判据只能是"回权威源查不到 **且** 违反音系" | 2026-08-12 | es 上 `eigenvector → eixembeɡˈtoɾ` |
| A52 | **两把尺子分开：`norm_ipa` 决定存什么，`cmp_key` 决定算不算同一个读音** | 2026-08-17（阶段 4） | 存要保留源头的音节切分（有信息量），比要折掉各版排版习惯。合成一把 ⇒ 要么每个词多出假变体（表凭空翻倍，es 曾把"多变体规模"虚报成 98.8%），要么把真差异吞掉 |
| A53 | **比对时折：音节点 / tie-bar / 连音符 / 空格 / `j→i`·`w→u`；但保留「元音.元音」的点** | 2026-08-17（阶段 4，两家顾问意见相反后回数据判） | 全折会把 `euro` 的 `ˈɛ.u.ro`(三音节 hiatus) 与 `ˈɛw.ro`(二音节) 合成一条 —— 实测多合并 998 组、真差异被吞；保留 V.V 点只合并 401 组且逐条都是纯记法差（`ˈai⇔ˈaj`）。⚠️ **不折长音符 `ː`**（19 条，可能是真信息）。已知残差：音节界正好落在重音符上时（`Nauru`）区别仍会被抹掉 |
| A54 | **`is_primary` 不是按来源"谁大牌"排，法语版只在 G2P 的盲点上有覆盖权** | 2026-08-17（阶段 4） | 三版互核实测「另两版一致、只有它不同」的次数 **fr 1,965 / it 1,038 / en 663**；G2P 的弱点只有重音位置·e/o 开闭·s/z 清浊三件事（辅音正字法完全规则）⇒ 分歧只落这三件事时 fr 顶替（`Gabon` ˈɡa.bon→ɡaˈbɔn），含音段差异时保留我们的值（`ˈaːvatar` / `abberˈrate` / `moˈŋgɔːlja` 的 `ŋg`）。档位 **en>it>rule:accent>fr(盲点)>rule:plain>fr(音段)>llm>unknown**。🔴 **2026-08-18 修正**：原来把 fr 排在两条规则路径之前（理由是七月记的"fr 与有背书的层一致 87%"），回数据一量该理由不成立 —— 以 en/it 版为真值比**重音位置**：`rule:accent` **83.9%** / fr **64.5%** / `rule:plain` **44.8%**；fr 版的意语**变形形音标本身就是按倒二默认机器生成的**（`passano` fr `pasˈsa.no` vs 权威 `ˈpas.sa.no`；-ano/-ono 两边都有值的 6,846 个词形里 **66.8% 对不上**）。改档位后默认值与列不同的词形从 42,033 降到 **23,004** |
| A55 | **规则来源要分两档记**：`rule:accent`（带重音形路径）/ `rule:plain`（光杆默认路径） | 2026-08-17（阶段 4） | 前者自证 strict≈83%，后者与有背书的层只有 57.2% 一致 —— 混成一个 `rule` 就没法只让权威源顶替后者 |
| A56 | **`src` 记「谁背书了这个读音」，不是「这串字节谁写的」** | 2026-08-17（阶段 4，两家顾问一致） | 行按读音去重，一个读音一行；`panama` 存我们的 `ˈpa.na.ma`、标 `src=it-edition`（意语版给的是 `ˈpanama`）。严格按字节标会说成"意语版没给过这个读音"，那才是失真。原始字节可由 `src_ref` 的回源坐标取回，不另存一份 |
| A57 | **`pronunciation.entry_id` 只在源头里这个读音唯一属于一个 entry 时才写；NULL = 该词形各词条共用** | 2026-08-17（阶段 4） | `UNIQUE(word_id,ipa,notation)` ⇒ 一个读音一行，挂不了两个 entry（`gratis` 形容词/副词同读音）。🔴 **查询要写 `entry_id = ? OR entry_id IS NULL`（wildcard），不是"取不到再回落"** —— 回落写法会让"有专属次读音的词条"看不到共用的主读音（v4-pro 指出，豆包在这点上给的是错的） |
| A58 | **`pronunciation.region` 一律 NULL**；方言限定留在 `tags` | 2026-08-17（阶段 4） | 意语没有 es 那种半岛/拉美两分；源头的限定是 `Milan` / `Romanesco` / `Monopoli` / `italiano settentrionale` 这类，全库仅约 600 条，硬塞成地区码就是编造 |
| A59 | **例句挂义项靠源头坐标，而坐标格式三版不同** —— 一律**解析库里的 `src_ref`**，不自己拼字符串 | 2026-08-18（阶段 5） | en 是 `kk-en:<词>:<词性>:<词源号>:<seq>#<义项序>.<释义序>`，it/fr 是 `kk-it:<词>:<词性>#<义项序>.<释义序>`。我按 en 的格式拼给三版都用，it/fr 的 28,771 条例句全部报「挂不上」，而数据没有任何问题 —— **尺子错，不是数据错**（A33） |
| A60 | **例句原文里混着外语译文的，整条跳过，不按破折号切** | 2026-08-18（阶段 5） | fr 版 884 条把法语译文塞在原文里（`Aspetta un attimo. — Attends une minute.`）且 `translation` 字段为空 ⇒ 切不开。意语句子本身就用破折号，按破折号切会毁掉真例句（A45 形式代理）。**缺一条例句 < 正文里出现法语** |
| A61 | **关系层只收语义关系，不收构词族** | 2026-08-18（阶段 5） | 收 synonym/antonym/hypernym/hyponym/coordinate/meronym/holonym（落表 606,524 条）；**不收 `derived`/`related`/`proverbs`** —— 那是构词族与联想词（`casa` 的 derived 是 `casalingo`/`casata`），十万级且划词弹窗用不上。等展示层真需要「词族」再收（A31：收进来才发现没人用是白做） |
| A62 | **关系的 `target` 只存词形字符串，不存 id** | 2026-08-18（阶段 5） | 目标词可能根本不在库里（`amico dell’uomo` 这类多词短语）。展示层查得到就跳转、查不到就纯文本 —— 与 A27（指针读取时 join、不在库里复制）同一个原则 |
| A63 | **`freq_zipf` 的 `0.0` 与 `NULL` 是两件事，不许混** | 2026-08-18（阶段 5） | `0.0`＝量过了、语料里没出现（1,267,847 个）；`NULL`＝**量不了**（多词/词缀/带符号，33,664 个）。判据只有一条 `tokenize(w,'it')==[w.lower()]`，否则 `a-` 会被剥成 `a` 返回 7.21、`moto d'acqua` 会被拆成三个词返回 4.59，**且没有任何报错** |
| A64 | **`examples[]` 里混着「根本不是例句」的东西，收词时就要认出来跳过** | 2026-08-18（阶段 5，翻译试跑时才发现） | wiktextract 把三族非例句塞进了 `examples[]`：①元指引 `For quotations…see Citations:gli.`（13 条）②构词公式 `ragazzo (“boy”) + -one → ragazzone (“big boy”)`（约 470 条）③书目当正文 `1320, Dante Alighieri, …, page 161`（11 条，`ref` 为空）。🔴 判据**不能是「含有 →」**——会误删真句子（`epentesi` 的「Iohannes → Giovanni…sono esempi di epentesi」、`causalmente` 的「L’ordine temporale P→P 1…」）；构词公式的真特征是**每个词后跟括号里的释义**（`(“…”)`/`(«…»)`）⇒ 判据＝有箭头 **且** 有括号释义。⚠️ 代价：`veloce → velocemente` 这种不带释义的公式（约 10 条）漏过去，记账不追 |
| A65 | **录音先收 URL，下载是单独的决定**；同一条录音的 mp3/ogg/wav 是**一行三列**，不是三行 | 2026-08-18（阶段 6a） | URL 在 dump 里白送（12,188 条 / 9,404 个词形，fr 版占 91%）；下载要跟 `upload.wikimedia.org` 的限流耗 6–9 小时。⚠️ 把三个 url 字段数成三条，正是「录音条数虚高 2–3 倍」的成因。录音人只认 Lingua Libre 的 `LL-Q652 (ita)-<人>-<词>` 命名，认不出留空不猜 |
| A66 | **跑批要按「已落库」判完成，不能按「跑完了」** | 2026-08-18（例句翻译） | flash 会**静默丢掉批里的一部分**——不报错、`失败 0`，但回的 JSON 里就是没有那几个 id。一轮全量后 2,069 条从没被答过，且每轮丢同一批词。⇒ 收敛靠**多轮补跑 + 把批调小**（2,069→273→139→83→64→26→9），判据是库里还缺多少，不是日志说跑完了 |
| A67 | **翻译类任务只喂被翻译的那个字段**（实测，不是原则） | 2026-08-18（例句翻译三臂消融） | A 只喂意语原文 120/120；C 多喂了英文译文 ⇒ `convalligiano` 那条的中文**来自英文字段、与意语原文无关**。多喂的上下文会直接变成输出（[[context-you-give-leaks-into-output]]） |
| A68 | **答案按 `id` 挂上去之后，就不许再重建那张表** | 2026-08-18（例句） | 判据是翻译跑完才补全的，但此刻重跑收词会重新分配 `example.id`，而 3.8 万条中文挂在这些 id 上 ⇒ 改成**删行**（删行不动幸存行的 id）。这正是 [[model-answer-files-key-by-id]] 那个事故的形状 |
| A69 | 🔴 **接上展示层是一道独立的闸，不是"把字段连上去"** | 2026-08-18（阶段 8） | 阶段 4/5/6a 的数据全部落库并各自过了闸，**界面上一条都没显示过**。真接上去才看见三类缺陷：①意语版把**音节切分**当读音给出来（`sudanese` → su/da/né/se，228 行，其中 116 行是 `is_primary`）②fr/it 版拿**撇号当重音符**（7,094 行，`pesca` 一个词并排显示出四条读音，其中两条只是记法不同）③关系/例句/录音三张表从来没人看。⇒ 每接一层数据，**渲染一遍常用词自己读**，别只看闸绿 |
| A70 | **展示层折读音只折「意语里不可能区别词义」的记法差**：长音符 `ː`、软腭前的 `ŋ` | 2026-08-18（阶段 8） | `ˈka.ne`(en) 与 `ˈkaːne`(it)、`anˈkora`(en) 与 `aŋˈkora`(it) 都是同一个读音的两种写法，不折就并排显示成多个读音。🔴 **重音符不折**（`subito` ˈsubito「立刻」/ suˈbito「遭受了」是真区别），**s/z 不折**（南北清浊是真变体）。这是展示层的第三把尺子，与 A52 的两把并列：`norm_ipa` 存什么 / `cmp_key` 跨版比 / `readingKey` 页面上算不算同一条 |
| A71 | **归一函数里替换的先后顺序本身就是判据** | 2026-08-18（阶段 8，干跑输出才发现） | 把「撇号→ˈ」加在「折 `.ˈ`」**后面**，fr 版的 `a.'ba.te` 出来是 `a.ˈba.te`，与 en 版的 `aˈba.te` 又成了两条假变体 —— 字符对了、顺序错了，全量跑完才看得出来。⚠️ 与 es 那条「字符替换表顺序是判据」同源，第二次踩 |
| A72 | **`inflection.entry_id` = 原形的那个 entry**，定不了写 NULL | 2026-08-18（阶段 8） | 原来按来源分裂成两套语义（en 版指变形形自己 509,651 条 / fr 版指原形 602,336 条），各自内部一致、展示层不读 ⇒ 查库看不出异常。判据不是偏好：变形形自己的 entry 从 `word_id` 一查就有（冗余），而「`porto` 是 `portare`(动词)的变位形、不是 `porto`(名词·港口)的」**推不出来**。确定性重指 97.75%，剩下的（原形同词性多词源）留 NULL 不猜 |
| A73 | **`dict.ipa` 列不删，冻结成迁移锚点**：只读、不进新内容、只跟着归一走 | 2026-08-18（阶段 8） | 展示层已改读 `pronunciation`。留着列是因为它是**唯一能让这次迁移可证伪的东西** —— 闸①从表逐字节重建这一列比对，删了列这条闸就没了；且回归闸的「写入侧 vs 读取侧」两条路径里，写入侧就是它。588,280 个值，零成本 |
| A74 | **截断了必须把总数说出来** | 2026-08-18（阶段 8） | 关系分类封顶 12 条（`buono` 有 181 个近义词、全库最多 763），只铺 12 个而不说「共 N 个」，用户会以为词典只收了这么多 —— 那是我们自己造出来的假象。契约闸里有专门一条 |
| A75 | 🔴 **归一必须跑在「残渣/噪声」判据之前** | 2026-08-18（阶段 8，外锚闸逮到） | 顺序反了，判据结论就反：英文版 `"/assi'sten'te/socia'le"`（定界符写坏）在归一前是纯拉丁字母 + 是词形拼写的子串 ⇒ 被残渣判据删掉；归一后它带两个 `ˈ`，一眼放行。**库内不变量/回归闸/契约闸当时全绿** —— 少一条读音长得跟"源头本来就没有"一模一样，只有**锚外部 dump 的闸⑤**逮得到。与 A71 是同一件事的两个层面：A71 是函数内替换顺序，这条是脚本之间的执行顺序 |
| A76 | **模板参数里 `<…>` 修饰块的逗号不是分隔符**，解析必须按深度走 | 2026-08-19（复数列回源） | `it-noun` 的参数写成 `para<g:f><l:archaic,&,rare><ref:… on line, Istituto …<<name:trec>>>`，按逗号裸切会 ① 把**文献标题里的词**当成复数形（`paro → line`，页面上就那么显示）② 把**真复数整条丢掉**（`para` 没被认出来）。一个缺陷两种后果。⇒ 深度 0 的 `\|` 处截断（后面是 `dim=` 这类别的具名参数）、只在深度 0 的逗号处切段、每段取第一个 `<` 之前那截。全库 284 条编造的复数已按源头改正 |
| A77 | **一个词的多个复数形落行不落列；判据是「`entry_id` 指向名词词条」** | 2026-08-19 | 意语双复数 `braccio → braccia`(阴，人的手臂)/`bracci`(阳，器物的臂) 全库 **1,833 个词**，`dict.plural` 单列装不下（A20：多值落行）。行早就在 `inflection` 里（1,730 个词的复数形全齐、带性别标签），**只需改读取**。🔴 但必须过滤 `entry_id → pos='noun'`：形容词的性数一致形在 `inflection` 里同样标着「复数」，不过滤 `bello` 会显示「另有 bei、begli、belli」—— 彻头彻尾的假信息。这条过滤用的正是 A72 定死的语义 |
| A78 | **`label_zh` 这类中文标签当判据时要精确相等，不许 `LIKE '%…%'`** | 2026-08-19 | 查名词复数写 `label_zh LIKE '%复数%'` 会捞进动词的「陈述式现在时第一人称复数」，全库 **274,668 行** —— `parlare` 的复数栏会冒出 `parliamo`。改成 `label_zh = '复数'` |
| A79 | **义项分组的键是「词条 + 词性」，不是词性** | 2026-08-19 | 只按词性分，两个**同词性不同词源**的词条会被并成一组（`pesca` 的桃子 etym1 与 捕鱼 etym2 都是名词），而它们读音不同 —— 分不开就没法把读音挂对地方。相邻聚合不变（`sense.rank` 的顺序有意义） |
| A80 | **专属读音标在它那一组义项的组头上；共用读音只在词头显示** | 2026-08-19 | `subito` 副词 /ˈsubito/「立刻」与动词 /suˈbito/「遭受了」，以前两条并排堆在词头，用户没法知道哪条配哪组。⚠️ 判据是「`entry_id` 恰好等于本组」，**不是 A57 的 wildcard**：A57 回答「这一组该读什么」（要带共用读音），这里回答「这一组有没有**自己的**读音」（共用的已在词头显示过，再铺一遍只是重复）。**两个问题，两套判据** |
| A81 | 🔴 **单个可空外键表达不了「一个读音属于哪几个词条」** | 2026-08-19（量清了，不改） | `pesca` 的 /ˈpɛska/ 同属 noun:1 与 adj:1 ⇒ 按 A57「唯一才写」的规矩落成 NULL，**区分力全丢**。实测英文版：同词形多读音 8,375 个词，其中归属**有区分力**的 2,129 个 —— 单 FK 表达得了 **1,515**（71%），剩 **614** 丢掉。要修得加 `pronunciation_entry` 关联表（A20：多值落行）。⚠️ 现在不是「错」是「不完整」：所有读音仍在词头并排显示，只是没标归属 ⇒ 记账不做 |
| A82 | **归一函数必须幂等**（`f(f(x)) == f(x)`），否则入口与闸永远差一步 | 2026-08-19（收尾当天踩） | `clean_base` 剥原形词尾的性别/数标记，源头有 `… quanti m pl` 两个标记连着的：剥一次得 `… quanti m`，它自己又以 ` m` 结尾 ⇒ **入口剥一次、闸再剥一次，外锚闸每次都报一条不符**。改成循环剥到稳定。与阶段 4 那条 `norm(norm(x))==norm(x)` 是同一条要求，只是这次忘了 |
| A83 | 🔴 **归一属于新结构，不属于对旧结构的复刻** | 2026-08-19 | 我把 `clean_base` 加在了 `replay()` 的共用抽取点上，闸①a（逐字节重建旧列 `dict.infl`/`exchange`）当场报 18 条不符 —— **那道闸的职责就是"原样复刻七月的行为"，带着毛病也得一样**。⇒ 同一处留两个变量：`raw` 给复刻、`base=clean_base(raw)` 给新表 |
| A84 | **同一词形下不许出现两条逐字相同的中文**：要么补出括注区分，要么合并（藏一条），**不许硬造伪差异** | 2026-08-19（两家顾问 + 回数据裁决） | 模板 `主名（能区分的最小信息）`：同组全同类型不加类别词、类型不同才加。🔴 三处分歧我自己裁的：①法国省名**带「省」字** —— 回我们自己的库验的，`Mayenne` 在库里就是「马耶讷（法国省份及河流）」，裸名本身有歧义（v4-pro 对、豆包错）②v4-pro 夸「艾马维尔**的**村庄」结构好，而它同一份答案里禁止「的」结构 —— **自相矛盾**，按已有约定去掉 ③豆包「查询概率低于 1% 的生僻地名不该动」**驳回**：形式代理（A45）且不可验证 |
| A85 | **模型只做一件事：回答区分信息；「要不要合并」由我按返回值确定性决定** | 2026-08-19 | 一组里全是 `null` ⇒ 源头无区分信息 ⇒ 合并。把两件事塞进一次调用，控制组就得同时验两个字段（A47 的代价）。⭐ 实测这个分工有效：`acacia`（一详一略）、`IA`（en 版与 it 版各写一遍）、`L'Aquila`（两版都说 province）模型都正确回了 null |
| A86 | **「伪差异」比重复更糟**，必须在 prompt 里给反例 | 2026-08-19（切片自读逮到） | 第一版规则只说"同一件事就回 null"，模型仍把 `being`/`existence` 写成「存在（存在物）」「存在（存在状态）」。补上这个**具体反例**+判据「你补的括注，中文母语者查词时用得上吗」之后消失。⇒ 抽象规则挡不住，得给反例 |
| A87 | 🔴 **页面已经按词性分组，靠词性就能区分的义项本来就不重复** | 2026-08-19（回归闸 B2 逮到我自己写的） | `beige` 的形容词义与名词义中文都是「米色」，我让模型补成「米色（形容词）」「米色（名词）」—— 而组头上已经写着「形容词」「名词」，**说了两遍**，且正是 `strip_pos_label_in_gloss` 当初修掉的形状。⇒ 判定"重复"之前先问**页面上到底重不重复**；判据已加进 `check()`，与回归闸 B2 共用 `LAB`/`TAIL` 一份实现 |
| A88 | **判据是「最终结果互不相同」，不是「每条都得有括注」** | 2026-08-19 | 第一版写成「组内有 null 就整组跳过」，白扔 27 组 —— 三条里两条补了括注、一条保持原样，结果照样三条各不相同，那就是达标的。⇒ null 那条按**原中文**参与判重 |
| A89 | 🔴 **模型"每轮都丢同一批"时，先单条送做消融，别加轮次** | 2026-08-19 | 12 组连跑 4 轮每次都被丢（批 2），把同一组**单独送一次就成功**（`mercantile` 正确回 null×2）—— **不是模型拒答，是批量路径丢的**。`--retry` 改成 B=1 后两轮清零。⚠️ A66 说"收敛靠多轮 + 把批调小"，这次的补充是：**调到 1** 才是那条路的终点，而定位手法是消融不是重试 |
| A90 | 🔴 **「改 prompt 重跑能不能修好」是可以先量的，别直接开跑** | 2026-08-19（实测否掉了一次重跑） | 拿我逐条读过的 6 条真错当负控 + 20 条正确当正控，新 prompt（把「词头字面 + 源文本点明的上位类」摆到判据位置）实测：**负控修对 2/6、部分改善 2/6；正控 20 条里 11 条被改写、至少 5 条明确变差**（`Davidina`「达维迪娜（女名）」→「达维迪娜」丢区分信息、`plissé`「褶裥的」→「褶裥」词性都变了、`licitazione privata` 直接改错）。⇒ **不重跑**：E+F 两层约 3,200 条真错，而重跑波及 39,823 条，按正控 25% 的明确变差率新造的错更多。⭐ 这是 [[prompt-self-harm-two-patterns]] 的反面 —— 那次两头都变好，这次一头好一头坏，**判据就是有没有正控** |
| A91 | 🔴 **修在生成那一侧、没回头修已落库的行 —— 修复照样是"消失"的** | 2026-08-19（清账时逮到 404 条） | `fill_blind_gloss.scan()` 的 `pos or "v"`（[[prompt-self-harm-two-patterns]] 记的第一条自伤）2026-08-17 已改成 `pos or "unknown"`，**但那是入参那一侧**；第一轮已经写进 `sense.pos` 的 `v` 一条都没改回来。证据是**现行代码写不出这个组合**（`dict.pos` 为空时落 NULL），库里却有 461 条 ⇒ `Meleagridi`（火鸡科）、`a pancia in su`（副词短语）、`DPCM`（缩写）在页面上全标着「动词」。⇒ 改完生成器要问一句「**已经落库的那批谁去改**」；判据用「现行代码产生不出的组合」就能确定性捞出来 |
| A92 | **判重的键必须是"用户看到的那个键"，不是我建表时用的那个键** | 2026-08-19（28 组重复中文里 13 组是假的） | 「同词形下重复中文」的判据按 `(word_id, 中文)` 分组，而页面按 `(词条, 词性)` 分块 ⇒ `beige` 的形容词义与名词义分列在【形容词】【名词】两个标题下，**用户看到的不是两条一样的**。同一形状还有 A80 的读音归属。⇒ 凡是「重复/冲突/歧义」类判据，先问一句"在页面上它们挨着吗" |
| A93 | 🔴 **一版一套判据：闸的豁免要按源声明，不能一刀切** | 2026-08-19（差点抹掉英文版一整族缺口） | 外锚闸把法语版 30 个词形报成「要收」，回源看全是 `intake_fr_words` 按两条成文规则有意不收的（`alt_of` 指针 + 法语版自己的占位符 `Définition manquante`）。我第一版把这两条豁免加到**所有源**上 —— 英文版的「变形指针」当场从 43.7 万涨到 51.8 万，那 8 万条是 `alt_of`，而**英文版的 alt_of 是要收的**（`build.py:147` 当年正是"把 alt_of 当 form_of"才造出 3,890 条编造标签）。⇒ 豁免跟着**那一版自己的收词器**走，并直接 `import` 它的判据（抄一份就是两边漂开的起点） |
| A94 | **基线要在动作之后取** | 2026-08-19（两次） | `finish_duplicate_zh` 的「词性各不相同」基线我按动作**前**的 10 写死，跑完当场红（合并之后变 13）；`recover_sublist_glosses` 的两条基线同理（`cenci` 从"没提升"挪进了"源头标了 form-of"）。⚠️ 反过来说，一个**会随别的脚本变**的锚不是坏锚：`hide_empty_senses` 的「已藏且有释义 708→719」正是靠红一次告诉我 `finish_duplicate_zh` 藏了 11 条 —— 要求的是"不许**悄悄**变"，不是"永远不变" |
| A95 | **多值关系落关联表时，与旧单值列取并集，不是替换** | 2026-08-19（`pronunciation_entry`） | 重算的归属与旧列 `pronunciation.entry_id` 有 3 行不一致：`tramonti` 的 `traˈmonti` 旧列挑动词（第二人称单数）、重算挑名词（复数）—— **两个都对，那个词形名词和动词读音本来就一样**。单外键逼着二选一，关联表不用选 ⇒ 并集。收回 21,666 个词形的归属（组头能标读音的词形 6,811 → 7,789） |
| A96 | 🔴 **一把尺子只在它校准过的那一族上有效** | 2026-08-20（差点误杀 326 条好数据） | 盲测给盲推释义的真错率 **24–25%**，我拿这个数当了全体 3,391 条的属性，并据此建议整批撤回。实际上那把尺子的正控写得清清楚楚 ——「**C2 生僻动词**，库里有中文释义（与目标词同类：低频、构词复杂）」，它**只量了生僻单词那一族**。按 `freq_zipf` 一分（A63 三分法），另外两族（英语外来词 + 多词固定搭配）根本**不是按构词猜**：`green`→绿色、`a pancia in su`→仰面朝天，意思就摆在词形里。逐条读 326 条，源头证实的错只有 4 条（2.4% 上下）。⇒ 引用一个错误率之前，**先回去看那把尺子的取样面是什么**；取样面与目标面不同分布时，那个数字只是**该族的**属性。⚠️ 与 [[measure-landing-not-source]] 同源：那条是「量错了对象」，这条是「把局部的数当成了全体的」 |
| A97 | **词头徽标的归属判据：名词/专名/形容词共享性数系统，混进没有性数的词类才叫归属不明** | 2026-08-21（点测评审，判据被打回两次） | 词头徽标取自 `dict` 行 —— 那是**词形级**汇总；`App.tsx` 的 `isNoun` 读的也是词形级的 `dict.pos` 斜杠串。两个词形级的东西凑在一起，就把只对某一个词条成立的属性顶到整词头上：`la` 显示「阳性」（那是名词「音名拉」的性，冠词/代词是阴性）、`di` 显示「阴性 单复同形」、`anche`（也）显示「阴性」、`fare` 显示「复数 fari」（`fari` 是 `faro` 灯塔的复数）。🔴 判据收窄两次，两次都是被更严的证据打回：① 「`dict.pos` 有 ≥2 个成分」→ 16,599，太宽（`acqua` 的 `n/v` 里那个 v 来自 `acquare` 的变位形、**一条可见义项都没有**，它的「阴性 复数 acque」是对的）② 「**有可见义项**的词条覆盖 ≥2 种词性」→ 7,527，**还是太宽** —— 契约闸逮到 92 处回归，`cecchino`(noun+name)、`sagro`(adj+noun) 的复数形全没了。⇒ 终版：性/复数/单复同形要求 `scope ⊆ {noun,name,adj}`；助动词/变位类**只可能属于动词**、不存在歧义 ⇒ `scope` 含 verb 就显示（`fare` 因此保住了正确的 `助动词 avere 第一变位`，只丢掉错的「复数 fari」）。真值 **3,844**。⏳ 这是**止血**：正解是把徽标下沉到各词性分组，需要先给 entry 层补性/复数（`entry` 表现在只有 aux/conj，`sense.gender` 仅 4,101/416,357 有值，`plural`/`number_note` 在 entry/sense 层**根本没有列**）|
| A98 | **判据改了，它的变异必须跟着改** | 2026-08-21 | D5 的判据从「看 `dict.pos`」收窄成「看有可见义项的 `entry.pos` 集合」之后，原来那条变异（`UPDATE dict SET pos='n/v'`）打在了**判据根本不读的列**上 ⇒ 改完数字纹丝不动。这与「闸写完要变异验证」是同一个道理的下一层：**变异验证自己也会过期**。⚠️ 同一天还踩了两个近亲：① 变异要挑**目前不在缺陷集里**的行（D1 第一版 `LIMIT 1` 选中的行本来就带断括号，报「没逮住」而闸是好的 —— 「变异没造出新缺陷」和「闸是摆设」打印出来一模一样）② 变异判据本身会失效：原来是 `bool(report(...))`「跑完还有红的就算逮住」，那在**全部基线为 0** 时成立，D 组进来后有 6 条常红 ⇒ 造任何变异都「通过」，13/13 全是假的。改成**逐条比对声明的那一条** |
| A7 | **不做自动判重合并** | `PLAYBOOK` 5.6 | es 186 条判重里 12.4% 是义项错配，已判定失败 |
| A8 | **行序一次性固化成 `sense` 主键**，之后任何地方不许再用"第几行"引用 | 阶段 0 | es 把 8 万条对齐存成"definition 第几行"，四个脚本重排后 752 万 tokens 作废 |
| A9 | **七月那 11 个直接写库的脚本已冻结**，`legacy_guard` 拦重跑；新写库一律走 `dbtool.session` | 2026-08-12（阶段 -2） | 重放式脚本会静默撤销已完成的修复，es 一天撞见三次 |
| A10 | **`TRACK` 覆盖所有承载值的列**，只有 id/word/word_norm/is_lemma 四个身份列除外 | 2026-08-12（阶段 -2） | 旧版只追 6 列，`definition` 在闸外，而阶段 0/1 动的就是它 |
| A11 | **结构大改前用 `keep-` 前缀的 tag 备份**，该备份永不被保留策略淘汰 | 2026-08-12（阶段 -2） | 里程碑靠文件名豁免，不靠"记得手动留" |
| A18 | **加了 `entry` 层**（词形 × 词性 × 词源号）：读音、词条级语法、变形指针都挂它；主键 `src_ref = kk-en:<词>:<词性>:<词源号>:<seq>`，**内容派生不是行序** | 2026-08-13 | 见 `SCHEMA` §10。两家顾问在这一问上分歧，两边前提都被回源核对推翻，理由与证据记在 §10.2 |
| A19 | **出版层要隐藏什么就写 `sense.hidden=1`，证据层 `sense_src` 永不删改** | 2026-08-13 | wiktextract 把「参见/同义词」小节抓成了义项（全库 3 条）。它们在 kaikki 原文里确实是 senses ⇒ 不能从证据层删；但不该给用户看。不删行还有个好处：复刻闸①仍逐条对得上 |
| A34 | **判官只定位，判据设计与最终裁决必须自己来** | 2026-08-14（实测） | 我读 26 条数出 ~8% 真错，两家判官在 92 条上报 1.1% —— `assolutivo`→「作格」（应为通格）、`imenectomia`→「切开术」（应为切除术，且法语源本身写错）判官都判 ok。⇒ **能自己读的就自己读**，把判据外包等于把标准降到判官的水平 |
| A35 | **词典 ≠ 百科**：收词形与词义，不收实体履历 | 2026-08-14 | `Alameda` 该给「西班牙市镇/加州城市」，不该给 Alameda Research 的崩盘经过 —— 后者没有任何维基词典源，只能靠模型编，违反「错比缺更伤权威」 |
| A33 | **闸报红时先分清：是数据错、是断言过期、还是我的尺子错** | 2026-08-13（阶段 3 复跑旧闸，一次遇齐三种） | ①数据错 → 修数据（占位符裁决没撤回、意语证据挂错大小写行）②断言过期 → 换结构性口径（A28）③**尺子错** → `ingest` 用 `w.lower()` 查 dict，拆大小写后指到了错的行，**库是对的**。⚠️ 判断顺序永远是"先假设我的度量错了" |
| A31 | **跨版收词的判据是「收进来有没有内容」，不是「覆盖率」** | 2026-08-13（阶段 3b） | fr 版有 170,968 个我们没有的词形，但实测 **94.1%（160,874 个）既无音标也无录音**，而 fr 的释义按 A3 不收 ⇒ 收进来就是搜得到、点开什么都没有的空壳。两位顾问一致：划词场景下**"未收录"比"空弹窗"诚实**。只收 it 版那 11,921 个（带真释义的） |
| A32 | **负控报错时先回源看输入，别默认是模型的锅** | 2026-08-13（真事故） | 翻译负控 30 条里 2 条模型输出空串，回查发现原文是意语版的占位符 `definizione mancante; se vuoi, aggiungila tu`（"缺定义，欢迎补充"）—— 模型按 prompt 规则拒绝编造，是对的，负控实际 30/30。**顺带逮到 4,031 条占位符被当成释义提升到了出版层**，其中 1,920 条整个义项只有这句话 |
| A30 | **拆行时新行只带词形级的值，词条级的一律不带** | 2026-08-13（阶段 3a） | `Abate` 从 `abate` 拆出来时带 `ipa`/`level`（意语大小写不影响读音、CEFR 是词形级），但 `gender`/`aux`/`conj`/`plural` 一律不带 —— 它们的真值在 `entry`/`sense_tag` 里，复制过来就是第二份真值（A21） |
| A28 | **闸里不许写死行数**（`count(*) == 198049` 这种），一律写成结构性口径 | 2026-08-13（一天栽三次） | 「锚自己上一版」的断言下一阶段一定红，而红的是断言不是数据 —— 人会习惯性地把它改绿，闸就废了。反例：`sense_gloss 一条没动`；正例：`出版层意语释义 ≤ 已裁决的证据行`。⚠️ 唯一例外是**当前阶段刻意不碰的表**，且必须在下一阶段开始时主动改口径 |
| A29 | **每个阶段结束要把之前所有阶段的闸复跑一遍** | 2026-08-13 | 阶段 2 加了 7,902 条义项后，语法层漏了 415 条及物性标签（Q1 跑在它们出现之前）—— 这个缺口**只有复跑旧闸才看得见**。写库脚本因此必须可增量重跑（只插缺的） |
| A25 | **`alt_of` 不是变形，是独立词条** | 2026-08-13（阶段 2a） | 七月 `build.py:390` 是 `form_of or alt_of` 一起丢，后果是 7,028 个词形界面上一条释义都没有、只显示错话「Banca Centrale Europea 的变位形式」。实测两者在**义项级**无交集（0 条同时有），判据干净；但**词形级会重叠**（`leggere` 既是 `leggero` 的阴性复数又是它的异体，全库 25 例） |
| A26 | **关系词条的中文标签用模板确定性生成，模板表经两位顾问审过** | 2026-08-13（阶段 2a） | 30 条模板见 `fixes/recover_alt_of.py`。分歧处的取舍：`apocopic`→「省尾形式」（v4-pro：叫"截尾"会与 `clipping` 的"截短"混）、`misspelling`→「X 的误拼（**非规范写法**）」（不能给错拼异体的正规地位）、`pronunciation spelling`→「读音拼写」。`src` 记 `template` 而不是 `unknown` —— 模板产出证得出来 |
| A27 | **指针类词条要跟随指针把目标词的中文一并显示，但不在库里复制** | 2026-08-13（阶段 2a） | 两位顾问一致：用户查到 `abaca` 时要的就是 `abacà` 的词义，裸指针没用。读取时 join，目标词释义改了自动跟着变 |
| A23 | **过滤只做源头自己的结构判定，自编的文本判据留到「提升到出版层」时才用** | 2026-08-13（阶段 1.5） | 我写的 `plurale di X` 正则能认 2,651 条指针，但反向核对逮到三类假阴性（目标是多词 `plurale di ospedale psichiatrico` / `vedi` 模板 / `per` 模板）。证据层收全 105,474 条，正则以后改好了**不用重灌数据** —— 这就是两层义项存在的理由 |
| A50 | 🔴 **闸只查数据库是不够的，必须有一道「渲染之后」的闸** | 2026-08-16（用户从界面上挑出三个我看不见的缺陷） | ①`tempo` 词性分组标题显示成英文 `noun`（库里混着两套词性词表，`POS_LABELS` 查不到就吐原始串）②`Dodoma` 被归到【短语】③`TVTB` 有两条义项却整块释义不渲染（组件里 `entry.isLemma &&` 挡住 **8,552 个词形**）。三者共同点：**只在渲染之后才存在**，查库查不到、**查接口也查不到**（③ 的接口返回完全正确）。⇒ `apps/web/src/contract-check.tsx`：用 `react-dom/server` 把真实数据渲染成 HTML 再断言，不装任何新依赖。第一次跑就逮到 `sym` 没有中文标签 |
| A51 | **展示层与数据层的口径差是独立的一类缺陷** | 2026-08-16 | 数据全对、接口全对，用户仍可能什么都看不到。判据要盯**用户看到的文字**（去标签后的可见文本），不是 DOM 结构、不是接口 JSON。⚠️ 判据同样会假阳性：意语 `null'`（`nulla` 的省音形式）**词形本身**就长这样，拿裸串判 "null" 会误报 —— 先把词头和各语言释义从可见文字里去掉再判 |
| A47 | 🔴 **输出有几个字段，控制组就必须有几组判据；大批量前先跑 1% 切片自己读** | 2026-08-15（代价：418 万 tokens 作废 + 用户账户欠费） | ④⑤ 一次调用产出 `i`（对齐下标）与 `zh`（新建义项的中文），我的控制组**只量了 `i`**，而 prompt 缺陷全在 `zh` 的规则里 ⇒ 拿一个对真正会坏的字段完全瞎的绿灯批准了 2,930 批，跑完才第一次读产出。写控制组前先列输出 schema，逐字段问「这一项错了闸会红吗」 |
| A48 | **`turbo-batch` 之前"用不了"是我误诊** | 2026-08-15 | 两次实测「十分钟不返回一批」被我记成延迟问题，实际是 **403 `AccountOverdueError`** 被 `acall` 重试 5 次 + 指数退避，看起来像卡住。🔴 记账时**报错原文要看完**，别拿现象当结论 —— 这个误诊让我在整整两轮大批量里用了全价的 online pro。⚠️ 补充：用户缴费后重测，403 消失但 **10 分钟仍 0/17 批** ⇒ 两个原因叠着。batch 端点确实是为隔夜大批量设计的，**要看着结果的活不能用它**；半价通路只留给「提交完就不管、隔天取」的场景 |
| A49 | **模型按活的难度分工，不按习惯** | 2026-08-15 | 同一套 100 题、同一 prompt、同一 payload 形状横评（为此给 DeepSeek 写了同协议的 `ds_batch.py`，否则比的是我的两套提示）：豆包 pro 91/100、flash 87/100，**干扰项不编造两家都是 100/100**。逐条读 flash 那 13 条分歧 ⇒ 真错 5 条、我们真值错 1 条、平局 7 条。⭐关键：**控制组比真实活儿难**（按构造只挑"我们有 ≥2 条义项"的，只占 41.9%）⇒ easy 桶（我们只有 1 条，判挂 or 新建）走 flash，hard 桶走 pro |
| A45 | 🔴 **判据不许用形式代理（长/短/多/少），要按义项本身的含义写** | 2026-08-15（用户点名） | 我写「输出对应词，**不是长句翻译**」没说约束作用在哪一段 ⇒ 模型把括号里的区分信息一起删了（`Saint-Léger (Charente).` → 「圣莱热」，十个法国同名市镇中文全一样，坏 1,528 条）。回头量数据，这个代理连自洽都做不到：**七月从英文翻的中位 7 字、我自己的地理模板中位 21 字、我的 prompt 逼出 4 字**，而三者**括号外**都是 4–6 字 —— 真分界是「括号外＝对应词本体 / 括号内＝区分信息，不限长度」。正确写法：「给出这条义项**指的那个东西**在中文里的说法；若同词形另有义项会撞车，**必须**用括号写出区分点」——后半句可由「同词形中文逐字相同」的闸确定性判定 |
| A46 | 🔴 **要被判断含义的文本，一律不按字数截断** | 2026-08-15 | ④⑤ 跑批时我把意语释义截到 200 字，切掉 **1,322 条（2.82%）**，`azzurro` 切在 `rep\|ubblicani` 中间；而完整送入只多花 **3.6%** 字符 —— 形式约束省下的是零，代价是含义。⚠️ 截断在**输入侧**比输出侧更隐蔽：输出坏了看得见，输入被切了模型只是安静地答错 |
| A43 | **「同一词形下中文重复」先分家再动手：多数不是多了一行，是中文翻粗了** | 2026-08-15 | 622 组重复拆开看：**B 中文翻粗 379 组**（`Epiro` 的「历史地区／地理区域／由地名转来的姓氏」三条义项中文全是「伊庇鲁斯」）、**C 法语证据能区分 132 组**（`Saint-Léger` 十个法国同名市镇中文全是「圣莱热」）、**真重复只有 A 21 组 + C-无从区分 90 组**。修法完全不同：真重复该隐藏，翻粗的该让中文区分开 |
| A44 | **顺序约束：新建义项必须排在去重前面** | 2026-08-15 | ④⑤ 会新建义项，新建完重复面就变了 —— 先去重是白做。这不是偏好，是依赖 |
| A40 | **控制组的判据要在看结果之前定死；「两个选项一模一样」的题目不算模型错** | 2026-08-14（阶段 ③ 对齐） | 我事前定「正控 ≥85%」，实测严格下标口径 **80.8% 不达标**；改用「选中的中文与真值相同也算对」得 91.7% —— 换口径的理由独立于结果（两条义项中文逐字相同时这题**无解**，量的是我们的数据缺陷不是模型），但**口径是看到结果之后换的，必须写在明处**。逐条读那 10 条真分歧：模型真错约 3/120=2.5%，且错的方向几乎全是**宁可不挂**（负控 100% 洁净）——对「挂错比不挂坏得多」正是安全偏向 |
| A41 | **换模型或换通路 = 控制组作废** | 2026-08-14 | 正式批必须用控制组验过的那条（online pro）。半价的 turbo-batch 延迟实测 11 分钟一批都没回，控制组这种要快反馈的场合不用它 |
| A42 | **跑批命令不许接 `\| tail`** | 2026-08-14（自伤第二次） | `ark_batch` 每批 flush 落盘就是为了随时 `wc -l` 看进度，我却把命令写成 `... \| tail -14` —— `tail` 缓冲到进程结束，可观测性全废。⚠️ 同日还栽过 `timeout` 在 macOS 不存在（rc=127、零输出）被我的 sweep 读成**全绿** |
| A36 | **「什么不算释义」必须先剪后判，且全项目只有一个家** | 2026-08-14（阶段 1.5 补跑，一天踩三轮） | `not_a_definition()` 落在 `fixes/strip_it_placeholder.py`，提升端 / 闸 / 变异验证共用。三轮：①只认「整条是占位符」⇒ 5 条夹在句中的灌进出版层 ②改成「带占位符就整条不算」⇒ **矫枉过正**，把 `lanzafina`「(Alisma lanceolatum), alisma, definizione mancante…」这种**有真内容只拖了个尾巴**的也判掉 ③正解＝**剪掉占位符后什么实质内容都不剩**（`(di frutto)` 这种纯域标签也算不剩）。⚠️ 判据一变，「意语给了几条」就变，1:1 的人群跟着变 |
| A37 | **重放式脚本必须挡「已处理过的」，但计数仍要把它们算进去** | 2026-08-14（差点重复插 3.3 万行） | `promote_it_gloss` 按当前库重新分档，而分档结果被后续阶段一直改（2a 加 alt 义项 / 3b 给零义项词形建义项 / 占位符清理藏义项）。重跑会给已提升的 33,049 条再插一遍。⇒ 过滤放在**发牌处**不放在**收集处**：一个词形已提升 1 条、还剩 1 条未提升，那是「意语 2 条」不是 1:1 |
| A38 | **判 X 是不是指向 Y 的指针，用「我们已记下的目标词」比对，别往正则里堆词** | 2026-08-14（阶段 1.5 补跑） | `variante di X` 这族 `PTR` 漏了，但**不能**加进正则 —— `variante del gioco del calcio praticata su spiaggia`（沙滩足球）是真定义。改用确定性判据：意语文末 `di X` 的 X 是否等于 `sense_relation.alt_of.target`。703 条切成 372 指针 / 331 真定义，后者正是收意语版的目的（`anitra` 雁形目鸟类 / `granturco` 玉米 / `acquasanta` 圣水，英文版只给「anatra 的异体形式」） |
| A39 | **闸红先分性质，别急着改数据**：数据错 / 断言过期 / **尺子错** | 2026-08-14（当天四种全遇到） | ①数据错＝5 条占位符灌回出版层（我造的）②断言过期＝「意语定义不许挂 alt 义项上」，写的时候 alt 上还没有真定义 ③尺子错＝「带占位符的证据不许被裁决」报红 17 条，实际是 `strip` 的既定策略（证据留原文、出版剪干净）④**跑批工具本身错**＝`timeout` 在 macOS 不存在，rc=127、零输出，我的 sweep 判据把它读成**全绿** |
| A24 | **跨版一致性优先于模型意见：能用第三方版本回答的，别问模型** | 2026-08-13（阶段 1.5） | 「1:1 直接对上安不安全」两位顾问都只给得出「词性一致」，实测词性门槛挡掉的主要是两版**词类划分口径差异**（`noun→phrase` 712）而非同形异义。改用**法语版义项数**当第三方判据：它也只给 1 条 = 三方共同认为单义 ⇒ 风险面从 25,393 收敛到 **110 个词形** |
| A20 | **互斥单值的落列，多值的落行** | 2026-08-13（语法层） | 及物性实测有 `ditransitive+transitive`、`impersonal+intransitive` 这类组合 ⇒ 落 `sense_tag(kind='grammar')`，值用源头原词；`gender` 互斥单值才留在列上。塞多值进单列 = 要发明编码 = 失真 + 第二份真值 |
| A21 | **下层能算出来的上层值，宁可读取时聚合，也不在库里复制一份** | 2026-08-13（语法层） | `sense.aux` 只在 `entry.aux='both'` 时写（覆盖值），单助动词词条的义项留 NULL，读取取 `sense.aux ?? entry.aux`；词头 aux 从 entry 聚合而不读 `dict.aux`。不复制 = 结构上不可能漂移，闸②有专门断言 |
| A22 | **`setdefault` 的键必须是判定用的那个键** | 2026-08-13（真事故） | entry 层按 `(词形,词性,词源号)` 去重、却按**音标集**拆 seq ⇒ 同键第二条 JSON 复用了第一条的音标，seq=1 那行永远建不出来（全库 2 例）。而「entry 总数 621,097」这个数**自洽**，纸上查不出来 —— 是语法层的外锚闸①报「entry.aux 少一条」才逮到 |
| A15 | **变形层的判据是 `translation == infl`（模板生成的指针文本），不是"有没有 definition"** | 2026-08-12（阶段 0） | 全库 432,512 个无 definition 的行里 432,490 行两者逐字节相同、可安全丢弃；但剩 **22 行不是变形**（`ganga` 恒河 / `sonata da chiesa` 教堂奏鸣曲），按"无 definition 即变形"会把它们的中文释义直接丢掉 |
| A16 | **`sense_gloss.kind` 一律先记 `equivalent`**（en 与 zh 都是）；`definition` 留给阶段 1.5 的意语单语释义 | 2026-08-12（阶段 0） | `kind` 记的是"这条中文从哪种原文翻来"。⚠️ `kind` **不能拿来选标题行**（es 实测被判成 definition 的 56% 本来就 ≤10 字），展示层取最短的那条 |
| A17 | **中文释义的来源写 `unknown`**（`sense_gloss.src`），不猜 | 2026-08-12（阶段 0） | `translation_src` 整列是空的，逐条来源证明不了；证明不了就写 unknown |
| A13 | **音标与录音的跨版主源是法语版**（音标 698,250 / 录音 13,343，各为英文版的 6.3 / 6.6 倍）；释义不收法语 | 2026-08-12（阶段 -1 实测） | 取数表；⚠️ fr 版重音符位置约定不同，合并前必须归一，否则误报 92% 冲突 |
| A14 | **跨版收割一律按 `lang_code` 过滤，不按词形匹配** | 2026-08-12 | es 曾因按词形匹配混进 63 条非西语录音 |
| A12 | **不修 `level`**，改为加客观频次列 `freq_zipf`（wordfreq，离线、同输入同输出） | `SCHEMA` §9.3 | `level` 是全库唯一没有源、也无法回源的字段；es 核心层 A1–B1 里 42.4% 是专名/词缀/缩写 |

**待定（到对应阶段再拍，别提前讨论）**

- 音标记法取宽式还是严式为主、变体怎么排序 → 阶段 4
- 性别冲突信谁（es 的结论是信 kaikki） → 阶段 2 / 4
- 变形层边界划在哪（`levantarsi` 这类自反式算词条还是变形） → 阶段 2
- 真人录音下不下载（URL 先入库是已定的） → 阶段 6

---

## 一之二、取数表（阶段 -1，2026-08-12 实测）

探测存档 `data/work/it/probe/probe_editions_it_20260812.txt`、
逐源实测 `data/work/it/probe/count_slices_20260812.txt`（复跑：`python3 probes/count_slices.py`）。
**下面每个数字都是自己扫出来的，不是索引页抄的**（与索引页声称的差全部在 ±0.1% 内）。

| 版本 | 意语条目 | 义项 | 其中变形指针 | **真释义** | 音标条数 | 录音条数 | 非指针词形 | 取否 | 负责哪些字段 |
|---|---:|---:|---:|---:|---:|---:|---:|:--:|---|
| **en** | 622,957 | 718,867 | 518,577 | 200,160 | 110,834 | 2,012 | 152,099 | ✅ 已有 | **结构基准**：词条/义项/英文释义/变形 |
| **fr** | 1,198,603 | 1,309,451 | 1,059,634 | 249,808 | **698,250** | **13,343** | 236,563 | ✅ 新下 | **音标 + 录音 + 词形并集**；释义不收（法语） |
| **it** | 560,357 | 714,867 | 608,726 | **105,474** | 54,976 | 674 | 68,913 | ✅ 已有整包 | **意语原文释义唯一来源** |
| zh-繁 | 165,948 | 194,842 | 162,762 | 32,068 | 20,092 | 811 | 23,345 | ✅ 新下 | 中文释义 / **盲测真值** |
| zh-简 | 2,855 | 3,515 | 1,939 | 1,572 | 1,290 | 155 | 1,023 | ✅ 新下 | 同上（同一语言的第二个本地名，必须单独取） |
| el | 174,040 | 175,183 | 45 | 175,132 | 392 | **0** | 173,630 | ⚠️ 下了但没用 | 见下方"判断错了" |
| tr | 153,683 | 154,681 | 584 | 154,071 | 64 | 112 | 153,040 | ⚠️ 同上 | |
| pl / de / ru / es / pt / nl / ko / cs | ≤52,386 | | | | | | | ⏸ 未取 | 需要时再取，切片 URL 在探测存档里 |

**三条结论**

1. 🔴 **法语版是 it 最大的跨版资产**：音标 698,250 条是英文版的 **6.3 倍**、
   录音 13,343 条是 **6.6 倍**。阶段 4/6 的主源是它，不是意语版。
   ⚠️ `PITFALLS`：fr 版重音符位置约定不同，没归一会误报 92% 冲突。
2. 🔴 **意语原文释义的真实规模是 105,474 条，不是 714,223** —— 后者 85% 是变形指针
   （`plurale di X`）。`IT_PLAN` 阶段 1.5 按 714,223 排的 3–4 天需要重估。
3. 各源与索引页声称的义项数差全部 ≤0.1%，**取数无损**。

**判断错了的一条（如实记）**：我按"录音/词形并集"取了 el + tr（5.2 MB），
实测 el 录音 **0** 条、tr **112** 条，音标各 392 / 64 条 —— 这个理由不成立。
它们真正有的是意语词形（各 15 万+），但 `form_of` 几乎不标（el 只有 45 条指针，
说明该版根本不做变形标注），所以"非指针词形"这个数在它们身上不可信。
留着不删（已落盘、5 MB），阶段 3 收词时再看要不要用，**不再为它们花时间**。

---

## 二、记账本

格式：`[阶段] 发现 —— 归属阶段 / 状态`。**只记不做**，除非属于「用户会看到错的内容」。

| 发现 | 归属 | 状态 |
|---|---|---|
| 🔴 **跨版义项合并可能把不相干的两个词并成一条，风险面 53,000 条（12.7%）**。2026-08-21 点测评审逮到三例：`dei` 的 en=`alternative form of dey`（阿尔及尔总督）/ it=`variante di dèi`（众神），中文只跟了 en；`fare` 的 en=`to gift`（给）/ it=`ricevere un regalo`（**收到**），方向相反；`libro` 的 en=`phloem; foliage` 而意语原文只说茎枝根的组织、**没有叶片**。判据：同一 `sense` 上 en 与 it 原文并存 = 53,000 条，那是**上界不是缺陷数**（绝大多数两版说的是一回事）。可确定性缩小的只有 `form of X` × `variante di Y` 一族 **21 条**，逐条读过 —— 只有 `dei` 是真错配，其余（`abadia`: abbazia / abbadia,badia）是拼写变体链。⚠️ 代价：53,000 那个面**无法确定性判定**，要判就得逐条送模型或人读 | 专门一轮 | 记账；21 条已读完，`dei` 待拆 |
| 🔴 **294 组「同一个搭配有两个译法」**（`abito blu` = 「正式礼服（男士或女士的正式套装，尤指深色西装）」/「蓝色正装」；`共融与解放（意大利天主教宗教运动，由朱萨尼神父创立）`/`共融与解放运动`）。全库 317 个重复组里只有 22 组中文完全一致、1 组一条无中文，其余 294 组是**同一批搭配被翻译了两次**（低 id 那批详细、高 id 那批简短）。🔴 **不能用长短选**：`abito blu` 字面就是「蓝色西装」，简短那条反而更准（A45 形式代理，`Saint-Léger` 那次因此坏掉 1,528 条）。⇒ 展示层按 `text` 去重、取 `rank` 最小的那条止血，**一条数据都没删**；「哪个译法更好」是内容裁决，留着 | 专门一轮 | 记账；展示层已止血 |
| **学名被当成独立的下位词条目**（`gatto` 的下位词渲染成 `gatto della giungla` `Felis chaus` 两条并排）。成因同上：源头 `gatto della giungla (Felis chaus)` 被按逗号切分切开，前半留了个孤立左括号（已由 `fix_unclosed_paren` 剥掉）、后半 `Felis chaus` **本来就没有括号**，判据够不到。判据难点：`^[A-Z][a-z]+ [a-z]+$` 这个二名法形状命中 324 条，其中 27 条是**真意语词组**（`Europa centrale` 中欧、`Cosa nostra`、`Dio mio`）⇒ 形状判据不够，要精确区分得回备份查「哪些条目原本以 ` (` 结尾」。⚠️ 危害有限：并排显示不是错误信息，比原来括号没闭合已是改进 | 专门一轮 | 记账，判据未定 |
| 🔴 **根因在收录脚本的切分逻辑**：意语版的同义词/下位词列表带括号说明，按逗号切分时把括号内容切碎，全库 808 条残片（573 缺右括号 + 235 缺左括号）。`fix_unclosed_paren` 只止血（剥 206 条 / 藏 602 条），**没动切分逻辑** ⇒ 重跑收录会再造一批。⚠️ 已知代价：藏的行留在表里（可逆，且 `INSERT OR IGNORE` 重放不会覆盖 `hidden`），但新收的行不会自动带 `hidden` | 重跑收录那一轮 | 记账，根因未修 |
| ✅ **点测评审的内容类已处置（2026-08-21）**：外审提出、我逐条回源证实的四条**跨版错配**已修 —— `treno` 首义补上「火车」（意语原文 `veicolo che viaggia su rotaie`，en 的 `connected sequence` 把最常用义整个丢了）· `libro` 去掉「叶片」（意语原文只说茎枝根，`foliage` 是 en 的错义支）· `fare` 改成「（罗马方言）收受贵重礼物」（意语原文 `ricevere`，与 en 的 `to gift` **方向相反**）· `dei` **拆成两条**（en 的 dey 阿尔及尔总督 / it 的 dèi 众神是两个词，两支都真 ⇒ it 那支挪到本来空着的 noun 词条上，顺带把空词条填上，那条 `/ˈdɛj/` 读音也因此第一次显示出归属）。🔴 `fare` 那条**把握最低**（两版直接冲突，我按「意语版对自己语言的方言用法更可能对」裁的），日后回源发现 en 对要翻回来。判据与名单写死在 `fixes/fix_cross_edition_sense.py`，回归闸 D9 锚着这四条防重译盖回去 | 2026-08-21 | ✅ 已闭环 |
| ✅ **音标位上的垃圾已藏（2026-08-21，8 条）**：`*` 当音标 3 条（**全是主音标**，用户直接看到 `/*/`）· `Vantaa` 只有一个长音符 `ː` · fr 版把 `acquietano` 的音标切剩 `ˈa.no` 等 4 条（A54：fr 版在音段分歧上无覆盖权）。`pronunciation` 加 `hidden` 列并同步改了 `pronQuery`。🔴 **判据修了四轮**，前三轮都太宽、全部停手记为上界：① 「含 qu 而无 kw」116 条（`bouquet`/`quinoa` 是外来词）② 「音标短于词形一半」167 条（`Doubs`→`ˈdu` 是真读音）③ 「是另一条读音的前缀/后缀」173 条（`B` 的 `b` 与 `ˈbi` 两个都对）④ 终版**反着定义**（列标记符号而非音段白名单）—— 第一版白名单漏了 `ɾ ʔ ə ɜ`，把 `r`→`ɾ`、`ah`→`ʔ` 判成垃圾，17 条里误伤 9 条。**残差 173 条只作上界记账，不据此动数据** | 2026-08-21 | ✅ 已闭环；173 条上界待判 |
| 🔴 **de 的建库源 dump 不在**：`de/paths.py` 声明了 `kaikki.org-dictionary-German.jsonl`，文件不存在（六语种里只有 de 断了）。de 库本身还在、不影响现有数据，但**重跑建库或加外锚闸时第一步就会失败**。用户 2026-08-20 决定：**重构 de 时再下**（约 300–400 MB）。已记进 `data/dumps/README.md` | de 重构那一轮 | 记账，用户已拍板 |
| 🔴 **法语版把法语译文粘在例句正文后，30 条**（`capitare` → `Sono cose che capitano. Ce sont des choses qui arrivent.`）。意语部分是对的，只是后面粘了法语译文 ⇒ 「粗糙」不是「错」。名单已逐条读过并写死在 `fixes/hide_non_italian_examples.py:GLUED`，那道闸**反向断言这 30 条仍在显示**（防谁把判据放宽顺手也藏了）。要修得把两半切开、法语那半塞回 `src_translation`，切分判据没把握 | 专门一轮 | 记账，名单已定 |
| **意语版 5 条子列表证据从没提升成出版义项**（`farfalla` 的日行蝶/夜行蝶、`gufo` 的长耳鸮/雕鸮、`retroterra` 的城市周边地带）。补它们要**新建义项行** —— 是「少了义项」不是「义项错了」 | 专门一轮 | 记账；`recover_sublist_glosses` 的闸锚着这个数 |
| **55 行变形行的原形仍无 entry / 23 个原形**（`manere`/`scotere` 等，连一条证据都没有；含 `’ndrangheta` 那几个撇号合并的余留） | — | 记账，无从建 |
| **36 条可见义项挂不上 entry**（21 条词和义项两级都没有任何证据；15 条词级证据同时给出 adj+noun 而 `sense.pos` 缩不到一个，如 `elitista`）。硬挑一个正是反错配闸要防的事 ⇒ 宁可留空 | — | 记账；`extend_entry_layer` 的闸锚着这两个数 |
| **13 组「同词形下中文相同」是词性不同**（`beige` 形容词/名词、`poco` 副词/限定词……）。页面按词条+词性分块，用户看到的不是重复 ⇒ 不动。见 A92 | — | 记账，已接受基线 |
| **法语版的 `alt_of` 指针与占位符 30 个词形有意不收**（`kilohenry` = kH 的拼写变体；`laserfoto`/`black noise` 源头写着 `Définition manquante ou à compléter.`）。外锚闸原来把它们报成「要收」，判据已改成 import 收词器那一份。见 A93 | — | 记账，闸已对齐 |
| ✅ **盲推释义已分族处置（2026-08-20，用户「动，看一下」）**：🔴 关键是**那 24–25% 只量了三族里的一族** —— 盲测的正控写着「C2 生僻动词」。按 A63 的频次三分法：**A 有频次 164**（英语外来词 `green`/`tag`/`editor` + 介词短语 `all’interno`）、**C 量不了 162**（多词固定搭配 `a pancia in su`/`sabato inglese` + 构词成分 `-terapeuta`）、**B zipf=0.0 共 3,065**（生僻单词，正控就是这一族）。A 和 C **不是按构词猜**，意思就摆在词形里。⇒ 藏 B 族 3,065 条；A+C 的 326 条我**逐条读完**，改 4 条源头证实的错（`sbronzare` 使成青铜色→灌醉、`rivalere` 重新有价值→求偿、`citara` 西塔拉→基萨拉、`sull’avviso` 持此意见→警惕）、藏 3 条我判为错但无源可对的（不拿我的猜换它的猜）。见 A96。`fixes/prune_blind_gloss.py`，全程可逆（`src` 标记一字节未动） | 用户 2026-08-20 拍板 | ✅ 已闭环 |
| 🔴 `ipa_src` / `translation_src` / `gender_src` 三列**存在但全空**（0 行）。`IT_PLAN` 一节写的"有 `ipa_src` 来源列"只对了一半 —— 列在、值没有；阶段 4 的验收判据 3（"`ipa_src` 为空的行数为 0"）因此不是核对而是**回填工作** | 阶段 4（`ipa_src`）／阶段 1（另两列） | ✅ `ipa_src` 已回填 588,280 行（2026-08-17）：`rule:accent` 269,303 / `rule:plain` 161,288 / `en` 84,160 / `fr` 62,048 / `it` 8,337 / **`unknown` 3,053** / `llm:doubao` 91。判据是**复现当初的生成路径**，证明不了的才写 unknown。⏳ `translation_src` / `gender_src` 仍空 |
| `level` 非空 152,414 > `definition` 非空 152,392，多 22 行 | 阶段 X（`level`） | 未查。按 A12 不修 `level`，只需知道这 22 行是什么 |
| `example` / `flag` 两列全空（0 行） | 阶段 5（例句层） | 符合预期，例句层还没建；`flag` 全 0 = 七月收尾已清零 |
| `collocation` 仅 20,986 行（3.59%） | 阶段 5 | 符合"够用即可"，不追全 |
| ⭐ **阶段 1 的期望值已经有外部锚**：库内 `definition` 按换行拆得 **198,026** 个义项（152,392 行词条），英文版 dump 实测 **200,160** 条真释义义项 ⇒ 缺口 **2,134 条（1.07%）**。es 的同一个数是 1.06% —— 同一套解析逻辑，同一个量级 | 阶段 1 立期望值 / 阶段 3 收缺口 | 已量清，等阶段 1 用 |
| el / tr 两个切片按错误理由取（说好的录音实测是 0 和 112 条） | 阶段 3 | 留盘不删，5.2 MB；不再为其花时间 |
| 🔴 **22 条无 definition、`infl` 为空、但中文是真释义的词条**（`ganga`/`autoritativo`/`sonata da chiesa`）已按词条层建 `sense`。它们很可能是 `SCHEMA` §9.1「被误判成变形层」的 it 版本 | 阶段 2（误判回收） | 数据已保住，**归属待判** |
| 2 条搭配切不出意语部分：`AC` → `AC米兰 AC Milan`（中英写反）、`pasticci` → `弄出乱子`（源头没有意语）。阶段 0 只搬不改，已原样入表、无中文 gloss | 阶段 5（搭配层） | 记账 |
| 3 行 wiktextract 残渣（`in men che niente` 的 `See also:` / `Synonyms:` 被当成义项，中文为空 2 行）—— 全库只有这一个词 | 阶段 1 | 记账 |
| `/api/stats` 的 `translated` 语义变了：原来数"translation 非空的行"= 584,904（100%，其实 67% 是指针文本，这个数没有意义），现在数"有义项的词条"= **152,414**。数字变小了，但**变准了** | 已生效 | 已知 |
| ✅ `ItalianEntry.flag` 字段已删（2026-08-18 阶段 8）—— 永远为 null 的字段与"永远为真的健康检查"是同一类东西 | 阶段 8 | ✅ 已闭环 |
| 库文件 155 MB → 212 MB（新表+索引；删掉的列空间要 `VACUUM` 才回收） | 阶段 8 | 不急 |
| ✅ **大小写折叠已拆开**（阶段 3a，2026-08-13）。🔴 **原记的 19,030 是错的** —— 那数的是"词形非全小写的 dump entry"（源头口径）；真正被压成一行的是 **3,821 个词形**，其余 dict 行本来就保留了原大小写（`Roma` 存成 `Roma`）。落点口径：新建 3,827 行 / 改挂 3,935 个 entry、4,385 条义项、50 条变形。查询排序已加**精确大小写为第一键**，全部 7,648 个同名词形实测命中自己、零错 | 阶段 3a | ✅ 完成 |
| 🔴 **1,249 个 entry 的义项被「按词形去重」吃光、整个不可见**，连带它的读音（`accettano` 两个不同动词释义文字相同，第二个 entry 整条消失）；被吃掉的义项共 **2,338 条** | 阶段 3 / 4 | 已量清 |
| 🔴 **同形异读 1,944 个词形**（重音位置 1,380 / 元音开闭 460 / 音段 74 / 噪声 30），现在每个词形只有一个 `ipa` ⇒ 至少一半的读音对某些义项是错的（`ancora` 的「锚」现在配的是副词重音） | 阶段 4 | ✅ 结构已解（2026-08-17）：`pronunciation` 1,205,807 行，**99,441 个词形有 ≥2 条读音**，读音能挂 entry 的 240,549 行（`subito` 的 adv `ˈsu.bi.to` 与 verb `suˈbi.to` 已各归各的词条）。⏳ 剩下的是**展示层要按 entry 取读音**（A57 的 wildcard 写法），排阶段 8 |
| ✅ **`inflection.entry_id` 两套语义已定死（2026-08-18 阶段 8）**：定成「原形的那个 entry」，`fixes/repoint_inflection_entry.py` 重指 499,966 条、清空 24,514 条，断言进回归闸 C8（A72） | 阶段 8 | ✅ 已闭环 |
| 🔴 **396,901 个词形（26.5%）一条读音都没有** —— 七月的 `b_ipa_fill` 跑在阶段 3 收词**之前**，fr 版收进来的 18 万变形与姓氏词头都没赶上。**2026-08-18 决定：留空，不用 G2P 补**。判据是实测（真值＝三版 dump 背书读音，全量比对）：带重音形路径 **57.2%**、光杆路径 **30.4%**（`-issimo` 族 0.6% / `-rebbero` 43.4% / `-ano·-ono` 55.9%），不一致里 77% 是重音位置或开闭元音 —— **用户会照着读错**。两家顾问独立给出同一结论。分堆：A 有唯一原形且原形有读音 151,369 / B 原形自己也没读音 76,091 / C 无原形指针 169,441（多为姓氏地名）。⇒ 后续真要补，只做 **形态派生**（`rule:morph`）：**原形读音必须来自 dump**（`rule:plain` 一律不作源，否则把 44.8% 的错扩散到 15 万行）、只覆盖实测稳定的词尾规则（`-erebbero` 100% 倒三 / `-vamo` 99.9% 倒二 / `-ate` 99.8% / `-issimo` 98.2% 倒三 / `-ono` 95.8% 倒三 / `-vano` 94.2% 倒三）、**B 堆不做级联**（两家一致：级联是错误放大器）、C 堆留空。⚠️ 我原来的方案「从原形重音直接搬」**自己量下来不成立**：变形与原形重音同位只有 82.2%，`-vano` 0.9%、`-rebbero` 7.1%（那几个时态重音本来就移到词尾） | 阶段 5 之后（未排期） | ✅ 决定已定：不补；形态派生另立一件事 |
| 🔴 **取数表的「录音条数」原来虚高 2–3 倍**：`count_slices.py` 把同一条录音的 `mp3_url`+`ogg_url`+`wav_url` 数成三条（en 2012+2012+0=4024 / fr 13343+13326+12159=38828 / it 674+669+72=1415，三版精确复现）。已按**录音条目数**改正 | 已修（2026-08-13） | ⚠️ 阶段 6 必须用改正后的数 |
| 🔴 **真人录音全版并集只覆盖 9,507 个词形；落到我们库上 6,884 个（全部词形的 1.2%、lemma 的 4.0%）** —— 四样东西里唯一不可能"集齐"的 | 阶段 6 | 已量清；缺口靠合成兜底 |
| `probe_editions.py` 的名称表原来漏认 `İtalyanca`（tr 版 154,681 条意语义项）和 `italština` | 已修（本阶段） | ✅ 已补进 `NAMES`，下一门语言直接受益 |
| ✅ **跨版缺口审计已建**（`pipeline/verify_vs_dump.py`，义项级、外锚永不过期）。实测：**英文版缺口 0**（源头 208,627 条真义项全部在库）；it 版缺 11,921 词形（已收）；fr 版缺 171,139 词形（按 A31 不收） | 阶段 3 | ✅ 完成 |
| 🔴 **`ingest_it_edition` 闸①仍红：212 条意语证据挂在大小写不对的 dict 行上**。成因：阶段 1.5 灌证据时库里还没拆大小写（按 `w.lower()` 查），阶段 3a 拆行时只搬了**已裁决**的那些。已修 609 条，剩 212 条要连义项一起搬 —— **不影响用户看到的内容**（出版层的闸全绿），故记账不追 | 阶段 4 或 8 | 🔴 **闸留红，不弯判据** |
| ✅ **fr 版 160,874 个空壳词形：阶段 8 决定仍然不收**（2026-08-18）。理由是它们在 `dict` 里根本不存在，要把弹窗文案从"未收录"改成"这是意语词，暂无释义"就得先把 16 万空壳行收进来 —— 而空壳行正是 es 上"搜到词却一片空白"的成因（`exactQuery` 至今为此留着两条排序键）。**缺一句友好文案 < 多 16 万个点进去空白的词条**。切片留盘不删 | 阶段 8 | ✅ 已决（不收）|
| 🔴 **法语中转中文的已知残差**：①词缀类中文里留着意语词 116 条（`-aggio的复数`）②`geo_zh` 只认意大利地理句式，西班牙/法国地名靠"旧版有地理词就保留"的守卫兜底 320 条 ③个案错误如 `grande muraglia`（天文学"星系长城"被译成"长城"）、`assolutivo`（通格被译成"作格"）、`imenectomia`（法语源写错 -otomia，我们照翻） | 阶段 8 或专门一轮 | 记账；`src` 分档可一句 SQL 定位 |
| ⭐ **我自己读 26 条数出 ~8% 真错，两家判官在 92 条上只报 1.1%** —— 差的不是样本是**能力边界**（通格/作格、-ectomia/-otomia 判官都判 ok）。⇒ 判官只做全量定位，**判据设计与最终裁决必须我来** | 已生效 | 见 A34 |
| ✅ 中文里的半角逗号/分号已全库改全角（**1,502 条**，一手中文里也有） | 已修 2026-08-14 | 闸只查过句末标点、漏了句中 |
| 🔴 **alt_of 悬空指针 533 条**（501 个不同目标）：415 个是正常词形只是库里没有（→阶段 3 收词），86 个是源头把英文说明混进了 `alt_of.word`（`su used before a vowel`） | 阶段 3 | 计划明写"悬空指针不删行，记账" |
| **变形层悬空 452 条**（原形不在库里，占 510,110 的 0.1%） | 阶段 3 | 同上 |
| **211 个 `-rsi` 自反式词形在变形层且无义项**（`porsi`/`opporsi`），标签是「变位形式」而非「自反形式」。改标签会让闸①a 对不上七月的 `dict.infl` 旧列 ⇒ 等阶段 8 删旧列时一起改 | 阶段 8 | 记账；es 的同类问题是 4,657 个，it 只剩 211 是因为 2a 已捞回大头 |
| **`dict.infl` / `exchange` 两列已被 `inflection` 表取代**，读取路径已切走，列还在（闸①a 靠它们锚"只搬不改"） | 阶段 8 删列 | 记账 |
| 🔴 **意语释义 50,302 条对不上出版义项**（②1:1被词性/法语版挡下 2,417 / ③意语1我们多 917 / ④意语多我们1 17,139 / ⑤多对多 29,816），留在证据层 `sense_src(sense_id IS NULL)`，展示层不露出。⚠️ 2026-08-14 两次重数：旧记的 60,009 混着占位符与指针（要**先过 `not_a_definition` 和 `PTR`**）；③ 当天做掉，6,846 → 917（剩下的是模型判「一条都挂不上」632 + 我们没有该词性 280，两者本质都是 ④） | ④是「我们义项收粗了」的信号，属**独立的义项拆分项目**，两位顾问一致反对随导入自动完成 | 已量清、数据已在库 |
| 🔴 **110 个 1:1 词形被法语版判为多义而挡下**（`nave`/`notte`/`marmo`）。⚠️ 抽看这 8 条**意语释义本身都是对的** —— 这个判据是**保守的、不是精确的**，挡下的多数其实可用 | 阶段 8 或专门一轮 | 记账；留证据层随时可提升 |
| **1,760 个 1:1 词形因两版词类划分口径不同被挡下**（`noun→phrase` 712 / `name→noun` 123 / `num→adj` 71） | 阶段 3（词类口径统一）| 记账 |
| **意语释义落在库里没有的词形上 13,089 条 / 11,742 个词形**，`sense_src.word_id` 是 NOT NULL，没词条就没挂载点 | 阶段 3（收词） | 已量清 |
| **提升时被文本正则滤掉的变形指针 2,643 条**（证据层保留）。正则改到第二版就停手，残差按上界记账 | — | 见 A23 |
| 🔴 **77 个双助动词词条带 `also` / `usually` 这类程度词**（`prendere`：avére + èssere[**also**]，即 essere 是次选）。现在一律显示成「avere / essere」，不算错但偏粗 | 阶段 8（展示打磨） | 记账，不为 77 条发明新列 |
| **213 个双助动词词条源头两个助动词都没给条件标签**（`sapere` / `potere`），逐义项定不下来，停在 `both`；涉及义项 489 条 | — | **源头就没有**，不是我们漏收；情态动词本来就随后接的不定式变助动词 |
| **29 条非动词义项带及物性标签**（`dare` 名词「借方」被标 `transitive` = 源头从动词段串下来；但 `ci`/`si` 的 `reflexive` 是正当标注）。数据层原样保留，展示层只在 `entry.pos='verb'` 时渲染 | — | 已定，见 A20 |
| ✅ **阶段 1.5 补跑（2026-08-14）**：意语版原文释义已裁决 35,782 → **37,240（35.3%）**。净提升 1,846（1:1 人群随 2a/3 变化后新浮出）+ 6（零义项词形），退回 372（自指指针）+ 19（占位符/纯域标签）+ 3（判据变化后不再 1:1）；藏掉 10 条无释义的空义项 | 阶段 1.5 | ✅ 闸全绿，变异 7/7 |
| 🔴 **法语版也有它自己的「缺失」占位符**（`Exemple d'utilisation manquant. (Ajouter)`），收词时没认出来，逮到 2 条实例已藏。**还没有系统性判据** —— 意语版那族有 9,360 条，法语版这族有多少不知道 | 阶段 3c 或专门一轮 | 记账，未量 |
| **`intake_it_words` 闸红：1,419 个空壳词形**（收进来了但一条可见义项都没有）。绝大多数是「意语版内容只有占位符，藏掉后就空了」。`--prune` 一直没跑 | 阶段 8 | 🔴 闸留红，已知 |
| 🔴 **中文完全相同的重复义项 611 个词形 / 788 行**（多义词形的 2.0%）：`passeriformi` 的「雀形目」并排显示两遍、`terra cotta` 的「赤陶」三遍。**用户直接看得到**。es 的同类问题是 61,205 条，it 只有 788 行 ⇒ 有界，可修 | 阶段 8（展示层）或专门一轮 | 已量清，未修 |
| 🔴 **`PTR` 正则第三族残差**：`variante di X`（在 alt_of 上已由 A38 的确定性判据处理，但**非 alt 的**没有）、`part. passato di X`（认全拼 `participio`、不认缩写）。混进了 ③ 的对齐面 | 阶段 8 | 记账；按 A4 不再改正则 |
| ⭐ **③ 对齐控制组实测（online pro，120 正控 + 60 负控）**：负控洁净率 **100%**（给错的义项列表，一条都没编）；正控严格 80.8% / 按义 91.7%；模型真错 ~2.5% 且偏向「宁可不挂」 | 阶段 ③ | ✅ 判为通过，见 A40 |
| ✅ **姓氏义项的中文已补标记（2026-08-15，6,951 条）**：英文明说 `surname`、中文却只有裸音译（`Polistena` 的姓氏义和市镇义中文都是「波利斯泰纳」）。按**既定约定** `X（姓氏）` 补（已写对的 4,607 条里 2,876 条用的正是它，不发明第二套）。确定性判据、可逆、闸 5 条 + 变异 3/3 | — | ✅ 完成 |
| 🔴 **法语地名族 1,871 条，其中 1,528 条中文仍是裸音译**：`Aisne (rivière française)` 与 `Aisne (rivière belge)` 都译成「埃纳河」；`Saint-Léger` 十个法国市镇全是「圣莱热」。`geo_zh` 只认意大利句式。要一张法国省/瑞士州/比利时省 → 中文的表才能模板化 | 阶段 8 或专门一轮 | 已量清，未做 |
| 🔴 **地名义项的中文没有地理框架**：姓氏那半已修，`Polistena` 的市镇义仍只是「波利斯泰纳」。意大利本土的可用现成的 `geo_zh` 模板补 | 同上 | 记账 |
| ⭐ **④⑤ 新建义项 16,302 条落库后实测：逐字重复 0、括号外与已有相同的 392 条（2.4%）**。⚠️ 我先前眼估「约 20%」是**拿 14 条的印象当比例**（`measure-landing-not-source` 那个错的又一次）。而且这 392 条大多**正当**：`marzo`「三月（古罗马历法中的第一个月）」、`giallo`「黄色（波长565至590纳米）」—— 对应词本来就该相同，区分靠括号，正是 A45 要的行为 | — | ✅ 已量清，无需处理 |
| ⚠️ **模型偶尔补源头没有的信息**（`lardo` 的意语原文只说"猪皮下脂肪"，中文补了「（未腌制）」） | 同上 | 记账 |
| ✅ **展示层契约闸已建**（`apps/web/src/contract-check.tsx`，6 条判据，变异验证 4/4）。⚠️ 慢：每条词条要完整取数+渲染，实测 **91ms/条**，全量 16,193 条要 25 分钟 ⇒ **常规用 `--limit 3000`（约 4 分钟）**，全量只在大改动后跑 | — | ✅ 完成 |
| 🔴 **`TVTB` 取数 0.42 秒，比同类词慢 10 倍**（`tempo`/`casa`/`aquila` 都是 0.02–0.04 秒）。特征是带 `alt_of` 关系。今天义项 38.8万→41.2万、释义 63万→70万，查询性能该重测 | 性能那一摊 | 记账 |
| ✅ `sym`（符号）补进 `POS_LABELS`——`POS_MAP` 里 `symbol→sym` 一直都在，标签表却漏了，`bo`/`ar`/`AQ` 的分组标题直接显示英文 | — | ✅ 展示层契约闸逮到的 |
| `dict.aux` 与新层不一致 **450 个词形**（旧值 `avere`、源头是 `both`，如 `rallentare`/`prendere`/`potere`）；旧值另有 4 个词形只能记一个值而源头给了两个词条（`sortire`）。读取路径已切走，数据未改 | 阶段 2（变形层建好后把 `dict.aux` 降级成派生值） | 已量清 |

---

| ✅ **已处理（2026-08-19）**：`example` 加 `hidden` 列（可逆，读取路径同步改），藏掉 **87 条**真非意语。🔴 账本原记的「47 条」与那把尺子都不能用：重跑得 167 条，逐条读完发现 `de`/`qui`/`et`/`un`/`la` 这些「法语功能词」**在古意语和罗马方言里就是意语词** ⇒ **50 条是尺子误伤**（《太阳兄弟赞歌》、彼特拉克、罗马方言全被判成法语）。167 条规模够小 ⇒ 逐条读、名单写死，不发明判据。见 A93 同族教训。**原记** ~~47 条例句正文是法语/拉丁/古法语，判据不够删数据~~ | 阶段 9 或以后 | ✅ 已闭环 |
| ✅ **已补（2026-08-19）**：2,119 → **55 行 / 23 个原形**。根因是 `extend_entry_layer` 只从**义项自己的**证据行取词性，而盲推那批义项没有证据行 ⇒ 退到**词级**证据（同词形上那条 `definizione mancante` 占位符的 `src_ref` 里带着 kaikki 原生词性），新建 588 个 entry、挂上 600 条义项，再跑 `repoint_inflection_entry` 重指 2,064 行。剩 55 行的原形连一条证据都没有，无从建。**原记** ~~2,097 条变形行的原形在 entry 表里没有任何词条~~ | 阶段 9 | ✅ 已闭环 |
| 🔴 **读音还没有挂到义项组上**：`ancora` 页面并排显示 `anˈkora`（副词「还」）与 `ˈankora`（名词「锚」），但**没说哪个属于哪条义项** —— 名词义项旁边显示的是副词的读音。数据齐了（`pronunciation.entry_id` 240,142 行 + A57 的 wildcard 语义），差的是展示层把义项按 entry 分组。影响 **1,944 个同形异读词形**（0.13%）。⚠️ 现在是「不完整」不是「错」：那两条读音都摆着，只是没标归属 | 阶段 9 | 记账 |
| `example.sense_id` 只挂上 39.0%（14,895/38,144），其余 23,249 条摆在词条末尾的「例句」区块 | 阶段 5 已定 | 符合预期，A59 |

| ✅ **已加校验（2026-08-19）**：`getInitialLang()` 拿**上一次缓存的语言表**（`dict-langs`，同步可读、零成本）核一次。原来的核对在 `/api/langs` 回来**之后**才做，那之前首帧和首批查询用的都是旧码 —— 后端回退是对的，但用户看到的是另一门语言的结果、而语言栏高亮的是他选的那个。缓存为空时不拦（那时没有可信判据）。契约闸加了 4 条断言。**原记** ~~getInitialLang 不拿 /langs 校验~~ | 阶段 9 发现 | ✅ 已闭环 |
| ✅ **`plural` 值已回源改正（2026-08-19）**：284 条编造值按英文版 dump 改对（`paro → para`、`uovo sodo → uova sode`、`uomo ragno → uomini ragno`），根因是模板参数解析器不认 `<…>` 里的逗号（A76）。⚠️ 改完仍有 2 条错在源头（`campo minato → campati minati`、`tocco finale → tocci finali`，英文版原文如此）。另清掉 3 条 `plural_gender==gender` 的假异性复数。**原记的 293 条** ~~（阶段 8 查 braccio 双复数时撞见）：`paro → line`、`mangiare → archaic`、`rosario → and` 是七月解析器把标签/邻词当成了复数值（**页面上就显示着「复数 line」**）；其余多数是多词词条只取了尾巴（`uovo sodo → sode` 应为 `uova sode`、`parola chiave → chiavi` 应为 `parole chiave`）| 阶段 8 发现 | 未修，垃圾值那几条属「用户看到错的内容」 |
| ✅ **双复数已能同时展示（2026-08-19）**：`braccio` 页面现在是「复数 braccia〈阴〉| 复数 bracci」+ 提示「异性复数：单数 braccio（阳）→ 复数 braccia（阴）；另有 bracci（阳）」。**没动表结构** —— 行本来就在 `inflection` 里，改的是读取（A77）。**原记的** ~~：`dict.plural` 是单列，而 `braccio` 有两个复数（`braccia` 阴=人的手臂 / `bracci` 阳=器物的臂），源头（it 版拆两条名词、en 版两条 `plural of braccio` 各带性别）说得很清楚。库里 `bracci`/`braccia` 两行都在且都正确指回，**缺的只是词头徽标显示不了两个**。规模：30 个（osso/dito/labbro/corno/muro/filo/grido/riso…）| 阶段 8 发现（豆包挑出，回源证实）| 记账，与「按 entry 归属」一起做 |

| ✅ **读音归属已收回（2026-08-19 当天）**：加了 `pronunciation_entry` 关联表（`pipeline/build_pron_entry_link.py`，282,060 行），**不重建 `pronunciation` 表**（那上面有当天五个修复脚本的成果）。收回 **21,666 个词形**的归属，远多于原估的 614；组头能标专属读音的词形 6,811 → **7,789**。展示层改读关联表，判据 `readingBelongsTo` 由组件与契约闸**共用一份**。**原记** ~~一个读音同属多个词条时单 FK 只能落 NULL，614 个词形丢掉区分力~~ | 阶段 9 之后 | ✅ 已闭环 |
| ✅ **读音/变位按 entry 归属已接上（2026-08-19）**：义项按「词条+词性」分组（A79）、专属读音标在组头（A80，覆盖 1,515 个词形）、变位形式块加了同形异义说明（`braccio` 那块不再看着像名词的变位）| 阶段 8 记账项 | ✅ 已闭环 |

| ✅ **同词形下重复中文已处理（2026-08-19）**：470 组 → **28 组**。①108 组「全部可得原文逐字相同」确定性藏掉（判据必须含 `sense_src.text`，fr 版的法语原文不落 `sense_gloss`）②172 组源头无区分信息 → 合并 ③114 组补出括注（`Saint-Léger` 十条各带省名、`Sutherland` 十条各不相同）。剩 79 组见 `fixes/distinguish_duplicate_zh.py` 的基线说明 | 阶段 8 记账项 | ✅ 已闭环 |
| ✅ **自反式标签已改（2026-08-19）**：`lavarsi`「变位形式」→「自反形式」**1,418 条**（账本原记 211 是按 `-rsi` 词尾数的，判据错）。用 A83 的两套 label 做到旧列一字节不动 | 阶段 8 记账项 | ✅ 已闭环 |
| ✅ **已定（2026-08-19）**：真正**可见却零释义**的义项全库只有 **3 条**（`chaperon`/`oi!`/`pbsl`），已藏。判据一放宽会捞出 300 个，其中 **254 个是撇号空壳**（`D’Agostino` 的内容在 `D'Agostino` 上）+ **10 个大小写空壳**（`B` 的内容在 `b` 上）—— 那两族是 `merge_apostrophe_variants` / `split_case_forms` **有意留的**，判它们零内容是尺子错。另 28 个词形源头原文就是 `definizione mancante; se vuoi, aggiungila tu`（缺定义，你来补），真的没有 ⇒ 留白。**原记** ~~25 个词形收进来了但零内容~~ | 阶段 9 之后 | ✅ 已闭环 |
| ✅ **已恢复（2026-08-19）**：**子列表根本没丢** —— wiktextract 把它放在 `glosses[1]`，收词器只取了 `glosses[0]`。已恢复 **18 条**义项的意语原文 + 手写中文（`Springfield` 六条从全是「斯普林菲尔德（地名）」变成各带州名）。🔴 账本原记的「362 条证据行 / 235 个词形」**量的是别的东西**（`sense_src.text` 以冒号结尾），而意语版拿冒号引出例句是常规写法；真判据是 `len(glosses) > 1`，全库只有 **23 条 / 11 个词形**。**原记** ~~wiktextract 把总述复制了 N 遍、具体是哪些没收~~ | 阶段 9 之后 | ✅ 已闭环 |

| ✅ **⑤「法语中转残差」已定性（2026-08-19）**：账本原记的「116 条 + 320 条 + 个案」没留判据，我连试三把尺子都太宽（A4 停手）。改用**分层质量普查**的现成数字：E 层（阶段3b新收词）8.6% / F 层（经法语）5.0% / I 层 2.5% / G·U 两层 0%，加权 **1.2%**，折算约 3,200 条真错。🔴 **实测「改 prompt 重跑」会造更多错**（A90），⇒ **决定不做**。真错的形状是「取了歧义源的错义支」，中文读得通、所有自动闸都抓不到，只能逐条读 | — | ✅ 已决（不做，带证据）|

## 三、阶段进度

| 阶段 | 状态 | 完成日 |
|---|---|---|
| -2 准备：`paths.py` + 写库闸门 | ✅ 完成 | 2026-08-12 |
| -1 版本探测与取数 | ✅ 完成 | 2026-08-12 |
| 0 表结构迁移 | ✅ 完成 | 2026-08-12 |
| **v3 `entry` 层**（计划外新增，见 `SCHEMA` §10） | ✅ 完成 | 2026-08-13 |
| 1 义项层迁移 | ✅ 完成 | 2026-08-13 |
| **Q1 语法层**（及物性 + 助动词，计划外新增，见 `SCHEMA` §10.4.1） | ✅ 完成 | 2026-08-13 |
| 1.5 意语原文释义层 | ✅ 完成（2026-08-14 两轮：35,782 → **43,169 / 40.9%**，含 ③ 语义对齐） | 2026-08-13 |
| 2 变形层归位 + 误判回收 | ✅ 完成 | 2026-08-13 |
| 3 收词 + 大小写拆分 | ✅ 完成 | 2026-08-13 |
| 4 音标迁移 | | |
| 5 例句 / 关系 / 搭配 / 频次层 | | |
| 6 发音合成 | | |
| 7 闸与验收 | | |
| 8 展示层与性能 | | |
| 9 产品化 | | |
