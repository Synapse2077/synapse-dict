# 📋 BACKLOG —— 全仓唯一的顶层账本

> 2026-09-20 建。用户原话：「**你只有记在顶层，你才知道所有语言留下来的所有问题**。」

## 为什么建这个文件

在它之前，**这个仓库没有顶层账本**，六门各自发明了一种记账形状：

| 文档 | 它的账记在哪 |
|---|---|
| `DE_PLAN` | `# 📋 de 收尾单` ＋ `## 二、🟡 可发布，记账` |
| `EN_PLAN` | `### 16.7 顺带记账` |
| `FR_PLAN` | 按阶段散落的若干个「记账」小节 |
| `IT_PLAN` | `#### ⚠️ 顺带发现、已记账未决` ＋ `# 四、it 收尾单` |
| `JA_PLAN` | `## 六、📋 欠账` ＋ `## 收尾单的交接` |
| `PT_PLAN` | `# 📋 pt 收尾单` ＋ `收尾单 C1–C59` |
| `PITFALLS` / `PLAYBOOK` / `FRAMEWORK` | **没有账本章节** |

于是**跨门的账只能寄存在「发现它的那一门」**，而做别门的人永远看不到它。
`JA_PLAN` 的 24 行欠账里，**12 行（50%）牵涉别门**。三个已经发生的后果：

- **en 的账躺在 `JA_PLAN` 里**（本文件 B1），从 ja 开工起没人看得到。
- **`split_colloc` 那条写着「日语开工前必须先改」**（B2），而这个前提早就失效
  （ja 压根没有搭配层）—— **没有人会去复查一个躺在别人账本里的前提。**
- 跨门的「收词之后哪些层要重跑」被写成**两行、结论相反**地躺在 ja 表里
  （2026-09-20 才发现并合并，顺带补了 P8 闸）。

🔴 **而最能说明问题的是：「这个仓库总共欠什么」这个问题，此刻没有任何人能回答，包括我。**
我试过用状态符号扫六份文档，得到 137 行「开着的」—— 那个数**不算数**：
🔴 在这些文档里**绝大多数是强调不是状态**（`🔴 ECDICT 的人工中文换身份`、
`🔴 阶段 1.5 实测这一段不花钱`）。`test_plan_ledger.py` 的 P7 注释里早记过这一课：
**同一个符号在六张计划表里含义不同，按符号判会在 37 行上误报。**
⇒ 信息不在一个可机读的形状里，这本身就是本文件要解决的问题。

## 规矩

1. **跨门的账、别门的账，一律记在这里**，不许寄存在某一门的计划表里。
   各门的计划表只记**它自己的**账。
2. 每条必须写清 **影响哪几门** 和 **什么算结清** —— 否则它就是一条永久前提
   （`[[record-the-negative-decision]]`：否定结论必须写什么会推翻它）。
3. 🔴 **本文件必须被闸盯着，否则它会变成一个更大的垃圾场。**
   per-language 的账本今天还活着，唯一原因是 `test_plan_ledger.py` 的 P1–P8 逐行读它。
   一个没有闸的顶层文档，下场就是「收词之后哪些层要重跑，没有清单」那条 ——
   **写下来了，然后烂在那儿**（`[[lesson-must-become-mechanism]]`）。
   ⇒ 已有 **P9**（各门的账的闸）：**这一门的欠账表里不许出现「不是 X 的账」那种行**，
     有就当场红，逼它搬到本文件来。本文件自己的闸尚缺，见 B5。

---

## 账目

| # | 欠什么 | 影响哪几门 | 规模 | 状态与详情 | 什么算结清 |
|---|---|---|---|---|---|
| **B1** | **en 的 `Late`/`Middle` 两个语域标签：代码认、库里没有** | **en** | 20 条 | 🔴 **代码与数据已漂移**。`en/pipeline/build_sense_tags.py` 的 `REGISTER` 集合 2026-09-15 补了这两个值，但**没有重跑建库** —— 重跑会撤销建库后「专名标记传播」那一轮的修复（`[[replay-scripts-undo-fixes]]`）。已随「界面优化4」提交进 git，所以更该记在账上<br>⚠️ **这不是 ja 的账，是 en 的**（在 ja 轮次里发现，所以记在这儿）。2026-09-20 复核仍然成立：`en/pipeline/build_sense_tags.py:118` 认 `Late`/`Middle`，而 **en 库里这两个值查出来 0 条** —— 代码与数据漂移，要重跑 en 建库才落地 | en 库里 `sense_tag` 的 `register` 出现 `Late`/`Middle` 两个值（现为 0 条）；且重跑不得撤销建库后「专名标记传播」那一轮的修复 |
| **B2** | **`split_colloc` 的空格判据** | **de / pt / fr** | 三门 | `de`/`pt`/`fr` 同名函数在无空格语言上会静默出错。日语开工前必须先改<br>⚠️ **这不是 ja 的账，是跨语种的**，而且**它写的前提已经失效**：本行原文是「日语开工前必须先改」，但 ja 的搭配层是**有意为空**的（欠账 24）—— `grep split_colloc ja/` **零命中**，ja 压根没有这个函数。<br>2026-09-20 复核 de/pt/fr 三门：`split_colloc` 至今仍是「第一个汉字前最后一个空格」判据，**没改**。⇒ 对 ja 不构成任何阻塞，留在表里只为不丢失这条跨语种事实 | 三门的 `split_colloc` 不再以空格为判据，且各自有一条断言锁住它（无空格语言上不静默出错）|
| **B3** | **收词之后哪些层要重跑，只有闸没有清单** | **跨门** | 结构 | 🔴 **挂着。闸已建、清单仍缺。**<br>`ja/dbtool.py:619` 的收词闸已经拦得住「插了新词形却不声明 `invalidates`」——**但那是逼人当场回答，不是一张可查的明细表**。<br>已知与词元数挂钩的层：**变形层 `base_id`**（pt 栽过）／**读音层 `entry.kana`**（ja 栽过）／**释义中文覆盖**／**声调**（ja 阶段 5d 收词后漏了 9 个词三个月）／**频次层**／**验收闸**。<br>⚠️ 本条原来在 `JA_PLAN` 里被写成**两行且结论相反**（一行 ✅ 一行 🔴），2026-09-20 合并，并补了 P8「欠账编号不许重复」 <br>🔴🔴 **2026-09-24 ko 又交了一次学费，而且是三次**：K13 补收 13,062 个词头之后，**罗马字**（22,868→77,741，`--topup`）／**关系层**（+3,838，定向补写）／**例句层**（+1）三层各自要单独补一遍，**三次都是我事后想起一个补一个**，最后那一条还是靠新建的外锚闸才发现的（`ko/pipeline/verify_layers_vs_dump.py`）。<br>⚠️ 收词闸确实拦住了「不声明 `invalidates` 就写库」—— 但它逼出来的是**当场那一句话**，而这三层里有两层是在**别的脚本**里重跑的，声明写在 A 处、活儿干在 B 处，**没有任何东西验证活儿干完了**。⇒ 本条的「什么算结清」里那句『怎么验它重跑过了』是关键的一半，ko 这轮证明它还缺着。<br>⭐ ko 这轮顺带给出一个**可复制的验法**：外锚闸按「源头 → 库」双向恒等式建，补收之后哪一层没跟上，它会直接报出来（例句那 1 条就是这么逮到的）| 有一张跨门的明细表：每一层写明「它与什么数挂钩、收词后怎么重跑、怎么验它重跑过了」，且被闸引用（不是再写一遍教训）|
| **B4** | **词源桥只认「有已出版义项」的词条，漏掉一批源头正文** | **ja / it / fr / pt**（`en`/`de` 不经过这段代码，`es` 结构上免疫）| ja +1,177 ／ it +4,449 ／ fr +919 ／ pt 未知 | 📋 **已量清楚、未动手**（用户 2026-09-20 收口「先记着」）。<br>机制与四门实测数字**记在 `scripts/ingest_etymology.py` 的 `db_keys()` 正上方**（知识住在它描述的那段代码旁边）；ja 自己那一半（194 条的分布、102 个空白页、必须先拆 Z3）见 `docs/JA_PLAN.md` 欠账 25。<br>🔴 **pt 独有**：宽桥会被对齐闸拦下（对不齐 4,719 ＝ 1.578%），因为库里对 `-a`/`-aco` 这类词多了一条 `etym_no='0'` 的无义项词条，而 dump 多词源时从 1 编号 —— **闸拦得对** | 四门各自：改桥 → dry 跑 → 先拆 Z3（Z3a 结构／Z3b 页面口径原样保留／Z3c 存档行记基线）→ 变异验证 → `--replace --run`。pt 另需先回答「那批 `etym_no='0'` 的无义项词条是什么」|
| **B6** | **`dbtool.py` 的 `__main__` 在「库还不存在」时崩** | **ja / de / fr / pt / en / es / it（七门全中）** | 七个文件各一行 | 🔴 **2026-09-20 开做 ko 时撞见。** `snapshot()` 对不存在的库**有意**返回 `{}`（文件头写着「新语种第一次写库之前它不在，这不是异常」），`_line()` 也处理了空快照 —— 但 `__main__` 直接 `s["__rows__"]` ⇒ `KeyError`。<br>⚠️ **七门都没撞上，唯一的原因是它们都等库建完了才跑这个入口**；而 `PLAYBOOK` 1.4 要求的恰恰是「闸门排在任何数据动作前面」，也就是**必须在库不存在时跑得起来**。⇒ 这是一条「只有开新语种的那一天才咬人」的缺陷，而那天恰好是最不该被绊住的一天。<br>已在 `ko/dbtool.py` 修好（空快照时打印一行说明并 `exit 0`）。**另七门原样存在**，各自的库都在，所以今天不发作 —— 但任一门要重建库就会撞上。<br>🔴 这不是 ko 的账，是七门的 | 七门的 `python3 <lang>/dbtool.py` 在库文件不存在时都能正常退出（不抛 `KeyError`），且各自有一条断言锁住它 |
| **B7** | **`TRACK_TABLES` 里躺着库里根本没有的表，没人审计** | **ja / en / de / pt（四门）** | 各一条 | 🔴 **2026-09-20 开 ko 建完库当天撞见。** 这是 ja 2026-09-16 修过的那个洞的**另一半**：`_track_audit()` 只审计 `TRACK`（列），**不审计 `TRACK_TABLES`（表）**。<br>实测：`pronunciation_entry` **只有 it 真实存在**（282,060 行），而 **ja / en / de / pt 四门的 `TRACK_TABLES` 里都列着它、库里都没有** —— `snapshot()` 静默跳过，行数闸对它完全失明，与当初那 19 个死列一模一样的形状。<br>🔴🔴 **同一个洞有两半时，修一半比不修更危险** —— 它会让人以为这类问题已经解决了。<br>ko 已修（`_track_audit()` 同时审计两者，变异 M7/M7b/M7c 验过：表和列的死条目都认得出）。<br>⚠️ 顺带一条**判据**：`pronunciation_entry` 该不该建要**各门自己量**，不是看别门有没有 —— es 实测符合条件的只有 14 个词形所以不建，ko 实测 429 个（0.75%）也不建（主键含 `entry_id`，复制行就装下了，冗余约 1,000 行），it 建了 282,060 行 | 四门的 `_track_audit()` 同时审计 `TRACK` 与 `TRACK_TABLES`；各自清掉死条目（要么建出来、要么从清单删掉）；且各有一条变异验证锁住「审计函数真的认得出死条目」 |
| **B8** | **`scripts/consult.py` 一直把"咨询"发给豆包 lite，而规矩写的是 pro** | **跨门（八门的每一次外审）** | 一行 | 🔴 **2026-09-21 ko 阶段 4b 要问 G2P 规则时撞见。** 用户 2026-08-01 定的原文是「同时咨询豆包 **pro** 和 deepseek v4-pro」，`.env` 里 `DOUBAO_SEED_2_1_PRO` 一直在，但 `ask_doubao()` 取的是 `DOUBAO_MODEL_ONLINE_LITE`。<br>🔴🔴 **这种漂移两边都不会报错**：请求照常返回、格式照常、落盘照常，**只是档次不对**。与 B7「死条目静默跳过」同形 —— 闸看的是"有没有输出"，看不到"输出是谁给的"。<br>⇒ 已改成默认 pro、`--lite` 留成显式选项（`scripts/consult.py:67`）。<br>⚠️ **但历史上所有用 consult.py 做过的外审都是 lite 给的**（it 的滑音/默认展示两问、es 的 coda 规则、ja 的 JLPT 词表…），那些结论的"两家独立一致"含金量要打折 —— 尤其 ja 那次豆包**四条授权全是编的**，现在知道那是 lite | ① `consult.py` 的模型选择有一条断言锁住（发出去之前打印实际 endpoint，且与 `.env` 的 PRO 一致）；② 回头标注哪些历史结论是 lite 给的，其中被采纳过的要重新过一遍 |
| **B9** | **`consult.py` 的失败分支打不出任何可诊断信息** | **跨门（每一次外审）** | 一行 | 🔴 **2026-09-24 ko 阶段 8 咨询活用类时撞见**：豆包跑满 **1200s** 挂掉，而 `print("🔴 %s 失败：%s" % (name, r))` 打出来是「🔴 doubao 失败：」**后面什么都没有** —— 超时类异常的 `str()` 是空串。我分不出是超时、4xx 还是网络断，**一次咨询的另一半就这么没了**。<br>⚠️ **最坏的地方是它看起来像"没什么可说的"**：空输出被读成"没有细节"，而实际是**信息丢了**。与 B7「死条目静默跳过」、B8「lite 冒充 pro」同一族 —— **闸/工具看的是"有没有输出"，看不出"输出里什么都没有"**。<br>✅ 已改成打「类型名 ＋ repr ＋ 用时」（`scripts/consult.py:123`）。<br>🔴 **但真正的账没结**：① 两家里挂了一家，这一轮就**没有「两家独立一致」**，而我此前的记忆里「两家收敛」是一个被反复引用的信号 —— **挂掉一家时必须显式降级，不能当成"一家说了算"**；② 豆包为什么 1200s 挂掉，没查。| ① `consult.py` 在只回来一家时**退出码非 0 或显式打「本轮不构成两家交叉验证」**，不让调用方默认拿到的是双份；② 查清 1200s 那次的真实原因（超时设置？模型不可用？）；③ 有一条断言锁住「失败分支打得出异常类型」|
| **B10** | **X-SAMPA 冒充 IPA：只修了 fr，另六门没查过** | **en / es / it / pt / de / ja（六门）** | fr 已修 150 条；ko 98 条；其余未知 | 🔴 **2026-09-25 ko 阶段 9 接展示层时又撞见。** X-SAMPA 是用 ASCII 表 IPA 的一套写法（`4`＝ɾ、`M`＝ɯ、`_h`＝送气、`ts\`＝t͡ɕ），维基各版的 `sounds.ipa` 里混着它。印在页面上就是一串乱码冒充音标。<br>🔴🔴 **这条教训 2026-08-26 在 fr 上记过**（`[[criteria-narrower-than-you-think]]`：「A5 收窄成『IPA 里不该有大写拉丁字母』，立刻逮到 **150 条 X-SAMPA 冒充 IPA**」）——**只修了 fr**。ko 建库时没人想起来查，直到把 `를` 渲染出来看见 `4mL\``。<br>⚠️ **闸为什么不响**：外锚闸是「源头 → 库」的恒等式，而**源头确实就这么写**。恒等式问「我们收得对不对」，问不了「源头给的是不是 IPA」。逮到它的是**渲染**。<br>⭐ 判据（ko 用的这条，可直接搬）：`ipa GLOB '*[0-9`\\_]*'` —— **必须 GLOB 不能 LIKE**，`LIKE '%_%'` 里 `_` 是通配符，会命中整库。<br>✅ ko 已修（`ko/pipeline/fix_xsampa_ipa.py`，98 行，删前验过每个词形都另有正确 IPA 兜底），并做成回归闸 **R12** ＋ 外锚闸源头侧同判据过滤 | 六门各自：① 用上面那条判据量一遍；② 有的话，**删前先验「删掉不会让词失去读音」**；③ 各自加一条回归断言锁住它（光修不建闸，下次重建库又会回来）|
| **B11** | **四门库没跑过 `ANALYZE`，查询计划器把驱动表选反** | **es / fr / it（ko 已修）** | ko 上实测**单条热查询 51 ms → 0.04 ms**，1,200 倍 | 🔴 **2026-09-25 ko 阶段 9c 的展示层契约闸跑了两分钟跑不完，量下去发现的。** 慢的是给每个关系目标取中文释义那条子查询：没有统计信息时计划器**从 `lang='zh'` 那 77,946 行扫起**，而不是从 `word_norm = ?` 的索引查起。`물` 有 263 条关系 ⇒ 一个页面 16 秒。<br>⭐ **一个索引都不用加**：`sense_gloss` 的 UNIQUE 自动索引本来就够用，计划器只是不知道 `lang='zh'` 占 51.8%（不挑剔）。🔴 我第一反应是加复合索引，加完只从 61ms 降到 48ms —— **因为计划器仍然不肯用它**。`[[enrich-perf-discipline]]`：优化前先确认「慢在哪一步」，这里的工具是 `EXPLAIN QUERY PLAN`。<br>🔴🔴 **实测八门：de / en / ja / pt 有 `sqlite_stat1`，es / fr / it / ko 没有。** 同一件事做对了四门、漏了四门，而每门自己的闸全绿 —— `[[decision-not-propagated-across-editions]]`。<br>⚠️ **统计信息会过期**：每次大批量写库（收词、生成变形）之后要重跑，幂等、几秒。<br>✅ ko 已做（`ko/pipeline/analyze_db.py`，带「提速必须真的发生」的回核：最热那条不快 5 倍就报红）| 三门各自：① 跑一次 `ANALYZE`；② **跑之前先用 `EXPLAIN QUERY PLAN` 量本门展示层最热的那几条**（照搬 ko 的 `PROBES` 写法：判据是「页面上真跑的那条」，不是随手编一条）；③ 把「大批量写库之后重跑 ANALYZE」写进各自的收词脚本尾部，否则它一次性、会过期 |
| **B5** | **六门账本六种格式，本文件自己还没有闸** | **跨门** | 六份文档 | 🔴 **挂着 —— 这是本文件的元账。**<br>① 另外五门的账**还没搬进来**：按状态符号扫得到 137 行「开着的」，但**那个数不算数**（🔴 在这些文档里大多是强调不是状态，P7 注释记过同一课）⇒ 要**逐份读**才能搬，不能靠符号匹配。<br>② 本文件**还没有自己的闸**：没有任何东西检查「每条有没有写影响哪几门 / 什么算结清」，也没有东西检查「✅ 了的是不是真的结清了」 | ① 五门的开着的账全部搬进本表；② 有一道闸逐行验本文件的必填列，并对声明结清的条目验交付物；③ 变异验证过 |

---

**当前开着：B1 · B2 · B3 · B5 · B6 · B7 · B8（🔴）／ B4（📋 已量未动）。**

> ⚠️ **B2 / B3 / B4 对 ko 的适用性**（2026-09-20 开工时逐条对过，见 `docs/KO_PLAN.md` §零）：
> **B2 不咬**（`split_colloc` 是搭配层的函数，ko 还没有搭配层，而且韩语**有**分词空格）；
> **B3 会咬**（ko 的收词规模预计是七门里最大的一次，那张清单仍然不存在）；
> **B4 会咬**（ko 的词源只有英文版有、38,796 条，一定会经过那段桥）。
> ⭐ **2026-09-24 更新：ko 没有走那段共享代码，因此 B4 在 ko 上不成立。**
> ko 的 `etymology` 表结构与共享脚本的 DDL 不同（多 `entry_id` / `src_ref`，唯一键也不同），
> 而且八门的规矩是**按语种解耦互不引用** ⇒ ko 自己写了 `ko/pipeline/build_etymology.py`，
> 桥从第一天就是 `FROM entry`。**实测窄桥会漏 1,967 条（5.1%）**，宽桥零损失。
> ⚠️ 这**不结清 B4** —— ja/it/fr/pt 四门那段代码原样躺着。ko 只是证明了「按词条而不是按义项」这条判据在第八门上也成立，且代价为零。
