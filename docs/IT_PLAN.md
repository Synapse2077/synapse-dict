# 意大利语分阶段计划

> 配套文档：结构照 `SCHEMA.md` v2（**不要重新设计**）、顺序照 `PLAYBOOK.md`、
> 动手前读 `PITFALLS.md`。本文件只解决一件 es 上始终缺失的事 ——
> **每个阶段"做到什么算完"，以及用什么判据证明它完了**。

---

## 零、为什么要有这份东西

es 花了约三周，其中**至少三分之一是返工**。返工不是因为做得不努力，而是因为
**es 从来没有「完成的定义」**：每天发现的缺陷都是真的，所以每个都值得做，于是永远做不完。

本计划对每个阶段规定三件事：

- **完成的定义** —— 一句可判定的话，满足了就是完了，不再"顺手多做一点"
- **验收判据** —— 用什么命令/数字证明它满足了，且这个判据必须**能失败**
- **明确不做** —— 这一阶段刻意不碰的东西，发现了也只记账

### 三条贯穿全程的规矩

1. **新发现默认进 backlog。** 只有「用户会看到错的内容」才允许打断当前阶段。
   `Guineo` 标成"形容词"是可打断的；某条定义有 247 个字不是。
2. **闸和数据同时做**，不是最后补。锚外部 dump 的闸永不过期，锚自己上一版的必然过期。
3. **闸写完必须变异验证** —— 故意改坏几条，闸必须报出来。一条永远通过的检查等于没检查。

---

## 一、it 的起点（2026-08-12 实测，不是印象）

| | 现状 |
|---|---|
| 库 | `data/db/synapse-dict-it.sqlite`，155 MB，**1 张表**（es 是 15 张） |
| `dict` 行数 | 584,904 |
| `ipa` | 99.9% 非空，且有 `ipa_src` 来源列 |
| `translation` | 100% 非空 |
| `definition` | 26.1%（152,392 行），多义项**用换行拼在一行里** |
| `gender` | 22.4% |
| 脚本 | `it/` 23 个（es 95 个） |

**七月已经买到手的资产**：dump 解析、音标、释义翻译、gender 裁决。
**八月 es 做的、it 一样没有**：v2 结构、义项两层、收词、例句/关系/搭配/频次层、
发音合成、闸、查询优化、展示层适配。

### 已验证的关键前提（决定工期的那个风险）

- `definition` ↔ `translation` 行数相等：**152,391 / 152,392 = 99.999%**
  （唯一例外 `in men che niente`，3 对 1，手工修）
- `definition` 行序 ↔ kaikki dump 义项序：抽 3,000 词形，**94.2% 逐字一致**；
  不一致的原因是确定性的（建库去重、变形义项过滤、子义项提升），不是乱序。
  ⚠️ 这个 5.8% 是**上界**，被度量方式放大了（拿库里所有 pos 行去对 dump 的第一个 entry）。

⇒ **行↔义项映射可确定性重建**，所以阶段 0/1 不需要重跑翻译。
（⚠️ 这条只解除了**一个**风险。整体工期见阶段表 **13–15 天** —— 决定工期的大头
不是这个映射，而是阶段 1.5 那整层缺失的意语原文释义。）

### 🔴 阶段 1.5 实测：**这一段不花钱**（2026-08-13，推翻本文对它的定性）

本文把 1.5 写成「全项目最贵的一段」「唯一花钱段」「3–4 天」。实测完全不是：

| 本文的假设 | 实测 |
|---|---|
| 要给 10 万条意语释义配中文 | 出版义项 198,049 条里 **198,047 条已经有中文**；缺的 2 条是已隐藏的 wiktextract 残渣 ⇒ **真实缺口 0** |
| 主要工作是「翻译」 | 主要工作是**灌证据 + 裁决对齐**，两步都是确定性的、免费的 |
| 意语释义 105,474 条都要处理 | 落到库里已有词形的 92,385 条；能确定性 1:1 对上的 **23,523 条已提升到出版层**；其余 60,009 条留证据层（`sense_id IS NULL`） |

⇒ **模型调用 0 次，花费 0 元。**

⚠️ 我在这份计划上已经三次估错工期，前两次都是低估。这次是**高估**，
根因相同：**凭对 es 的印象定性，没有先扫一遍数据**。
es 的 `sense_es` 要花钱是因为它当时**没有中文**；it 的中文七月就买好了。
下一门语言：**「这一段贵不贵」也必须先量再说，不许沿用上一门的定性。**

### 阶段 -1 实测带回的三条修正（2026-08-12，取数表见 `docs/lang/it-CONVENTIONS.md`）

1. 🔴 **意语原文释义的真实规模是 105,474 条，不是 714,223** —— 后者 85%（608,726 条）
   是 `plurale di X` 这类变形指针。本文原文已注明"714,223 是上界、要过滤后才是工作量"，
   现在过滤完了：阶段 1.5 的量是 es `sense_es`（173,422）的 **61%**。
   ⚠️ **工期仍按 3–4 天**，不下调 —— 这份计划已经因为凭印象改数字低估过两次。
2. 🔴 **法语版是 it 最大的跨版资产，超出原计划的预期**：意语音标 **698,250 条**
   （英文版的 6.3 倍）、录音 **13,343 条**（6.6 倍）。
   ⚠️ 2026-08-13 更正：原记 38,828 是把同一条录音的 mp3/ogg/wav 数成了三条。
   ⇒ 阶段 4 不再是"一列展开成多行"的搬运，多了一份真正的跨版裁决工作（原估 1 天偏紧）；
     阶段 6 的录音 URL 主源是 fr 版而不是意语版（意语版只有 674 条）。
3. ⭐ **阶段 1 的期望值提前有了外部锚**：库内 `definition` 按换行拆得 **198,026** 个义项，
   英文版 dump 实测 **200,160** 条真释义义项 ⇒ 已知缺口 2,134 条（1.07%，与 es 的 1.06% 同量级）。

### 🔴 一条硬约束

**行序一次性固化成 `sense` 主键，之后任何地方不许再用"第几行"引用。**

es 的教训不是"对不上"，而是**当时对上了、后续四个脚本重排了它**，752 万 tokens 算出来的
8 万条映射全部作废。it 现在的 94.2% 是**此刻**的状态，只要还留着按行号引用的写法，
它随时会再烂一次。

---

## 二、阶段表

| # | 阶段 | 工期 | 依赖 |
|---|---|---|---|
| **-2** | **准备：`paths.py` + `dbtool.py` 写库闸门** | **0.5 天** | — |
| -1 | 版本探测与取数 | 0.5–1 天 | -2 |
| 0 | 表结构迁移（30 列 → 21 列 + 十二张表） | 1.5 天 | -1 |
| 1 | 义项层迁移（英文版侧） | 1 天 | 0 |
| **1.5** | **意语原文释义层 `sense_it` + 翻译** | **3–4 天** | 0, 1 |
| 2 | 变形层归位 **+ 误判回收** | **1 天** | 0 |
| 3 | 收词（跨版）**+ 大小写拆分** | **1.5 天** | -1, 1 |
| 4 | 音标迁移（列 → `pronunciation` 表） | 1 天 | 0 |
| 5 | 例句 / 关系 / 搭配 / 频次层 | 1 天 | 1 |
| 6 | 发音：合成 + **真人录音决策** | 0.5 天 + 跑批 | 2 |
| 7 | 闸与验收 | 1 天 | 全部 |
| 8 | 展示层与性能 | 0.5 天 | 7 |
| **9** | **产品化** | **0.5 天** | 8 |

**合计 ≈ 13–15 天**（es 实际约 21 天，约 **1/1.5**）。

阶段 2 / 4 与 1 无依赖，可并行；6 的跑批是机器时间，不占人力。

> ⚠️ **本表 2026-08-12 上修过两次，两次都是我低估。**
>
> - 初版 6.5 天：漏了阶段 -1，把阶段 0 写成"建空表"、阶段 4 写成"只核对"
> - 二版 8–8.5 天：仍漏五处 —— **意语原文释义整层缺失**（最大）、写库闸门没排期、
>   产品化整节没写、`SCHEMA` §9 四条已知缺陷没排、约定宪法与真人录音没决策
> - 三版 13–15 天：**逐节对照四份文档**得出，不再凭印象
>
> 🔴 两次低估的原因相同：**凭记忆写计划，而不是照文档写**。
> 这四份文档存在的理由恰恰是记忆会漏。下一门语言（fr/pt/de/en）排期时，
> **先把 PLAYBOOK 的每一节、PITFALLS 的每一组、SCHEMA 的 §9 机械地对一遍**，
> 对不上的地方要么排期要么写明不做，不许跳过。

---

## 阶段 -2 — 准备：`paths.py` + 写库闸门（0.5 天）

⚠️ `PLAYBOOK` 1.4 的原话：**「不能推后。es 是先修了一周数据才有闸门的，
那一周的写库全部无法追溯。」** 本阶段必须在**任何一次写库之前**完成。

1. `it/paths.py` —— 本语种数据位置的**唯一真相源**，照 `es/paths.py` 抄。
   代码目录下不放任何数据字节。
2. `it/dbtool.py` —— 写库闸门。**未声明的列变了就报错**；插行必须显式声明增量。
3. `it/test_tools.py` —— 金标准测试，照 es 的 62 例结构建。

**完成的定义**
`it/paths.py`、`it/dbtool.py`、`it/test_tools.py` 三件就位；此后所有写库都过闸门。

**验收判据**
1. 故意改一个未声明的列 → `dbtool` 必须报错（**变异验证，不是跑通就算**）
2. 故意插行不声明增量 → 必须报错
3. `it/` 下 grep 确认没有绕过 `dbtool` 直接 `sqlite3.connect` 写库的路径
4. 备份保留策略生效：同 tag 同天留最新、上限 12、tag 带 `keep` 永不淘汰
   （es 上 `dbtool` 每次全量复制 697 MB，十天攒了 118 个备份、50.9 GB）

**明确不做**
不动数据；不建业务表（那是阶段 0）。

---

## 阶段 -1 — 版本探测与取数（0.5–1 天）

**这一阶段在 es 上是花过学费的，别跳。**

维基词典按**版本**组织，每个版本是"某社区用该语言写的、讲多种语言的词典"。
所以「做意语就下意语版」是**错的直觉**，`probe_editions.py` 的文档里记着实测：

> **意语版 38 MB 只有 40,090 条意语音标，而法语版里的意语有 684,198 条（17 倍）。**

⇒ 手上那个 `data/dumps/itwiktionary.jsonl.gz`（38 MB）**不能当主源**。

### 四条必须守的规矩

1. **必须实测，不能猜。** 每个语言在各版本里的排名都不一样（社区活跃度不同）。
   `kaikki.org/<xx>wiktionary/` 索引页直接列出该版本覆盖的每个语种及义项数，免费、不下载。
2. **下切片，别下整包。** 非英文版也有 per-language 切片（rawdata 页说只有整包，那是不全的）。
   法语版五个目标语言切片合计 229 MB vs 整包 676 MB，**省 66%**；
   中文版整包下过 215 MB，其实西语切片 10.3 MB 就够。
3. **语种名是用该版本自己的语言写的**（Spanish / Español / Espagnol / Spaans / 西班牙語），
   靠名称模式识别；识别不到时脚本会列出该版本全部语种供人工确认，不假装万无一失。
4. **同一版本可能有多个本地名**（中文版的「西班牙語」和「西班牙语」是两个独立切片，都得取）。

### 版本的分工不是"谁更全"，而是按字段分

- **英文版 = 结构基准，不是内容上限**（六语种里只有 en 自己排第一）
- **词形、录音可以取并集**；**音标、释义、语法要裁决**，不能无脑合并
- **释义只保留三语**（中文 + 英文 + 意语）。de/fr/pt/es 版的释义**不收** ——
  实测每个维基版只给"自己语言的词"写定义，对外语词只写翻译

**完成的定义**
产出一张《it 版本取数表》：用了哪几个版本、每个版本取哪个切片、各贡献什么字段、
文件落在哪、各多大；并按此下载完毕。

**验收判据**
1. `python3 scripts/probe_editions.py --lang it` 的完整输出存档，
   排名前 5 的版本及其意语义项数逐个列出（**这是选择的依据，不是事后追认**）
2. 每个下载的切片：行数 + 意语条目数实测，与索引页声称的数字比对
3. 明确写下每个版本**负责哪些字段**，以及取并集还是要裁决

**明确不做**
不下整包（除非该版本确实没有切片）；不收 de/fr/pt/es 版的释义；
这一阶段不解析、不入库，只取数和记账。

---

## 阶段 0 — 表结构迁移（1.5 天）

**这不是"建空表"，是完整的结构重构。** es 上这一步是 08-07 那次返工的核心，
`SCHEMA.md` 已经把结论写死了 —— **照着做，不要重新设计一遍**。

三件事：

1. **`dict` 表从 30 列降到 21 列**（`SCHEMA.md` §3）。搬走的列各有去处，不是删。
2. **建十二张表**（§2），按 §5 的**迁移顺序**做，每一步过 §5.0 的**三道闸**。
3. **把"库内第 N 行 ↔ kaikki 第 M 个义项"固化成 `sense` 主键**，不留行号引用。

### 🔴 §2.0.1 是 v2 最硬的一条理由：映射不能存成行号

es 把 8 万条对齐映射存成"definition 第几行"，而四个脚本会重排它 ——
752 万 tokens 算出来的东西全部作废。it 现在的 94.2% 顺序一致是**此刻**的状态，
只要还留着按行号引用的写法，它随时会再烂一次。

**完成的定义**
`dict` 已降到 21 列；十二张表全部建成且结构与 `SCHEMA.md` v2 逐列一致；
每条 definition 行都有一个 `sense.id`，全库不存在任何"第几行"式引用。

**验收判据**
1. 表结构与 `SCHEMA.md` v2 **逐表逐列**比对，差异为 0
2. **可逆性回核**：从新表重建原始 `definition` / `translation` 两列，
   与迁移前**逐字节双向**比对，100% 一致（非抽样 —— 这是"有没有错配"能否判定的关键）
3. 从 `dict` 搬走的每一列，在新表里逐行核对搬运无损
4. 全库 grep 确认没有残留的行号引用写法
5. 变异验证：打乱 3 条映射 + 改坏 2 条搬运结果，五条必须全被报出

**明确不做**
不改任何列的**值**（只搬不改）；不去修那 1 条行数不等的记录（留到阶段 1）；不碰展示层。

---

## 阶段 1 — 义项层迁移（1 天）

`definition` / `translation` 按行拆进 `sense` + `sense_gloss` + `sense_src`。
`sense_src` 是证据层，每条锚回 dump。

**完成的定义**
152,392 行的每一条 definition 行都成为一条 `sense`，中英文进 `sense_gloss`，
dump 原文进 `sense_src`；`in men che niente` 那条已处理。

**验收判据**
1. `sense` 条数 == 全部 definition 行数之和（先算出期望值，**差一条都要查到底**）
2. `sense_src` 逐条能在 it dump 中找到原文（锚外部 dump 的闸，永不过期）
3. 可逆性回核同阶段 0 第 2 条，仍需 100% 逐字节
4. 变异验证：改坏 5 条 `sense_gloss`，闸必须全报出

**明确不做**
- **不做自动判重合并**。es 上 186 条判重里 12.4% 是义项错配，`PLAYBOOK` 5.6 已判定失败。
- 不为"某条释义质量不好"停下来重译，记账。

---

## 阶段 1.5 — 意语原文释义层 `sense_it` + 翻译（3–4 天）

🔴 **这是初版计划整个漏掉的一层，也是全项目最贵的一段**（`PLAYBOOK` 第五节：
「义项与释义，一周，最贵的一段」）。

`it.dict` 的 `definition` 全是**英文**（`fare` → "to do / to make"），来自英文版。
**意语版自己写的释义，库里一条都没有。** es 的对应物是 `sense_es`（173,422 条、100% 有中文）。

2026-08-12 实测 `itwiktionary.jsonl.gz`：

```
总行数         799,600
lang_code=it   560,357
带释义的词条   559,690
意语原文义项   714,223   ← it 库里为 0
```

⚠️ 714,223 是**上界**，混着 `plurale di X` 这类变形指针，要按 es 的做法过滤后才是真实工作量。

### 五条纪律（`PLAYBOOK` 5.1–5.6，每条都是踩出来的）

1. **结构三层，一次到位** —— 证据层 / 出版层 / 释义层，别先做两层回头再拆
2. ⭐ **翻译与对齐一次调用产出** —— 多吐 `en_i` 下标零成本。
   es 实测 46 tokens/词 + 50 tokens/义项
3. **模板在模型前面** —— 变形描述这类能确定性生成的，绝不送模型
4. **释义只保留三语** —— 中文 + 英文 + 意大利语。其余版本的释义不收
5. 🔴 **不做自动判重合并** —— es 上 186 条判重里 12.4% 是义项错配，已判定失败

### payload 四条纪律（5.5）

交叉引用要喂被引词的中文且限定条数／meta 标签要说明否则会被写进释义／
句末标点／wiktextract 残渣要清。

**完成的定义**
`sense_it` 建成并 100% 有中文；每条能锚回 `itwiktionary` 原文；
与英文版侧的义项完成对齐（同一义项的两个来源并成一条，不重复显示）。

**验收判据**
1. 条数 == 过滤后的期望值，**差一条查到底**
2. 逐条锚回 dump（外部闸，永不过期）
3. 对齐质量：抽样盲测（抽掉证据、对另一版真值），bad 率报出
4. **判官用前必跑负控** —— 负控不过就换判官，不是形式
5. 变异验证：改坏 5 条对齐关系，闸必须全报出

**明确不做**
不做判重合并；不为个别释义质量停下重译（记账）；不收 en/it 以外版本的释义。

---

## 阶段 2 — 变形层归位 + 误判回收（1 天）

432,512 行"有 translation 无 definition"的变形形（`pio 的 复数`、
`abalienare 的 陈述式现在时第二人称复数`）归入变形层。

**完成的定义**
所有变形行都带有指向 lemma 的指针，且指针目标全部存在。

**验收判据**
1. 变形指针 100% 指向库内存在的 lemma，悬空数为 0
2. 变形描述文本与 kaikki 的 `form_of` 确定性比对，冲突率报出上界

### 🔴 同时处理 `SCHEMA` §9 第 1 条：被误判成变形层的词

es 有 **5,701 个词被误判成变形层**：`levantarse`（起床，词典里是独立词条）标成
"levantar 的 不定式·自复"、`Méjico`（异体拼写）标成 "México 的 阳性"。
后果是**界面上一条释义都没有**，而且本语种版给它们的 8,506 条义项因此收不进来。

根因是 `build.py` 把 **`alt_of` 当成了 `form_of`**。it 用同一套解析逻辑，**必然复现**。

⇒ 本阶段必须查一遍 it 有多少个，并把它们移回词条层。

**验收判据（补）**
5. `alt_of` 与 `form_of` 分开处理，抽 50 条人工核对
6. 移回词条层的词，界面上必须有释义（**查落点，不是查列**）

**明确不做**
不用 LLM 生成变形描述（模板确定性生成即可，es 已验证）；
悬空指针不删行——那是源头真缺词头，记账。

---

## 阶段 3 — 收词（跨版，1 天）

从英/法/德/葡各版 dump 里收割意语词条。

**完成的定义**
`verify_vs_dump.py` 的 it 版本跑完，缺口清单产出且每一类都有归属（收了/判为变形/记账）。

**验收判据**
1. 缺口按**义项级**统计，不是词形级（es 就是漏了这一层，`derived` 少收 20,193 条）
2. 收词前后核对**预期增量 vs 实际增量**，差一条查到底
3. 堵住自我背书：新收的词不得只由自己的收录动作确认

### 🔴 同时处理 `SCHEMA` §9 第 2 条：小写专名与普通名词压成一行

es 有 **1,899 个**：`virgo`（处女）/`Virgo`（处女座）、`be`（字母 B）/`Be`（铍）、
`chile`/`Chile`。必须**建新行**拆开，并改 `exactQuery` 把**精确大小写**加成第一排序键
——否则搜小写会命中大写专名（es 至今如此：搜 `gracias` 排第一的是洪都拉斯的城镇 `Gracias`）。

**验收判据（补）**
4. 拆分后，搜小写词首条必须是小写词本身，抽 200 个高频词实测
5. `ingest` 路径确认没有大小写折叠去重（es 因此吃掉 573 个大写专名词头）

**明确不做**
不做大小写折叠去重；不改已有行的值。

---

## 阶段 4 — 音标迁移：`ipa` 列 → `pronunciation` 表（1 天）

**这不只是核对。** it 现在的音标是 `dict` 上的**一个列**，而 v2 里音标是一张独立的表
（es 的 `pronunciation` 有 386,557 行）。拆表的起因是 `SCHEMA.md` §1.3 记的那条缺陷 ——
**一个列只能存一个值，音标的变体正在被丢掉**（半岛音/拉美音、宽式/严式、多重读法）。

it 的 `ipa` 99.9% 覆盖、且有 `ipa_src` 来源列，所以**内容不用重做**，
要做的是把一列展开成多行，并在展开过程中把源头本来就有、却被列结构挤掉的变体找回来。

**完成的定义**
`pronunciation` 表建成；每条音标带 `notation`（宽式/严式）与来源；
`dict.ipa` 单列里丢失的变体已从 dump 找回并落表。

**验收判据**
1. **回源计数**：直接扫 dump 数出意语条目应有的音标条数，与落表条数比对，
   缺口按**条**报出上界（不是按词形 —— es 就是漏了这层，`derived` 少收 20,193 条）
2. 可逆性回核：从 `pronunciation` 表能重建出原 `dict.ipa` 列，逐字节一致
3. `ipa_src` 为空的行数为 0（证明不了来源就写 `unknown`，**不猜**）
4. normalize 往返：`normalize(normalize(x)) == normalize(x)`，全量
5. 存裸、展示层加 `/.../`（六语种统一约定）
6. 变异验证：删 3 条音标 + 改 2 条 notation，闸必须全报出

**明确不做**
- **不用 G2P 重算**（es 上 `eigenvector → eixembeɡˈtoɾ`）
- 判据只能是"回权威源查不到 **且** 违反音系"，不能是"看着不像意语"
- 变形层不是风险面，别去大改（es 实测变形层标出率比 kaikki 原生还低）

### ✅ 完成（2026-08-17）

`pipeline/build_pronunciation_layer.py` **1,205,807 行 / 1,102,055 个词形**；
`dict.ipa` 一个字节没动（列的去留留给阶段 8）。六条判据全绿、变异验证 6/6 + 判据变异 18/18。

| | |
|---|---|
| 回源计数（判据 1，按**条**不按词形） | 三版并集 772,085 条 ⇒ **被单列结构挤掉 183,805 条**；真·一词多读 22,627 个词形 |
| 落表后 | 99,441 个词形有 ≥2 条读音；挂上 entry 的 240,549 行；严式 826 行 |
| `ipa_src` 回填（判据 3） | 588,280 行全有值：`rule:accent` 269,303 / `rule:plain` 161,288 / `en` 84,160 / `fr` 62,048 / `it` 8,337 / **`unknown` 3,053**（证明不了不猜）/ `llm:doubao` 91 |
| 🔴 最大发现 | **73.4% 的 `dict.ipa` 是我们自己 G2P 算的**，dump 背书的只有 26% —— 计划里"内容不用重做"这句只对了一半 |

**四条新约定**（`it-CONVENTIONS` A52–A58，都是踩出来或量出来的）：
两把尺子分开（存/比）、比对时折什么不折什么、`is_primary` 让 fr 版**只在 G2P 盲点上**有覆盖权、
`src` 记「谁背书了这个读音」而非字节出处、`entry_id` 的 wildcard 语义、`region` 一律 NULL。

⚠️ **留给阶段 8 的一个决定**：`is_primary` 与 `dict.ipa` 不是同一条的有 **23,004 个词形**
（fr 16,620 / it 6,315 / en 69）—— 切展示层时用户会看到读音变化，多数是修对了
（`Gabon` ˈɡa.bon → ɡaˈbɔn），但这批要不要一次性切，是产品决定。

### 🔴 2026-08-18 补：两个数被回源推翻，档位已改

1. **fr 版的重音不可信**（`it-CONVENTIONS` A54 已改）：以 en/it 版为真值，
   `rule:accent` 83.9% > fr 64.5% > `rule:plain` 44.8%。fr 版的意语变形形音标
   本身就是按倒二默认机器生成的，与我们的光杆路径同病。
   ⇒ `rule:accent` 提到 fr 之前，`--restamp` 重标 38,060 行；受影响面 42,038 → 23,004。
2. **396,901 个词形（26.5%）没有任何读音**，且不能用 G2P 补：
   以 dump 为真值实测，带重音形路径 **57.2%**、光杆路径 **30.4%**
   （`-issimo` 族 0.6%、`-rebbero` 43.4%）。两家顾问独立给出同一结论：
   **两条路径都不够格进正文，留空**。真正该做的是**形态派生**
   （从原形的可靠读音 + 词尾规则推），那是另一件事，见记账本。

---

## 阶段 5 — 例句 / 关系 / 搭配 / 频次层（1 天）

**完成的定义**
四张层表建成，各自的来源可追溯。

**验收判据**
1. 例句**只认 kaikki**，Tatoeba / OPUS 一律排除（用户 2026-08-03 方针）
2. 每张表抽一条回源核对到原始 dump 行
3. 频次用 `wordfreq`，判据必须是 `tokenize(w) == [w.lower()]`
   （否则词缀被静默剥掉再查，`a-` 会返回 7.36）

**明确不做**
例句不做翻译质量评审；搭配"够用即可"，不追求全。

### ✅ 完成（2026-08-18）

| 层 | 结果 | 脚本 |
|---|---|---|
| **例句** | **38,144 条 / 18,8xx 个词形**，其中约 1.5 万条挂到具体义项；**中文 38,135 条（100.0%）**、英文 9,547 条 | `pipeline/ingest_examples.py` + `translate_examples.py` |
| **关系** | **606,524 条**语义关系（同/反/上下位/同级/部分-整体），27,993 条挂到义项；原有 7,995 条 `alt_of` 一条没动 | `pipeline/ingest_relations.py` |
| **搭配** | 沿用 23,877 条（七月 LLM 产出 + 阶段 1.5 的 it 版子条目），全部有中文；来源可查 `collocation_gloss.src` | — |
| **频次** | `dict.freq_zipf` 写 **1,465,292 行**；33,664 个词形**量不了**留 NULL | `pipeline/build_freq_layer.py` |

闸：例句 11 + 关系 8 + 频次 4 + 译文 6 + 清理 3 条全绿，各自带外锚（回三版 dump 双向核对）与变异验证。

### 例句中文（2026-08-18）

**38,135 条中文，覆盖 100.0%**，flash 关思考，约 780 万 tokens（我事前估 150 万，**低估 5 倍**）。
先跑 120 条 × **三臂消融**自己逐条读，再全量：

| 臂 | 喂什么 | 结果 |
|---|---|---|
| **A（采用）** | 只喂意语原文 | 答出 120/120，token 最省 |
| B | 原文 + 词头 | 118/120 |
| C | 原文 + 词头 + 英文译文 | 119/120，但**把英文译错成了另一句**（`convalligiano` 的中文来自 en 字段，与意语原文无关）|

⇒ 实测印证 `context-you-give-leaks-into-output`：**多喂的上下文会直接变成输出**。

⚠️ 两个只有跑起来才暴露的问题：
1. **模型会静默丢批里的一部分**（不报错，回的 JSON 里就没那几个 id）。
   一轮下来 2,069 条从没被答过，且每轮丢的是同一批词。多轮补跑收敛
   2,069 → 273 → 139 → 83 → 64 → 26；批调小（`EX_BATCH=2`）才彻底收干净。
2. **59 条「根本不是例句」是靠模型弃权才发现的**（`Coordinate term: stazione` /
   `(please add an English translation…)` / `<>` / `:Modèle:€xemple`）。
   ⇒ 判据补进收词脚本，并**删行而不重建表** —— 此刻重建会重新分配 `example.id`，
   而 3.8 万条中文正挂在这些 id 上（`model-answer-files-key-by-id` 那个事故的形状）。

**三个当场逮住的坑**
1. `sense_src.src_ref` 的格式**三版不一样**（en 带词源号与 seq，it/fr 不带）。
   我按 en 的格式拼给三版都用 ⇒ it/fr 的 **28,771 条例句全部"挂不上"**，
   而真实原因只是我的键长得不一样。⇒ 改成**解析库里的 ref**，不自己拼字符串。
2. fr 版 **884 条例句把法语译文塞在意语原文里**（`Aspetta un attimo. — Attends une minute.`），
   且 `translation` 字段是空的 ⇒ 无法确定性切开，整条跳过（A3 + 错比缺更伤）。
   ⚠️ 不用「按破折号切一刀」——意语句子本来就用破折号。
3. **例句表里混着「根本不是例句」的东西**（构词公式/元指引/书目当正文，489 条）——
   是**翻译试跑时**才发现的：模型翻出一堆莫名其妙的东西，回头看原文就不对。
   ⇒ 判据加进收词脚本重建（A64）。判据不能是「含有 →」，那会误删真句子。
4. **闸的判据必须与写入用同一份证据**：写入时看源头的 `translation` 字段，
   落库后它分到了两个地方（出版语言进 `example_gloss`、其余进 `src_translation`），
   闸只看后者 ⇒ 把 85 条带英文译文的 en 版引文误报成"混着外语"。

---

## 阶段 6 — 发音合成（0.5 天 + 跑批）

**完成的定义**
主力音全量生成，哈希契约两端一致，文件零缺失零字节。

**验收判据**
1. **哈希契约 Python 与 TS 各写一遍并逐条比对**（差一个字节全库静默降级）
2. 抽 100 个含重音符/特殊字符的词，两端哈希 100% 一致
3. 生成文件数 == 期望数，零字节文件为 0，HTTP 200 且 MIME 正确
4. 挑词判据用 `freq_zipf`，**不要用 `level`**（es 上 `level` 给语法词全 NULL，
   导致 `essere`/`avere` 这类最常用词一个音都没有）

### 真人录音：这是一个**决策**，不是默认丢弃

录音 URL 在语言版 dump 里是**白送的**（`mp3_url` 直接带），de 覆盖 76%、fr lemma 67.7%。
贵的只是下载：`upload.wikimedia.org` 是读者 CDN、**有意限流**，es 实测 8 并发只到
0.6 条/秒、220 次重试、25 条 429，全量约 6–9 小时（落盘仅 276 MB）。

⇒ **先把 URL 收进 `audio` 表**（几乎零成本），下不下载单独决策。
⚠️ 跨版收割必须**按语言码过滤**，es 曾因按词形匹配混进 63 条非西语录音。

**明确不做**
不在本阶段下载录音文件（URL 先入库）；不做 TTS 替代真人录音的质量评比。

### ✅ 6a 完成（2026-08-18）：录音 URL 已入库

`pipeline/ingest_audio_urls.py` → `audio` **12,188 条 / 9,404 个词形**（占全部词形 0.6%、
词头 5.5%）。来源 fr 11,131 / en 766 / it 291 —— 再次印证 A13：**录音主源是法语版**。
认出录音人 10,961 条（Lingua Libre 命名）、带地区限定 9,613 条。
闸 8 条全绿（含**双向**外锚：dump 有的表里都有、表里有的 dump 里都有），变异验证 4/4。

⚠️ 同一条录音的 `mp3/ogg/wav` 落在**同一行的三列**，不是三行 ——
记账本里那条「录音条数虚高 2–3 倍」的成因就是把它数成三条。

**下载与否仍未决**：约 12,188 个文件，`upload.wikimedia.org` 有意限流（es 实测 6–9 小时）。
URL 在库里，随时可下；不下载也不影响展示（可直接放 Commons 直链播放）。

### ⏸ 6b 合成音（2026-08-18 **搁置**，产物已删）

`it/pipeline/gen_tts.py`（从 es 抄过来改的，**不 import**，六语种解耦）：
取词判据 `freq_zipf > 0` ⇒ **197,445 个词形**，约 2.4 GB。

🔴 **音色选 medium 不是 high，依据是实测**：同 60 词、同机器、各自预热后计时 ——

| 音色 | 速度 | 全量耗时 |
|---|---|---|
| `it_IT-serena-high` | 4.6 词/秒 | **12 小时** |
| `it_IT-paola-medium` | **22.2 词/秒** | **2.5 小时**（4.8 倍） |

产出体积两者相当（1.7 vs 1.9 MB / 60 词）。词典念的是**孤立单词**，high 档的收益
主要在长句韵律 ⇒ 拿 4.8 倍机器时间换它不划算。
⚠️ 两个音色的文件路径**一样**（`sha1(词|it)`），换音色必须先清空旧文件，
否则库里混着两个人的声音、而且从路径上看不出来（已清）。

**哈希契约两端已对齐**（判据 1）：`packages/dict-core/src/italian.ts` 的 `ttsFor()`
与 `gen_tts.py` 的 `digest()` 各写一遍，`it/probes/tts_contract.ts` 拿清单**逐行**比对
（不是抽样），变异验证 5/5（音色标签/大小写/重音符/撇号写法都参与哈希）。
`/api/audio/it/...` 路由本来就是按语种通配的，`data/tts/it` 一存在就自动生效。

#### 🔴 为什么搁置：**用户试听后判定质量不够**（2026-08-18）

跑到 4,764 个（2.4%）时暂停试听，用户听完的结论是「质量不是蛮好」⇒ **停跑、删除产物**
（`data/tts/it` 4,764 个文件 / 49 MB 已删），这一层往后放。

**留下的东西**（都很小，重启时直接用）：
- `data/work/it/audio_ab/compare.html`（3.5 MB）—— 同一批 30 个词、**五种合成 + 真人 + 浏览器**并排试听页，
  分组是按「考什么」挑的：双辅音 `babbo/anno/cappello`、末音节重音 `città/perché`、
  `gl/gn/sc` 组合、外来词、长变位形、以及**同形异读** `ancora/subito/principi`
- 五个候选各生成好的 30 个样本：`medium_10` / `medium_12`（放慢）/ `high_10` / `say_alice` / `say_eddy`
- Piper 音色文件 `data/tts/voices/it_IT-*.onnx`（178 MB）

**重启这件事时，先看这三条已经量清的东西**：
1. **速度**：`paola-medium` 22.2 词/秒（全量 2.5 h）vs `serena-high` 4.6 词/秒（12 h），体积相当
2. **语速可能才是"糊"的原因**：孤立单词按整句语速念会把辅音和重音糊在一起，
   `length_scale 1.2` 的那一档就是为验这个准备的
3. **同形异读（`ancora` 锚/还）任何 TTS 都会错一半** —— 这不是音色问题，
   解法是把 `pronunciation` 表里的重音喂给合成器，而不是让它猜
4. ⚠️ **macOS `say` 的音色（Alice/Eddy）本机用没问题，但把生成的音频当产品分发是另一个授权问题**；
   Piper 是开源模型，分发没这个顾虑

**没有白做的部分（已经在库里/代码里，不随产物删除）**：
`pipeline/gen_tts.py`（可续跑、带验收闸与变异验证）、
`packages/dict-core/src/italian.ts` 的 `ttsFor()` 与 `entry.tts` 字段、
`it/probes/tts_contract.ts`（哈希契约逐行比对，变异验证 5/5）、
以及 `/api/audio/it` 路由（按语种通配，目录一在就生效）。

---

## 阶段 7 — 闸与验收（1 天）

**完成的定义**
三类闸齐备并挂进 `it/dbtool.py` 写库闸门；回归闸带**已接受基线 + 理由**。

**验收判据**
1. 三类闸：①可逆性回核 ②不变量断言 ③锚 dump 的外部闸，缺一不可
2. 每张出版层表至少一道锚在 dump 上的闸
3. **同一判据在写入列和读取列各查一次** —— es 的 `pronunciation` 表就是
   换了读取路径绕过整轮音标修复，查原列永远绿而用户看到的是错的
4. 全部闸跑变异验证，逐条确认能失败
5. 回归闸必须带已接受基线和理由，否则永远红、没人看

**明确不做**
抽样只用于确定性验证做不到的部分；判官（LLM）只在确定性方法不可行时用，
且用前必须跑负控。

### ✅ 完成（2026-08-18）

**回归闸 `it/tests/test_no_regression.py` 建成并挂进 `dbtool.session`**：15 族断言、
每族**在「修复写入的地方」和「App 实际读取的地方」各查一次**，变异验证 **10/10**
（含专门的一条：写入列干净、读取路径有货 —— es 那次事故的形状）。

判据**全部 import 自各修复脚本自己的定义**，不另写一套。跑一遍 25 秒。

#### 🔴 建闸当天就逮到 4 类真问题

| | 发现 | 处理 |
|---|---|---|
| **被绕过** | `dict.ipa` 里只有 1 条拉丁 `g`，而 `pronunciation` 读取侧有 **404 条** —— it/fr 版把浊软腭塞音写成 U+0067，与本库的 ɡ（U+0261）不是一个码位 | `fixes/normalize_ipa_latin_g.py` 归一 884 行、删同码位重复 149 行；**并改 `norm_ipa` 在入口归一**，否则下次重跑 dump 又灌回来 |
| 连锁 | 归一之后又冒出 **317 组**「折排版后是同一个读音」的重复行 | `fixes/dedupe_pronunciation_after_g.py`：**锚点行永远留**（否则可逆性断），其余按来源优先级 |
| 出版层残渣 | 22 条 `See X.` 交叉引用仍可见 | 藏 10 条；**3 条留着**（藏了这个词就一片空白）写进基线 |
| 假中文 | 3 条中文与词形逐字相同（`pbsl`/`chaperon`/`oi!`）—— 模型没答上来把原词抄了回来 | 删掉，留诚实空白 |

#### 🔴 旧闸的红：**全部是断言过期，不是数据错**（A33 的三分法）

一共 20 条红，逐条查完没有一条是数据问题，全是**当初写死了行数或写死了单一来源**：

- `build_entry_layer` / `build_inflection_layer` / `intake_fr_words`：期望值是**从英文版 dump
  现场重推**的，而表里后来合法地进了 fr 版 18 万条、it 版若干 ⇒ 两侧都限定同一来源
- `merge_apostrophe_variants`：四条写死总行数的断言换成**结构性口径**
  （「没有 word_id 指向不存在的 dict 行」），行数会长、结构不会
- `ingest_it_edition`：比对键里带 `word_id`，而 `reroute_subentry_defs` 有意改挂过 ⇒ 只比文本不比归属

⚠️ **三条已接受基线，都写了理由**（没有基线的闸永远红、久了没人看）：
`fill_from_en_edition` 补的 5 个词、`fix_surgical_suffix` 手工判定为正确的 3 条、
改挂子条目的 377/249 条。**每条都写明「数字变大就要查」**。

#### ⚠️ 顺带发现、已记账未决

`inflection.entry_id` **有两套语义**：724,582 条指向原形的 entry、510,110 条指向变形形
自己的 entry，两族各自内部一致、没有第三种。`SCHEMA` §10.3 没规定是哪一个。
展示层目前不读这一列 ⇒ 用户看不到，排阶段 8 定语义后再补断言。

---

## 阶段 8 — 展示层与性能（0.5 天）

**完成的定义**
it 词条页读新结构；F 组性能判据在**数据补齐之后**重跑通过。

**验收判据**
1. 展示层映射表全部走 `@synapse-dict/dict-labels`，**不在 `App.tsx` 里新增任何映射**
2. 索引与 `COLLATE` 一致（写了 `COLLATE NOCASE` 的列，索引也必须是 NOCASE，
   否则静默全表扫，es 的 `mano` 词条页跑了 6.3 秒）
3. 新建索引后**重启服务**（对已预编译语句无效）
4. 常用词词条页 P95 < 200ms，在**全量数据**下测

**明确不做**
不在这一阶段做版面设计打磨；发现的排版问题记账。

### ✅ 完成（2026-08-18）

词条页接上了阶段 4/5/6a 落库、**界面上一条都没显示过**的四层数据。

| 接上的 | 规模 | 用户看到的变化 |
|---|---|---|
| 读音（`pronunciation` 表取代 `dict.ipa` 列）| 120 万行 / 110 万词形 | **513,775 个词形第一次有音标**（列只填过 588,280 个）；22,908 个词形显示的读音变了；99,048 个多读音词形并排显示其余读法 |
| 真人录音（`audio`）| 12,188 条 / 9,404 词形 | 方针④第一级第一次露面，排在合成音之前 |
| 例句（`example` + `example_gloss`）| 38,144 条，**中文 100%** | 39.0% 挂在义项下、其余成块 |
| 语义关系（`sense_relation`）| 614,519 条 / 68,167 词形 | 近义/反义/上下位，分类封顶 12 条并给总数 |

#### 🔴 真接上去才看见的三个缺陷（闸全是绿的）

| | 发现 | 处理 |
|---|---|---|
| 音节切分残渣 | 意语版把 `su-da-né-se` 的**每个音节**当成一条 `sounds` 给出来 —— **228 行，其中 116 行是 `is_primary`**，切完展示层用户在 `sudanese` 页面上看到的音标会是 `/su/` | `fixes/drop_hyphenation_fragments.py`，判据三条（无 IPA 专有符号 + 是词形拼写的子串 + 词形长 ≥3 个字母）。第三条不是凑数：没有它会误伤 `in-`→`in`、`bi-`→`bi` 这类前缀词条的真读音 |
| 撇号冒充重音符 | fr/it 版把 `ˈ`（U+02C8）写成键盘上的 `'`，**7,094 行**。`pesca` 一个词并排显示出四条读音，其中两条只是记法不同 | `fixes/normalize_ipa_stress_mark.py` 改写 4,750 行、删同读音重复 2,344 行；**并改 `norm_ipa` 在入口归一**。⚠️ 顺序是判据：加在「折 `.ˈ`」之后会出来 `a.ˈba.te`，与 en 版又成假变体（A71）|
| 展示层折读音的尺子 | `ˈka.ne`(en) 与 `ˈkaːne`(it)、`anˈkora`(en) 与 `aŋˈkora`(it) 是同一读音的两种记法，不折就并排显示成多条 | 第三把尺子 `readingKey`：只折长音符与软腭前的 `ŋ`（意语里都不可能区别词义），**重音符与 s/z 不折**（A70）|

#### 验收判据的实际输出

```
判据 1  展示层映射表全走 dict-labels        ✅ 新增 IT_AUDIO_PLACE_LABELS + itAudioRegion()
                                            App.tsx 未新增任何映射表
判据 2  索引与 COLLATE 一致                 ✅ 五条新查询逐条 EXPLAIN QUERY PLAN，全部 SEARCH … USING INDEX
                                            🔴 audio/example 按 word 查**故意不写 COLLATE NOCASE**
                                               —— 索引是 BINARY，加了就退化成全表扫
判据 3  新建索引后重启服务                   ✅ 本阶段没新建索引（现有五条索引全部命中）
判据 4  常用词词条页 P95 < 200 ms           ✅ probes/entry_perf.ts：最差一组 P95 = 2.9 ms
                                            高频词 1.7 / 多义项 2.5 / 多关系 2.9 / 多例句 2.1 / 多变形 2.8
                                            端到端（API，含 JSON 序列化）3–9 ms，单条 5–11 KB
```

展示层契约闸 `apps/web/src/contract-check.tsx` 加 6 条检查（读音/例句/录音行/地区映射/
关系截断说总数/关系分类映射），**全量渲染 20,733 个词条全绿**，变异 10/10。

⚠️ 三个只有**渲染出来读一遍**才发现的（闸都不报，因为它们不是"错"是"噪声"）：
① `ancora` 并排显示三条读音 —— `ŋ` 的前瞻正则漏了中间的重音符（`aŋˈkora` 的 ŋ 后面
隔着一个 `ˈ` 才是 k），一条都没折到 ② `pesca` 显示四条 —— en 版两条音位式与 fr 版
同样两条严式文字一模一样，去重键里带了 `notation` ⇒ 同样两个读音显示两遍
③ 关系目标的"点不点得动"只查了 `word` 列，而 `getEntry` 落空会走归一列
⇒ `amico dell’uomo`（源头弯撇号、库里直撇号）被标成点不动、其实点得开。

#### 🔴 契约闸的变异验证本身是坏的，一并重写了

原来的变异全是「改数据」，而这些检查问的是「**组件有没有把这个字段渲染出来**」——
改完数据、组件照样忠实渲染，检查当然不红。也就是说那几条变异**在构造上就不可能成立**，
给出的绿灯是假的。⇒ 改成两类：`data` 改数据（只验只看数据的检查）、
`html` **先正常渲染再把渲染结果打坏**（这才是"组件漏写了一行"的真实形状）。

#### 两个悬着的决定，本阶段定死

- **`dict.ipa` 列不删**，冻结成迁移锚点（A73）：展示层已不读它，但它是唯一能让这次迁移
  **可证伪**的东西 —— 闸①从表逐字节重建这一列比对，删了列这条闸就没了；
  回归闸的「写入侧 vs 读取侧」两条路径里，写入侧就是它。
- **`inflection.entry_id` = 原形的那个 entry**（A72，`SCHEMA` §10.3 已改）：
  原来按来源分裂成两套语义，展示层不读它 ⇒ 查库看不出异常。
  确定性重指 **499,966 条**、清空 24,514 条（97.75% 可定，其余留 NULL 不猜），
  断言进回归闸 **C8**。

回归闸从 15 族涨到 18 族（A4 撇号、A5 音节残渣、C8 变形层语义），变异 **13/13**。

⚠️ **删库不挡入口 = 白删**：两条归一（撇号、音节残渣）都必须同时改 `ipa_variants` 的
收词入口，否则下次重扫 dump 又灌回来，而**外锚闸⑤会一直红着报这 228 条"漏收"**
（`replay-scripts-undo-fixes`；判据本体因此搬进 `ipa_variants.is_fragment`，
修复脚本与回归闸 A5 都 import 那一份）。
⚠️ 动完 `norm_ipa` 必须重跑 `fixes/dedupe_pronunciation_after_g.py` ——
折 `.ˈ` 的范围变了，又冒出 **1,140 组**同读音重复行。

#### 🔴 外锚闸逮到一条我自己删错的读音（顺序也是判据）

两条修复当天的执行顺序是「先删残渣、后归一撇号」，而英文版给 `assistente sociale` 的是
一条**定界符写坏**的音标 `"/assi'sten'te/socia'le"`。删残渣的时候撇号还没归一，
`assi'sten'te` 在判据眼里就是**纯拉丁字母**、又正好是词形拼写的子串 ⇒ 被当成音节残渣删了。
归一之后它是 `assiˈstenˈte`，带两个 `ˈ`，判据一眼放行 —— **同一个判据、同一条数据，
只因为跑的顺序不同，结论相反**。

🔴 **库内不变量、回归闸、展示层契约闸当时全是绿的** —— 少一条读音在库里长得跟
「源头本来就没有」一模一样。逮到它的是 `build_pronunciation_layer` 的**外锚闸⑤**
（dump 有、表里没有），228 → 1 → 0。这正是 `external-anchor-gates` 说的：
锚外部 dump 的闸永不过期，锚自己上一版的必然过期。
⇒ `fixes/restore_stress_alias_reading.py` 补回，并写死「先归一、后判残渣」。

#### 记进 backlog 的新发现

- 47 条例句正文是法语/拉丁/古法语（0.12%，多为词源引文）
- 2,097 条变形行的原形在 `entry` 表里没有任何词条
- 读音还没挂到义项组上（`ancora` 名词义项旁显示的是副词读音；1,944 个同形异读词形）
- fr 版 160,874 个空壳词形：**决定仍然不收**（收进来就是 16 万个点进去空白的词条）

---

## 三、每阶段的交接动作

一个阶段做完，我给你一张交接单，只包含三样：

1. **完成定义是否满足** —— 是/否，不含"基本完成"
2. **验收判据的实际输出** —— 命令 + 数字，含变异验证结果
3. **本阶段记进 backlog 的新发现** —— 清单，不展开

你点头才进下一阶段。中途发现的东西一律进 backlog，除非它属于「用户会看到错的内容」。

---

## 阶段 9 — 产品化（0.5 天）

`PLAYBOOK` 第九节。**数据做完 ≠ 能对外用**，以下每条都是 es 上补过的：

- **统一 JSON 错误出口** —— 原来 `?limit=abc` 回 HTTP 500 + 完整调用栈和服务器绝对路径
- **入参校验** —— `Math.max(NaN, 1)` **还是 NaN**，钳位对 NaN 完全无效
- 🔴 **LIKE 通配符要在应用层挡，不能加 SQL 的 `ESCAPE`** ——
  实测加了就从索引搜索退化成**全表扫**。判据：库里一个词头都不含 `%` `_` `\`
- **`/api/v1` 版本前缀**，旧路径留作兼容
- **启动探活：真开一次库跑一条查询** —— `fs.existsSync` 只能说明文件在，
  说明不了库能用（传了一半 / 权限 / schema 不对，它全返回 true）
- **健康检查要反映真实探活结果** —— `return {ok:true}` 是永远为真的健康检查
- 🔴 **数据目录必须能用环境变量指定**。默认值若从源码位置往上数几层推出来，
  打包后必然指错，症状是「语言列表空着」，**一条报错都没有**

**完成的定义**
以上七条全部就位，it 在 `/api/langs` 中可见并可查。

**验收判据**
1. `?limit=abc`、`?limit=NaN`、`?q=%`、`?q=_` 逐个实测，均返回结构化 JSON 错误
2. 词头含通配符的行数为 0
3. 把 `SYNAPSE_DATA_DIR` 指向不存在的路径 → 启动必须**报错退出**，不能静默起来
4. 故意传一个截断的库文件 → 探活必须失败

**明确不做**
不做鉴权、限流以外的运维功能。

### ✅ 完成（2026-08-19）

七条**代码里早就都在**（es 那轮补的），所以本阶段的活是**逐条实测**——
「代码里有」和「按判据跑过」是两回事，这个项目在这上面栽过
（`return {ok:true}` 那个永远为真的健康检查）。

#### 四条验收判据的实际输出

```
判据① 入参校验
   ?limit=abc      400  {"error":"limit_invalid","message":"limit 必须是数字","detail":{"got":"abc"}}
   ?limit=NaN      400  同上（Math.max(NaN,1) 还是 NaN，所以必须在解析处拦，不能只靠钳位）
   钳位实测        -5→1  0→1  1→1  20→20  51→50  99999→50
   ?q=%            200  {"items":[]}      ← 应用层挡掉，不下沉到 SQL
   ?q=_            200  {"items":[]}
   /entries/%      404  {"error":"word_not_found",...}
   /nope           404  {"error":"no_such_route",...}

判据② 库里词头含通配符的行数
   %  0 行     _  0 行     \  0 行        ← 这是「应用层敢挡」的前提：挡了不会漏掉真词

判据③ SYNAPSE_DATA_DIR 指向不存在的路径
   退出码 1，并打印推算出的路径与两种修法（SYNAPSE_DATA_DIR / DATABASE_PATH_<CODE>）
   —— 六个语种逐个列出「DB 文件不存在」，不是静默起来

判据④ 传一个截断的库文件（拷前 2 MB）
   启动自检   ⛔ it 意大利语 不可用：database disk image is malformed
   /health    status=degraded，it 那条 ok=false 且带 error 原文
   查 it      503 {"error":"language_unavailable","detail":{"reason":"database disk image is malformed"}}
   —— 真开库跑查询才逮得到；`fs.existsSync` 对这种文件返回 true
```

**完成的定义**：`/api/v1/langs` 里 it 可见（en/es/it/fr/pt/de 六种），
`braccio` 查得到（11 条义项、4 条录音）。旧路径 `/api/*` 与 `/api/v1/*` 都是 200。

#### 🔴 实测逮到一处：统一错误出口在限流那里是断的

打 130 次触发限流（109×200 + 21×429，`Retry-After` 与 `X-RateLimit-*` 都对），
但响应体是 `{"error":"Too many requests"}` —— **`error` 位上放的是人话，不是机器可读码**，
而其余所有错误都是 `limit_invalid` / `word_not_found` 这种码。客户端按错误码分支
就会漏掉限流这一支。全局那条 503 同病。

⇒ 已归一成 `rate_limited` / `service_busy`，都带 `message` 与 `detail.retryAfterSec`。

#### ⚠️ 查过、判定不是缺陷的一条

`?lang=` 传未知语种（`lang=xx`）会**回退到默认语种**而不是报 400。
第一眼像缺陷（代码注释自己批评过"用户搜西语词拿到英语结果"），但：
① 响应体里 `lang` 字段如实写着实际用的语种，不是静默的；
② 前端 `getInitialLang()` 直接读 localStorage 的 `dict-lang`，**不拿 `/langs` 校验** ——
   改成硬 400 会把「存了个已下线语种码」的用户页面直接打死。
⇒ 保留回退。真正该补的是前端拿 `/langs` 校验 localStorage，记进 backlog。

⚠️ 本阶段踩了一次 A39：macOS **没有 `timeout` 命令**，`rc=127` 且零输出，
差点被我读成"判据③通过"。判据③改成 `kill -0` 轮询确认进程真的退了。

---

## 阶段 X — 贯穿全程的两件事

### `FRAMEWORK` 五之二：写 it 的「约定宪法」

每个语种要有一份自己的约定记录：音标记法取哪一种、性别冲突信谁、
变形层边界划在哪、释义收哪几语。es 的教训是这些约定**散在对话里**，
回头没人说得清当初为什么那么定。**边做边写，不要最后补。**

### `SCHEMA` §9 第 3 条：`level` 是全库唯一没有源、也无法回源的字段

es 核心层 A1/A2/B1 里 **42.4%** 是专名/词缀/缩写/纯符号（`« »` 被判成 A1）。
结论是**不要试图修 `level`**，而是加客观频次列 `freq_zipf`（wordfreq，离线、
同输入永远同输出），`level` 一个字节不动 —— 加列是加法操作，不用就不查。

🔴 wordfreq 三个陷阱：多词的 zipf 是**拼**出来的／**词缀被静默剥掉再查**
（`a-` 拿到介词 `a` 的 7.36）／大小写折叠。
判据只能是 `tokenize(w) == [w.lower()]`，**不能数 token 个数**。

---

# 四、it 收尾单（2026-08-19）

**阶段 -2 → 9 全部完成**，只有 **6b 合成音搁置**（用户试听后判定质量不够，产物已删，
代码/音色/哈希契约全留着）。以下数字都是收尾当天现查的，不是过程中记的。

## 成品清单

| | 数 | 备注 |
|---|---|---|
| 词形 | **1,498,956** | 其中 lemma 337,700（22.5%）|
| 有可见义项的词形 | 344,447（23.0%）| 其余是变形形，指回原形 |
| 词条 `entry` | 807,155 | 词形×词性×词源，同形异义的分界单位 |
| 义项（出版层 / 证据层）| 416,357 / 479,662 | 两层可双向回核 |
| 释义 `sense_gloss` | 713,143 | 中/英/意三语（A3）|
| 读音 | **1,201,630 行 / 1,102,049 个词形（73.5%）** | 95,706 个词形有多读音 |
| 变形关系 | 1,238,816 | `entry_id` = 原形的那个词条（A72）|
| 语义关系 | 614,519 | 近义/反义/上下位/同类/整体部分 |
| 例句 | 38,144 | |
| 搭配 | 23,877 | |
| 真人录音 URL | 12,188 / 9,404 个词形 | 方针④第一级 |
| 频次 `freq_zipf` | 1,465,292（97.8%）| 量不了的 33,664 个留 NULL（A63）|

**中文覆盖 —— 这是给中文用户的词典，这一栏最要紧：**

```
可见义项  414,055   有中文 414,017   100.0%
例句       38,144   有中文  38,135   100.0%
搭配       23,877   有中文  23,874   100.0%
```

读音来源：fr 版 652,449 / 规则派生 430,591 / en 版 93,278 / it 版 22,168 /
unknown 3,053（证明不了来源就写 unknown，不猜）/ 七月模型残留 91。

## 三道闸的现状

```
回归闸        it/tests/test_no_regression.py   18 族，写入列与读取路径各查一次，变异 13/13
展示层契约闸   apps/web/src/contract-check.tsx  15 条，全量渲染 38,580 个词条，变异 12/12
外锚闸        build_pronunciation_layer --verify  重扫三版 dump，双向核对均为 0
性能          it/probes/entry_perf.ts          最差一组 P95 3.0 ms（预算 200）
```

## 工期：估 13–15 天，**实际 8 天**（08-12 → 08-19）

这是这个项目第一次没有超期。**但别把它读成"提速了"**，三条得说清楚：

1. **6b 合成音没做完**（搁置），阶段 6 的"跑批"部分没发生。
2. **backlog 里留了 5 件**（见下），都是 0.1% 量级，但确实是没做的活。
3. 真正的差别在**阶段 1.5**：计划按 es 的印象写成「全项目最贵的一段、3–4 天」，
   实测意语版的义项与英文版能确定性 1:1 对上，**模型调用 0 次、花费 0 元**。
   ⇒ 省下来的不是效率，是**一次没有发生的返工** —— 而它之所以没发生，
   是因为动手前先回源量了「行序 vs dump 94.2% 逐字一致」。

有记录的模型花费：例句翻译约 **780 万 tokens**（flash，我事前估 150 万，**低估 5 倍**）；
④⑤ 那轮在豆包上烧 509 万、其中 **418 万因我 prompt 的缺陷作废**并导致账户欠费。
⚠️ **本项目没有统一的花费账本**，上面两笔是散记在阶段小结里的 ——
下一门语言开工前应该先建一个（每次跑批记 模型/tokens/用途/是否作废）。

## 没做的（backlog，按规模排）

> 🔴 **2026-08-19 清账：上面这张单子已经全部做完**（用户问「这些不能一次性解决吗」）。
> 七件里五件是小修、两件要动结构，一天做完。**逐条的结论比数字更值得看** ——
> 七件里有 **四件的账本数字是错的**，而且错的方向一致：**我拿形式特征当了内容判据**。
>
> | 账本原记 | 实测 | 错在哪 |
> |---|---|---|
> | 读音归属丢 614 个词形 | 收回 **21,666** 个 | 原数只量了「同词形多读音」那一小面 |
> | 变形行原形无词条 2,097 行 | 2,119 → **55** | 根因没查（是 entry 层覆盖缺口，可确定性补） |
> | 非意语例句 47 条 | 真非意语 **87** 条，另有 **50 条是尺子误伤** | 「法语功能词多于意语功能词」—— 而 `de/qui/et/un/la` 在古意语和罗马方言里就是意语词 |
> | 意语版子列表丢了 362 条 | **子列表根本没丢**，在 `glosses[1]`；真缺口 23 条 | 「以冒号结尾」是形式特征，真判据是 `len(glosses)>1` |
>
> ⇒ 给下一门语言：**记账时把判据和它的已知代价一起写下来**。只记数字，
>   下次接手的人（就是我自己）会把那个数字当成事实，而它可能从来没成立过。

| 已做完 | 结果 |
|---|---|
| 读音归属（`pronunciation_entry` 关联表）| 282,060 行关联，组头能标专属读音的词形 6,811 → **7,789**；`pesca` 的桃子组/捕鱼组各标自己的读音 |
| 变形行原形补 entry | 新建 588 个 entry、挂 600 条义项、重指 2,064 行变形；剩 55 行连一条证据都没有 |
| 非意语例句 | `example` 加 `hidden` 列，藏 87 条；读取路径同步改，契约闸加一条「藏起来的不许渲染」（变异确认逮得到 78 处）|
| 重复中文收尾 | 28 组 → 0；其中 13 组是词性不同、页面本就分开（判据过宽），15 组真处理（10 组合并 + 8 组手写区分）|
| 零内容词形 | 真正「可见却零释义」的只有 3 条，已藏；300 个疑似里 264 个是撇号/大小写空壳，有意留的 |
| 前端语种码校验 | `getInitialLang()` 用缓存的语言表同步核一次，契约闸 4 条断言 |
| 意语版子列表 | 恢复 18 条义项的意语原文 + 手写中文 |

**顺手逮到、不在原单子上的两件**：

| 发现 | 规模 | 处理 |
|---|---|---|
| 🔴 `pos or "v"` 的默认值**只修了生成侧、没修已落库的行** | 404 条义项 | 已修（A91）。`Meleagridi`（火鸡科）、`a pancia in su`（副词短语）、`DPCM`（缩写）在页面上都标着「动词」 |
| 🔴 外锚闸把法语版 30 个词形误报成「要收」 | 30 | 判据改成 import 收词器那一份（A93）。⚠️ 我第一版一刀切到所有源，差点把英文版 8 万条 `alt_of` 从闸的视野里抹掉 |

### 2026-08-21 点测评审（12 词渲染成品送两家外审）

用户提议「把展示给用户的结果发给 pro、v4-pro 一起看」。12 个词、材料 20 KB、两家并行开思考，
成本 43k tokens。做法与结论：

- **负控是必需的**：三个负控词我事先掌握缺陷清单。v4-pro 只对 `gatto` 说无问题、在
  `libro`/`acqua` 上都挑出了东西（「叶片」是我漏的）；豆包对三个全说无问题，
  而它在 `la`/`fare` 上都指出了同类徽标污染 —— **覆盖不稳定，它的「无问题」不可当证据**。
- 🔴 **两家都漏了 `gatto` 的断括号**（`( Felis chaus`，全库 573/800 = 71.6%）。
  **形式特征明确的残渣是确定性判据的活儿，模型看不见；模型强在语言学判断**
  （它逮到 `treno` 首义漏「火车」、`fare` 的「赠送」与意语原文 `ricevere` 方向相反）。
  两者互补，谁也替代不了谁 —— 这条比这轮找出的任何单条都重要。

**已修（数据层）**：括号残渣 808 条（剥 206 / 藏 602，`sense_relation` 新增 `hidden` 列）·
变形原形 `avere and` 30 条拆成 60 行（源头本来就写着两个原形，`havere` 库里有）。

**已修（展示层，由 `contract-check` 守，回归闸守不住）**：变形提示去重（读取路径 2,428 行多余）·
搭配去重（326 行）· 词头徽标归属守卫（3,844，见 A97）· 自反形式措辞（1,418，见 D6）。

**已修（内容裁决）**：`treno` 首义补「火车」· `libro` 去掉「叶片」· `fare` 改「（罗马方言）收受贵重礼物」·
`dei` 拆成两条（dey 与 dèi 是两个词，两支都真）。四条都是**跨版错配**：一条 `sense` 上
en 与 it 说的不是一回事，中文只跟了一支。另藏掉音标位上的 8 条垃圾
（`*` 当主音标 3 条 · `Vantaa` 只有 `ː` · fr 版切剩词尾 4 条），`pronunciation` 加 `hidden` 列。

**闸**：回归闸新增 D1–D9，19 条旧断言全绿；契约闸新增 5 条，变异 17/17。
🔴 D9 专门锚着那四条人工裁决 —— 它们写在 `sense_gloss` 上，而重译脚本会整批覆盖那一列。
🔴 建闸过程中发现**变异验证自己已经变成摆设**（判据是「跑完还有红的就算逮住」，
D 组常红后造任何变异都通过），已改成逐条比对，见 A98。

**仍未做（记账，全在 `it-CONVENTIONS` 记账本里带数字和理由）**：
法语版把译文粘在例句后 30 条 · 子列表证据未提升成义项 5 条 · 变形行原形无证据 55 行 ·
义项挂不上 entry 36 条 · 源头自己写错的复数 2 条 · **合成音待用户判断** ·
✅ **盲推中文已分族处置（2026-08-20）**：藏 **3,065** 条生僻单词（盲测正控就是这一族，实测 24–25% 错），留下 **326** 条我逐条读过的（英语外来词 + 多词固定搭配 —— **那把尺子从没量过这两族**，见 A96），其中改 4 条源头证实的错、藏 3 条无源可对的。全程可逆：`sense_gloss.src` 的 `blind-morph` 标记一字节未动。

## 给下一门语言的三条

1. **动手前把 PLAYBOOK 每节 / PITFALLS 每组 / SCHEMA §9 机械对一遍** ——
   本计划两次低估都是"凭记忆写计划"，第三版逐节对照才准。
2. **每接一层数据就渲染一遍常用词自己读**（A69）。阶段 4/5/6a 的数据各自过了闸、
   库里查不出任何异常，接上展示层立刻看见三类真缺陷。
3. **闸红先分性质：数据错 / 断言过期 / 尺子错**（A33）。收尾这两天我自己写错了
   **五条**断言，全是第三种 —— 差点去改正确的数据。判据与被验的实现**共用一份代码**
   （闸 import 修复脚本的判据、契约闸 import 组件的 `groupItSenses`）能挡掉大部分。
4. **改归一函数会连累三个地方**，一次性想全（收尾当天这三条连着踩）：
   ① 入口要改（只修库不改入口 = 下次重收灌回来）② 函数要**幂等**（A82，否则入口剥一次、
   闸再剥一次，外锚闸永远差一步）③ **对旧结构的复刻路径不能跟着归一**（A83，
   那条路的职责是"原样重建"）。


---

## 封版后的改动记录

**2026-09-11 例句挂回义项：+7,279 条（39.0% → 58.2%）**

来历与判据同 `PT_PLAN` 那条（用户让「其他几门语言也 grep 一下同样的写法」，
六门横扫，it/pt/de 三门中招，fr 是收录边界不是 bug）。

🔴 it 的文件头原来写的是「挂不上的主因不是解析错，是**我们本来就没收那条义项**」——
**只对了一半**：23,203 条未挂义项里 **7,279 条本来就挂得上**。
根子是**坐标桥建在意语释义层还不存在的时候**，阶段 1.5 补出来之后没人回头挂。

⇒ 两侧对称地改：
- **存量** `fixes/relink_examples_to_senses.py`：按 (词形, 意语释义原文) 逐字匹配
  `sense_gloss.lang='it'`，命中 7,279、歧义键 10 整条不挂。
- **生成侧** `pipeline/ingest_examples.py`：坐标桥之后加文本桥。
  干跑复现 **「文本桥补挂 7,278」**，与存量补挂数对得上 ⇒ 重跑不会再抹掉修复。

🔴 **写这道桥时我自己踩了一个键**：第一版用 `w`（dump 原词）当键，
而库里的桥按 `dict.word` 建、本行的规范词形是 `word`（撇号空壳合并后的写法）。
**用错键不会报错，只会静默匹配 0 条** —— 下面那条「义项已被搬到别的词头」的护栏
比的也是 `word`，是照着它才发现的。
⇒ **同一段代码里出现两个词形变量时，先问清楚哪个是库里的那个。**
