# pt 作业计划 —— 葡语从当前状态到成品

> 建于 2026-08-29，fr 完结次日。**本文件的每个数字都是当天实测的**，不是印象。
> 凡是没量过的，写「未知（阶段 -1 先量，不写数字）」——
> `[[it-plan-2026-08-12]]`：it 的工期两次低估都是「凭记忆写计划」，第三版逐节对照才准。
>
> | 配套文件 | 管什么 |
> |---|---|
> | `docs/FRAMEWORK.md` | 做什么、凭什么（目标、家底、方法论、里程碑）|
> | `docs/PLAYBOOK.md` | 换语种的作业顺序（准备→建库→音标→收词→义项→例句→闸→性能→产品化）|
> | `docs/PITFALLS.md` | 只记教训不记成绩，**开工前读一遍**（A 修数据 / B 扫 dump / C 度量 / D 加字段 / E 写闸 / F 性能 / G 修复消失 / H 判官）|
> | `docs/FR_PLAN.md` | 上一门的全过程，本计划逐节对照它排工期 |

---

## 一、pt 的起点（2026-08-29 实测）

### 1.1 库

`data/db/synapse-dict-pt.sqlite`，109 MB，**只有 `dict` 一张表、411,802 行**，索引 3 个
（`idx_word` / `idx_norm` / `idx_norm_nocase`）。

| | 行数 | 占比 |
|---|---|---|
| lemma (`is_lemma=1`) | 71,440 | 17.3% |
| 变形词形 | 340,362 | 82.7% |

列的填充情况：

| 列 | 非空 | 占比 | 是什么 |
|---|---|---|---|
| `word` / `word_norm` / `pos` / `is_lemma` | 411,802 | 100% | |
| `translation` | 411,802 | **100%** | 中文。lemma 是真译文（多义项挤在一格里用换行分隔）；变形行是确定性生成的语法说明（`piar 的 虚拟式现在时第一/三人称单数`）|
| `infl` / `exchange` | 346,289 | 84.1% | 变位说明 / 反查原形 |
| `ipa_br` | 84,786 | **20.6%** | 巴西葡语 |
| `ipa_pt` | 84,625 | **20.5%** | 欧洲葡语 |
| `level` | 71,434 | 17.3% | CEFR |
| `definition` | 71,420 | 17.3% | **英文**，来自英文版 |
| `meta` | 71,420 | 17.3% | 逐义项 JSON |
| `gender` | 58,612 | 14.2% | |
| `collocation` | 13,551 | 3.3% | |
| `plural` | 13,254 | 3.2% | |
| `adj_pos` | 11,615 | 2.8% | |
| `feminine` | 11,338 | 2.8% | |
| `pp` | 6,936 | 1.7% | 过去分词 |
| `vconj` | 6,277 | 1.5% | 变位类 |
| `transitivity` | 2,729 | 0.7% | |
| `government` | 1,053 | 0.3% | 介词支配 |
| `pronominal` | 575 | 0.1% | |
| `pp_short` | 62 | 0.0% | 短过去分词（葡语特有）|
| `comparative` | 6 | 0.0% | 硬编码 |
| `example` / `flag` | 0 | **0%** | 从来没填过 |
| `ipa_br_src` / `ipa_pt_src` / `translation_src` / `gender_src` | 0 | **0%** | 🔴 **四列建了从没回填** |

🔴 四个 `_src` 来源列全空 —— 与 fr 开工当天同一个毛病（`[[ipa-provenance-columns]]` 那轮
六语种统一加列时建的，pt 一直没回填）。**音标阶段之前必须补**，否则"这个音标是谁给的"
永远答不出来。pt 比 fr 多一列（双读音各一个来源），也更需要它。

### 1.2 🔴 与 fr 开工当天并排 —— 只有一处差异是决定性的

| | fr（8-21 开工当天）| pt（今天）|
|---|---|---|
| 表 | 只有 `dict` | 只有 `dict` |
| 行数 | 385,216 | **411,802** |
| lemma 占比 | 23.6% | 17.3% |
| `translation`（中文）| 100% | 100% |
| `definition`（英文）| 23.5% | 17.3% |
| `infl`/`exchange` | 77.7% | 84.1% |
| `example` / `flag` | 0 / 0 | 0 / 0 |
| `_src` 来源列 | 全 0（3 列）| 全 0（**4 列**）|
| service `.ts` | 299 行老单表版 | **287 行老单表版** |
| 已下载的 per-language 切片 | **12 个源** | **0 个** |
| **音标覆盖** | **82.3%** | **20.7%** |

**形状同款，规模略大 —— 唯一决定性的差异是音标。**

🔴 而且这个差异**不是"pt 还没做"，是"pt 做不了 fr 那一招"**：

```
fr 的 82.3% 里，387,826 条是从 lemma 的 forms 数组收割来的（fr 音标的 76.9% 靠它）。
FRAMEWORK.md:446 的 2026-08-01 全量复查：

  语种   forms 带 ipa      库内缺音标    英文版还能补
  es          0              4,169            0
  it          0                444            0
  fr    387,826             68,174            2
  de          0            239,651            0
  pt          0            327,016          314          ← 一条路都没有
```

⇒ **pt 的变形层 340,362 行里只有 4.1% 有音标，缺 326,511 个**（与 2026-08-01 记的
32.7 万对得上）。这是全项目已知的最大单项缺口，也是 pt 这一门唯一确定会比 fr 贵的一段。

### 1.3 🔴 参照系不是「一门语言」，是分维度选

> 用户 2026-08-29 问：「pt 和 fr 是近亲吗，为啥不同时参考 es 和 it？」
> **他是对的，本计划第一版把 fr 当成了唯一参照系。** 回源核对之后：

`pt/pipeline/build.py` 的文件头自己写着 **pt 独立管线，不 import 其他语种、不复用其他语种
schema**，而且两处明确写的是**与法语相反**：

```
· 明确不设 aux（葡语无 avoir/être 式逐动词助动词选择）
· 变位形 IPA 无法从 lemma forms 收割（葡语 forms 不带 ipa，与法语相反）
```

⇒ 它把 fr 当**对照组**写的，不是当模板。而「pt∩fr 列重合 81%」（实测，es 71% / de 65% /
it 42%）是**工程重合不是语言学亲缘** —— fr 与 pt 的 `build.py` 是同一周、同一套
「把本质字段升为一等字段」思路写出来的兄弟。拿它当近亲的证据＝把相关当成了因果。

**语言学上 pt 最近的亲戚是 es**（同属西伊比利亚语支），fr 是高卢-罗曼、it 是意大利-罗曼。

四门的 `dict` 列（实测）：

```
es (33)  reflexive conjugation stem_change pp transitivity feminine plural comparative
it (20)  aux conj transitivity pronominal gender plural plural_gender number_note
fr (31)  aux vgroup transitivity pronominal pp feminine invariable adj_pos government
pt (31)  vconj transitivity pronominal pp pp_short feminine plural adj_pos government
         ipa_br ipa_pt                                ← 四列没有任何其他语种有
```

| 维度 | 参照谁 | 依据 |
|---|---|---|
| **词法概念**（变位类/代词式/阴性形/过去分词）| **es** | 西伊比利亚语支。es `conjugation`+`stem_change` ↔ pt `vconj` 是同一件事；es `reflexive` ↔ pt `pronominal` 同一件事换个名。fr 的 `aux`/`vgroup` 是法语特有，pt **明确不设** |
| **G2P 造不造、怎么验收** | **it** | 六门里唯一趟过 G2P 的（73.4% 自己算），并留下标尺：规则裸路径 vs 人工源一致率 57.2%，有背书层 87% |
| **双读音落表结构** | **谁都不是** | `ipa_br`/`ipa_pt` 是 pt 独有 ⇒ 必须自己量（`[[es-v3-structure-backfill]]`）|
| **v3 表结构 / 三道闸 / 记账纪律** | **fr** | 最新最完整（16 表 + 契约 25 + 回归 28 + 账的闸）|
| **跨版收词的方法** | **fr** | 12 个源的探测流程最完整 |
| **判重合并** | **es（反面教材）** | es 186 条判重 12.4% 是义项错配 ⇒ PLAYBOOK 明令下一门不做自动判重 |

🔴 **本计划其余各节凡是写「同 fr」「照抄 fr」的，都要按本表复核一遍** ——
`FR_PLAN` 那轮的教训是「五处照搬 it 就会错，**全是量出来的**」，
以 fr 为唯一参照系是同一个错误换了个方向。

### 1.4 代码现状

| 已有 | 行数 | 状态 |
|---|---|---|
| `pt/dbtool.py` | 192 | 七月版，**需对照 `fr/dbtool.py` 补齐**（expect 闸 / session 钩子 / 备份抽稀）|
| `pt/kaikki_util.py` | 149 | 七月版 |
| `pt/pipeline/build.py` | 572 | 七月版，老单表 schema |
| `pt/pipeline/infl_compose.py` | 114 | 七月版 |
| `pt/pipeline/b_translate.py` | 364 | 🔴 **豆包流水线，整份作废**（用户 2026-08-15 明令跑批不用豆包）|
| `pt/pipeline/fill_ipa_dual.py` | 139 | 七月版，双读音补空 |
| `pt/fixes/` 3 个 | 546 | 七月版 |
| `pt/probes/quality_study.py` | 212 | 七月版 |
| `pt/tests/test_tools.py` | 119 | 金标准测试 |
| `packages/dict-core/src/portuguese.ts` | 287 | **老单表版**（es 908 / it 939 / fr 747 是 v3 版）⇒ **要重写不是改**|
| `pt/tests/test_plan_ledger.py` | — | 🔴 **不存在**，阶段 -2 建（fr 那道闸这一轮拦了我两次）|

⚠️ 展示层已接线但接的是老单表：`index.ts:362` 有 pt 行、`:458` 造 `PortugueseDictService`、
`App.tsx:2562` 有 `PortugueseEntryView`。**接了不等于接对了** —— `[[it-display-layer-stage8]]`：
接上展示层是独立一道闸。

---

## 二、阶段表

> 判据抄 fr：**声明 ✅ 就必须交得出东西**（`pt/tests/test_plan_ledger.py` 会查）。
> "工期"一列只在**有实测依据**时写数字；没有的写「未知」，不写猜的。

| # | 阶段 | 状态 | 依赖 | 与 fr 的差别 |
|---|---|---|---|---|
| **-2** | 准备：`dbtool.py` 补齐 + 建 `test_plan_ledger.py` | **✅ 已完成（2026-08-29）** | — | `dbtool` TRACK 7→**27 列**（双读音+两个来源列全进闸）；账的闸变异 5/5；备份策略 9 条全绿；🔴 顺带修了一个**从 fr 抄来的洞**：账的闸原本挂在回归闸末尾，而回归闸文件不存在时提前 return ⇒ 它在阶段 7 之前是死的，已解耦 |
| **-1** | 版本探测与取数 | **✅ 已完成（2026-08-29）** | -2 | 7 源 1.87 GB 全量扫，**零下载**（切片按记账①先不下）。产出 `docs/lang/pt-CONVENTIONS.md` + 三份探测存档；逮到三处自伤（判据漏 `raw_tags` ／ 截断样本出结论 ×2 ／ **残差漏数 `forms`，被用户问倒**）|
| **0** | 表结构迁移（一次建 **11 张新表**，见 §2.1）| **✅ 已完成（2026-08-29）** | -1 | `sense` 104,030／`sense_gloss` 208,039／`sense_tag` 18,878／`collocation` 15,369；闸①四列逐字节一致（全量非抽样）+ 闸② 11 条全绿 + **变异 5/5**；🔴 闸①逮到 2 行不可逆（搭配双空格分隔符），前置清洗后重跑；🔴 `pronunciation` **一开始就带 `pos` 列**（fr 是族 D 返工才加）|
| **1** | `entry` 词条层 + `sense_src` 证据层 | **✅ 已完成（2026-08-29）** | 0 | `entry` 432,917／`sense_src` 111,957（可见 104,009 + **alt_of 证据 7,948**）；闸①复刻回核 **0 失配**（全量非抽样）+ 闸② 9 条全绿 + **变异 5/5**；🔴 逮到 **6,518 个词形因 `alt_of` 被当变形丢掉而零释义**（交阶段 2）|
| **1.5** | 葡语原文释义层 + 翻译 | **✅ 已完成（2026-08-30）** | 0,1,**3** | **1.5a** 收葡语原文释义：`sense` 112,002 → **175,724**，`sense_gloss(pt)` 60,767，**中文版白送 2,955 条中文**。只收葡语版（`[[gloss-three-languages]]`：各版只给自己语言的词写定义）。<br>**1.5b** 翻译 60,167 条，实跑 **366 万 token（入 265 万／出 101 万）**，星期日空闲时段 **≈8.9 元**（切片预测 8.8，差 1%）。`sense_gloss` +56,133。<br>🔴 **判据被反向查打回一次，避免了 1,338 条误伤**：想按「指针句式」隐掉无中文义项，反查发现同族有 1,379 条模型给了**有用**中文（`Antárctica → 南极洲`）⇒ 收窄成**只隐变位指针**（1,746 条，误伤面 6 且那 6 条本身是错的）。<br>🔴 闸在写入时救过一次：判据走 `entry JOIN sense` 而 `sense.entry_id` 可空 ⇒ 20 个词被误判成零义项，**`UNIQUE(word_id, rank)` 当场拦住、事务回滚** |
| **2** | 变形层归位 + `alt_of` 误判回收 | **✅ 已完成（2026-08-29）** | 0,1 | **2a** 补回 7,948 条义项、**6,518 个零释义词形 → 0**，中文模板生成 7,927（零模型调用），267 个词形的 alt 义项按原位插入、437 条重排 rank；**2b** `inflection` **400,330** 行（只收 `form_of`），闸①双向重建 `infl`/`exchange` 各 **0 失配**、变异 5/5 |
| **2c** | **变形层补链**（阶段 3 新收的变形接回词元）| **✅ 已完成（2026-08-30）** | 2b,**3** | 🔴 **阶段顺序造成的洞**：2b 先建变形层（只读英文版 `form_of`），3 后收词 ⇒ 阶段 3 收进来的 30 万个变形**从没人给它们连过线**。实测 **355,605 个词形（46.2%）**既无义项也无变形链＝点进去空白页，其中 **309,590 个 `is_lemma=0`**。⚠️ 对照组：**fr 的孤儿率只有 1.8%**（37,239 条，几乎全是词缀）⇒ 这不是语言属性，是我的顺序错。父词元在 dump 里现成（`intake_edition_words.scan()` 走 `forms` 那层，本条目词头就在手里），**重扫即可、零模型调用**。<br>**结果**：`inflection` 400,330 → **876,910**（+476,580），**孤儿 355,605 → 64,785，降 290,819（-81.8%）**；八条闸全绿、抽样 25/25 干净。<br>🔴 **判据被数据打回四次**：v1 凭印象列 tags → v2「变形页的变位表整个不收」→ v3 收窄成「**除了指针什么都没有**才算变形页」（保住 3,989 个混合页如 `amo`）→ v4 拒收单独成格的 `não` + 双向链两边一起丢。抽样逮到 `átonos → átonas` 横向链、`não` 被 **618** 个词元当变形、假词 `metaphonic`（英文表格标签）被 **571** 个当变形 —— 阶段 3 漏网，已删 |
| **2d** | **页面级 `form_of` 补进变形层** | **✅ 已完成（2026-08-30）** | 2b,2c,3 | 🔴🔴 **fr 没有这一步 —— 它是 1.5a／2b／2c 三步之间的缝**：1.5a 见指针义项就「归变形层」跳过、2b 只读**英文版** `form_of`、2c 只读词元页的 `forms`（方向相反）⇒ 源头给了独立页面和释义的变形（`plural de junqueira`）**三步谁也没收**，19,404 个词形点进去一片空白。`inflection` **+5,613**（`src='pt-edition-page'`），空白页 -5,613。判据 import `ingest_prose_inflections.PTR`，**不信 `form_of.word`**（wiktextract 在 pt 版上把散文片段塞进去：`baderna → "terceira pessoa"`）。157 条词形已有完整词条 ⇒ **有意不补**（C37）|
| **3** | 收词（跨版）| **✅ 已完成（2026-08-29）** | -1,1 | `dict` 411,802 → **769,012**（lemma 71,440 → 118,960），`entry` 432,917 → **1,710,199**。3a 拆大小写 +1,627／3b-1 葡语版 +244,582／3b-2 法语版 +45,591／3b-3 英文版 forms +62,128／3c 残差 +3,282。⚪ 3b-0 撇号归一跳过。🔴 **抽样反验挡下 283,136 条假词形**（法语版把主语代词写进变位表单元格），见 `PITFALLS` B1.5 |
| **4** | **音标层** | **✅ 已完成（2026-08-29）** | 0 | `pronunciation` **1,139,833** 行，覆盖 352,364 / 769,012 词形 = 45.8%（**有可见义项的词形 94.0%**）。4a 迁两列 162,025 行、闸①逐字节回核；4b 跨版收割 +977,808。region pt-BR 244,302／pt-PT 221,999／NULL 511,526。🔴 逮到四类源缺陷（定界符四版四种约定／X-SAMPA 两层排除 4,375／空音标／段内含定界符 19）。📋 **G2P 决策推迟到阶段 5 之后**，见 §四.2 |
| **5** | 例句 / 关系 / 搭配 / 频次层 | **✅ 已完成（2026-08-30）** | 1 | **5a 频次** `freq_zipf` 656,116 行（量不了的 112,895 留 NULL 不填 0），四条闸**全量复算**、变异 11/11。<br>**5b 关系** `sense_relation` +140,937（synonym 69,667／antonym 9,871／上下位等），**义项级挂载 63,070 = 95.1%**（坐标反解 `src_ref`，不重放 `occ`）。有意**不收** derived/related/proverbs 共 87,001 条 —— 构词族与联想词不是语义关系。<br>**5c 例句** `example` 50,745（挂到义项 10,731）／`example_gloss` 9,653 **全是源头白送的**（英 8,850 + 中 803）。<br>**5d 翻译** 39,454 个不同句子，实测 **入 59.1／出 28.8 token/条**，**实跑 336.5 万 token（入 227.6 万／出 108.9 万），星期日＝空闲时段半价，****实花 ≈8.7 元**。`example_gloss` 9,653 → **58,442**，例句中文覆盖 **97.7%**。<br>⭐ **payload 带 `src_gloss` 修掉了 fr 最大一族**（族 A 例句译文取错义项，风险面 22.1 万、重跑 41 元）——交叉真值实证：`roubar minha mulher` 在「勾引」义项下译成「想**勾引**我老婆」而不是「偷我老婆」。<br>🔴 **控制判据第一版全是形式代理**，1% 切片上 10 条红**全是正确译文**被数字/术语撑破（`人均收入1900美元，失业率17.9%…` / `10♠,J♠,Q♠,K♠,A♠`）⇒ 分母改成**原文的可翻词数**。<br>🔴 **搭配层阶段 0 就落了 15,369 条**，不用重做 |
| **6** | 发音（只落元数据）| **✅ 已完成（2026-08-30）** | 2 | 🔴 **复核推翻了旧结论的一半**：`[[cross-edition-harvest]]` 记的「pt 录音为 0」只对**葡语版自己**成立，**跨版有 16,584 条**（fr 版 9,605／en 版 6,203）⇒ 去重后 `audio` **11,287** 条、覆盖 **5,119** 个词形。零网络请求（只落 Commons URL）。<br>地区三级来源、每级记进 `region_src`：tag 6,153 ／ filename 748 ／ **speaker 1,615**（同一录音人 ≥3 条一致才外推；实测 26 位录音人**零地区冲突**，前提有据）⇒ pt-BR 3,378／pt-PT 2,972／**未定 4,937 留空不猜**（两位最大录音人 5,733 条一条带标记的都没有）。<br>🔴 **逮到一个任何闸都逮不到的 bug**：去重时只留第一条、**把后来者的地区信息整个丢了** ——合并后每条记录都合法，是**独立探针与收割器给出不一致的数**（6 位 vs 0 位）才照出来的。<br>🔴 闸判据写窄过一次：`upload.wikimedia.org` 漏掉了 `commons.wikimedia.org/wiki/Special:FilePath/…`，抽样当场逮到 |
| **7** | 闸与验收 | **✅ 已完成（2026-08-30）** | 全部 | `tests/test_no_regression.py` **32 条断言 / 7 组**（A 表结构·B 词条变形·C 音标·D 频次·E 关系·F 例句·G 录音·**L 阶段 8 债务**），**判据一律 import 各脚本自己那份**，**变异 7/7**，已挂进 `dbtool.session`（每次写库跑四道：不变量→回归→账→字面量）。<br>🔴 **建闸当天就逮到真缺陷**：B2 报 16 条 —— 不是 2a 的修复丢了，是`[[case-folding-contaminates-columns]]` 那一族的新一处：`alt_of` 关系留在小写词形上（`leia → Lia` 而 `leia` 是 `ler` 的虚拟式、`doe → Diário Oficial do Estado` 而关系属于缩写 `DOE`），**阶段 3a 拆了行没搬属性**，已搬 16 条。<br>🔴 **闸自己的假绿也是变异逮到的**：C2 我自己写了一版 SQLite `GLOB '*[/[\\]|]*'`，而 GLOB 字符类里 `]` 必须放最前 ⇒ **恒真**，加回定界符都不报 ⇒ 判据抽成 `harvest_pronunciation.has_delim` 一份，闸 import 它。<br>**契约闸 `contract-check-pt.tsx`**（阶段 8 建）：14 条断言、按形状取样 248 词、变异验证通过。⭐ **建出来第一次跑就逮到它文件头点名的缺陷** —— 视图里一个 `entry.isLemma &&` 挡住 **6,520 个词形**的整块释义（与 it 的 `TVTB` 8,552 个同一形状）|
| **8** | 展示层与性能 | **✅ 已完成（2026-08-30）** | 7 | `portuguese.ts` **287 → 433 行**，v3 八张表全接（sense/sense_gloss/pronunciation/inflection/example/sense_relation/audio/collocation）；`App.tsx` 补三节（真人发音/例句/语义关系）＋源语言行语种标签；`render-dump` 加 pt 分支。回归闸 **L 组归零**。<br>⭐ **契约闸 `contract-check-pt.tsx` 建出来第一次跑就逮到它文件头点名的那个缺陷**：视图里一个 `entry.isLemma &&` 挡住 **6,520 个词形**的整块释义（`no`／`thesaurus`／`frei`／`abada` 有义项却不是词元）—— 与 it 的 `TVTB` 8,552 个同一形状，**接口返回完全正确，查库查接口都看不见**。14 条断言、变异验证通过。<br>🔴 **真渲染逮到三族源头残渣**（`render-dump` 按用户真正看到的样子导出）：例句出处是孤立标点 200 条／含空字段 812 条（**展示层清，不动数据**）／`text` 本身是引文行 252 条（**不是例句，`hidden=1`**）。<br>🔴 **性能：契约闸卡 14 分钟跑不完** —— 根因是 `dict.word` 只有 NOCASE 索引而比较是 BINARY，每个关系目标扫一遍 77 万行（**46ms → 0.01ms，快 4600 倍**）。加 `idx_word_bin`。**今天第三次撞这个坑** |
| **9** | 产品化 | **✅ 已完成（2026-08-30）** | 8 | **搜索下拉 5,891 ms → 0.5–1.4 ms；词条页 `a` 684 ms → 1.4 ms。**<br>三步，前两步是**真缺陷**不是顺手优化：<br>① 中文摘要写成候选行上的相关子查询 ⇒ 规划器**从选择性最差那头入手**（`lang='zh'` 34 万行）且**对每个候选跑一次** —— 占总耗时 **99.5%**。移到 LIMIT 之后。<br>② 🔴 **加了正确索引规划器仍然不用**，因为库里**从没跑过 `ANALYZE`**，它在猜。`ANALYZE`（25.5s）之后计划才翻转，同一段 200 次调用 **11,748 ms → 4 ms**。⭐ **「索引建了」不等于「索引被用了」，EXPLAIN 会直说。**<br>③ `search_prefix` 预计算 13,253 前缀 / 245,928 行；闸①**全量逐位回核不一致 0**（非抽样）、变异 **5/5 含负控**；服务已接上（未命中正确回退）。<br>⭐ 移植时避开了文件头点名的雷：`LIVE` 必须与 `portuguese.ts` **逐字相同** ——pt 是**两级**精确匹配（fr 只有一级），第一级 `word = ?` 正是为阶段 3a 拆开的`Cefalópodos`/`cefalópodos` 存在的，抄丢了等于把那次拆分作废。已用 876 个前缀逐位验等价 |

**工期**：不写总数。fr 实测 8 天（08-21→08-28）、it 实测 8 天，但两门的音标阶段都不长；
pt 的音标阶段是未知量，等阶段 -1 和 §三 的决策定了再排。

### 2.1 🔴 十六张表，各自的内容在哪一阶段定

> 用户 2026-08-29 问「几张表，每张表的内容确定是阶段几」。
> **「阶段 0 建 11 张」≠「v3 一共 11 张」** —— 阶段 0 只是一次把 11 张空表建出来，
> 另外 5 张分别属于建库、阶段 1、阶段 2、阶段 9。fr 最终是 **16 张**（实测）。
> ⚠️ **建表 ≠ 填内容**：`pronunciation` / `audio` / `example` 在阶段 0 就建好了，
> 但空到阶段 4 / 6 / 5 才有东西。下表第三列才是"内容什么时候定"。

| 表 | 建于 | **内容定于** | fr 实测行数 |
|---|---|---|---:|
| `dict` 词形 | 建库 | **阶段 3**（收词，fr 38.5 万 → 208.6 万）| 2,086,292 |
| `entry` 词条 | **阶段 1** | 阶段 1；阶段 3 收词时跟着涨 | 2,543,172 |
| `sense_src` 证据层 | 阶段 0 | 阶段 1 灌入；阶段 1.5 再进原文释义 | 835,475 |
| `sense` 出版层 | 阶段 0 | 阶段 0 从扁平列搬入，**阶段 1.5 裁决后定型** | 659,167 |
| `sense_gloss` 释义 | 阶段 0 | 阶段 0 搬 en/zh；**阶段 1.5** 进本语言原文 + 翻译 | 1,430,100 |
| `sense_tag` 标签 | 阶段 0 | 阶段 0（从 `meta` 的 lex/reg）| 415,279 |
| `sense_relation` 关系 | 阶段 0 | **两段**：阶段 2 的 `alt_of` + **阶段 5** 的语义关系 | 318,370 |
| `inflection` 变形 | **阶段 2** | 阶段 2；阶段 3 收词时跟着涨 | 2,269,888 |
| `pronunciation` 音标 | 阶段 0 | **阶段 4** | 1,901,705 |
| `example` 例句 | 阶段 0 | **阶段 5** | 740,366 |
| `example_gloss` 例句译文 | 阶段 0 | **阶段 5** | 752,760 |
| `collocation` 搭配 | 阶段 0 | 阶段 0 从 `dict.collocation` 搬；阶段 5 处理残缺 | 13,984 |
| `collocation_gloss` | 阶段 0 | 同上 | 13,980 |
| `audio` 录音 | 阶段 0 | **阶段 6**（只落 URL）| 392,013 |
| `search_prefix` | **阶段 9** | 阶段 9 | 506,040 |
| `search_prefix_meta` | **阶段 9** | 阶段 9 | 2 |

⚠️ **频次不是表是列**：`dict.freq_zipf`，内容定于**阶段 5**（fr 1,776,527 行）。

🔴 **pt 可能是 16 张也可能是 17 张** —— 第 17 张是 `pronunciation_entry`
（it 建了、收回 21,666 个词形；es **有意不建**，因为全量回源"不同词条读音不同"只有 14 个词形；
fr 没建）。**pt 是双读音语言，形状与三者都不同，必须自己量**，见 §四.3。
这就是为什么阶段 4 的交付物判据现在还是宽的（收尾单 C3）。

---

## 三、🔴 pt 唯一确定的难点：变形层 32.7 万个词形没有音标

### 已知能走的路，和各自的天花板

| 路 | 能补多少 | 拦路的是什么 |
|---|---|---|
| 英文版 `forms` 收割（fr 那一招）| **0** | 实测 pt 的 forms 不带 ipa（FRAMEWORK.md:446）|
| 英文版剩余 | **314 行** | 已挖干净 |
| 法语版跨版收割 | **约 18.0 万** | 🔴 **只补 `ipa_pt`（欧葡）**，`ipa_br`（巴葡）一条都补不到 |
| 葡语版 `ptwiktionary`（33.6 MB，在磁盘）| **音标只有 33,450 条** | 2026-08-29 实测。399,087 个葡语条目里带音标的只有 20,515（**5.1%**）—— "本语言版最懂本语言音标"在 pt 上不成立 |
| G2P 造音标 | 补最后的 44.8% | 见下 |

### ✅ 落点实测（2026-08-29，`pt/probes/ipa_landing.py`，全量非抽样）

```
库现状：有音标词形 85,104 ／ 缺音标 326,698（其中变形 326,511 = 99.94%）

源      带ipa词形    ①在库里    ②补缺口   其中变形   ③库里没有   独有贡献
en        64,125     63,239         0        0       886          0
pt        16,611     14,208       666      665     2,403        202
fr       289,352    205,700   179,722  179,720    83,652    173,744
es         5,342      4,593       599      598       749        224
de         7,345      7,281     5,380     5,380       64        151
zh           620        587         0        0        33          0
it           377        356         8        8        21          0
──────────────────────────────────────────────────────────────
并集                            180,303  180,299
```

⇒ **跨版并集能补 180,303 个（55.2%），音标覆盖 20.7% → 64.5%**；剩 146,395 个靠 G2P 或留空。

- **法语版不可替代**：独有 173,744 = 并集的 96.4%。`[[cross-edition-harvest]]` 记的「pt +18.0 万」**是准的**（落点 179,722），错的只是"仅欧葡"那半句。
- **de 命中率最高**：7,345 个带音标词形里 5,380 个正中缺口（73.2%），小而准。
- **en 独有 0**：建库主源早挖干净，印证 `FRAMEWORK.md:446`。
- **大小写/NFC 折叠最多多匹配 7 个** ⇒ 不折叠是对的，不用冒 `[[case-folding-contaminates-columns]]` 的险。
- **收词残差**：法语版有 83,652 个带音标的葡语词形不在我们库里，是阶段 3 的料。

🔴 **这一步我自己栽了一次**：先用 `--quick`（每源前 20 万行）读出「并集只补 0.3%」，
据此报了「法语版几乎不落在缺口上」。**法语版 dump 有 742 万行，20 万行只是 2.7% 且按字母序有偏。**
全量是 55.2%。⇒ **截断样本可以看形状，不能出结论。**

### ✅ 阶段 -1 已实测（2026-08-29，`pt/probes/count_slices.py`，7 源 1.87 GB 全量）

**① `forms 带 ipa` 在七个源上全部是 0。** `FRAMEWORK.md:446` 那条只量过英文版，
现在扩到七版 —— **fr 那招（一次收割 38.8 万条变位形音标）在 pt 上彻底堵死。**

**② 各版画像**（源头数）

```
源   葡语条目    义项      指针     真释义   指针%   词形     非指针  带音标条目  音标条数   录音
en   434,036   514,235  408,546  105,620  79.4%  413,429  71,507   76,795   563,942  12,406
pt   399,087   563,267  407,716  155,329  72.4%  380,587  82,130   20,515    33,450       0
fr   304,628   388,751  359,189   29,562  92.4%  296,658  21,202  297,079   811,074  28,063
es     5,896     7,396    1,121    6,275  15.2%    5,345   4,572    5,892    10,637   1,016
de     7,595    11,043    9,507    1,536  86.1%    7,372     828    7,564     8,502     240
zh   124,246   144,650  120,541   24,096  83.3%  121,014  20,069      764     1,295     779
it     1,794     2,054      150    1,888   7.3%    1,668   1,573      448       551      27
```

🔴 **别按"真释义"分配注意力**：`it 7.3% / es 15.2%` 的指针占比说明**那两版根本不标变形**，
它们的"真释义"数是虚高的 —— 同 fr 那轮 tr/el/ru 三版的形状。

**③ 我的一个假设被打回**：§1.3 写西语版是「西伊比利亚近亲，从没量过」暗示可能有料。
实测 **es 版只给 5,896 条葡语条目 = 法语版的 1.9%**。⇒ **语言学近亲 ≠ 维基收录量**，
这是同一份计划里第二次栽在「拿相关当因果」上（第一次是拿列重合度当近亲证据）。

**④ 🔴 葡语版的音标标的是巴西内部方言，不是 br/pt 二分** ——
`Carioca` 1,413 / `Outros lugares` 1,111 / `Caipira`·`Paulistana`·`Gaúcha`·`Paranaense`…
而 `(Portugal)` 只有 47 条。**我们的 `ipa_br`/`ipa_pt` 两列模型装不下它**，
这是 §四.3 结构决策的新输入（已进收尾单 C4）。

**⑤ 「法语版补的是欧葡」这个旧说法不成立。** 法语版 811,074 条音标里带地区标记的只有
**8,133 条（1.0%）**，而且排头的是 `États-Unis` 2,479 和 `Yangsan (Corée du Sud)` 553
—— **那是录音地点不是方言标注**。`[[cross-edition-harvest]]` 记的「仅 ipa_pt 欧葡」
是当年的推断，数据上没有这个标记。

⚠️ **这一条我自己的判据先错过一轮**：第一版只查 `sounds[].tags`（法语版/葡语版把地区写在
`raw_tags` 里，`tags` 恒空）且只认英文串，据此报「一条都没标」，真值 8,133。
方向碰巧对，但**那不是判据对**。判据已修（`_region()`，同时看两个键 + 子串匹配），
修完 `en` 的巴葡数从 90,859 跳到 154,097。

**⑥ 录音那条 ⚠️ 结掉了**：葡语版自己确实 0 条，但**跨版合计约 42,531 条**
（fr 28,063 / en 12,406 / es 1,016 / zh 779 / de 240 / it 27）。

**⑦ 阶段 1.5 比 fr 便宜得多**：葡语原文释义 **155,329**，fr 是 697,814（pt 只有 22%）。

**⑧ ✅ 词汇残差已实测**（`pt/probes/residual.py`，全量非抽样）

🔴 **第一版的数是错的，被用户一句话问倒**：「pt 的词汇量没过百万，比 fr/es/it 少很多，
这是正常的吗，还是说我们漏掉了重要来源？」—— **漏了，而且是我的判据漏的。**

第一版只数每行 JSON 的顶层 `word`。这在 fr 上够用（**法语维基给每个屈折形式单独建页**），
在 pt 上**漏掉整个变位层**（**葡语维基不建那些页**，变位形只活在词条内部的 `forms` 变位表里）。
实测每条目 forms 条数：葡语版 **51.9**（＝完整动词变位表；葡语还多出**人称不定式**和
**将来虚拟式**两个西/法都没有的时态）、英文版 9.1。

```
              第一版（只数顶层）    修正后（顶层 + forms）
en 残差              1,627               95,627
pt 残差            167,082              247,931
fr 残差             88,025              398,658
并集残差           219,756              643,117
收完                631,558            1,054,919
```

⚠️ 第一版还据此下过一个明确的错误结论：「英文版实际能补 0 个新词形、已经榨干了」——
真值是它的变位表里藏着 94,000 个。

**修正后的落点**

```
源           词形       残差    其中真词头    其中变形   独有贡献
en        507,429     95,627      1,360      94,267     60,734
pt        512,566    247,931     42,033     205,898    165,605
fr        612,010    398,658      4,594     394,064    324,329
zh        127,214      7,896      3,178       4,718      2,213
es          8,724      1,515        652         863        871
de          7,647        154         57          97         93
it          1,914        174        147          27         95
──────────────────────────────────────────────────────────
并集    1,054,919    643,117     47,581     595,536
```

⇒ 预测**收完约 1,054,919 词形**。

🔴🔴 **这个预测偏高 37%，实际落点 769,012**（2026-08-29 阶段 3 收词实测，lemma 118,960）。
差的 285,907 **不是**"上界本来就偏高"—— fr 那轮上界 36,007 → 落点 35,336 = **98.1%**，很准。
真因是**本探针与收词脚本用了两把不同的尺子**：探针的 `forms` 过滤只挡三个 tag，
而收词时抽样反验逼出了收窄的判据 —— 法语版**代词单元格 238,556**（`que eu une`，
实测末词与词头逐条相同 100.0%）+ **法语时态名表头 511,000+**（`Indicatif`/`Présent`，
根本不是葡语）+ 音节划分 2,041，**全是垃圾**，却被探针当成了新词形。
⇒ 已把 `probes/residual.py` 改成 **import 收词脚本的 `_real_forms`**，判据只许一份。
⚠️ 与 `[[regex-alternation-order]]` 同源、方向相反：那次手抄一份**窄**的漏掉 43 条，
这次手抄一份**宽**的把预测多报了 37%。
⭐ 769,012 仍是开工时的 **1.87 倍**，但只有 es（1,148,071）的 **2/3**；
lemma 118,960 对 es 的 177,836 也是 2/3 —— **葡语维基确实比西语维基小**，这是真事实。

⭐ **教训不是"记得数 forms"，是：同一个判据在 A 语言上成立不等于在 B 语言上成立** ——
而这个脚本是我从 fr 抄的，连它的隐含前提一起抄了过来。
这与本计划 §1.3 那条（把 fr 当唯一参照系）**是同一个根**，同一天里第二次。

**⑨ 🔴 词汇涨 2.9 倍，但翻译成本一分没涨**

```
              第一版        修正后
新增词形     219,756      643,117   ×2.9
其中真词头    47,581       47,581   ×1.0  ← 一个没变
其中变形     172,175      595,536   ×3.5
```

变位表里全是变形、没有新词头，而变形走 `infl_compose` 确定性生成，**零模型调用**。
⇒ 收词成本仍是 47,581 个新词头（25% 有频次），**pt 的收词工作量是 fr（442,178）的 1/9**。

```
              fr（实测）                pt（上界）
开工词形       385,216                 411,802
收完词形     2,086,292  ×5.4         1,054,919  ×2.56
新词头         442,178                  47,581   ← fr 的 10.8%
新词头有频次      13%                      25%
翻译成本      46.3 M tokens          约 5 M（外推，**不作承诺**）
```

🔴 那个 5 M 只作参照：fr 的 46.3 M 本身就是按 es 单价外推的估算。
真开跑前照 `FR_PLAN` §五.1 那两条办 —— ① 先跑 **1% 切片逐条读**；
② **输出有几个字段，控制组就要有几组判据**（`[[control-must-cover-every-output-field]]`
那次烧掉 418 万作废 tokens 直接导致欠费，根因就是控制组只量了一个字段）。

**⑩ 两次「截断样本出结论」的教训，同一天里第二次**
落点：`--quick` 报并集补 0.3%，全量 55.2%（差 184 倍）；
残差：`--quick` 报并集 22,808，全量 219,756（差 9.6 倍）。
⇒ **`--quick` 只能用来验脚本跑不跑得通、看形状，任何数字都不许进结论。**

### 🔴 因此 pt 会撞上一个 fr 没有的决策：要不要造 G2P

fr 明确**不造**（哑音/联诵/一音多写，规则性价比低，83.4% 就认了）。
pt 不一样，`FRAMEWORK.md:303` 早就把它列进待办：「pt / fr 的 G2P —— pt `ipa_br` 的剩余缺口
—— 📋 随 pt 里程碑」。而 it 已经趟过这条路，`[[it-pronunciation-layer]]` 的实测结论是：

> **73.4% 的音标是我们自己 G2P 算的** —— 计划里「内容不用重做」只对一半。

⚠️ 但 it 那轮同时留下了验收标尺的教训：`FRAMEWORK.md:664` —— it 的规则裸路径与人工源
一致率只有 **57.2%**（有背书的层是 87%），**低 30 个百分点＝独立证实那一层在猜**。
⇒ **pt 若造 G2P，必须先有独立验收依据**（法语版给的 18.0 万条欧葡人工值正好可以当标尺），
否则就是 it 阶段 ③ 的重演。

**这件事不在本计划里决定** —— 等阶段 -1 量完葡语版能给多少、法语版落点多少，
再带着实测数字请你定。⚠️ 已知的坏消息：法语版的 18.0 万只覆盖欧葡，
**巴葡（`ipa_br`）没有任何人工源可依**，而巴葡才是使用人口的大头。

---

## 四、开工前要你定的几件事（现在不必答，阶段 -1 之后带数字来问）

1. **收词收多少？** fr 那轮你定的是「全收」（`[[dict-scope-four-rules]]` 第①条压过成本）。
   pt 默认照此办理，除非阶段 -1 量出来规模差一个数量级。
2. **G2P 造不造？** 🔴 **2026-08-29 阶段 4 后推迟：现在做这个决定的判据还不存在。**
   实测缺口 416,648，但**有可见义项的词形已覆盖 94.0%**，缺的 90% 是没有自己义项的变形。
   而**能不能只补最常用的那批变形**，取决于 `freq_zipf` —— 那一列**阶段 5 才建**。
   ⚠️ `[[es-audio-and-examples-wip]]` 的教训正是这个：第一轮按 `level` 挑词挑错了
   （六个最常用词一个合成音都没有，反而给了 zipf 1.01 的生僻词），**判据必须换 `freq_zipf`**。
   ⇒ 阶段 5 建好频次层后，用「最常用的 N 万个变形里缺多少」重新算账再定。
   ⭐ 验收标尺现成：那 **352,364 个已有人工音标的词形**就是尺子，可在动手前量出规则的天花板

   ### ✅ 2026-08-30 定案：**不造 G2P**（阶段 5a 频次层落地后重算）

   | 判据 | 实测 |
   |---|---|
   | 读者面缺口 | **有可见义项的词形 94.0% 已有音标**（74,376/79,111）；按频次排最常用 1 万个词形覆盖 **92.4%**，只缺 763；前 5 万覆盖 85.0%，缺 7,499 |
   | 最大收益面 | 缺音标的 416,647 个词形里，**238,107 个是「变形、且词元有音标」** —— 规则 G2P 唯一有把握的那一类 |
   | 🔴 **而那一类正是规则必错的地方** | **元音变换（metaphony）**：`novo ˈno.vu → novos ˈnɔ.vus`、`porto ˈpoʁ.tu → portos ˈpɔʁ.tus`、`corpo ˈkoʁ.pu → corpos ˈkɔʁ.pus` —— 拼写只加一个 `-s`，词干元音从**闭 /o/ 开成 /ɔ/**。而它是**词汇性的不是音系性的**（`bolo/bolos` 就不变）⇒ 规则算不出来 |
   | it 留下的标尺 | it 的 G2P 裸路径与人工源一致率 **57.2%**（有背书 87%），而**意语既没有元音变换、也不需要双读音** ⇒ pt 只会更差 |
   | pt 的错会翻倍 | 每个词要出**两个**读音（`pt-BR`/`pt-PT`），G2P 的每个错乘以 2 |
   | 第一原则 | `FRAMEWORK`：**错比缺更伤权威**。45.8% 的总覆盖率里，分母塞满了读者永远查不到的生僻变位 |

   ⇒ 与 fr 同一处置（「不造 G2P ＝ 源头上限」）。**缺口记进收尾单，不记进计划缺口** ——
   它不是"还没做"，是"量过之后决定不做"（`[[record-the-negative-decision]]`：
   否定结论也是结论，要落账，否则下一轮又从头算一遍）。
   —— it 那轮最大的问题恰恰是**没有独立验收依据**（规则裸路径一致率 57.2% vs 有背书 87%）。
3. ~~**双读音怎么落表？**~~ ✅ **2026-08-29 已量完并定案**（照 `[[es-v3-structure-backfill]]`
   的规矩：照搬别的语言结构前先量这门语言有没有那个病）。两半分别是：

   **(a) 巴葡/欧葡二分 —— 照搬 fr/it/es 的 `pronunciation` 表，不新造列。**
   实测库内 84,307 行两种读音都有，**逐字节不同的 76,921 行 = 91.2%**，
   且抽读全是真音系差异不是记法噪声（词尾 `-s` 巴葡 `/s/`↔欧葡 `/ʃ/`、
   非重读 `e` `/i/`↔`/ɨ/`、`/ti/` 塞擦化、词尾 `-l` 元音化 `/w/`↔`/l/`）。
   ⇒ 双读音必须完整保留。而现有表结构本来就装得下三种情况：

   | 情况 | 落点 |
   |---|---|
   | 巴葡 / 欧葡 | `region = 'pt-BR' / 'pt-PT'` |
   | 巴西内部方言（`Carioca`/`Paulistana`…，见 C4）| `region='pt-BR'` + `tags=["Carioca"]` |
   | 法语版那 179,722 条不标地区 | `region = NULL`（注释原文「通用或**判不出**」）|

   🔴 我一度提议给 `pronunciation` 加 `variety` 列 —— **多余，已收回**。
   为一个已经解决的问题发明第二套结构，正是本项目反复吃亏的「契约的第二份抄写」。

   **(b) 建不建 `pronunciation_entry`？→ 不建，改用 fr 的 `pronunciation.pos` 列。**
   实测同一词形不同词性读音不同的有 **484 个**（es 那次真值 14，it 那张表是为 21,666 个建的
   —— pt 差 it 45 倍，用轻的解法）。484 条的规律是葡语的**元音交替（metaphony）**：
   名词闭元音 `/o e/` ↔ 动词开元音 `/ɔ ɛ/`（`acordo` 名 `/aˈkoɾ.du/` 动 `/aˈkɔɾ.du/`），
   **系统性的形态音位对立，压平会抹掉词义信息**（同 fr 那次「鼻化是音位对立」）。
   ⚠️ 484 是**下界** —— 只统计了英文版里有音标的 64,125 个词形，收词+跨版后会涨。

   ⭐ fr 是在阶段 8 之后的**族 D 评审**里才发现"读音要按词性归位"、补 `pos` 列是返工；
   pt 在阶段 -1 就量出来，可以一次建对（`PLAYBOOK` 开篇：顺序错了要返工，而返工最贵）。
4. **`pp_short` 62 行、`comparative` 6 行要不要留列？** fr 那轮 `comparative` 只有 5 行，
   结论是硬编码进展示层。pt 同形状，等你点头才删列。
5. **葡语本质字段怎么归位？**（整词级进 `entry` / 逐义项进 `sense` / 标签进 `sense_tag`）
   🔴 **这一条按 §1.3 的表参照 `es` 不参照 `fr`** —— 词法概念上 es 才是 pt 的近亲：

   | pt 的列 | es 的对应 | fr 有没有 |
   |---|---|---|
   | `vconj`（-ar/-er/-ir/pôr）| `conjugation` + `stem_change` | ❌ fr 是 `vgroup` 三组，判据不同 |
   | `pronominal` | `reflexive`（同一件事换个名）| ✅ 但 fr 的代词式动词行为与葡语不同 |
   | `pp` / `pp_short` | `pp`（无 short）| ✅ `pp`，无 `pp_short` |
   | `feminine` / `plural` / `comparative` | 三个都有 | ✅ 三个都有 |
   | — | — | 🔴 fr 的 `aux`/`vgroup`：pt **明确不设**，别照搬 |

   **归位方法**照 fr（`FR_PLAN` §五.4 那份表的形状），**归位内容**照 es。
   ⚠️ `gender` 大概率要两处都放（fr 实测 4.8% 的词逐义项不同 = `livre` m书/f斤 那族），
   但 pt 的比例**必须自己量**，不许照抄 fr 的 4.8%。

---

## 五、贯穿全程的三条（`PLAYBOOK` 十一节，一字不改）

1. **错比缺更伤权威。** 拿不准就不写，别猜。
2. **先问「是不是已经有人做好了」。** 跨版收割、真人录音、西语版的 `ks` —— 三次都是
   "我们没去拿"被当成了"没有数据"。
3. **可逆优先。** 发现自己在为一个不可逆方案设计一整套前置验收/分档/抽样流程时，
   先回头看方案本身能不能变可逆。

## 六、fr 那轮最该带过来的一条

> **闸问「有没有」，判官问「对不对」，而「判官的池子有没有洞」只有第三双眼睛问得出来。**

fr 的七个评审族里，四个是外审逮到的、两个是我读渲染成品读出来的、**只有一个是闸逮到的**。
⇒ pt 的验收里从一开始就排进**外审轮**（导渲染成品给两家挑错，实测约 3 元），
不要等到"做完了"才想起来。

并且：**做成闸的规矩守住了，写成文字的一条没守住**（`[[lesson-must-become-mechanism]]`）。
所以阶段 -2 的交付物里，`pt/tests/test_plan_ledger.py` 与 `pt/dbtool.py` 同等重要。

---

## 七、外审（2026-08-30，两轮）

### 7.1 用户问的是「你的 prompt 有偏袒性吗」——所以先做了偏袒性实测

> 「我担心你的 prompt 会存在引导性，因为前面都是你主导做的，我怕你的 prompt 会存在
>   给自己开脱的嫌疑，导致外审的结果偏向已有结果。」

**做法**：两版 prompt × 两家模型，材料完全相同（`render-dump` 走 contract-check 同一条渲染路径）。
一版带 8 条方向提示，一版纯开放式；**串行跑，跑第二版之前不读第一版**。
取样按固定种子在 10 个桶里分层（不是随机），四份原文全部落盘在 `data/work/pt/review/`。

**结论：那 8 条方向提示没有实质改变结果。**
- 所有大族**两版都出现**；
- 差异沿**模型分界线**（v4-pro 强在音系/时态，豆包逮到一条语义硬错），**不沿 prompt 分界线**；
- 最能说明问题的两条：我提示 ③ 点名「例句取错义项」，**开放版自己也逮到了** `quebra`；
  我提示 ④ 解释了巴/葡两行，**两版都没找到"标反"**，反而各自找到别的音标问题。

⚠️ 这个结论只对**这一次**成立，而且**不该由我判断** —— 四份原文都在盘上，可自行核对。

### 7.2 七个族，五个是我全部的闸够不着的

| 族 | 事 | 规模 | 我的闸为什么没响 |
|---|---|---|---|
| A | 非葡语混进语义关系（德语 3 + 希腊/西里尔/汉字 8）| 11 | 5b 收关系时**没有判据管 target 是不是葡语** |
| B | 同一 target 重复渲染 | **14,386 组** | 去重键含 `sense_id`，**同词不同义项指向同一个近义词**就绕过去了 |
| C | 例句译文用繁体字 | **466** | 控制表十条判据**没有一条查字形** |
| D | 近义标注行被当成例句 | 29 | 同 fr 收尾单「456 行根本不是例句」 |
| **E** | 🔴🔴 **变位时态标签**：复合时态名被拆成「A/B」，`comeria` 标成**陈述式将来时**（实为条件式）| **222,030** | `compose()` 把 `"/".join(tense)` 当并列，而**葡语时态名本身是复合的** |
| F | 小语种版塞进变位层的垃圾（西语变位表 68／德语散文句／`inv` 标记）| 100 | 从没有判据问过「这条变形是不是葡语」 |
| G | 变位行内容重复（读者看到同一行两遍）| **106,950** | B6 只查**跨来源**重复，`en+en` 自己撞的 1,322 组从建成起就看不见 |
| H | 🔴 **重音符夹在声母与韵核之间**（`bola /bˈɔlɐ/`）| **500,596** | 四道闸没有一条在看重音符的位置 |
| I | 葡语版没有「专名」类别，地名/人名全标成「名词」| 3,904 义项 | 忠实反映了源头 —— **缺陷在源头的类别体系，不在我们的转换** |
| J | 例句和出处**装反**（`text`＝书名，`ref`＝例句；日/中版 `text`＝译文）| 1,201 | 没有判据问过「这条例句里到底有没有这个词」 |
| K | 同一段引文从两个版本各收一遍（只差一个句点/一处源头错拼）| 163 | `UNIQUE(word,text)` 防的是"同一条进两遍"，不是"同一句话的两个抄本" |

⭐ **族E 是这一轮最值钱的**，而它是**外审先看见、我才顺着查清楚的**：
两家只点了 4 个词（`depenicará`/`desenluvássemos`/`sanguificarias`/`vociferaríeis`），
我以为是几条零散的标签啰嗦；回源用规则动词 `falar/comer/partir` 把**全部 29 个时态签名**
逐个对上之后才看清是 22 万行的系统性缺陷，而且最狠的一档是**说成了另一个时态**：

    comerei  future+present  → 将来时
    comeria  future+past     → **条件式**      ← 修之前两个都印「陈述式将来时」

信息本来就在源头：`continuative` 这个 tag 区分两个过去时，而它**根本不在 `COMPOSE_TAGS` 里**，
被静默丢弃 —— `[[prompt-self-harm-two-patterns]]` 的「缺失值被默认值填平」在确定性代码里的同一形状。

### 7.3 🔴 判据在这一轮被数据打回**七次**

一次都不是读代码看出来的，全是干跑/抽样/渲染打回的：

| # | 我写的判据 | 数据怎么打回的 | 正确的判据 |
|---|---|---|---|
| 1 | 繁体字：自己列 40 个字 | `我從沒去過英國` → `我从沒去過英国`（`沒`/`過` 不在表里）| 用 OpenCC，**283 → 466** |
| 2 | 标签去重：连续子串 | `podaste` 的「陈述式第二人称单数」不是「…简单过去时第二人称单数」的连续子串 | 切成**语法成分**比子集 |
| 3 | 外来变位：「权威版给了 ≥6 个形式还多出来的」| `aprazer→aprazera` 是**正确的葡语形式**，只是权威版收得不全 | 量小到能读完 ⇒ **逐条点名，不用判据** |
| 4 | 外来变位 v2：「同一格填了不同词形」| 678 对全 100% 冲突，逐条看是**冲突不是错误**（`boiar bóia/boia` 是 1990 正字法前后，**两个都对**；`togar` 反倒是**权威版错**）| 归 `[[conflict-deferred-final-pass]]`，记账 C25 |
| 5 | 🔴🔴 重音符：「必须紧跟 `.` 或串首」| `teˈzaw.ɾus` → `ˈtezaw.ɾus`，**差点毁掉 34.7 万条对的** —— **`ˈ` 本身就是音节分界** | **每个音节必须有元音核** |
| 6 | 重音符 v2 | `ɐɾ.kjˈtɛk.tu` → `ɐɾ.ˈkjtɛk.tu` 改坏了：欧葡 `qui` 弱化成的 `kj` **是一个音节，j 作韵核** | 再加「**ˈ 后面必须是元音**」（缺陷的定义就是"夹在声母与韵核之间"）|
| 7 | 例句装反：「例句里没有词头」| 抽 22 条大部分是**真例句**（`dar uma força → Me dê uma força`／`empandorgado → empandorgada`／`pau no cu de → pau no cu do`）| **缺席不是证据，在场才是** —— 必须加「而出处里有」|

⭐ 第 5、6、7 三次的**负控用例就是上一版判据误杀过的真实数据** ——
`fix_stress_position.CASES` 18 例里 10 例是负控，全部来自第一版的误伤。

### 7.4 逐条裁决表 —— 包括我判外审说错了的

> 用户要的就是这一张：「最后给你的会是一张逐条裁决表，包含我判它说错了的那些和理由 ——
>   那部分你可以专门盯着看我是不是在为自己开脱。」
> 🔴 每一条都回源核对，**不取多数**（`[[verify-before-claiming-confirmed]]`：模型共识不是证据）。

**判「外审对」并已修（按族归入 7.2）**：`outono` 德语／`parabéns` 希腊语与繁体字／
`rotar`·`esquentar`·`meio`·`molibdénio`·`melancia-da-praia`·`metias`·`musaranhos pigmeus`·
`acabado`·`adocicado`·`embalada`·`podaste` 各类重复／`depenicará`·`desenluvássemos`·
`sanguificarias`·`vociferaríeis` 时态／`bola`·`magoado`·`feriar`·`metias` 重音符位置／
`acérrima`·`gamões`·`musaranhos pigmeus` 的「变位形式」标题／`Itaúna do Sul` 词性／
`godo` 例句重复。

**判「外审说错了」——逐条给理由**：

| 外审说 | 谁说的 | 我的裁决 | 理由（回源） |
|---|---|---|---|
| `bolar` 只该是动词，不该标「形容词/动词」| 两家都说 | ❌ **外审错** | 源头有一条真形容词义项：`clayey, argillaceous`（黏土的），来自 `bolo/bol`。**它有 9 条动词义 + 1 条形容词义，标签是对的** |
| `bola` 的引文日期 `29 mai 2026` 是未来日期 | v4-pro | ❌ **外审错** | 今天是 2026-08-30，5 月 29 日是**过去**。模型的知识截止造成的误判 |
| `laroteiro` 释义完全错，该是「好说谎的/骗子」| 豆包 | ⚠️ **不改，记账 C29** | 葡语版源头**原文就是** `preguiçoso` / `astuto`，我们的中文是忠实翻译。豆包给的是词源学论证（`larota`＝谎言），**一家之言不足以覆盖源头**。要改得先回权威葡语词典 |
| `Colorado` 的市镇义项标阴性是错的，该是阳性 | 豆包说阳、v4-pro说不确定 | ⚠️ **不改，记账 C30** | 源头逐义项给的就是 `f`（河流/州给 `m`）。葡语里市镇名的性本就摇摆（`o município` 阳 vs `a cidade` 阴），**两家自己都没达成一致** |
| `Itaúna do Sul` 缺音标、缺阴阳性 | 两家都说 | ⚠️ **只修词性** | 缺音标是源头没有（`[[dict-scope-four-rules]]` ②：价值在背书不在填空，不造 G2P）。缺性同上一条 |
| `acabado` 词头的「名词后」是误标 | 两家都说 | ⚠️ **记账 C26** | `adj_pos` 列，源头确有此标注（形容词位置）。渲染成「名词后」确实容易读成病句，属**展示措辞**问题不是数据错 |
| `acrianceis` 该拼 `acriançeis`／`deçamos` 该是 `desçamos`／`homicidiarem` 该是 `homicidar`／`electroencefalograma` 该是 `eletro-` | 各一家 | ⚠️ **不改，记账 C27** | 四条都是**源头收录的词形**（含历史正字法与异体）。词典收异体是对的；改拼写＝**替源头做正字法裁决**，超出这一轮范围 |
| `godo` 的近义 `gode`/`gogo` 语义无关 | 豆包 | ⚠️ **记账 C28** | 源头（葡语版）确实这么列的。这是族A 的同一根因（关系层没有语义合理性判据），但**判"两个葡语词是不是近义"不是确定性判据能做的**，只能逐条送判官 |
| `quadro` 的中文里混着 `or`、近义里夹着英文 `frame`、EN 写成 `an theater act` | v4-pro | ✅ **对，记账 C31** | 确认存在。`or` 与英文残渣是 1.5b 译文与 5b 关系的残留，可确定性清；`an theater act` 是**源头**的语法错，不动 |
| `quebra` 例句译文「考练/考官/驾校」 | 两家给了**三个互相矛盾**的答案 | ⚠️ **记账 C31** | 英文原文 `a bribe given to the instructor`，葡语原句 `o suborno que os alunos dão à autoescola` —— **英葡两边本身就不一致**（教练 vs 驾校）。不是我们译错，是源头两句说的不是一回事 |
| `fibra de vidro` 例句原文 `é feita e um` 是语法错 | v4-pro | ✅ **对，但不改** | **源头原文如此**。改例句原文＝伪造引文 |
| `colégio` 的「校区」误译 `campus` | v4-pro | ✅ **对，记账 C31** | 确认；1.5b 译文问题 |
| `fazer amor` 标注「pôr 类」 | v4-pro（自标不确定）| ⚠️ **记账 C31** | 需核 `vconj` 的模板归类，本轮未查 |
| `melancia-da-praia` 的 EN 释义不完整（`A plant in the genus Solanum:`）| v4-pro | ✅ **对，不改** | **源头就是这样截断的**，属 `[[dict-scope-four-rules]]` 的源头上限 |
| 一批词「释义（待补）」（`gamões`/`panspermias`/`xosa`/`machangana`/`processo coracoídeo`/`lóbulo occipital`）| 两家都说 | ✅ **对 —— 这就是 C20** | 我自己的收尾单早已记着：**1,800 条交叉引用义项能免费补中文**（去库里取被引词的中文）。外审独立指出同一件事，提高了它的优先级 |

### 7.5 这一轮暴露的两个**机制**缺口（已做成闸，不是记在文档里）

1. 🔴 **闸打印的回滚命令指向一个已被删掉的文件。**
   实测：同一个脚本连着跑两版，第一版抛 `IntegrityError` 走异常路径**自删备份**（那次是对的，
   事务已回滚），第二版闸红时打出的 `cp` 路径已经不在 —— 照着敲得到 `No such file`，
   **而我以为自己手上有回滚点**。⇒ `dbtool` 现在先确认文件存在，不存在就打「🔴🔴 没有回滚点，只能向前修」。

2. 🔴 **`MAX_BACKUP_BYTES = 8GB` 在 pt 上已经吃满**：11 个 `keep-` 里程碑约 7.2 GB，
   于是**每次写库的备份一生成就被保留策略砍掉**，回滚点实际上从不存在。
   这是既定策略（用户 2026-08-25「该删就删，果断一点」），**不擅自改**，但后果需要你知道。

3. 🔴 **闸和它守的那段逻辑用了两个不同判据**（`[[fix-regression-and-gate]]` 第 N 次）：
   F7 我在闸里用 SQL 重写了一版 `INSTR(LOWER(...))`，与修复脚本的 `fold()`（去变音符）
   不是同一件事 ⇒ 报 5 条红，**其中 3 条是修复脚本漏掉的真缺陷、2 条是闸自己的假红**。
   统一成**词边界**匹配、闸 `import` 生成侧那份之后两边都对，并因此多修了 8 条。

---

## 八、C20/C33 补中文（2026-08-30）——「先证明免费路径走不通，才许开口报价」

用户问「大概多少」。按 `[[prove-free-path-before-quoting]]`：**报价必须带「试过哪些免费路径、
各自为什么不行」，写不出就是没调查完**。所以先把免费路径走到收敛，分母才是真的。

### 8.1 免费路径：义项侧 1,107 条、例句侧 13 条，一分钱没花

| 路径 | 结果 |
|---|---|
| 取被引词的中文 | ✅ 899 |
| 指针链走到底（`Baharem → Bahrein → Barém → 巴林`）| ✅ 23（跳 1 次）|
| 词性消歧（`abissim [adj] 阿比西尼亚的` / `[n] 阿比西尼亚人`）| ✅ 107 |
| 同词异体（`Primavera ← primavera`，归一后同词 ⇒ **多义不是歧义**）| ✅ 48 |
| 构词/裸词指针（`pequenico → 指小形式`、`abelh- → abelha`）| ✅ 约 90 |
| 例句原文已被译过（答案文件按内容作键）| ✅ 13 |
| 🔴 **归一模糊查被引词** | **否掉** —— 9 条里就有 `parquet ← parqué` 匹配到 `parque`（**拼花地板 vs 公园**）。葡语变音符是区别性的，归一只许用于「词头与被引词是同一个词」 |
| 🔴 **源头自带中文译文** | **0 条**。589 条有 `src_translation`，全是俄/波/捷/日；那 25 条「含汉字」是**日语汉字**（`src_lang='ja'`）|

跑到**连续两轮 0 条**才停。

🔴 **判据在这一步被打回三次**：①直接抄 ⇒ 被引词的中文自己也是指针 ⇒ 要走链
②"指针式中文"判据一跑圈 **4,956 条全是真释义**（`tesauro → 同义词词典`／`dar → 同意发生性行为`）
—— **同一形状第三次**，收窄成「整条只由拉丁词＋关系标签构成」
③`sense_gloss` 有 `kind`/`seq` 两列我没看，把同一义项的两行当成"多义"整条跳过。

### 8.2 顺带捞回 363 条从没进过库的屈折关系

渲染 `gamões` 时看见「（待补）」正下方就是「词形变化 gamão 的 复数」⇒ 问变形层覆盖了多少：
541 条名词性指针里**只有 105 条**有。剩下的源头明明白白写着 `Cipiões → plural de Cipião`，
而 **阶段 2b 只读结构化 `form_of`、2c 只读变位表 `forms`，没有任何一步读过「写成散文的 form_of」**。

⇒ **先补结构（+363）、再按「变形层已经说清楚了吗」隐掉冗余指针（488），顺序不能反。**
🔴 **反向查第三次立功**：命中的 509 条里 **21 条已有真中文**（`cento e uma → 一百零一`，
源头写的是「指针**加**释义」，分号后面才是内容）⇒ 放过。

### 8.3 报价与实花

报价 **≈0.9 元**（拿本项目实测单价折算：1.5b 60,167 条 /366 万 token/8.9 元；
5d 39,454 条 /336.5 万 token/8.7 元）。**实花 ≈0.63 元**（例句 138,043 token + 义项 ≈105,000 token，
周日空闲时段半价）。

| | 之前 | 之后 |
|---|---|---|
| 可见义项有中文 | 172,046（99.17%）| **172,930 / 173,490 ＝ 99.68%** |
| 可见例句有中文 | 48,132（94.3%）| **49,906 / 51,019 ＝ 97.8%** |

### 8.4 三条纪律在这一步的实际作用

🔴 **① 这批不能靠重跑 1.5b 解决 —— 模型早就答过了，答的是空。**
1,299 条全部在 `def_zh.jsonl` 里躺着空答案：它老实执行了我写的规则 4「参见…→空」和
规则 8「看不出意思→空」。**同一套 prompt 重跑必然还是空**（`[[retry-must-converge-or-drop-loud]]`）。
而那两条规则对这一批恰恰是误伤（`[[prompt-self-harm-two-patterns]]` ①）——
对一个缩合词，「de 与指示代词的缩合」**就是**它的释义，不是"元描述"。
⇒ 新写 `pipeline/translate_xref.py`，`translate_defs.py` 一个字没动
（判据跟着任务走，不跟着代码复用）。

🔴 **② A1 桶（被引词不在库里）模型自己留空 63.3%**（656 条给不出 415 条）。
这是我在规则里写死的「不确定就留空，宁可空着也不要编」—— 这一桶我**无从核对**
（`[[blind-gloss-inference-ceiling]]`：无源可查时错误率卡在 24–25%）。**它不答，我就不逼它答。**

🔴 **③ 硬闸红了一次（4.08% > 2%），没放宽阈值。** 53 条输出是 `agerato 的替代拼写`
这种纯指针 —— 对读者和空白一样。处置是**不写它们**（与留空同类计入并单独打印），
**变的是写进去的数据，不是「什么算坏」的定义**（`[[proxy-metric-gets-optimized]]` 的反面）。
⚠️ 同时逮到自己又在两处写了两份判据：`translate_xref` 里那份锚在串尾，
把 `hamartiologia → 研究罪的…神学分支；更佳形式为…` 这种**真释义**也圈了 ⇒ 改成 import 共用那份，
扩标签表后负控 9 条全过。

---

## 九、外审第二轮（2026-08-30）——「一整轮工作的账，之前只写在脚本注释里」

⚠️ **这一节是 2026-08-31 补写的。** 上一轮把推理写得很足，但**全写在两个脚本的
docstring 里**，`PT_PLAN` 正文与收尾单一个字都没有 —— `ingest_page_form_of.py`
里明写着「157 条记账（C37）」而收尾单里根本没有 C37 这一行。
`[[record-the-negative-decision]]` 的同一形状：**结论有了、台账没有，下一轮无从知道已经问过。**

### 9.1 第二轮怎么跑的

用户问「pt 是不是也得到了两家的肯定」—— **没有**。第一轮 prompt 第一句就是「找出其中错的地方」、
末句「没问题的不用列出来」，四份回来的全是**错误清单**，**没有一句是"这部词典可以了"**；
而且第一轮之后又改了七族（50 万条音标移位、22 万条时态标签、3,904 条词性…），
**送审的那份材料已经不是现在这份**。

⇒ 拿**修完之后**的渲染成品重跑一轮：59 个词条（按七族分层取样 ＋ **混进第一轮我判"外审说错了"的
16 个词、不做标记**），prompt 与第一轮**逐字相同**（同一把尺子才能比），两版 × 两家。

⭐ **最重要的一条结果：这一轮四份报告没有一条指向我这次改的七族。** 它们挑的音标问题
全在辅音与词尾元音（`casa /ˈkasɐ/` 该 `/ˈkazɐ/`），**没有一条是重音符位置** —— 而重音符
正是我动了 50 万行的地方。弱证据，但方向是对的。

### 9.2 三件已做（都已落库，各自带一道闸）

| 族 | 事 | 规模 | 闸 |
|---|---|---|---|
| L | 例句的「中文」栏里一个汉字都没有（`model:example` 65 跑批回了英文／`zh-edition` 23 源头把英译标成中文）⇒ **删该译文行，例句本身留着** | 88 | **F9**（变异已打） |
| M | `Rio Grande do Norte` 丢了「北」，与 `do Sul`（南）混掉 | 5 补「北」＋2 归一 | **A9**（变异已打） |
| 2d | **页面级 `form_of` 补进变形层** | **+5,613**（`src='pt-edition-page'`） | 账的闸阶段表 |

🔴🔴 **阶段 2d 才是这一轮最值钱的：「归变形层」和「变形层去收」之间有一条缝，东西掉进去了。**

    1.5a 义项层   读页面的 senses   → 「指针义项归变形层」⇒ 跳过
    2b   变形层   读 form_of        → 只读英文版，没读 pt/fr/zh 版
    2c   变形补链  读词元页的 forms   → 方向相反（词元→形式），覆盖不到

三步各自都有道理，**合起来谁也没收**。19,404 个词形点进去一个字都没有，
而源头明明白白给了它们独立页面和释义（`plural de junqueira`）。
**这类洞不会被任何单步的闸发现 —— 每一步的不变量都成立，缺的是"步与步之间"。**

⇒ 顺出来的第二个洞在 `ingest_pt_senses.py`：`d.is_lemma=1` 这个限定
**把一整类排除在取数之外**（`[[llm-as-evaluator-discipline]]` ⑫）。`is_lemma` 是**我们自己打的标**，
拿它决定"要不要去源头找释义"＝用自己的分类限制自己的取数。放开后实测
**5,198 个词形 / 8,843 条义项**是源头有而我们从没收的。

### 9.3 判据在这一步被打回两次

🔴 **`form_of.word` 不可信** —— wiktextract 在 pt 版上常把散文释义的片段塞进去
（`baderna → form_of=[{word:"terceira pessoa"}]`，真实原形是 `badernar`）。
🔴 **「最后一个 de 之后」也不行** —— 形式代理，多词原形上就断
（`anéis de Einstein` → 取到 `Einstein`，正确是 `anel de Einstein`）。
⇒ 判据 **import `ingest_prose_inflections.PTR`**（判据只许一份）。

⭐ **范围按目的定，不按"源头有没有"定**：全量 5,770 条里 157 条的词形**已经有完整词条**，
那些**不补** —— `estrela` 有 11 个义项「恒星」，印上「estrelo 的阴性」是误导；
`Filipinas → Filipina 的复数`**根本是错的**。这一步的目的是「让空白页有内容」，
不是「把源头的每条指针都搬进来」。⇒ 157 条记账 **C37**。

---

# 📋 pt 收尾单

> 建于开工前，空表。**规矩同 fr**：从现在起「这条缺陷是真的」不再是「现在就做」的理由，
> 记账只能进这张表，不许在正文里留游离的 📋。

| 档 | 判据 | 处置 |
|---|---|---|
| 🔴 **出版阻断** | 读者看到的是**错的** | 发布前必须清 |
| 🟡 **可发布** | 读者看到的是**没有** | 记账，发布后按价值排期 |
| ⚪ **不是缺陷** | 我当初量错了 / 已被后续步骤解决 | 划掉 |

## 一、🔴 出版阻断

| # | 事 | 规模 | 现状 |
|---|---|---|---|
| — | （空）| | |

## 二、🔴 计划缺口

| # | 事 | 规模 | 备注 |
|---|---|---|---|
| B1 | ~~变形层 32.7 万词形无音标~~ **⚪ 已裁决（§四.3）：不造 G2P ＝ 接受源头上限**，与 fr 同一处置。理由有实测支撑：可推导的 238,107 个词形**恰恰是元音变换（metaphony）那一类**，而它是词汇性的不是音系性的（`novo/novos` 变、`bolo/bolos` 不变）⇒ 规则必错；it 的 G2P 裸路径一致率只有 57.2%，而意语既无元音变换也不需双读音。缺口转记 C7（读者看到的是「没有」不是「错的」）| 词形音标覆盖 352,364/771,585＝45.7% | 已裁决，非缺口 |

## 三、🟡 可发布，记账

| # | 事 | 规模 |
|---|---|---|
| C1 | 四个 `_src` 来源列建了从没回填 —— **2026-08-31 复核：仍然是 0**，且现在量得出分母：`ipa_br` 84,786 行有值／`ipa_pt` 84,625／`translation` 412,884／`gender` 59,253，**这些值一条都说不出自己是哪来的** | 4 列，说不出来源的值 64.2 万 |
| C2 | ⚪ **已解决**（阶段 5）：`example` 现在 **51,315** 条 | ~~0~~ → 51,315 |
| C4 | 🔴 **葡语版的音标标的是巴西内部方言**（`Carioca`/`Paulistana`/`Caipira`/`Gaúcha`/`Paranaense`）。**2026-08-31 复核，账上 6,163 是 dump 上的数、不是落库的数**（`[[measure-landing-not-source]]`）：落库后葡语版来源 **1,316** 条带这些标记，**全来源 24,219** 条 —— 标记原样存在 `tags` 里没丢，但两列 `region` 模型仍装不下内部方言 | 落库 1,316（全来源 24,219）|
| C5 | ⚪ **已解决**：灌库时排除了 —— 全库 `is_sampa` 判据实测 **0 条**（回归闸 C3 常查）| ~~132~~ → 0 |
| C7 | **有可见义项却无音标的词形** —— 🔴 **2026-08-31 复核：4,735 → 43,197，涨了 9 倍**，但**不是回归**：阶段 1.5a 与 2d 让大批词形**第一次有了可见义项**，分子跟着变大（分母 771,585，有音标的 352,364）。仍是 🟡「读者看到的是没有」 | ~~4,735~~ → **43,197** |
| C8 | ⚪ **已解决（`fixes/fix_inflection_dedup.py`）**：`tôdas → tôda 复数` 从 5 行变 1 行，全库「同 (word_id, base, label_zh) 重复组」＝ **0**。🔴 **是 2026-08-31 给回归闸的 ACCEPT 锁数字之后才发现它早好了** ——旧机制按名字豁免，B4 这条断言即使非零也不会红，等于**从修好那天起就没人再看过**。基线已撤（见 C38） | ~~1,370 组 / 2,766 行~~ → 0 |
| C9 | ⚪ **阶段 2b 有 20 条「变形指向自己」，逐条读完大部分是对的**：葡语规则动词的**人称不定式**与**虚拟式将来时**同形于不定式（`que eu orlar`）、`-e` 结尾形容词阴阳同形（`avolescente`）。只有 `fugitiva 的阴性是 fugitiva`、`rafeiros 的阳性复数是 rafeiros` 两三条可疑 ⇒ **不是缺陷族，是我把别的语言的直觉当断言了** | 20（其中可疑 ≈3）|
| C10 | 孤儿词形（点进去空白页）—— ⭐ **2026-08-31 复核：64,785 → 21,184**（阶段 2d 补了 5,613，1.5a 的新义项吃掉其余）| ~~64,785~~ → **21,184** |
| C11 | `inflection.entry_id` 留空 —— 🔴 **2026-08-31 复核：132,660 → 138,601，涨了 5,941**：**阶段 2d 与 C21b 新插的 6,092 行也没有 `entry_id`**（根因同旧账：只在 `forms`/页面里出现过的词形只有 `dict` 行没有 `entry` 行）。⚠️ 展示层查变形**必须走 `inflection.word_id`** | ~~132,660~~ → **138,601** |
| C12 | `label_zh` 退化成兜底「变位形式」（`compose()` 认不出那组 tags）—— 复核 3,371 → **3,494**（+123 来自 2d/散文屈折）| ~~3,371~~ → 3,494 |
| C13 | 🔴 **`example` 表里有 539 条不是句子，是词缀词条下的构词式**（`livro (“book”) + -eto → livreto`）。**已排除出翻译池**（1% 切片实测：给它们付钱翻出来的是**错的** —— `alemão`（阳性）`+ -ã → alemã`（阴性）被译成「德语 + -ã → 德语」，性别对立整个丢了）。⚠️ **数据一个字节没动**，照 fr 先例「别在数据里砍」⇒ 交阶段 8 决定渲染成什么 | 539 |
| C14 | 阶段 5c 有 **29 条 `bold` 高亮坐标指向别的字符串**（源头按英文译文算的偏移），已整组置空、文本未动 | 29（已处置）|
| C15 | 源头噪声：把人称代词标成反义。**2026-08-31 复核：不是 1 条是 3 条**（`eu`/`tu`/`você` → `ela`）。照实收、记源，展示层不特殊处理 | ~~1~~ → 3 |
| C16 | `lang='zh'` 的释义里没有汉字 —— 复核 3 → **45**，⚠️ **但绝大多数不是缺陷**：`C Sharp → C#`／`DVD-ROM`／`ALGOL`／`666`／`-opia`，**这些词的中文本来就没有汉字**。⇒ 🔴 **有意不给 `sense_gloss` 加 F9 那样的闸** —— 例句译文必须有汉字（句子），释义不是（`[[criteria-from-meaning-not-form]]`：那会是个形式代理）| 45（真缺陷远少于此）|
| C17 | `sense.entry_id` 有 **21 条为空**（20 个词的义项没链到词条）—— 阶段 1.5a 的判据事故就是它引出来的 | 21 |
| C18 | 🔴 **1,441 个词头的"释义"来自非葡语版**（`abdomen → "Abdomen."`／`autogiro → "автожир"`）—— `[[gloss-three-languages]]`：各版只给自己语言的词写定义、对外语词只写翻译 ⇒ **性质不同、判据不同，没混进 1.5a**，另议 | 1,441 |
| C19 | **618 个词头源头压根没有释义**（九版全扫）—— 花钱也买不到 | 618 |
| C20 | ✅ **已做（2026-08-30）：免费补了 1,007 条中文，一分钱没花**（`fixes/fill_xref_zh.py`，`src='xref:resolved'` 一条 SQL 可全撤）。做法＝取被引词的中文；**判据被打回三次**：①直接抄 ⇒ `Baharem → Bahrein → 「Barém 的异体形式」`，**被引词的中文自己也是指针** ⇒ 要顺着链走到底（23 条走了 1 跳）②"指针式中文"判据一跑圈 4,956 条，里面全是真释义（`tesauro → 同义词词典`／`dar → 同意发生性行为`）—— **同一形状第三次**，收窄成「整条只由拉丁词＋关系标签构成」③`sense_gloss` 有 `kind`/`seq` 两列我没看，把同一义项的两行当成"多义"整条跳过。**剩余 1,887 条见 C34–C36** | ~~1,800~~ → 已补 1,007 |
| C34 | 交叉引用剩余①：**被引词有多个带中文的义项、词性也分不开** ⇒ 不猜（猜错就是用户点名的「义项和释义错配，那才是真灾难」）。**2026-08-31 按今天的可见口径重数：423 → 20** | 20 |
| C35 | 交叉引用剩余②：**被引词不在库里**，免费路径到此为止。重数 409 → **398**；另有 **12 条被引词在库、但它自己也没有中文** | 398 + 12 |
| C36 | 无中文的可见义项里**判据够不着**的（不是交叉引用也不是变位指针）。重数 1,055 → **87**；另有 **45 条连葡语原文都没有**（无从判断） | 87 + 45 |
| C21b | ✅ **已做（2026-08-30）：源头写成散文的屈折关系补进变形层 363 条**（`fixes/ingest_prose_inflections.py`）。⭐ 这不是"隐噪声"是**免费捞回真结构** —— 阶段 2b 只读结构化 `form_of`、2c 只读变位表 `forms`，**没有任何一步读过"写成散文的 form_of"**（`Cipiões → plural de Cipião`）。补完之后按「变形层已经说清楚了吗」隐掉 488 条冗余指针义项（**顺序不能反**：先补结构再判冗余）。🔴 **反向查第三次立功**：判据命中的 509 条里有 **21 条已有真中文**（`cento e uma → 一百零一`，源头写的是「指针**加**释义」，分号后面才是内容）⇒ 放过。不收 `diminutivo`/`aumentativo` 39 条（构词不是屈折，见 C26）| +363 / 隐 488 |
| C20old | ⚪ **已被 C20 取代**（同一件事的旧口径，1,800 条里 1,007 条已免费补上）| ~~1,800~~ |
| C21 | ⚪ **已被 C34–C36 取代**：这 1,094 条是补中文**之前**的口径，其中 `plural de X`/`feminino de X` 那部分已由阶段 2d 与 C21b 收进变形层。**2026-08-31 重新分桶，A2 的 610 条已 100% 归位**（398+87+45+38+20+12+10），不再需要这条 | ~~1,094~~ → 已拆分 |
| C23 | `ipa_br`/`ipa_pt` 仍作兜底：**12 行有 `ipa_br` 却没有 `pronunciation` 行**，去掉兜底那些词的音标就没了。不是债务，是阶段 4a 的源列 | 12 |
| C24 | 例句出处里 10 条英文著录（`page 13`／`in:`）判据够不着，未隐 | 10 |
| C25 | 🔴 **678 对(词元,版本)在同一格填了不同词形，2,278 行** —— 逐条看是**冲突不是错误**，四种情况混在一起：`apanhar→aparelha`(fr 版挂错了动词) / `convencer→convencce`(fr 模板造假词形) / `boiar bóia vs boia`(**两个都对**，1990 正字法前后) / `togar togue vs toge`(**权威版才错**)。归 `[[conflict-deferred-final-pass]]`，要逐条裁 | 2,278 |
| C26 | `acabado`/`adocicado` 词头印「名词后」（`adj_pos` 列，源头确有此标注）。数据不错，**展示措辞**容易读成病句 ⇒ 阶段 8 措辞题 | 未量 |
| C27 | 外审点名的四条拼写：`acrianceis`／`deçamos`／`homicidiarem` **三条在库、都是源头收录的历史/异体形式**，改＝替源头做正字法裁决 ⇒ 不改。⚪ **第四条 `electroencefalograma` 复核不成立** —— 库里根本没有这个拼写，只有 `eletroencefalograma`（现行正字法）| ~~4~~ → 3 |
| C28 | `godo` 的近义 `gode`/`gogo` 语义无关（族A 同根因：关系层没有**语义合理性**判据）。⚠️ 判"两个葡语词是不是近义"**不是确定性判据能做的**，只能逐条送判官 —— 全库 153,452 条关系，要先估成本 | 未量 |
| C29 | `laroteiro` 的中文（懒惰的/狡猾的）忠实翻自葡语版原文 `preguiçoso`/`astuto`，但豆包按词源（`larota`＝谎言）判该是「好说谎的/骗子」。**一家之言不足以覆盖源头**，要改先回权威葡语词典 | 1 |
| C30 | `Colorado` 市镇义项的性：源头逐义项给 `f`，两家外审自己都没达成一致（葡语里 `o município` 阳 vs `a cidade` 阴本就摇摆）| 未量 |
| C31 | 1.5b 译文残渣与源头不一致的一小把：`quadro` 中文里混着 `or`／近义里夹着英文 `frame`／`colégio` 的 `campus` 误译成「校区」／`quebra` 的英葡原文本身就不一致（教练 vs 驾校）／`fazer amor` 的 `vconj` 归类待核 | 5 词 |
| C32 | 族J 对调的 1,193 条里**约 342 条其实是「一句话被拆到两个字段」**（`reificação`：`…uma mercadoria (` ⊕ `commodity) exemplifica…`），不是装反。⚠️ **对调不丢数据** —— 旧 text 原样留在 `ref` 里，前半句还在行内，随时能拼回 | 约 342 |
| C33 | ✅ **已做（2026-08-30，见 §八）**：族J 对调后的 1,193 条例句连同库里原有的无中文例句一起补完，**可见例句有中文 48,132（94.3%）→ 49,906 / 51,019 ＝ 97.8%**（实花约 0.63 元，周日空闲半价）。⚠️ 剩余的没有再逼模型答 —— 它留空的那些是我自己写的规则「看不出意思就空着」在起作用 | ~~1,193~~ → 已补 |
| C22 | 1.5b 译文的**一致性**：同一概念两种写法 —— `男性名`(**189**) vs `男性名字`(**68**)、`巴西巴拉那州市镇` vs `…城市`。复核后小幅增长（新义项带进来的），不影响正确性，可确定性归一 | 约 360 |
| C6 | **只有 it/es 版词条的词形，`is_lemma` 不可信** —— 那两版根本不标变形。复核 483 → **454** | 454 |
| C3 | ⚪ **已解决（2026-08-31）**：阶段 4 的交付物判据已收紧成「`pronunciation` 非空 **且** pt-BR 非空 **且** pt-PT 非空」（实测 321,540 / 297,241）。旧版只查整表非空 —— **补了一种读音、另一种是 0 也能过** | ~~1 条断言~~ → 3 条 |
| C37 | **页面级 `form_of` 里有 157 条，词形已有完整词条 ⇒ 有意不补**（`estrela` 11 个义项「恒星」，再印「estrelo 的阴性」是误导；`Filipinas → Filipina 的复数` 根本是错的）。**源头这么写不等于该这么展示。**⚠️ 这是一条**否定结论**，落账是为了下一轮知道「已经问过」（`[[record-the-negative-decision]]`）| 157 |
| C38 | ⭐ **2026-08-31 回归闸的「已接受基线」改成锁数字**：原来 `ACCEPT` 按**名字**豁免，「已接受」等于「不再看」。当场兑现在三条上 —— A2 从 2,888 掉到 610 而理由文案还写着 2,888；F3 从 539 涨到 643（**+104**）闸一声没吭；B4 期望 2,766 实际早已是 0。⇒ 期望值写死：**超了红、低了要求收紧**，判据一个字没改。🔴 顺手修了「闸的闸」：`mutate()` 的临时连接漏注册 `same_sentence`，**变异验证从 F8 加进来那天起就一次都没跑起来过**。现在 10/10（新增 F9 / A9 / M-ACCEPT 三条）| 3 条基线 |
| C39 | ⚪ **我记错了，已改正（2026-08-31 当天）**：这不是「38 条源头噪声」——**16 条是我判据宽误圈的真释义**（`cimão → forma parte da locução de cimão…`／`azinhavrar → infinitivo impessoal`，后者与 C9 同族＝葡语真事实），另 **22 条是「单复同形」的真信息**：回源核 `ptwiktionary` 证实源头给不变复数**单开一页**（`Dóris [noun] 'prenome feminino'` 与 `Dóris [noun] 'plural de Dóris'` 是两个词条）。**隐掉＝删掉真信息。** ⇒ 处置从「隐」改成「补中文」，已补 **21 条**（`fixes/fill_invariant_plural_zh.py`，`src='invariant-plural'` 一条 SQL 可全撤）。⚠️ `fura-filas` 有意排除 —— 它另一条义项写着 `o mesmo que fura-fila`，自指那条是源头笔误 | ~~38 噪声~~ → 补 21 |
| C40 | ✅ **已做（2026-08-31）**：交叉引用又免费补了 **8 个义项 / 9 行**（`arrancamento → o mesmo que arrancada → 拔出；抽出；起步`）。它们是 C20 跑完**之后**才由阶段 1.5a/2d 新收进来的义项，不是判据漏了 ⇒ **直接重跑 `fill_xref_zh.py`，没写新脚本**。⚠️ 我手搓时数出 10 条，脚本只认 8 —— 另 2 条被它的不变量挡下（补进去的中文本身还是指针）| ~~10~~ → 8 |

## 四、2026-08-31 全表复核：哪些是真的重数了，哪些不是

用户「把收尾单整张表拿库跑一遍，把已经不成立的划掉」。**43 条里 27 条能确定性回库核，16 条不能。**

**⚪ 五条已经不成立**（C2 例句 0→51,315／C5 SAMPA 132→0／C8 变形重复 2,766→0／
C3 音标断言已收紧／C20old 与 C21 被 C34–C36 取代），另有 **C27 的第四条**不成立
（库里根本没有 `electroencefalograma` 这个拼写）。

**🔴 三条是涨的，都查清了不是回归**：C7 4,735→43,197（1.5a/2d 让大批词形第一次有可见义项，
分子跟着变大）／C11 132,660→138,601（**2d 与 C21b 新插的 6,092 行也没有 `entry_id`**）／
C16 3→45（**而且绝大多数不是缺陷**，`C#`/`DVD-ROM` 的中文本来就没有汉字）。

⭐ **A2 的 610 条第一次 100% 归位**：398＋87＋45＋38＋20＋12＋10 ＝ 610。
分桶过程里判据被打回一次 —— `referent()` 返回的是 `(词, 词性)` 元组，我拿它跟字符串集合比，
**565 条全落进同一个桶**。形状不对（一个桶吃掉全部）比数字不对更早暴露问题。

**⚠️ 十条有意标成「未重核」，不是忘了：我的快查判据比账上那条宽，硬报就是假变化**
（`[[measure-landing-not-source]]`）：

| # | 账上的判据 | 我的快查圈到 | 为什么不能算 |
|---|---|---|---|
| C14 | 29 条 bold 坐标已置空 | `bold IS NULL` 7,618 | 分不出「置空过的」与「本来就没有的」 |
| C18 | 释义来自非葡语版的词头 1,441 | `sense_src.lang<>'pt'` 82,783 | 证据层来自英文版是**常态**，与"释义是外语"是两回事 |
| C24 | 出处里的**英文**著录 10 | `in:%`/`page` 2,692 | `in: Crónica de D. Fernando` 是**葡语**著录格式 |
| C25 | 同(词元,版本)同格不同词形 678 对 | 同(base,label) 14,858 组 | 少了「同一版本、同一格」这两个限定 |
| C32 | 一句话被拆到两个字段 约 342 | ref 首字母小写 4,732 | 那是形式代理，判「是不是同一句」要读 |
| C19 · C26 · C28 · C29 · C30 · C31 | — | — | 要回权威源或逐条读，库里查不出来（C29 `laroteiro` 复核仍是「懒惰的/狡猾的」，未动）|

